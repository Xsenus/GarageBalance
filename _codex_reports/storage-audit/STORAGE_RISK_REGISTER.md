# Реестр рисков storage, backup и restore

Дата: 22.09.2026  
Baseline: `master` / `e5309e8ae851d6a5f4ce4960e01066ded651e8fb`

План mitigation не означает устранение риска. Статус `OPEN` сохраняется до реализации и доказательной проверки.

| ID | Severity | Data class | Сценарий и влияние | Evidence | Current control | Mitigation / Stage-Task | Остаточный риск | Статус |
|---|---|---|---|---|---|---|---|---|
| `RISK-001` | `CRITICAL` | PostgreSQL, backup, keys/config | Потеря единственного ПК/VPS/диска одновременно уничтожает production DB, локальные dumps и, вероятно, key ring/config; необратима потеря финансовой истории или расшифрование интеграционных секретов. | `EVID-002`, `EVID-016`, `EVID-017`, `EVID-032` | Local `.pgdump`, release artifacts, ручные инструкции. | `STG-04..08`; `TASK-008`, `TASK-011`, `TASK-019`, `TASK-020`, `TASK-021` | Компрометация общих credentials/KMS или двух providers остаётся возможной; требуется независимость accounts и drill. | `OPEN` |
| `RISK-002` | `HIGH` | PostgreSQL backup | API может быть выключен во всём окне 02:00–05:00; stale backup не вызывает alert. Наблюдался локальный lag около 6 суток. | `EVID-004`, `EVID-005`, `EVID-014`, `EVID-025`, `EVID-030` | Worker проверяет ежечасно внутри окна; UI показывает последнее время. | `STG-01`; `TASK-004`, `TASK-017` | Scheduler/host outage всё ещё влияет на RPO; нужен внешний monitor. | `OPEN` |
| `RISK-003` | `HIGH` | Backup artifacts | Нет logical catalog, checksum, generation, replica/per-copy states и durable retry. Partial success и UNKNOWN невозможно доказать/восстановить. | `EVID-010`, `EVID-033` | File-name inventory, non-empty и TOC check. | `STG-01..03`; `TASK-003`, `TASK-006`, `TASK-007`, `TASK-010` | DB catalog сам остаётся dependency; manifests должны позволить rebuild. | `OPEN` |
| `RISK-004` | `HIGH` | PostgreSQL restore | `pg_restore --list` не доказывает полный restore, constraints, keys и app startup; регулярный DR drill не подтверждён. | `EVID-006`, `EVID-023`, `EVID-024`, `EVID-030` | VPS pre-deploy temporary restore; guarded manual script. | `STG-08`; `TASK-019`, `TASK-021` | Provider outage/retrieval delay может увеличить RTO; проверять каждую tier policy. | `OPEN` |
| `RISK-005` | `HIGH` | Data Protection keys, config | DB restore без исходного key ring может оставить encrypted integration secrets нечитаемыми; plaintext `.env`/key archive может утечь. | `EVID-017`, `EVID-032` | Persistent volume/path; manual protected-copy guidance. | `STG-08`; `TASK-020`, `TASK-021` | Потеря независимого encryption key/secret manager остаётся DR blocker. | `OPEN` |
| `RISK-006` | `HIGH` | Raw Access import | Crash между file write и DB row или между processing и delete оставляет PII `.pending` без TTL; возможны утечка и disk exhaustion. | `EVID-018` | Delete on known failure/success; strict 50 MiB limit and file permissions. | `STG-01`; `TASK-005` | Активные/неоднозначные файлы нельзя удалять автоматически без quarantine window. | `OPEN` |
| `RISK-007` | `MEDIUM` | PostgreSQL backup | Несколько API instances могут параллельно создавать/ротировать backups: lock process-local. | `EVID-008` | `SemaphoreSlim` в одном процессе. | `STG-03`; `TASK-010` | DB outage мешает lease; fail closed, не запускать независимые writers. | `OPEN` |
| `RISK-008` | `MEDIUM` | Backup delete/retention | Файл удалён до audit/metadata commit; delayed retry может удалить новую generation либо audit может отсутствовать. | `EVID-009` | Имя ограничено regex; audit обычно создаётся. | `STG-06`; `TASK-015` | Provider lifecycle действует вне SQL; нужны retention/version gates и reconciliation. | `OPEN` |
| `RISK-009` | `MEDIUM` | Backup administration | `users.manage` одновременно разрешает управление пользователями и скачивание/удаление дампа с PII/финансами. UI раскрывает server directory. | `EVID-012`, `EVID-013` | Backend authorization, audit, path validation. | `STG-01/06`; `TASK-004`, `TASK-016` | Администратор с backup-delete всё равно высокопривилегирован; нужен audit/MFA policy вне scope. | `OPEN` |
| `RISK-010` | `HIGH` | Backup protection | Health может быть green при stale/missing/unprotected backup; отказ remote destination после внедрения может долго оставаться скрытым. | `EVID-022` | UI/log on explicit operation. | `STG-09`; `TASK-017` | Alert channel availability не гарантируется; требуется периодическая проверка доставки. | `OPEN` |
| `RISK-011` | `MEDIUM` | Reports/diagnostic package | Крупные exports/ZIP собираются в RAM, что может вызвать memory pressure; storage replication для них не нужна. | `EVID-019`, `EVID-020` | Текущие limits для diagnostic package; bounded query design. | `TASK-023` performance measurement; streaming threshold только при фактической необходимости. | Без production metrics объём неизвестен. | `OPEN / DEFERRED` |
| `RISK-012` | `HIGH` | Multi-destination backup | Наивный async upload может потерять replayable bytes после local retention/restart; durable job без source не является копией. | `EVID-009`, `EVID-010`; owner requirement | Local dump persists until current count retention. | `STG-05`; `TASK-010`, `TASK-011`; запрет cleanup до protection policy. | Одновременная потеря staging до remote confirmation остаётся окном риска, измеряемым как replication lag. | `OPEN` |
| `RISK-013` | `HIGH` | Reads/restores | Healthy provider без нужной/current replica может вернуть missing/stale data; простой failover по endpoint недостаточен. | `EVID-010`; owner requirement | Сейчас одна local copy, поэтому ложного cross-provider fallback нет. | `STG-06`; `TASK-012`, `TASK-014`, `TASK-021` | Если актуальная копия только в недоступном A, успех невозможен; вернуть честную ошибку. | `OPEN` |
| `RISK-014` | `HIGH` | New backup delivery | Потерянный ответ после upload создаёт UNKNOWN; blind retry может создать дубли, а одна доступная копия может быть ошибочно объявлена полной protection. | owner requirement; отсутствие state в `EVID-010` | Нет remote writes сейчас. | `STG-05`; `TASK-010`, `TASK-011`; logical `OperationId`, deterministic key, reconcile-before-retry, distinct ack states. | Provider без conditional write/version semantics потребует adapter-specific verification. | `OPEN` |
| `RISK-015` | `HIGH` | Recovery/failback | После возврата A новые B-only backups могут исчезнуть из выдачи, stale data может перезаписать current, tombstones — воскреснуть. | owner requirement; каталог отсутствует (`EVID-033`) | Не применимо к single-local current state. | `STG-06/07`; `TASK-014`, `TASK-018`; DB committed generation, tombstones, verify/catch-up gates. | Mass corruption/compromise требует operator quarantine, не auto-repair. | `OPEN` |
| `RISK-016` | `HIGH` | Security/tenancy | Fallback в произвольный account/bucket может нарушить data residency, ACL, encryption или tenant isolation. | owner requirement; provider config отсутствует (`EVID-002`) | Нет cloud route сейчас. | `STG-02/04`; `TASK-006`, `TASK-008`, `TASK-009`; explicit capability/policy graph, secret refs, private defaults. | Ошибочная operator policy остаётся риском; validate-on-start + dry-run. | `OPEN` |
| `RISK-017` | `MEDIUM` | Existing integrations | Универсальная storage abstraction может случайно затронуть 1C/receipt adapters или local import/log flows, хотя они не являются storage providers. | `EVID-018`, `EVID-019`, `EVID-027` | Раздельные namespaces/services. | `STG-00/02`; `TASK-001`, `TASK-006`; scope isolation and characterization tests. | Будущие attachments потребуют отдельной data-class policy. | `OPEN` |
| `RISK-018` | `HIGH` | Migration/backfill | Включение failover до verified backfill создаёт ложное ожидание защиты исторических backup; rollback после B-only writes может потерять доступ. | `EVID-010`, owner requirement | Existing files remain local and named deterministically. | `STG-07/09`; `TASK-018`, `TASK-022`; inventory/checkpoints/delta/coverage gate; no source delete. | Corrupt source object cannot be repaired without another valid copy. | `OPEN` |
| `RISK-019` | `MEDIUM` | Operations/cost | Retry storm, repair/backfill и large uploads могут исчерпать disk/network/quota и задержать runtime operations. | owner requirement; production size `NOT VERIFIED` | Small observed local dumps; no remote traffic. | `STG-05/09`; `TASK-010`, `TASK-017`, `TASK-023`; budgets, jitter, backpressure, metrics. | Тарифы/quotas меняются; нужен периодический review. | `OPEN` |
| `RISK-020` | `HIGH` | DR catalog | Если replica catalog существует только в production DB, потеря DB затруднит поиск backup и ключей восстановления. | `EVID-033`; owner requirement | Filename convention and operator docs. | `STG-03/08`; `TASK-007`, `TASK-019`; signed/checksummed sidecar manifests and independent catalog export. | Manifest не должен содержать secrets; доступ к encryption keys остаётся внешней зависимостью. | `OPEN` |

## Решения, которые нельзя считать уже принятыми владельцем

- Target RPO/RTO, retention, бюджет и конкретные providers/accounts/regions.
- Допустимость Object Lock/WORM и срок необратимой retention.
- Protection policy: предложено `required=2` независимые copies total (local + минимум одна off-site), `desired=3` (local + две off-site), но это `PROPOSED` до согласования.
- Разрешение на real-provider test, production restore drill, cutover и удаление source отсутствует.

