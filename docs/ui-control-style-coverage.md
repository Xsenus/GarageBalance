# Инвентаризация UI Контролов И Стилей

Документ фиксирует текущую карту контролов GarageBalance после проверки `frontend/src/App.tsx`, `frontend/src/App.css`, `frontend/src/App.test.tsx` и frontend shared-тестов. Он нужен, чтобы единый стиль текущих контролов оставался проверяемым контрактом, а новые экраны добавлялись по списку, а не по визуальным догадкам.

## Источники

- `frontend/src/App.tsx`: основные рабочие экраны, прототипы форм, модальные окна и таблицы.
- `frontend/src/App.css`: общие классы кнопок, форм, таблиц, вкладок, диалогов, состояний и адаптива.
- `frontend/src/App.test.tsx`: пользовательские workflow-тесты по авторизации, главному меню, тарифам, контрагентам, показаниям, платежам, фондам, пользователям, справочникам, отчетам, импорту и истории изменений.
- `frontend/src/accessibleStatus.test.ts`: структурные проверки доступных имен полей, кнопок, icon-only кнопок, сообщений статуса и `detail-dialog`.
- `frontend/src/responsiveLayout.test.ts`: проверка scrollable dialog-layout внутри viewport.
- `frontend/src/shared/focusHooks.ts` и `focusHooks.test.tsx`: единые hooks для Escape, focus-on-open, focus-trap и восстановления фокуса.
- `frontend/src/shared/changePreview.ts` и `changePreview.test.ts`: единый формат `было -> стало` для confirmation изменений.
- `frontend/src/shared/PageNavigator.tsx` и `PageNavigator.test.tsx`: единая нумерованная пагинация со стрелками, многоточием, активной страницей и быстрым переходом для больших списков.

## Семейства Контролов

| Семейство | Текущий общий стиль | Проверенные области | Открытые хвосты |
| --- | --- | --- | --- |
| Primary buttons | `primary-button` | Авторизация, основные CTA там, где нужен главный action | Проверить, что новые прототипы не используют primary там, где действие вторичное |
| Secondary buttons | `secondary-button` | Сохранение, проведение, экспорт, рабочие действия | Продолжить проверку disabled/loading состояний во всех новых разделах |
| Ghost buttons | `ghost-button` | Отмена, нейтральные переходы, показать еще | Проверить единый hover/focus в локальных прототипах |
| Icon-only buttons | `icon-button`, `topbar-back-button` | Sidebar, topbar, закрытие dialogs, поиск, меню строк, возврат в главное меню | У всех новых icon-only actions сохранять `aria-label`, `type` и tooltip/title при неочевидной иконке |
| Destructive buttons | `danger-button` вместе с `secondary-button` или action-specific классом | Архивирование, удаление, отмена финансовых записей, удаление прототипных строк | Проверить визуальное отличие для будущих фондов, rollback импорта и чеков |
| Link-like actions | `link-button` | Быстрые переходы, вспомогательные действия | Не использовать для опасных действий без confirmation |
| Text inputs | Общие правила внутри `dictionary-form`, compact/forms, finance filters | Логин, пользователи, справочники, тарифы, платежи, отчеты, импорт, история | Дальше сверять width/mobile behavior для новых модалок контрагентов и платежей |
| Number inputs | Общие input-правила плюс business validation | Гаражи, тарифы, суммы, показания, фонды, платежи | Проверить money/quantity alignment в таблицах после backend-моделей сборов и фондов |
| Date/month/year controls | Общий `LocalizedDatePicker` для видимых дат и месяцев; год в прототипе показаний | Тарифы, финансы, отчеты, импорт, платежные прототипы, показания | Year-control показаний должен оставаться стилизованным и валидироваться 1900-9999 |
| Select controls | Общий `SelectControl` для одиночного выбора, role-based tabs и специализированные editable-combobox | Роли, группы, тарифная база, типы операций, отчеты, история | Проверять человекочитаемые option labels при новых enum/backend-моделях |
| Textarea | Общие правила `dictionary-form textarea`, dialog reason/comment поля | Причины удаления/отмены, комментарии, импорт, платежи, контрагенты | Для обязательных reason сохранять `aria-invalid` и error text |
| Checkbox/toggle | Styled form controls and native checkbox where appropriate | Архивные списки, регулярные платежи, участники сборов, пороговая тарификация | Нужна отдельная визуальная проверка будущих toggles после стабилизации моделей сборов |
| Tabs | Role-based tabs and section tab buttons | Контрагенты, платежи, отчеты, настройки, dashboard navigation | Проверить mobile overflow для всех новых трехвкладочных/многовкладочных экранов |
| Dialogs/modals | `modal-backdrop`, `detail-dialog`, `detail-dialog-header`, `detail-dialog-actions` | Редакторы, confirmations, карточки истории, прототипные формы | Новые dialogs обязаны подключать focus trap, Escape, cancel, accessible title/description и restore focus |
| Context menus | `context-menu`, `context-menu-group`, `context-menu-separator` | Контрагенты, справочники, пользователи, платежи, тарифы и сборы | Команды изменения записи остаются в одной группе, переходы к отчетам/истории — в отдельной; новые меню обязаны сохранять короткие подписи, иконки, клавиатурную навигацию и доступные имена |
| Tables | Рабочие tables с accessible names, states and row actions | Тарифы, контрагенты, показания, платежи, фонды, справочники, отчеты, история | Для больших backend-таблиц сохранять pagination/server filters или горизонтальный scroll |
| Pagination/filter states | `PageNavigator` и `TablePagination` для нумерованной навигации, shared pagination helpers, filter labels, status messages | Тарифы и сборы, контрагенты, показания, платежи, отчеты, справочники | Все большие таблицы должны переиспользовать единый нумерованный навигатор; серверные filters обязаны сбрасывать offset, локальные страницы — номер страницы, а навигация — блокироваться во время загрузки |
| Empty/loading/error states | `empty-state`, `form-error`, `role=status`, `role=alert` | Авторизация, справочники, финансы, импорт, отчеты, история | Дальше расширять на будущие backend-модели фондов, сборов и персонала |

## Экраны И Текущий Статус

| Экран | Контролы | Статус |
| --- | --- | --- |
| Авторизация | Email/login input, password input, one submit button | Minimal GPT-style реализован, лишние Google/admin buttons убраны |
| Главное меню | `dashboard-tile`, `topbar-back-button`, collapsed sidebar | Есть hover/focus/shadow, текст кнопок не должен выходить за рамки |
| Sidebar/topbar | Icon-only buttons, active states, back button, user actions | Требуется ручная visual-проверка скриншотами после каждого крупного UI-среза |
| Пользователи | Таблица, editor dialog, role select, password input, delete/restore confirmations | Confirmation diff и restore focus покрыты тестами |
| Справочники | Forms, archive/restore dialogs, table/list actions, date/select/money controls | Общий editor и confirmation flow покрыт, отдельные будущие модели еще открыты |
| Тарифы и сборы-прототип | Editable cells, add service/fee dialogs, threshold action, one-time delete/restore | Каталог собран по форме заказчика без повторения служебных строк: дата оплаты занимает одну ячейку значения, технические маркеры порогов и действие архивации услуги скрыты; нерегулярные платежи и сборы расположены в двух колонках |
| Контрагенты-прототип | Tabs `Гаражи`/`Поставщики`/`Персонал`, dialogs per section, delete/restore confirmations, нумерованная server pagination, editable combobox-подсказки DaData, контекстные меню строк | Все три таблицы используют единый `PageNavigator`; карточка гаража разделяет основные и финансовые поля; карточка поставщика расширена, показывает не менее трех контактов, отдельные телефон и почту, расчетную задолженность и единообразные подсказки по ИНН/адресу с сохранением ручного ввода; строки отделов поддерживают редактирование и удаление через кнопки и ПКМ, архивные отделы — восстановление |
| Показания-прототип | Year input, type select, editable table cells, confirmation dialog | Нужна backend-модель истории и сохранения показаний по гаражам |
| Платежи-прототип | Payments table, многоколоночный поиск и список выбранных гаражей, add accrual/payment/bank dialogs, full payment dialog | Мультивыбор закрывается по клику снаружи/Escape, каждый гараж исключается отдельно, действия выбранного гаража идут одной строкой; требуется дальнейшая привязка оставшихся прототипных частей к backend-логике |
| Управление фондами-прототип | Fund table, deposit/withdraw dialogs | Confirmation UI есть, backend/audit для фондов остается открытым |
| Финансы/backend экран | Forms, month/date controls, tables, cancel confirmations | Текущие create/update/cancel workflows покрыты тестами |
| Отчеты | Tabs, filters, date/month controls, export buttons, tables | Есть workflow tests, export tests и общий style contract; новые отчеты должны переиспользовать эти controls |
| Импорт Access | File input, dry-run button, quarantine table, export actions | File picker, dry-run, JSON export, quarantine resolve и close confirmation actions имеют иконки/подсказки и workflow tests; rollback confirmation остается открытым, потому что фактический rollback еще не реализован |
| История изменений | Styled combobox/date filters, pagination, export buttons, icon-only detail action, adaptive detail dialog | Журнал показывает локальное время с секундами, имя/почту/полный ID пользователя и значения `Было`/`Стало`; отдельный доступ к истории внутри рабочих разделов не нужен |
| "Что нового" | Read-only release list | Admin UI управления release notes еще не реализован |
| Настройки | Вертикальная внутренняя навигация с role-based tabs, форма смены пароля | Раздел доступен отдельной плиткой главного экрана, а администратору также из боковой навигации; вкладка «Безопасность» доступна внутри раздела, вкладка интеграций и ее запросы временно отключены общей настройкой интерфейса |

## Уже Закреплено Тестами

- Все `<button>` в `App.tsx` должны иметь явный `type`.
- Все `icon-button` должны иметь accessible name.
- Все `input`, `select` и `textarea` должны иметь `aria-label` или `aria-labelledby`.
- `detail-dialog` должен быть `role="dialog"`, `aria-modal="true"` и иметь `aria-labelledby`.
- Высокие dialogs должны прокручиваться внутри viewport, а header/actions оставаться доступными.
- `focusHooks` покрывают Escape, focus-on-open, focus-trap и восстановление фокуса.
- `PageNavigator` покрыт тестами диапазонов страниц, стрелок, нумерованных кнопок, быстрого перехода и disabled-состояния; workflow контрагентов проверяет запрос второй серверной страницы.
- Editable combobox для ИНН и адреса поставщика сохраняет общий стиль input, использует роли `combobox`/`listbox`/`option`, не запускает поиск при простом открытии карточки и не блокирует ручной ввод при недоступности DaData.
- Основные workflow-тесты открывают и проверяют dialogs тарифов, контрагентов, показаний, платежей, фондов, пользователей, справочников, отчетов, импорта и истории.
- Настройки проверяются для администратора и пользователя без административных прав: видна только вкладка «Безопасность», интеграционные панели отсутствуют и их status API не вызываются.
- Все реализации ПКМ проверяются структурным тестом: действия собраны в общие группы, разные категории разделены доступным разделителем, а переход к отчету в контрагентах подписан кратко как «Финансовый отчет».

## История выполнения

- 2026-07-15 — завершена унификация редактора тарифов в справочниках: уже переведённый на общий `SelectControl` выбор базы расчёта дополнен общим `LocalizedDatePicker` для даты начала в активном и совместимом представлениях `DictionaryPanel`. Дата показывается как `дд.мм.гггг`, ISO остаётся только во внутреннем состоянии и API, календарь поддерживает русскую локализацию, клавиатуру, очистку и единые проектные состояния. Структурная регрессия теперь запрещает в модуле как нативные `<select>`, так и `type="date"`, и требует оба локализованных календаря. Добавлена запись «Что нового» `0.640.0`. Проверено целевыми сценариями 3/3, проверками общих контролов 30/30, полным frontend-набором 462/462, backend-набором 1508/1508, lint, production-сборкой, bundle-бюджетом, `dotnet format`, privacy-проверкой, уникальностью release JSON, строгой UTF-8 и генерацией idempotent migration SQL. PostgreSQL 17 на порту 5433 доступна; схема и бизнес-данные не менялись. Архивные документы не изменялись.
- 2026-07-15 — нативный браузерный список владельца в карточке гаража заменён общим `SelectControl` с проектным оформлением раскрытия, текущего варианта, фокуса и клавиатуры. Чтобы дефект не повторился в соседних формах справочника, на тот же контрол переведены привязка существующего гаража, группа поставщика и база расчёта тарифа; в активном и совместимом legacy-представлении `DictionaryPanel` больше нет нативных одиночных `<select>`. Добавлена запись «Что нового» `0.639.0`; структурная регрессия требует не менее семи общих комбобоксов и запрещает возврат нативного списка. Проверено целевыми workflow 3/3, полным frontend-набором 462/462, backend-набором 1508/1508, lint, production-сборкой, bundle-бюджетом, `dotnet format`, privacy-проверкой, release JSON и генерацией idempotent migration SQL. PostgreSQL 17 на порту 5433 доступна; схема и бизнес-данные не менялись. Архивные документы не изменялись.
- 2026-07-15 — карточка владельца расширена до рабочего двухколоночного макета: фамилия и имя расположены в одной строке, отчество — отдельной строкой под ними, телефон и адрес используют ширину формы рациональнее. Адрес получил доступные подсказки DaData через существующий защищённый backend-прокси с задержкой поиска, защитой от устаревших ответов, клавиатурной навигацией и сохранением ручного ввода при недоступности сервиса. Длинная подсказка стартового баланса заменена на компактные доступные значки справки у стартового баланса, воды и электричества; на узком экране сетка складывается в одну колонку. Добавлена запись «Что нового» `0.638.0`. Дополнительно тестовый проект явно помечен `IsTestProject`, чтобы .NET 10 SDK действительно запускал тесты. Проверено целевыми workflow 2/2, полным frontend-набором 462/462, backend-набором 1508/1508, lint, production-сборкой, bundle-бюджетом, `dotnet format`, release JSON и генерацией idempotent migration SQL. PostgreSQL 17 на порту 5433 доступна; схема и бизнес-данные не менялись. Архивные документы не изменялись.
- 2026-07-15 — переработана форма «Сформировать начисления»: пояснение вынесено в самостоятельный информационный блок, месяц и комментарий собраны в устойчивую сетку с подписями над полями, серверная ошибка занимает отдельную полноширинную строку, а основное действие явно называется «Сформировать». На экранах до 640 px поля складываются в одну колонку без наложений; локализованный выбор месяца, фокус, Escape и возврат фокуса сохранены. Добавлена запись «Что нового» `0.637.0`. Проверено целевым workflow 1/1, структурными адаптивными тестами 22/22, полным frontend-набором 460/460, backend-набором 1508/1508, lint, production-сборкой, bundle-бюджетом, `dotnet format`, release JSON, строгим UTF-8 и генерацией idempotent migration SQL. PostgreSQL 17 на порту 5433 доступна; схема и бизнес-данные не менялись. Архивные документы не изменялись.
- 2026-07-15 — денежные поля таблицы тарифов и нерегулярных платежей получили единое поведение ввода: при Enter или уходе из поля целое число, завершающая точка/запятая и дробное значение приводятся к формату с двумя знаками (`100.00`, `7.50`, `1 000 000.00`) до подтверждения изменения. Целочисленные настройки периодичности и просрочки не затронуты; для мобильной клавиатуры указан decimal input mode. Контракт закреплен formatter- и workflow-тестами. Проверено целевыми сценариями 11/11, полным frontend-набором 459/459, backend-набором 1508/1508, lint, production-сборкой, bundle-бюджетом, `dotnet format`, privacy-проверкой, release JSON и генерацией idempotent migration SQL. PostgreSQL 17 на порту 5433 доступна; схема и бизнес-данные не менялись. Архивные документы не изменялись.
- 2026-07-14 — перед публикацией `feature/home` объединена с актуальным `master`, включавшим параллельные оптимизации фоновых таблиц, фильтров и автоматических начислений. Конфликты разрешены с сохранением общей локализованной стилистики, повторно используемых retryable-загрузчиков, shared skeleton/error boundary и оптимизаций серверных запросов из основной ветки. Архитектурные регрессии обновлены под вынесенный `workspaceSectionLoader`, асинхронные проверки статуса интеграций ожидают фактическое завершение запроса. 29 ещё не опубликованных записей «Что нового» перенумерованы в свободный диапазон `0.607.0–0.635.0`, поэтому все 635 release ID и версии уникальны. Объединённый frontend прошел 451/451, backend — 1508/1508, целевые проверки конфликтных зон — 22/22 и 18/18; lint, production-сборка, `dotnet format`, idempotent migration SQL и bundle-бюджет успешны (стартовый JS 74,5/180 КиБ gzip, суммарно 215,9/260 КиБ).
- 2026-07-14 — пятый независимый аудит всех согласованных UI-пунктов нашел оставшиеся системные поля месяца в серверной рабочей таблице платежей. Фильтры «Период с» и «Период по» переведены на общий `LocalizedDatePicker`: пользователь видит `мм.гггг`, может вводить период с клавиатуры, открыть единый календарь или очистить значение, а API по-прежнему получает ISO `YYYY-MM`. Workflow-тест дополнительно ожидает завершение асинхронной загрузки строки перед открытием контекстного меню, поэтому больше не зависит от скорости тестовой среды. Целевые сценарии прошли 2/2, два полных frontend-прогона — 439/439 каждый, backend-проверка release notes — 20/20, lint и production-сборка успешны. Bundle не вырос: стартовый JS 74,2 из 180 КиБ gzip, суммарно JS/CSS 214,5 из 260 КиБ. Изменений схемы и бизнес-данных нет, поэтому проверка PostgreSQL не требовалась. Архивные документы не изменялись.
- 2026-07-14 — четвертый независимый аудит оптимизации устранил повторные Promise-цепочки при последовательных событиях наведения, фокуса и открытия одного ленивого раздела. Общий `createRetryableLazyLoader` теперь разделяет один выполняющийся запрос между предварительной загрузкой и `React.lazy`, сохраняет успешно загруженный модуль и очищает ошибочный запрос для безопасной следующей попытки. Покрыты объединение параллельных вызовов, кэш успешного результата, восстановление после асинхронного отклонения и синхронного исключения загрузчика. Целевой набор прошел 21/21, два полных frontend-прогона — 439/439 каждый, backend-проверка release notes — 20/20, lint и production-сборка успешны. Bundle сохранил запас: стартовый JS 74,2 из 180 КиБ gzip, суммарно JS/CSS 214,5 из 260 КиБ. Повторная визуальная проверка VPS снова остановлена внешней ошибкой `ERR_NAME_NOT_RESOLVED` для `sgk.blagodatry.ru`; проверочные вкладки закрыты. Изменений схемы и бизнес-данных нет, поэтому проверка PostgreSQL не требовалась. Архивные документы не изменялись.
- 2026-07-14 — третий независимый аудит всех согласованных UI-пунктов дополнительно усилил оптимизацию рабочих разделов. Двенадцать лениво загружаемых экранов теперь предварительно загружаются только при наведении указателя или клавиатурном фокусе на разрешенной плитке/пункте меню, не увеличивая стартовый пакет. Общая `AsyncErrorBoundary` изолирует ошибку загрузки или рендера одного раздела: оболочка приложения, навигация и другие разделы остаются доступны, пользователь получает понятный `role="alert"`, возврат в главное меню и перезагрузку страницы. Ошибочное предварительное подключение подавляется без необработанного Promise и может быть повторено. Добавлены компонентные и структурные регрессии; целевой набор прошел 18/18, два последовательных полных frontend-прогона — 436/436 каждый, backend-проверка release notes — 20/20, lint и production-сборка успешны. Bundle сохранил большой запас: стартовый JS 74,1 из 180 КиБ gzip, суммарно JS/CSS 214,4 из 260 КиБ. Повторная визуальная проверка VPS остановлена внешней ошибкой `ERR_NAME_NOT_RESOLVED` для `sgk.blagodatry.ru`; проверочные вкладки закрыты. Изменений схемы и бизнес-данных нет, поэтому проверка PostgreSQL не требовалась. Архивные документы не изменялись.
- 2026-07-14 — второй независимый аудит платежных форм закрыл оставшиеся нативные элементы в согласованных сценариях: выбор начала и конца периода поступлений, месяц выплат, дата и учетный месяц редактируемого платежа переведены на общие `SelectControl` и `LocalizedDatePicker`. Общий комбобокс получил поиск по первым буквам с поддержкой кириллицы и очистку таймера после размонтирования; календарь ограничивает длину локализованного значения и отмечает полностью введенную невозможную дату через `aria-invalid`. Двенадцать тяжелых рабочих разделов переведены на отложенную загрузку через общий доступный skeleton: по контрольному budget-скрипту начальный JavaScript production-сборки уменьшен с 179,1 до 73,5 КиБ gzip (примерно на 59%), крупнейший функциональный фрагмент остается около 30,7 КиБ gzip. Добавлены компонентные, workflow- и структурные регрессии. Повторные полные прогоны выявили и позволили устранить нестабильные синхронные ожидания лениво загружаемых экранов, после чего финальный frontend-набор прошел 435/435; также успешно выполнены 20/20 backend-проверок release notes, линтер, production-сборка и бюджет bundle. Изменений схемы и бизнес-данных нет, поэтому проверка PostgreSQL не требовалась. Визуальная проверка через встроенный браузер была остановлена из-за `ERR_NAME_NOT_RESOLVED` для домена со скриншотов, созданная вкладка закрыта. Архивные документы не изменялись.
- 2026-07-14 — повторно проверены модальные формы начислений и выплат. Общие `SelectControl` и `LocalizedDatePicker` усилены клавиатурной навигацией, автопрокруткой активной опции, синхронизацией внешнего значения, Escape, проверкой невозможных дат и быстрым выбором текущей даты/месяца. Модалка начисления сбора в тарифах переведена на тот же локализованный выбор месяца; добавлены компонентные и workflow-регрессии. Проверено двумя полными frontend-прогонами (432/432 каждый), целевыми component/accessibility/responsive-тестами (49/49), сквозными сценариями начислений и выплат (3/3), backend-тестами каталога релизов (20/20), линтером, production-сборкой и бюджетом bundle.
- 2026-07-14 — форма сотрудника упорядочена по согласованному макету: отдел и ставка объединены в двухколоночный ряд со ставкой справа; кнопки сохранения и отмены собраны в отдельную группу у правого края, а финансовый отчет сохранен слева. На узком экране поля и действия снова перестраиваются в одну колонку. Компоновка закреплена workflow- и responsive CSS regression-тестами. Проверено полным frontend-набором (372/372), backend-набором (1491/1491), линтером, production-сборкой, бюджетом bundle, `dotnet format`, privacy-проверкой, release JSON/UTF-8, генерацией idempotent migration SQL и доступностью локальной PostgreSQL 17 на порту 5433. Встроенный браузер не подключился из-за ошибки подготовки kernel assets среды Codex, поэтому визуальный контракт подтвержден DOM/workflow/CSS-тестами. Архивные документы не изменялись.
- 2026-07-14 — форма создания и редактирования поставщика переведена на фактическую ширину до 1280 px с ограничением по viewport; одновременно исправлен каскад, из-за которого общий стиль мог перекрывать ширину специализированных форм гаража и поставщика. Наименование и услуга, ИНН/задолженность/адрес, а также телефон/почта/комментарий собраны в компактные адаптивные ряды. Высота таблицы контактов уменьшена без потери согласованного места для трех контактов. Нативный список услуги заменен общим доступным `SelectControl`; мобильная форма остается одноколоночной. Поведение закреплено workflow-, styled-control- и responsive CSS regression-тестами. Проверено полным frontend-набором (371/371), backend-набором (1491/1491), линтером, production-сборкой, бюджетом bundle, `dotnet format`, privacy-проверкой, release JSON/UTF-8, генерацией idempotent migration SQL и доступностью локальной PostgreSQL 17 на порту 5433. Встроенный браузер не подключился из-за ошибки подготовки kernel assets среды Codex, поэтому визуальный контракт подтвержден DOM/workflow/CSS-тестами. Архивные документы не изменялись.
- 2026-07-14 — форма создания и редактирования гаража расширена до 1120 px с безопасным ограничением по ширине окна. Верхние основные и финансовые показатели сохранили согласованное двухколоночное расположение, владелец с телефоном и поля счетчиков с комментарием размещены попарно, а вертикальные и межколоночные интервалы уменьшены. На экранах до 720 px все группы возвращаются в одну колонку. Поведение закреплено workflow- и responsive CSS regression-тестами. Проверено полным frontend-набором (370/370), backend-набором (1491/1491), линтером, production-сборкой, бюджетом bundle, `dotnet format`, privacy-проверкой, release JSON/UTF-8, генерацией idempotent migration SQL и доступностью локальной PostgreSQL 17 на порту 5433. Встроенный браузер не подключился из-за ошибки подготовки kernel assets среды Codex, поэтому визуальный контракт подтвержден DOM/workflow/CSS-тестами. Архивные документы не изменялись.
- 2026-07-14 — центральная история изменений приведена к рабочему формату проверки: native combobox/date-поля заменены общими доступными контролами с русским вводом даты и месяца; время выводится с секундами как `дд.мм.гггг (чч:мм:сс)`; пользователь показывается именем, почтой и полным ID; открытие карточки стало иконкой. Карточка расширена до трех колонок на широком экране и адаптивно перестраивается в две/одну. Backend теперь возвращает пользователя по `ActorUserId`, берет `Поле`/`Было`/`Стало` из структурных metadata, сохраняет все пары при нескольких изменениях и восстанавливает значения старых событий показаний из прежнего описания. CSV/XLSX дополнены именем и почтой пользователя. Проверено полным frontend-набором (369/369), backend-набором (1491/1491), целевыми component/workflow/formatter/responsive-тестами, линтером, production-сборкой, бюджетом bundle, `dotnet build`, `dotnet format`, privacy-проверкой, release JSON/UTF-8, генерацией idempotent migration SQL и доступностью локальной PostgreSQL 17 на порту 5433. После выявленного CI-различия проверка формата времени отдельно повторена с `TZ=UTC`, чтобы тест не зависел от часового пояса runner. Встроенный браузер не подключился из-за ошибки подготовки kernel assets среды Codex, поэтому визуальный контракт подтвержден DOM/workflow-тестами. Архивные документы не изменялись.
- 2026-07-14 — раздел «Что нового» уплотнен для быстрого просмотра: карточки выводятся по три в ряд на широком экране, по две на среднем и по одной на узком. У администраторов скрыты кнопки ручного добавления и изменения записей, так как опубликованная история формируется автоматически; публикация ранее созданных черновиков сохранена. Поведение закреплено workflow- и responsive CSS regression-тестами. Проверено полным frontend-набором (365/365), backend-набором (1490/1490), линтером, production-сборкой и бюджетом bundle. Встроенный браузер не подключился из-за ошибки подготовки браузерных файлов среды Codex, поэтому визуальный контракт подтвержден DOM/CSS-тестами. Архивные документы не изменялись.
- 2026-07-14 — в таблице управления фондами заголовки и кнопки колонок «Изъятие» и «Пополнение» выровнены по центру через отдельный класс action-колонки; таблица истории операций не затронута. Центрирование закреплено workflow- и CSS regression-тестами. Проверено полным frontend-набором (364/364), backend-набором (1490/1490), линтером, production-сборкой и бюджетом bundle; архивные документы не изменялись.
- 2026-07-14 — мультивыбор гаражей в платежах переработан для ежедневной работы: выпадающий список использует три колонки на широком экране, две на планшете и одну на телефоне; выбранные гаражи показаны отдельными строками с владельцем, переключением активной карточки и индивидуальным крестиком удаления. Меню закрывается кликом снаружи, Escape, Enter и выбором активного гаража. Действия по гаражу собраны в одну строку, показатели карточки выровнены вправо, служебная подпись «Поиск по гаражу» удалена. Проверено полным frontend-набором (363/363), backend-набором (1490/1490), целевыми workflow и responsive/style тестами, линтером, production-сборкой, бюджетом bundle, `dotnet build`, `dotnet format`, privacy-проверкой, release JSON/UTF-8, генерацией idempotent migration SQL и доступностью локальной PostgreSQL 17 на порту 5433. Встроенный браузер не подключился из-за ошибки подготовки kernel assets среды Codex, поэтому визуальный контракт подтвержден DOM/workflow-тестами. Архивные документы не изменялись.
- 2026-07-14 — просроченная задолженность в таблице гаражей выровнена по правому краю и всегда показывается как денежная сумма с двумя знаками после запятой, например `4 620,84 руб.`. Общий разбор денежной строки исправлен для сочетания копеек и сокращения `руб.`, чтобы фильтр должников и денежная сортировка продолжали работать. Проведен аудит всех реализованных ПКМ: команды управления записью собраны в логические группы, переходы к финансовому отчету и истории отделены линией, а подпись перехода сокращена до «Финансовый отчет». Единая структура закреплена в AGENTS.md и структурном regression-тесте. Проверено полным frontend-набором (362/362), backend-набором (1490/1490), линтером, production-сборкой, бюджетом bundle, `dotnet build`, `dotnet format`, privacy-проверкой, release JSON/UTF-8, генерацией idempotent migration SQL и доступностью локальной PostgreSQL 17 на порту 5433; схема и бизнес-данные не менялись. Встроенный браузер не подключился из-за ошибки подготовки kernel assets среды Codex, поэтому визуальный контракт подтвержден DOM/workflow-тестами. Архивные документы не изменялись.
- 2026-07-14 — повторный аудит всех рабочих разделов закрыл оставшиеся исключения из правила пагинации. В импорте общий `TablePagination` подключен к журналу, истории запусков, созданным записям и карантину; операции фондов переведены с выборки последних 20 записей на отдельную серверную страницу с общим количеством строк. Удалены неиспользуемые стили native-select размера страницы. Структурный regression-тест теперь проверяет наличие общего компонента во всех десяти разделах с большими таблицами и запрещает возврат native-select. Проверено полным frontend-набором (355/355), backend-набором (1490/1490), линтером, production-сборкой, бюджетом bundle, `dotnet format`, privacy-проверкой и генерацией idempotent migration SQL; локальная PostgreSQL 17 принимает подключения на порту 5433, схема не менялась. Встроенный браузер не подключился из-за ошибки подготовки kernel assets среды Codex, поэтому визуальный контракт подтвержден DOM/workflow-тестами. Архивные документы не изменялись.
- 2026-07-14 — состав главного экрана приведен к согласованному макету: оставлены только «Тарифы и сборы», «Контрагенты», «Счётчики», «Платежи», «Отчёты», «Настройки» и «Управление фондами». Плитка «Справочники» убрана с главного экрана, но вместе с остальными административными разделами сохранена в левом меню администратора. Workflow-тест закрепляет точное количество и названия семи плиток, отсутствие административных плиток, открытие настроек с возвратом и доступ ограниченного администратора к скрытым разделам через боковую навигацию. Проверено полным frontend-набором (344/344), backend-набором (1488/1488), линтером, production-сборкой, бюджетом bundle, `dotnet format`, privacy- и migration-проверками; изменений схемы и бизнес-данных нет. Встроенный браузер не подключился из-за ошибки подготовки kernel assets среды Codex, поэтому визуальный контракт подтвержден DOM/workflow-тестами. Архивные документы не изменялись.
- 2026-07-13 — в таблицах раздела «Справочники» закреплено единое расположение пагинации: кнопки размера страницы и нумерованный переход находятся слева, счетчики — справа, native-select не используется. Колонка действий выровнена по правому краю и показывает только компактные иконки удаления или восстановления с доступными названиями и подсказками. Проверено полным frontend-набором (344/344), дополнительными workflow-сценариями удаления и восстановления, backend-набором (1488/1488), линтером, production-сборкой, бюджетом bundle, `dotnet format`, privacy- и migration-проверками; локальная PostgreSQL работает, а изменений схемы или бизнес-данных задача не требует. Встроенный браузер не подключился из-за ошибки подготовки kernel assets среды Codex, поэтому визуальный контракт подтвержден DOM/workflow-тестами. Архивные документы не изменялись.
- 2026-07-13 — панель тарифов приведена к форме заказчика: настройки регулярной услуги объединяются с каноническими строками вместо создания пяти повторных строк на каждую услугу, день и месяц оплаты находятся в одной ячейке значения, технические крестики у порогов удалены, кнопка архивации услуги временно скрыта, а нерегулярные платежи и объявленные сборы размещены рядом. Единая пагинация теперь использует кнопки размера страницы и номера страниц слева без накладывающегося native-select; тот же компонент подключен к крупным таблицам пользователей, аудита, показаний, контрагентов и отчетов. Преднаполнение наружного освещения связано с единственным соответствующим тарифом, а отдельная безопасная миграция исправляет эту связь в уже развернутой БД без перезаписи пользовательских ставок. Архивные документы не изменялись.
- 2026-07-13 — из плиточного главного меню удален дублирующий пункт «Настройки». Сам раздел, его маршрутизация и пункт в боковой навигации администратора сохранены; workflow-тест закрепляет отсутствие плитки и наличие бокового пункта. Проверено полным frontend-набором (343/343), backend-набором (1487/1487), линтером, production-сборкой, бюджетом bundle, `dotnet format`, release JSON/UTF-8 и доступностью локальной PostgreSQL; встроенный браузерный smoke-тест не запустился из-за ошибки подготовки kernel assets среды Codex, поэтому визуальное состояние подтверждено DOM/workflow-тестом.
- 2026-07-13 — единая нумерованная пагинация распространена на все большие таблицы из согласованных экранов: тарифы и услуги, нерегулярные платежи, объявленные сборы, гаражи, поставщики, отделы, персонал, общая ведомость платежей, консолидированный и остальные постраничные отчеты, справочники. Серверные таблицы сохранили ограниченную выборку, поиск и сортировку; локальные ограниченные списки используют общий `TablePagination`. Добавлены компонентные и workflow-проверки диапазона, размера страницы и нумерованных переходов; архивные документы не изменялись по прямому указанию пользователя.
- 2026-07-13 — страница настроек разделена на внутренние вертикальные вкладки по образцу системных настроек. Интеграционная вкладка целиком скрыта у всех ролей через единый временный frontend-флаг; backend-разрешения, endpoints и защищенные значения не изменялись.

## Открытые Хвосты

- Закрепить единые классы для всех новых primary/secondary/ghost/icon/destructive/link buttons по мере переноса прототипов на backend-модели.
- Для новых backend-моделей и интеграционных экранов добавлять строки coverage по тем же семействам controls до закрытия их roadmap-пунктов.
- Добавлять visual/DOM regression tests для экранов, где появятся новые реальные backend-данные сборов, персонала, фондов и чеков.
- Сделать скриншоты ключевых экранов и получить ручное подтверждение заказчика после завершения UI style audit.

## Связь С Roadmap

- Закрывает Milestone 0: `Найти все кнопки, input, textarea, select, date/month controls, tabs, dialogs и проверить стили`.
- Закрывает Milestone 8 по текущему UI: инвентарь, единые классы, destructive states, date/month/year, select, text inputs, textarea, checkbox/toggle, tabs, tables, dialogs, sidebar/topbar/dashboard, palette и DOM regression.
- Не закрывает ручную acceptance-приемку скриншотов: она остается отдельным пунктом roadmap.
