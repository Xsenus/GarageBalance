# Roadmap полного соответствия формам ГСК

Дата создания: 17.07.2026.

## Цель

Устранить все подтвержденные расхождения между GarageBalance, книгой `Формы ГСК (1).xlsx` и пояснительной запиской, а затем доказать корректность полного месячного цикла автоматическими тестами, проверкой на PostgreSQL и ручной приемкой в изолированном контуре.

Roadmap не разрешает commit, push или deployment: эти действия выполняются только по отдельному указанию пользователя.

## Исходные материалы

- `C:\Users\User16\Downloads\Формы ГСК (1).xlsx`, SHA-256 `8983B13BE3E2F99CF0467D0C9A5E9271962150D84AE3861BBE051CA9BFB6BF1F`.
- `C:\Users\User16\Downloads\Пояснительная записка в Формам ГСК (1).docx`, SHA-256 `704CA03CF22D09FF833F00A2EE8BD44A09A18C99335A5BDCBC36094BC26D797A`.
- `docs/source-analysis.md`.
- `docs/income-service-rules-decision-checklist.md`.
- `docs/expense-excel-scenario-decision-checklist.md`.
- `docs/balance-model-decision-checklist.md`.
- `docs/contractor-green-columns-decision-template.md`.
- `docs/monthly-cycle-checklist.md`.
- Код и тесты ветки `master` на 17.07.2026.

## Подтвержденная исходная точка

- [x] Изучены значения, формулы, подписи, примечания и визуальная структура всех восьми листов исходной книги.
- [x] Извлечен и изучен весь текст пояснительной записки: 86 абзацев, таблиц и изображений в документе нет.
- [x] Требования сопоставлены с frontend, backend, моделью EF Core и активными проектными документами.
- [x] Зафиксирован baseline backend: `1674/1674` теста проходят.
- [x] Зафиксирован baseline frontend: `528/528` тестов проходят; lint, production build и bundle budget проходят.

## Текущий статус

На 18.07.2026 в активной части roadmap 112 проверяемых пунктов:

- завершено — 62 из 112, или 55,36%;
- в работе — 0 из 112, или 0%;
- не начато — 48 из 112, или 42,86%;
- заблокировано — 0 из 112, или 0%;
- ожидает бизнес-решения — 0 из 112, или 0%;
- ожидает ручной приемки — 2 из 112, или 1,79%.

## Принципы реализации

- Денежные расчеты выполняются в backend на `decimal`; frontend только отображает результат и отправляет команды.
- Срок оплаты и дата наступления просрочки фиксируются на начислении при его создании. Последующее изменение настройки услуги не переписывает историю.
- Платежи распределяются по начислениям явно и проверяемо; одна общая разница `начислено − оплачено` недостаточна для расчета просрочки.
- Отмена и восстановление начисления, платежа, выплаты или распределения должны пересчитывать результат транзакционно и оставлять audit-событие.
- Месяцы и годы не должны быть зашиты в UI или backend.
- Все списки, отчеты и агрегаты, растущие с историей, фильтруются и агрегируются в PostgreSQL.
- Долг и аванс по выплатам переносятся расчетно из неизменяемой истории; отдельная операция переноса не создается, поэтому повторное открытие месяца идемпотентно и не требует audit-события.
- Проверки выполняются на синтетических данных или изолированной копии. Реальные финансовые данные не изменяются без отдельного разрешения и backup.

## Решения, которые нужно зафиксировать до изменения модели

- [x] Утверждено правило распределения поступления: сначала старейшее непогашенное начисление того же гаража и вида поступления, затем более новые; остаток хранится как нераспределенная переплата.
- [x] Утверждено условие прекращения показа годового взноса: только полное погашение начисления за соответствующий год; частичная оплата не скрывает остаток.
- [x] Утверждена семантика положительного остатка по поставщику: аванс уменьшает будущий остаток к оплате в той же паре `поставщик + вид выплаты`, но не переносится на другого поставщика или другой вид выплаты.
- [x] Утверждено бухгалтерское назначение: `Прочие оплаты` и `Прочие доходы` получают стабильные системные коды и явную связь с фондами назначения без двойного учета.
- [x] Утвержден принцип фильтрации «зеленых» колонок: фильтр определяется фактическим типом данных колонки — текст, диапазон, список или да/нет; точный инвентарь фиксируется перед реализацией UI.

## Этап 1. Даты оплаты, распределение платежей и настоящая просрочка

### Backend и доменная модель

- [x] Ввести отдельный доменный расчет `DueDate` и `OverdueDate` для ежемесячных, годовых и действующих до конца года услуг.
- [x] Реализовать правила из пояснительной записки: вода/электричество/мусор — месяц погашения плюс 30 дней; годовые членские/целевые — срок 30 июня и просрочка после льготного периода; наружное освещение — после окончания года.
- [x] Сохранять снимок срока оплаты и даты просрочки на каждом начислении, включая ручные начисления и объявленные сборы.
- [x] Ввести сущность распределения платежа по начислению с суммой распределения и уникальными ограничениями от двойного учета.
- [x] Реализовать распределение полной, частичной и избыточной оплаты согласно утвержденному правилу.
- [x] Реализовать обратный пересчет распределений при изменении, отмене и восстановлении платежа или начисления.
- [x] Рассчитывать задолженность, просрочку и режим «Только должники» только из непогашенных начислений, срок которых наступил.
- [x] Не считать нераспределенную переплату просрочкой; отрицательный итоговый баланс должен сохраняться.
- [x] Добавить audit-события для автоматического и ручного перераспределения платежа без записи чувствительных данных.

### База данных

- [x] Добавить EF Core migration для снимков срока и распределений платежей, внешние ключи, ограничения суммы и индексы для гаража, вида поступления, срока и статуса отмены.
- [x] Подготовить детерминированный backfill исторических начислений; неоднозначные строки не угадывать, а вывести в отчет сверки.
- [x] Проверить migration `up`, idempotent script и запуск на PostgreSQL; отдельно проверить существующие частичные оплаты, переплаты и отмененные операции.

### Frontend

- [x] Показывать отдельно общий баланс и действительно просроченную сумму в карточке гаража и таблице контрагентов.
- [x] Добавить доступную расшифровку просрочки по услуге, месяцу начисления, сроку и непогашенному остатку.
- [x] Сохранять серверную сортировку и фильтрацию по просрочке после пагинации и обновления данных.

### Автоматические тесты

- [x] Покрыть граничные даты до срока, в дату срока, до/в/после даты просрочки, конец месяца, високосный год и переход года.
- [x] Покрыть полную, частичную, досрочную и избыточную оплату, несколько начислений, изменение и отмену платежа.
- [x] Покрыть сортировку и режим должников так, чтобы свежий долг не считался просроченным.
- [x] Добавить PostgreSQL integration-тесты распределения, конкурентных платежей и ограничений БД.

## Этап 2. Полный месячный цикл выплат

### Backend и данные

- [x] Рассчитывать входящий остаток по каждой паре `поставщик/сотрудник + вид выплаты` из всей истории до выбранного месяца.
- [x] Разделять стоимость месяца, оплату месяца, входящий долг/аванс и исходящий долг/аванс; не маскировать перенос одной общей суммой.
- [x] Реализовать перенос неоплаченного долга и утвержденного положительного остатка в следующий месяц без создания дублей.
- [x] Сделать операцию `Добавить выплату` для аванса/выплаты без чека атомарной: одновременно увеличить стоимость и оплаченную сумму либо откатить обе записи.
- [x] Сохранить правило: банковская выплата запрещена при недостатке денег на счете, но допускается при недостатке собранного по услуге с отрицательной разницей.
- [x] Проверить инвариант `касса + банк = доступно/собрано` после оплаты, отмены, восстановления, сдачи кассы и операций фонда.
- [x] Добавить audit-события атомарной выплаты; для расчетного переноса без изменения данных отдельное audit-событие не создается.

### Frontend

- [x] Удалить жестко заданные апрель–июнь 2026 года; использовать текущий месяц и произвольный локализованный выбор месяца.
- [x] Показывать входящий и исходящий долг/аванс, стоимость, оплачено, собрано и разницу без двусмысленных знаков.
- [x] После перезагрузки подтверждать, что аванс и выплата без чека сохраняют одинаковые стоимость и оплату.
- [x] Сохранить отсутствие кнопки `Оплатить` для строк аванса и выплаты без чека.

### Автоматические тесты

- [x] Покрыть последовательность минимум из трех месяцев: полный расчет, частичный долг, аванс и следующий месяц.
- [x] Покрыть смену года, пустой месяц, отмену прошлого платежа и перерасчет последующих месяцев.
- [x] Покрыть атомарность при ошибке второй записи и конкурентной выплате.
- [x] Покрыть динамический выбор любого месяца и отсутствие календарных констант в UI.

## Этап 3. Счетчики непосредственно в форме поступлений

### Backend

- [x] Добавить команду безопасного создания/изменения показания из платежной формы с optimistic concurrency или эквивалентной защитой.
- [x] Разрешать обычное изменение показания только в текущем учетном месяце; историческая корректировка требует отдельного разрешения, причины и audit.
- [x] После изменения показания пересчитывать расход и связанное начисление в одной транзакции либо возвращать понятный конфликт, если начисление уже оплачено.
- [x] Реализовать серверное определение предыдущей оплаты электроэнергии и признак предупреждения при интервале менее 30 календарных дней.
- [x] Для воды поддержать строго ручное показание и не подставлять фиктивное значение.

### Frontend

- [x] Сделать показания воды и электроэнергии редактируемыми в строке выбранного гаража за текущий месяц.
- [x] Пересчитывать начисление только по подтвержденному сервером показанию; не оставлять оптимистическую сумму после ошибки.
- [x] Мягко подсвечивать отсутствующее обязательное показание красным, сохраняя доступные начисления по другим услугам.
- [x] Показывать неблокирующее подтверждение при оплате электроэнергии менее чем через 30 дней после предыдущей оплаты.
- [x] Обработать loading, validation, stale response, conflict, permission denied и повторную отправку.

### Автоматические тесты

- [x] Покрыть стартовое показание, последовательные месяцы, уменьшение показания, одинаковое показание и дробное значение.
- [x] Покрыть запрет обычного редактирования прошлого/будущего месяца и разрешенную аудируемую корректировку.
- [x] Покрыть интервалы оплаты 29, 30 и 31 день и продолжение операции после подтверждения.
- [x] Покрыть ручную воду, отсутствие показания, красную подсветку и независимые начисления.

## Этап 4. Годовые платежи, сборы и назначение фондов

### Годовые начисления

- [x] Связать членский, целевой взнос и наружное освещение с явным учетным годом и утвержденным правилом погашения.
- [x] После полного погашения не показывать будущие строки того же годового обязательства; частично погашенный остаток продолжать показывать.
- [x] Не создавать дубли годового начисления при повторной генерации; смена владельца не должна терять историю гаража.
- [x] Проверить сроки просрочки годовых начислений совместно с Этапом 1.

### Маршрутизация поступлений

- [x] После бизнес-решения ввести стабильные коды назначения, не зависящие от отображаемого русского названия.
- [x] Автоматически направлять нерегулярные платежи в утвержденное назначение `Прочие оплаты`.
- [ ] Автоматически направлять объявленные сборы в утвержденное назначение `Прочие доходы`.
- [ ] Согласовать операции назначения с общей доступной суммой фондов и запретить двойное распределение одного поступления.
- [ ] Реализовать отмену/восстановление поступления вместе с обратной операцией назначения и audit.

### Автоматические тесты

- [ ] Покрыть полную и частичную оплату каждого годового вида, переплату и новый календарный год.
- [ ] Покрыть сбор для всех гаражей и выбранных участников, изменение состава до генерации и запрет изменения исторического состава после начисления.
- [ ] Покрыть маршрутизацию, отмену, восстановление и недостаточный доступный остаток фонда.

## Этап 5. Карточка гаража и фильтры контрагентов

- [ ] Добавить телефон действующего владельца в DTO/проекцию гаража без загрузки лишних персональных данных.
- [ ] Показывать телефон в шапке платежной формы и корректно обновлять его после смены владельца.
- [ ] После утверждения зеленых колонок реализовать серверные фильтры с пагинацией, сортировкой и явным сбросом.
- [ ] Реализовать одинаковые фильтры в UI, доступные с клавиатуры и не конфликтующие с режимом должников.
- [ ] Покрыть пустое значение, несколько фильтров, архивные записи, смену страницы, stale response и permission denied.

## Этап 6. Отчеты: множественный выбор и сортировка

### Backend

- [ ] Для каждого затронутого отчета утвердить allowlist полей сортировки и единый контракт `sortBy/sortDirection`.
- [ ] Выполнять сортировку, множественные фильтры и пагинацию в PostgreSQL; экспорт должен использовать те же фильтры и порядок, что экран.
- [ ] Поддержать выбор одного, нескольких или всех гаражей, владельцев, поставщиков, сотрудников и видов операций согласно форме.
- [ ] Сохранить корректные итоги по полной выборке независимо от страницы и группировки.

### Frontend

- [ ] Заменить одиночные текстовые фильтры гаража и контрагента на доступный множественный выбор с режимом `Все`.
- [ ] Добавить сортируемые заголовки, видимое направление, сброс и стабильный порядок при равных значениях.
- [ ] Сохранять фильтры и сортировку при пагинации, группировке и экспорте.
- [ ] Проверить empty, loading, error, stale response и permission denied для каждого отчета.

### Автоматические тесты

- [ ] Покрыть одиночный, множественный и пустой выбор, неизвестные идентификаторы и архивные справочники.
- [ ] Покрыть сортировку по каждому разрешенному полю и отклонение неизвестного поля.
- [ ] Сверить экран, XLSX и PDF на одинаковом наборе синтетических операций и итогах.
- [ ] Добавить PostgreSQL query/performance-проверку реалистичного объема истории.

## Этап 7. Сквозная проверка качества и данных

### Автоматический gate

- [ ] Запустить focused-тесты каждого этапа до полного набора.
- [ ] Запустить полный backend suite и зафиксировать точное число тестов и coverage gate.
- [ ] Запустить полный frontend suite и зафиксировать точное число тестов и coverage gate.
- [ ] Запустить frontend lint, production build и bundle budget.
- [ ] Запустить `dotnet build`, `dotnet format --verify-no-changes`, privacy/secret tests и `git diff --check`.
- [ ] Сгенерировать idempotent migration SQL и применить все migrations к чистой и обновляемой PostgreSQL-базам.
- [ ] Проверить UTF-8 измененных текстовых файлов и JSON release notes.
- [ ] Выполнить performance-проверки списков должников, месячной ведомости и отчетов.

### Сквозные сценарии на изолированной PostgreSQL

- [ ] Сценарий гаража за 14 месяцев: показания, ежемесячные начисления, годовые взносы, частичные оплаты, переплата и просрочка.
- [ ] Сценарий поставщика за три месяца: счет, частичная выплата, долг, аванс, перенос и отмена.
- [ ] Сценарий сборов и фондов: все гаражи, выбранные гаражи, поступление, распределение, изъятие, отмена и восстановление.
- [ ] Сверить все восемь вкладок отчетов с независимыми контрольными суммами.
- [ ] Перезагрузить UI между ключевыми шагами, чтобы исключить результат только из локального React state.
- [ ] Проверить роли без прав чтения/записи/экспорта и наличие серверного запрета.
- [ ] Проверить audit trail всех финансовых корректировок без утечки персональных и секретных данных.

### Документация и выпуск

- [ ] Обновить активные decision-checklist после утверждения правил, не меняя `docs/archive/`.
- [ ] Обновить `docs/monthly-cycle-checklist.md` и пользовательские инструкции по закрытию месяца.
- [ ] Добавить понятные пользователю записи `Что нового` по просрочке, выплатам, счетчикам, фондам и отчетам.
- [ ] Описать migration/backfill, резервное копирование, rollback и сверку после обновления.
- [ ] Обновлять раздел `История выполнения` этого roadmap после каждого завершенного этапа.

### Deployment и приемка

- [ ] Перед будущим deployment получить отдельное разрешение пользователя на commit/push/deploy.
- [ ] После разрешения дождаться успешного GitHub Actions и deployment в staging.
- [ ] Проверить на VPS service, nginx, `/health`, frontend и task-specific smoke-сценарии без изменения реальных данных.
- [acceptance] Провести ручную приемку бухгалтером/председателем на изолированной копии реальных данных после backup.
- [acceptance] Сверить контрольный закрытый месяц с исходными формами и подписать список допустимых отличий интерфейса.

## Definition of Done

Roadmap считается завершенным только когда:

- все пункты реализации и автоматической проверки имеют статус `[x]`;
- ни одного `[decision]` или `[!]` не осталось;
- просрочка подтверждается на граничных датах и частичных оплатах;
- месячный долг/аванс поставщика сохраняется после смены месяца и перезагрузки;
- счетчики редактируются по утвержденным правилам непосредственно в форме поступлений;
- годовые обязательства и фонды ведут себя согласно утвержденной бухгалтерской модели;
- экранные отчеты и их XLSX/PDF дают одинаковые фильтры, порядок и итоги;
- миграция проверена на чистой и обновляемой PostgreSQL;
- полные тестовые, quality, privacy и performance gates проходят;
- обе ручные приемки отмечены `[x]` после отдельного разрешения на работу с изолированной копией данных.

## Риски и открытые вопросы

- Исторические платежи могут не содержать достаточно данных для однозначного распределения по начислениям; нужен отчет сверки вместо безусловного автоматического исправления.
- Изменение модели просрочки затронет должников, карточки гаражей, отчеты и возможно начальные остатки одновременно.
- Нельзя смешивать бухгалтерский аванс поставщику, свободные деньги на банковском счете и средства конкретной услуги.
- Автоматическое назначение фонда должно исключать двойной учет поступления в доступной сумме.
- PDF может иметь иной визуальный layout, но фильтры, порядок строк и денежные итоги должны совпадать с экраном и XLSX.
- Проверка на реальных данных и deployment требуют отдельного разрешения пользователя.

## История выполнения

- 19.07.2026 — завершена автоматическая маршрутизация нерегулярных начислений Этапа 4. Форма гаража теперь загружает активные шаблоны из справочника «Нерегулярные платежи», не позволяет вручную подменить сумму и вызывает отдельную серверную команду. Сервер повторно проверяет активность и сумму шаблона, находит системное назначение исключительно по стабильному коду `other_payments`, требует его явную связь с фондом и сохраняет в начислении ссылку на конкретный шаблон. Разные шаблоны разрешены одному гаражу в одном месяце, повтор того же шаблона блокируется приложением и частичным уникальным индексом PostgreSQL; архивирование больше не зависит от совпадения русских названий. Строки формы хранят фактический `incomeTypeId`, поэтому одиночная и полная оплата отображаемого разового начисления направляется в `Прочие оплаты`. Добавлена migration `RouteIrregularAccrualsToOtherPayments`, защищенный endpoint, audit `finance.irregular_accrual_created` и запись «Что нового» `0.739.0`. Первый полный backend-прогон выявил два устаревших guard-теста, первый полный frontend-прогон — пропущенный результат API при переносе долга; проверки и runtime-ошибка исправлены, затронутые повторы прошли `12/12` и `2/2`. Итоговые проверки: backend на временной PostgreSQL 17 — `1792/1792`, пропущено `0`, coverage `96,73%` строк / `72,01%` ветвей; frontend — `561/561`, coverage `81,68% / 72,82% / 77,50% / 82,24%`; TypeScript, lint, production build, bundle budget (`230,5/260,0 KiB`), Release-сборка без предупреждений, `dotnet format`, privacy `760` файлов, JSON, строгая UTF-8/no BOM и `git diff --check` успешны. На чистой временной базе применены все `63/63` migrations, повторный update не внес изменений, EF-модель синхронна, подтверждены новая колонка и три индекса с `IrregularPaymentId`, idempotent SQL сформирован (`189968` байт). Прогресс roadmap — `64/112` (`57,14%`).

- 18.07.2026 — завершена основа маршрутизации поступлений Этапа 4: системные назначения `Прочие оплаты` (`other_payments`) и `Прочие доходы` (`other_income`) больше не зависят от отображаемого русского названия и явно связаны внешним ключом с системным фондом `Прочее`. Migration безопасно использует существующие русские записи с произвольными идентификаторами либо создает недостающие детерминированные записи, восстанавливает системный статус, фиксирует изменение в audit и запрещает удаление связанного фонда. Автоматическое создание операций назначения остается отдельными следующими пунктами, поэтому двойной учет пока не вводится. Добавлена запись «Что нового» `0.738.0`. PostgreSQL-тесты проверяют чистую установку, обновление существующего каталога, поиск после изменения отображаемых названий и физический `RESTRICT`; старые fixtures полной очистки фондов адаптированы к новой связи. Первый полный backend-прогон выявил BOM в трех сгенерированных EF-файлах и три старые fixtures, удалявшие связанный фонд; кодировка нормализована, fixtures очищают только тестовые связи, целевые повторы прошли `1/1`, `2/2` и `6/6`. Итоговые проверки: полный backend на временной PostgreSQL 17 — `1785/1785`, пропущено `0`, coverage `90,33%` строк / `73,07%` ветвей; frontend — `561/561`, coverage `81,71% / 72,79% / 77,61% / 82,26%`; lint, TypeScript, production build, bundle budget (`230,4/260,0 KiB`), Release-сборка без предупреждений, `dotnet format`, privacy `757` файлов, JSON и строгая UTF-8/no BOM успешны. На чистой базе применены все `62/62` migrations, повторный update не внес изменений; подтверждены обе связи с фондом, незакрытых изменений EF-модели нет, idempotent SQL сформирован (`187702` байта). В журнале PostgreSQL только ожидаемые первичные проверки отсутствующей migration history, намеренные нарушения ограничений тестами, отклонение удаления связанного фонда и диагностические ошибки исправленных прогонов; `PANIC` и ошибок итоговой миграции нет. Прогресс roadmap — `63/112` (`56,25%`).

- 18.07.2026 — завершена проверка сроков просрочки годовых начислений Этапа 4 совместно с правилами Этапа 1. Исправлен разрыв ручного и позднего формирования: членский/целевой взнос теперь сохраняет срок 30 июня и просрочку с 31 июля своего учетного года, наружное освещение — срок 31 декабря и просрочку с 1 января следующего года; дата больше не переносится на следующий год и не подменяется месячным сроком, если начисление создано после срока. Связанная годовая настройка имеет приоритет, при ее отсутствии стабильный системный код дает безопасный срок; запрос настроек ограничен двумя кандидатами до materialization. PostgreSQL-сценарий на реально примененном системном каталоге подтверждает границы 30/31 июля и 1 января, частичный остаток членского взноса, исключение полностью оплаченного целевого взноса и включение наружного освещения. Добавлена запись «Что нового» `0.737.0`, обновлен decision-checklist. Проверки: focused backend/PostgreSQL/release/roadmap `40/40`, дополнительные исправленные fixtures `2/2`; полный backend на временной PostgreSQL 17 — `1783/1783`, пропущено `0`, coverage `90,33%` строк / `73,07%` ветвей; frontend `561/561`, coverage `81,70% / 72,83% / 77,58% / 82,26%`; lint, TypeScript, production build, bundle budget (`230,4/260,0 KiB`), Release-сборка без предупреждений, `dotnet format` и privacy `754` файла успешны. Первый полный backend-прогон выявил два старых месячных ожидания на годовом fixture; они разделены по фактической периодичности, повтор прошел полностью. Изменения схемы не потребовались: на чистой PostgreSQL применена `61/61` migration, повторный update не внес изменений, незакрытых изменений EF-модели нет, idempotent SQL сформирован (`181632` байта). В журнале PostgreSQL только ожидаемые первичные проверки отсутствующей migration history, намеренные нарушения ограничений интеграционными тестами и единичное Windows-сообщение о резервировании shared memory при параллельном suite; сервер остался доступен, ошибок годовых сроков, `FATAL` и `PANIC` нет. Прогресс roadmap — `62/112` (`55,36%`).

- 18.07.2026 — завершена идемпотентность регулярных годовых начислений Этапа 4. Для одного гаража и вида поступления генератор учитывает уже существующее обязательство во всем учетном году, а не только в выбранном месяце; параллельные PostgreSQL-запуски сериализуются по тем же ключам `гараж + вид поступления`, новый календарный год остается отдельным. Создание, изменение и восстановление регулярного годового начисления возвращают понятный конфликт при дубле. Финансовая история остается связанной с гаражом после смены текущего владельца; исторические дубли одного гаража считаются как один гараж и не мешают сформировать обязательство для пропущенных гаражей. Добавлена запись «Что нового» `0.736.0`, обновлен decision-checklist. Проверки: focused backend/PostgreSQL/release/roadmap `28/28` и дополнительный edge-case с PostgreSQL `4/4`; полный backend на временной PostgreSQL 17 — `1776/1776`, пропущено `0`, coverage `90,27%` строк / `72,67%` ветвей; frontend после одного невоспроизведенного нагрузочного сбоя старого сценария поставщика прошел изолированно `1/1` и полным повтором `561/561`, coverage `81,70% / 72,79% / 77,58% / 82,26%`; lint, TypeScript, production build и bundle budget (`230,4/260,0 KiB`) успешны. Первый полный backend-прогон выявил три устаревших ожидания месячных fixtures и лишнюю раннюю загрузку владельцев; fixtures разделены по периодичности, запрос блокировки ограничен идентификаторами, performance-guard усилен. Изменения схемы не потребовались: на чистой PostgreSQL применена `61/61` migration, повторный update не внес изменений, незакрытых изменений EF-модели нет, idempotent SQL сформирован (`181632` байта). В журнале PostgreSQL только ожидаемые первичные проверки отсутствующей migration history, намеренные нарушения ограничений интеграционными тестами и один ошибочный read-only подсчет history; `FATAL`, `PANIC` и ошибок годовой генерации нет. Прогресс roadmap — `61/112` (`54,46%`).

- 18.07.2026 — завершено помесячное отображение годовых обязательств Этапа 4. Единственное бухгалтерское начисление членского/целевого взноса или наружного освещения проецируется в форму поступлений на каждый месяц своего учетного года до полного погашения: частичная оплата сохраняет остаток, полная скрывает будущие строки, отмена платежа возвращает их, восстановление снова скрывает. Полный расчет дедуплицирует строки одного обязательства и отдельно учитывает только входящий долг, который ими не представлен; итоговые начисления и поступления не дублируются. Добавлена запись «Что нового» `0.735.0`. Проверки: focused backend `15/15`, PostgreSQL integration `1/1`, focused frontend `3/3`; полный backend с PostgreSQL `1772/1772`, пропущено `0`, coverage `90,24%` строк / `72,59%` ветвей; frontend `561/561`, coverage `81,70% / 72,83% / 77,58% / 82,26%`; lint, TypeScript, production build, bundle budget, Release-сборка без предупреждений, `dotnet format`, privacy `754` файла, JSON и строгая UTF-8 — успешно. Изменения схемы не потребовались: на чистой PostgreSQL 17 применена `61/61` migration, повторный update не внес изменений, незакрытых изменений EF-модели нет, idempotent SQL сформирован (`181632` байта). В журнале PostgreSQL только ожидаемые первичные проверки отсутствующей migration history, намеренные нарушения ограничений интеграционными тестами и два ошибочных read-only запроса проверки history; `FATAL`, `PANIC` и ошибок годового расчета нет. Прогресс roadmap — `60/112` (`53,57%`).

- 18.07.2026 — годовые членский и целевой взносы и наружное освещение Этапа 4 связаны с явным учетным годом. Начисления системных видов `membership`, `target`, `outdoor_lighting` получают год из расчетного месяца во всех путях создания и изменения; обычные ежемесячные услуги оставляют поле пустым. Утвержденное правило закреплено доменной политикой: только распределение всей положительной суммы считается полным погашением, частичная оплата год не закрывает. Год показан отдельной колонкой в таблице начислений и включен в audit; исторические годовые строки получают значение безопасным migration-backfill, добавлены индекс и ограничение диапазона. Добавлена запись «Что нового» `0.734.0`, обновлен decision-checklist. Проверки: focused backend `13/13`, PostgreSQL `1/1`, frontend `20/20`; полный backend на временной PostgreSQL 17 — `1770/1770`, пропущено `0`, coverage `90,14%` строк / `72,52%` ветвей; frontend — `560/560`, coverage `81,72% / 72,79% / 77,66% / 82,25%`; lint, TypeScript, Release-сборка, production build, bundle budget (`230,2/260,0 KiB`), `dotnet format`, privacy (`754` файла), строгая UTF-8/no BOM — успешно. На обновляемой PostgreSQL migration заполнила `membership=2025`, оставила `water=NULL`; на чистой базе применена вся схема, ограничение отклонило год `1800`, незакрытых изменений EF-модели нет, idempotent SQL сформирован. Frontend console содержит только известные сообщения jsdom о неподдерживаемой навигации документа. Прогресс roadmap — `59/112` (`52,68%`).

- 18.07.2026 — завершено сквозное автоматическое покрытие ручной воды, отсутствующего показания и независимых начислений Этапа 3. PostgreSQL-сценарий использует системные виды поступлений из реально примененных migrations: пустое ручное значение отклоняется без записи, первое показание требует реальный стартовый остаток, последующие значения берут только предыдущую сохраненную ручную запись; до ввода текущего показания ведомость содержит пустую строку воды с нулевой суммой и независимый членский взнос `700`, после ручного ввода воды `18,5` фиксирует расход `0,125`, а членский взнос остается `700`. Frontend workflow проверяет пустое поле, `aria-invalid`, красную ячейку, доступную подсказку, ручное сохранение, исчезновение подсветки, серверные расход `5` и начисление `250`, а также доступность и неизменность членского взноса `700`; отдельный guard закрепляет светло-красный фон и красный текст подсказки. Первый focused PostgreSQL-прогон выявил дублирование системного справочника в тестовой fixture; fixture переведена на существующие migration-записи, повтор `3/3` и полный suite прошли. Производственная логика не менялась, поэтому новая запись «Что нового» не требуется. Проверки: focused backend/PostgreSQL `3/3`, frontend `2/2`; полный backend на временной PostgreSQL 17 — `1759/1759`, пропущено `0`, coverage `90,13%` строк / `72,50%` ветвей; frontend — `560/560`, coverage `81,73% / 72,81% / 77,69% / 82,25%`; lint, TypeScript, Linux publish, production build, bundle budget (`230,1/260,0 KiB`), `dotnet format`, privacy (`749` файлов), `git diff --check` и строгая UTF-8/no BOM — успешно. На чистой временной PostgreSQL применены все `60` migrations, незакрытых изменений EF-модели нет, idempotent SQL сформирован (`179925` байт). В журнале нет `PANIC`; `ERROR` относятся к первичным проверкам migration history, намеренным нарушениям ограничений интеграционными тестами и исправленному дублю fixture, `FATAL` — к ожидаемой проверке еще не созданной чистой базы. Frontend console содержит только известные сообщения jsdom о неподдерживаемой навигации документа. Прогресс roadmap — `58/112` (`51,79%`).

- 18.07.2026 — завершена автоматическая проверка границ редактирования показаний Этапа 3. Service- и PostgreSQL-сценарии теперь явно подтверждают, что обычные попытки изменить прошлый или будущий месяц оставляют сохраненные значения без изменений и не создают ложное событие `finance.meter_reading_updated`; отдельная историческая команда меняет только прошлую запись, требует специальное право и непустую причину, обновляет версию и сохраняет единственное аудируемое событие с исполнителем, причиной и diff. Существующий frontend workflow подтверждает отдельный диалог, запрет будущего месяца и отсутствие команды без права. Первый полный frontend-прогон выявил реальный нагрузочный дефект старого сценария управления пользователями: таймер успешного уведомления запускался до завершения обновления таблицы. Уведомления создания, изменения, отключения пользователя и изменения прав перенесены после синхронизации, а управляемая задержка запроса закрепляет порядок без увеличения таймаута; добавлена запись «Что нового» `0.733.0`. Проверки: focused service `3/3`, PostgreSQL `1/1`, frontend/API `3/3`, детерминированный user workflow `1/1`; итоговый полный backend на временной PostgreSQL 17 — `1759/1759`, пропущено `0`, coverage `90,13%` строк / `72,50%` ветвей; frontend — `559/559`, coverage `81,72% / 72,80% / 77,66% / 82,25%`; lint, TypeScript, Linux publish, production build, bundle budget (`230,1/260,0 KiB`), `dotnet format`, privacy (`749` файлов), release JSON, `git diff --check` и строгая UTF-8/no BOM — успешно. На чистой временной PostgreSQL применены все `60` migrations, незакрытых изменений EF-модели нет, idempotent SQL сформирован (`179925` байт). В журнале нет `PANIC`; `ERROR` относятся к первичным проверкам migration history, намеренным нарушениям ограничений интеграционными тестами и одной исправленной диагностике регистра таблицы, `FATAL` — к ожидаемым обращениям EF к еще не созданной чистой базе после диагностического прерывания. Frontend console содержит только известные сообщения jsdom о неподдерживаемой навигации документа. Прогресс roadmap — `57/112` (`50,89%`).

- 18.07.2026 — завершена автоматическая матрица граничных значений показаний Этапа 3. Аудит существующих тестов подтвердил отдельное покрытие стартового значения, последовательных месяцев, запрета уменьшения и округления до трех знаков; закрыт отсутствовавший случай одинакового показания с нулевым расходом. PostgreSQL-интеграция расширена единой цепочкой: реальное стартовое значение `10`, первый месяц `15`, следующий `18`, одинаковое `18` с расходом `0`, дробное `18,375` с расходом `0,375` и отклоненное уменьшение `18,25` без записи пятой строки. Обновлен evidence сценария оператора; запись «Что нового» не добавлялась, потому что производственная логика и интерфейс не менялись. Проверки: focused service `5/5`, PostgreSQL `1/1`; полный backend на временной PostgreSQL 17 — `1759/1759`, пропущено `0`, coverage `90,13%` строк / `72,50%` ветвей; frontend — `559/559`, coverage `81,77% / 72,83% / 77,69% / 82,29%`; lint, TypeScript, Release build/publish (`24` файла), production build, bundle budget, `dotnet format`, privacy (`749` файлов), строгая UTF-8/no BOM — успешно. На чистой временной PostgreSQL применены все `60` migrations, повторный update сохранил `60/60`, незакрытых изменений EF-модели нет, idempotent SQL сформирован (`179925` байт). В журнале нет `FATAL` и `PANIC`; `ERROR` относятся к ожидаемым первичным проверкам отсутствующей migration history и намеренным нарушениям ограничений интеграционными тестами. Frontend console содержит только известные сообщения jsdom о неподдерживаемой навигации документа. Прогресс roadmap — `56/112` (`50,00%`).

- 18.07.2026 — завершена обработка рабочих состояний сохранения показаний в форме поступлений (Frontend Этапа 3). Короткая операция показывает вращающийся индикатор и доступное сообщение, блокирует повторное нажатие только в своей строке; пустое и отрицательное значение отклоняются до API. Финансовый API сохраняет код и HTTP-статус ошибки: при `meter_reading_conflict` ведомость получает актуальные ID/версию с сервера, но оставляет введённый черновик и сообщение, поэтому повторная отправка действительно использует новую версию. Ответ сохранения или загрузки от уже покинутого гаража больше не заменяет активную ведомость; пользователи без `payments.write` видят подтверждённое показание без редактора. Добавлена запись «Что нового» `0.732.0`. Проверки: focused frontend `4/4`; backend на временной PostgreSQL 17 — `1758/1758`, пропущено `0`, coverage `90,13%` строк / `72,50%` ветвей; финальный frontend — `559/559`, coverage `81,73% / 72,80% / 77,69% / 82,25%`; lint, TypeScript, Release build/publish (`24` файла), production build, bundle budget, `dotnet format`, privacy (`749` файлов), release JSON и строгая UTF-8/no BOM — успешно. Первый полный frontend coverage-прогон дал единичный сбой ожидания несвязанного старого диалога тарифа; изолированный повтор `1/1` и следующий полный прогон `559/559` прошли без изменения теста или таймаута. Изменения схемы не потребовались: на чистой временной PostgreSQL применены все `60` migrations, повторный update сохранил `60/60`, незакрытых изменений EF-модели нет, idempotent SQL сформирован (`179925` байт). Журнал временного PostgreSQL не содержит записей; диагностические `ERROR` EF относятся к ожидаемой первичной проверке отсутствующей migration history. Frontend console содержит только известные сообщения jsdom о неподдерживаемой навигации документа. Прогресс roadmap — `55/112` (`49,11%`).

- 18.07.2026 — завершено неблокирующее подтверждение ранней оплаты электроэнергии и связанное автоматическое покрытие Этапа 3. Перед созданием операции из строки платежной таблицы frontend запрашивает серверный preview только для активного системного вида `electricity`. При интервале менее 30 календарных дней доступный модальный диалог показывает дату предыдущей оплаты и число прошедших дней: оператор может вернуться к неизмененной сумме без записи операции либо выбрать `Все равно провести`; подтвержденная отправка не запускает повторное предупреждение, а фокус возвращается в строку после завершения операции. Серверные unit- и PostgreSQL-тесты уже закрепляют границы `29/30/31`, а frontend workflow теперь подтверждает preview-запрос на 29-й день, отсутствие операции до решения, отмену, повторный вызов, успешное продолжение и восстановление фокуса. Добавлена запись «Что нового» `0.731.0`. Проверки: focused frontend `1/1`; полный backend на временной PostgreSQL 17 — `1758/1758`, пропущено `0`, coverage `90,16%` строк / `72,53%` ветвей; финальный frontend — `557/557`, coverage `81,69% / 72,67% / 77,60% / 82,22%`; lint, TypeScript, Release build/publish (`24` файла), production build, bundle budget, `dotnet format`, privacy (`749` файлов), release JSON и строгая UTF-8/no BOM — успешно. Изменения схемы не потребовались: на чистой временной PostgreSQL применены все `60` migrations, повторный update не внес изменений, незакрытых изменений EF-модели нет, idempotent SQL сформирован (`179925` байт). В журнале нет `PANIC` и ошибок preview/подтверждения; `ERROR` относятся к первичным проверкам отсутствующей migration history и намеренным нарушениям ограничений интеграционными тестами, два `FATAL` — к диагностике готовности без роли `User16` и ожидаемой конкурентной проверке уже удаленной тестовой БД. Frontend console содержит только известные сообщения jsdom о неподдерживаемой навигации документа. Прогресс roadmap — `54/112` (`48,21%`).

- 18.07.2026 — завершена мягкая подсветка отсутствующих обязательных показаний воды и электроэнергии в форме поступлений (Frontend Этапа 3). Серверная ведомость теперь в том же единственном SQL-запросе получает активные системные виды счетчиков и добавляет их строки за текущий месяц даже до появления показания и начисления; архивные виды исключаются. Пустая ячейка имеет светло-красный фон, доступную подсказку `Введите обязательное показание` и `aria-invalid`, при этом независимое членское начисление, его сумма и поле платежа остаются видимыми и доступными. После сохранения показания подсказка исчезает по подтвержденным данным повторно загруженной ведомости. Добавлена запись «Что нового» `0.730.0`. Проверки: focused backend `9/9` и performance guard `1/1`; полный backend на временной PostgreSQL 17 — `1758/1758`, пропущено `0`, coverage `90,13%` строк / `72,50%` ветвей; frontend — `557/557`, coverage `81,63% / 72,66% / 77,54% / 82,17%`; lint, TypeScript, Release build/publish (`24` файла), production build, bundle budget, `dotnet format`, privacy (`749` файлов), release JSON и строгая UTF-8/no BOM — успешно. Первый полный backend-прогон выявил устаревший performance guard на пять объединенных запросов; guard обновлен с явной проверкой шестого источника каталога, после чего полный повтор прошел. Изменения схемы не потребовались: на чистой временной PostgreSQL применены все `60` migrations, повторный update не внес изменений, незакрытых изменений EF-модели нет, idempotent SQL сформирован (`179925` байт). В журнале PostgreSQL нет `FATAL` и `PANIC`; `ERROR` относятся к первичным проверкам отсутствующей migration history, намеренным нарушениям ограничений интеграционными тестами и одной исправленной диагностической команде с неверным регистром таблицы. Frontend console содержит только известные сообщения jsdom о неподдерживаемой навигации документа. Прогресс roadmap — `52/112` (`46,43%`).

- 17.07.2026 — завершена защита формы поступлений от оптимистического пересчёта начисления по неподтверждённому показанию (Frontend Этапа 3). UI сохраняет введённое значение только как черновик и не рассчитывает расход, начисление или долг самостоятельно: до ответа сервера строка остаётся на прежних подтверждённых `180.00`, после успешного сохранения и повторной загрузки ведомости показывает серверные `240.00`, а при конфликте черновик `93` не превращает сумму в `250.00`; подтверждённый повтор переводит её в `250.00`. Добавлена отдельная запись «Что нового» `0.729.0`. Проверки: focused frontend workflow `1/1`; полный backend с PostgreSQL `1757/1757`, пропущено `0`, coverage `87,55%` строк / `71,57%` ветвей; повторный полный frontend `557/557`, coverage `81,62% / 72,66% / 77,51% / 82,17%`; lint, TypeScript, Release-сборка, production build, bundle budget, `dotnet format`, privacy `749` файлов, JSON и строгая UTF-8 — успешно. Первый полный frontend-прогон дал единичный таймаут несвязанного старого сценария отключения пользователя; изолированный повтор `1/1` и следующий полный прогон `557/557` прошли чисто, изменения теста или таймаута не потребовались. Изменения схемы не потребовались: на чистой временной PostgreSQL 17 применены все `60` migrations, повторный update не внёс изменений, незакрытых изменений EF-модели нет, idempotent SQL сформирован (`179925` байт). В журнале PostgreSQL нет `FATAL`, `PANIC` и ошибок подтверждённого пересчёта; `ERROR` относятся к первичной проверке отсутствующей migration history и намеренным нарушениям ограничений интеграционными тестами. Frontend console содержит только известные сообщения jsdom о неподдерживаемой навигации документа. Прогресс roadmap — `51/112` (`45,54%`).

- 17.07.2026 — завершено редактирование показаний воды и электроэнергии в строках формы поступлений за текущий месяц (Frontend Этапа 3). Ведомость теперь возвращает идентификатор, версию и дату выбранного показания; текущая строка позволяет создать новое или безопасно изменить существующее показание по Enter либо кнопке сохранения, а прошлые месяцы и пользователи без права `payments.write` остаются в режиме чтения. После подтверждения сервером форма обновляет расход и повторно загружает ведомость с пересчитанными начислениями; пустой/некорректный ввод, конфликт и повторная отправка покрыты UI-сценарием. Общий `MeterReadingInput` сохраняет точность показаний и единое доступное поведение в форме поступлений и годовой таблице. Добавлена запись «Что нового» `0.728.0`. Проверки: focused backend worksheet `8/8`, focused frontend `6/6` и основной workflow `1/1`, PostgreSQL worksheet `1/1`; полный backend с PostgreSQL `1757/1757`, пропущено `0`, coverage `87,55%` строк / `71,57%` ветвей; frontend `557/557`, coverage `81,63% / 72,66% / 77,54% / 82,17%`; lint, TypeScript, Debug/Release-сборка, production build, bundle budget, `dotnet format`, privacy `748` файлов, JSON и строгая UTF-8 — успешно. Изменения схемы не потребовались: на чистой временной PostgreSQL 17 применены все `60` migrations, повторный update не внёс изменений, незакрытых изменений EF-модели нет, idempotent SQL сформирован (`179925` байт). В журнале временной PostgreSQL нет `PANIC` и прикладных ошибок новой ведомости; `ERROR` относятся к первичной проверке отсутствующей migration history и намеренным нарушениям ограничений интеграционными тестами, один `FATAL` — к ожидаемой конкурентной проверке уже удалённой временной БД. Frontend console содержит только известные сообщения jsdom о неподдерживаемой навигации документа. Прогресс roadmap — `50/112` (`44,64%`).

- 17.07.2026 — завершено backend-правило строго ручного показания воды Этапа 3. Поле текущего показания стало nullable на транспортном уровне и обязательным по API-контракту, поэтому пропущенное JSON-поле сохраняет состояние `нет значения`, а не превращается в `0`; service-защита одинаково действует при создании, сохранении из платежной формы, обычном изменении и исторической корректировке. Первое показание воды без предыдущего месяца теперь требует реальное стартовое значение из карточки гаража и не создает строку, начисление или audit от фиктивного нуля; последующие месяцы используют сохраненное предыдущее ручное показание, даже если стартовое поле позже очищено. Добавлены запись «Что нового» `0.727.0`, contract/controller/service-тесты и реальный PostgreSQL integration-сценарий. Проверки: focused meter-reading `66/66`, итоговый focused contract/service/controller `7/7`, PostgreSQL `1/1`; полный backend с PostgreSQL `1756/1756`, пропущено `0`, coverage `90,09%` строк / `72,40%` ветвей; frontend `555/555`, coverage `81,60% / 72,52% / 77,44% / 82,15%`; lint, TypeScript, Release-сборка, production build, bundle budget, `dotnet format`, privacy `746` файлов, JSON и строгая UTF-8 — успешно. Один промежуточный объединенный focused-прогон завершился сбоем процесса `testhost` после двух успешных тестов без падения сценария; два раздельных повтора `2/2 + 2/2`, итоговый focused и оба полных backend-прогона прошли чисто. Изменения схемы не потребовались: на чистой PostgreSQL 17 применены все `60` migrations и повторный update не внес изменений; обновляемая база поднята с предпоследней migration до `60/60`, незакрытых изменений EF-модели нет, idempotent SQL сформирован (`179925` байт). В журнале PostgreSQL нет прикладных ошибок ручной воды и `PANIC`; единственный `FATAL` создан проверкой готовности без явного имени БД, остальные `ERROR` относятся к ожидаемым первичным запросам migration history, намеренным нарушениям ограничений интеграционными тестами и одному ошибочному read-only подсчету с потерянным регистром. Frontend console содержит только известные сообщения jsdom о неподдерживаемой навигации документа. Прогресс roadmap — `49/112` (`43,75%`).

- 17.07.2026 — завершено серверное определение предыдущей оплаты электроэнергии Этапа 3. Новый защищенный preview-endpoint по гаражу, виду поступления и фактической дате операции находит последнюю активную оплату того же вида, исключает отмененные и будущие операции, а при редактировании — саму изменяемую запись. Для системного вида `Электроэнергия` он возвращает дату предыдущей оплаты, число календарных дней и признак подтверждения только для интервала `0–29` дней; границы `30` и `31` день предупреждения не требуют. Платеж не блокируется и обязательный комментарий не вводится; типизированный frontend-клиент подготовлен для отдельного следующего пункта с неблокирующим диалогом. Добавлены запись «Что нового» `0.726.0`, service/controller/API-тесты и реальный PostgreSQL integration-сценарий. Проверки: focused backend `76/76`, PostgreSQL `1/1`, focused frontend `226/226`; полный backend с PostgreSQL `1750/1750`, пропущено `0`, coverage `90,08%` строк / `72,39%` ветвей; frontend `555/555`, coverage `81,60% / 72,48% / 77,44% / 82,15%`; lint, TypeScript, Release-сборка, production build, bundle budget, `dotnet format`, privacy `745` файлов, JSON и строгая UTF-8 — успешно. Изменения схемы не потребовались: на чистой PostgreSQL 17 применены все `60` migrations и повторный update не внес изменений; обновляемая база поднята с предпоследней migration до `60/60`, незакрытых изменений EF-модели нет, idempotent SQL сформирован (`179925` байт). В журнале PostgreSQL нет `FATAL`, `PANIC` и ошибок нового запроса; остальные `ERROR` относятся к ожидаемым проверкам отсутствующей migration history и намеренным нарушениям ограничений интеграционными тестами. Прогресс roadmap — `48/112` (`42,86%`).

- 17.07.2026 — завершен атомарный пересчет связанного начисления после изменения показания Этапа 3. Для активного регулярного начисления система находит тариф того же вида счетчика и месяца, пересчитывает сумму по подтвержденному сервером расходу и сохраняет показание, начисление, перераспределение и два audit-события одним `SaveChanges`; при любой активной частичной или полной оплате возвращается `meter_reading_accrual_paid`/HTTP 409 без изменения версии показания и суммы. Отдельный audit `finance.accrual_updated_from_meter_reading` хранит сумму `было/стало`, добавлены пользовательские правила и выпуск «Что нового» `0.725.0`. Проверки: focused backend/documentation `107/107`, focused PostgreSQL `1/1`; полный backend с PostgreSQL `1740/1740`, пропущено `0`, coverage `90,05%` строк / `72,35%` ветвей; frontend `554/554`, coverage `81,60% / 72,52% / 77,44% / 82,15%`; lint, TypeScript, production build, bundle budget, Release-сборка без предупреждений, `dotnet format`, privacy `744` файлов, JSON и строгая UTF-8 — успешно. Изменения схемы не потребовались: на чистой PostgreSQL 17 применены все `60` migrations и повторный update не внес изменений; обновляемая база поднята с предпоследней migration до `60/60`, незакрытых изменений EF-модели нет, idempotent SQL сформирован (`179925` байт). В журнале PostgreSQL только ожидаемые первичные проверки отсутствующей migration history, намеренные нарушения ограничений интеграционными тестами, отклоненная первая тестовая fixture с дублем системного вида «Вода» и четыре ошибочных read-only запроса подсчета history; `FATAL`, `PANIC` и ошибок пересчета начисления нет. Прогресс roadmap — `47/112` (`41,96%`).

- 17.07.2026 — завершено правило редактирования показаний по учетному месяцу Этапа 3. Обычное изменение существующей записи разрешено только за текущий локальный месяц; прошлый месяц исправляется отдельной командой и диалогом `Историческая корректировка` с обязательной причиной, optimistic concurrency и специальным правом `payments.meter_readings.historical_correct`, которое по умолчанию выдано только администратору; изменение будущего месяца запрещено. Годовая выборка возвращает версию записи, интерфейс передает ее при сохранении, а audit `finance.meter_reading_historical_updated` хранит причину и diff `было/стало`. Добавлена запись «Что нового» `0.724.0`, обновлены пользовательская инструкция, месячный checklist и audit coverage. Проверки: focused backend `10/10`, focused PostgreSQL `1/1`, focused frontend `13/13`; полный backend с PostgreSQL `1733/1733`, пропущено `0`, coverage `90,01%` строк / `72,20%` ветвей; frontend `554/554`, coverage `81,60% / 72,52% / 77,44% / 82,15%`; lint, TypeScript, production build, bundle budget, Release-сборка без предупреждений, `dotnet format`, privacy `744` файлов, JSON и строгая UTF-8 — успешно. Изменения схемы не потребовались: на чистой PostgreSQL 17 применены все `60` migrations, повторный update сохранил `60`, незакрытых изменений EF-модели нет, idempotent SQL сформирован (`179925` байт). В журнале PostgreSQL только ожидаемые первичные проверки отсутствующей migration history, намеренные нарушения `CHECK`/`UNIQUE` интеграционными тестами и два ошибочных read-only запроса подсчета history; `FATAL`, `PANIC` и ошибок исторической корректировки нет. Прогресс roadmap — `46/112` (`41,07%`).

- 17.07.2026 — завершена безопасная серверная команда создания и изменения показания из платежной формы Этапа 3. Каждое показание имеет публичную версию, которая меняется при редактировании, отмене и восстановлении; устаревшая или отсутствующая версия при изменении возвращает понятный `meter_reading_conflict`, а конкурентное создание одной пары `гараж + вид счетчика + месяц` оставляет ровно одну запись. PostgreSQL использует эту версию как concurrency marker, поэтому второй контекст не может затереть уже сохраненное значение. Добавлены защищенный endpoint, frontend API-контракт, migration `MeterReadingOptimisticConcurrency` и запись «Что нового» `0.723.0`. Первоначальное имя маркера не прошло security-guard как похожее на секрет и было заменено нейтральным `Version`; сгенерированный EF BOM удален без ослабления проверок. Итоговые проверки: focused SQLite/controller `6/6`, PostgreSQL concurrency/create-race `2/2`, полный backend с PostgreSQL `1722/1722`, пропущено `0`, coverage `89,99%` строк / `72,00%` ветвей; frontend `552/552`, coverage `81,53% / 72,47% / 77,42% / 82,06%`; lint, TypeScript, production build, bundle budget, Release-сборка без предупреждений, `dotnet format`, privacy `744` файлов, JSON и строгая UTF-8 — успешно. Один несвязанный сценарий управления пользователем не дождался уведомления под первым полным параллельным coverage-прогоном, затем прошел `3/3` изолированно и в полном повторе `552/552` без изменения таймаута. На чистой PostgreSQL 17 применены все `60` migrations, повторный update сохранил `60`, колонка `Version` подтверждена как обязательный `uuid`, незакрытых изменений EF-модели нет, idempotent SQL сформирован (`179925` байт). В журнале PostgreSQL только ожидаемые первичные проверки migration history, намеренные нарушения `CHECK`/`UNIQUE` интеграционными тестами и два ошибочных read-only запроса подсчета с потерянным регистром; ошибок конкурентного изменения, `FATAL` и `PANIC` нет. Прогресс roadmap — `45/112` (`40,18%`).

- 17.07.2026 — завершено оставшееся автоматическое покрытие месячного цикла выплат Этапа 2. Одинаковые SQLite/service- и PostgreSQL integration-сценарии проходят границу декабря 2026 и января 2027, проверяют пустые январь и март без фиктивных начислений или выплат, частичную выплату февраля и отмену декабрьской выплаты с обязательной причиной и audit. До отмены рассчитанный долг переносится `60 → 80`, после отмены все последующие месяцы без ручной операции пересчитываются `100 → 120`; число начислений и выплат не увеличивается. Производственная логика уже корректно восстанавливает ведомость из активной истории, поэтому менялись только тесты и roadmap, а запись «Что нового» не требуется. Целевые проверки SQLite `1/1` и PostgreSQL `1/1`; полный backend с PostgreSQL `1716/1716`, пропущено `0`, coverage `89,93%` строк / `71,87%` ветвей; frontend `551/551`, coverage `81,52% / 72,47% / 77,38% / 82,05%`; lint, TypeScript, production build, bundle budget, Release-сборка без предупреждений, `dotnet format`, privacy `740` файлов и строгая UTF-8 — успешно. На чистой PostgreSQL 17 применены все `59` migrations, повторный update сохранил `59`, незакрытых изменений EF-модели нет, idempotent SQL сформирован (`179287` байт). В frontend console только известные сообщения jsdom о неподдерживаемой навигации документа; в журнале PostgreSQL только ожидаемые первичные проверки migration history и намеренные нарушения `CHECK`/`UNIQUE` интеграционным тестом, `FATAL`, `PANIC` и ошибок перерасчета нет. Прогресс roadmap — `44/112` (`39,29%`).

- 17.07.2026 — завершен динамический выбор месяца формы выплат Этапа 2. Ведомость больше не стартует с жестко заданного июня 2026 года и не ограничена списком апрель–июнь: начальным значением служит текущий локальный месяц устройства, а общий русскоязычный `LocalizedDatePicker` принимает любой корректный месяц и год. Заголовок и backend-запрос формируются из одного выбранного значения; некорректное или очищенное промежуточное значение не отправляет запрос. Workflow-тест проверяет переход с октября 2027 на февраль 2029, а Node guard запрещает возврат календарных констант в `FinancePanel`. Первый полный frontend-прогон обнаружил устаревшие ожидания двух guards; после их исправления повторный прогон дал один несвязанный нагрузочный таймаут редактора гаража, который не воспроизвелся в трех изолированных повторах, а следующий штатный полный прогон прошел чисто. Итоговые проверки: frontend `551/551`, coverage `81,52% / 72,47% / 77,38% / 82,05%`; backend с PostgreSQL `1714/1714`, пропущено `0`, coverage `89,93%` строк / `71,86%` ветвей; lint, TypeScript, production build, bundle budget, Release-сборка без предупреждений, `dotnet format`, privacy `740` файлов, JSON и строгая UTF-8 — успешно. На чистой PostgreSQL 17 применены все `59` migrations, повторный update сохранил `59`, незакрытых изменений EF-модели нет, idempotent SQL сформирован (`179287` байт). В frontend console только известные сообщения jsdom о неподдерживаемой навигации документа. В журнале временной PostgreSQL только ожидаемые первичные проверки отсутствующей migration history, намеренные нарушения `CHECK`/`UNIQUE` интеграционным тестом и два неудачных read-only подсчета с потерянным регистром имени таблицы; итоговый подсчет через stdin подтвердил `59`, `FATAL`, `PANIC` и прикладных ошибок нет. Добавлена запись «Что нового» `0.722.0`, обновлены пользовательская инструкция и месячный checklist; прогресс roadmap — `43/112` (`38,39%`).

- 17.07.2026 — завершена проверка инварианта кассы и банка Этапа 2. Устранено смешение двух разных действий: фактическая сдача наличных в банк теперь имеет явный признак, а обычное пополнение, изъятие и повторное распределение между фондами меняют только назначение средств и больше не увеличивают банк повторно. Backend запрещает сдачу, восстановление или увеличение перевода сверх доступной кассы, а также отмену/уменьшение уже израсходованного поступления; audit различает банковский перевод и обычную операцию фонда. Отчет «Сдача кассы в банк» показывает только реальные переводы. Migration `DistinguishCashToBankFundTransfers` на обновляемой PostgreSQL пометила историческую строку `Сдача кассы в банк...`, сохранив обычное распределение без признака; на чистой базе применены все `59` migrations, повторный update и idempotent SQL (`179287` байт) успешны, незакрытых изменений модели нет. Автоматические сценарии проверяют поступление, банковскую и кассовую выплату, отмену, восстановление, сдачу кассы, перераспределение фондов, недостаток кассы и PostgreSQL-поведение. Первый полный прогон обнаружил BOM в сгенерированных EF-файлах и устаревший performance-guard; кодировка нормализована, guard усилен проверкой явного признака. Итоговые проверки: backend `1714/1714`, coverage `89,93%` строк / `71,86%` ветвей; frontend `549/549`, coverage `81,49% / 72,38% / 77,34% / 82,03%`; lint, Release-сборка без предупреждений, production build, bundle budget, `dotnet format`, privacy `739` файлов, JSON и UTF-8 — успешно. Добавлена пользовательская запись «Что нового» `0.721.0`; временные PostgreSQL, coverage и build-артефакты удаляются перед коммитом.

- 17.07.2026 — закреплено правило банковской выплаты Этапа 2. Отдельный лимит по собранной сумме услуги не вводится: выплата поставщику разрешена при отрицательной колонке `Разница`, если хватает общего остатка банка; сумма сверх остатка банка отклоняется до сохранения финансовой операции и audit. SQLite-регрессия и реальный PostgreSQL-сценарий проверяют начисление `500`, собранную сумму `100`, разрешенную выплату `250`, отрицательную разницу `−400`, остаток банка `50` и отказ следующей выплаты `50,01` без второй записи. Frontend-тест подтверждает доступность кнопки `Оплатить` в строке с отрицательной разницей. Добавлена запись «Что нового» `0.720.0`, обновлены пользовательская инструкция и месячный checklist. Проверки: focused SQLite `1/1`, focused PostgreSQL `1/1`, focused frontend `1/1`, guard `2/2`; полный backend `1707/1707`, пропущено `0`, coverage `97,23%` строк / `71,42%` ветвей при порогах `85% / 70%`; frontend `549/549`, coverage `81,49% / 72,42% / 77,34% / 82,03%`; lint, Release-сборка, production build, bundle budget, `dotnet format`, JSON и строгая UTF-8 — успешно. На чистой PostgreSQL 17 применены все `58` migrations, повторный update сохранил `58`, незакрытых изменений EF-модели нет, idempotent SQL сформирован. Первый полный backend-прогон выявил два guard-теста, требующих стабильную фразу в имени frontend-сценария; обязательная фраза возвращена без ослабления guard, после чего оба полных набора прошли. В журнале PostgreSQL только ожидаемые первичные проверки migration history и намеренные нарушения `CHECK`/`UNIQUE` интеграционным тестом; `FATAL`, `PANIC` и ошибок правила банковской выплаты нет. Frontend console содержит только известные сообщения jsdom о неподдерживаемой навигации документа, падений и ошибок приложения нет.
- 17.07.2026 — завершена атомарная выплата Этапа 2. Для системных видов аванса и выплаты без чека команда `Добавить выплату` теперь одним `SaveChanges` сохраняет одинаковые стоимость поставщика и фактическую оплату вместе с единым audit-событием; ошибка второй SQL-записи откатывает всю тройку. Перед проверкой кассового или банковского остатка PostgreSQL берет отдельную advisory-блокировку соответствующего счета, поэтому конкурентные выплаты не могут вместе превысить доступную сумму. UI после ответа перечитывает ведомость с backend, показывает подтвержденные сервером равные значения и определяет строки без кнопки `Оплатить` по стабильному системному коду с fallback для старого имени. Добавлена запись «Что нового» `0.719.0`, обновлены пользовательская инструкция и месячный checklist. Проверки: focused backend `2/2`, финансовый backend `142/142`, PostgreSQL concurrency `1/1`, focused frontend `4/4`; полный backend `1705/1705`, пропущено `0`, coverage `97,23%` строк / `71,41%` ветвей при порогах `85% / 70%`; frontend `549/549`, coverage `81,49% / 72,42% / 77,34% / 82,03%`; lint, Release-сборка, production build, bundle budget, `dotnet format`, JSON и строгая UTF-8 — успешно. На чистой PostgreSQL 17 применены все `58` migrations, повторный update не внес изменений, незакрытых изменений EF-модели нет, idempotent SQL сформирован. В журнале PostgreSQL только ожидаемые первичные проверки migration history, намеренные нарушения `CHECK`/`UNIQUE` интеграционным тестом, одна проверка готовности без одноименной БД и три неудачных read-only запроса подсчета history с потерянным экранированием; итоговый подсчет через stdin подтвердил `58`, ошибок атомарной выплаты, `PANIC` и падений сервера нет. Frontend console содержит только известные сообщения jsdom о неподдерживаемой навигации документа, падений и ошибок приложения нет.
- 17.07.2026 — завершен расчетный перенос долга и аванса Этапа 2. Отдельная финансовая операция переноса намеренно не создается: входящий остаток каждого месяца восстанавливается из всей предшествующей истории той же пары `получатель + вид выплаты`, поэтому повторное чтение идемпотентно, не создает дублей и не требует audit-события без изменения данных. Четырехмесячные SQLite- и PostgreSQL-сценарии закрепляют полный расчет, частичный долг `150`, аванс `50`, его поглощение новой стоимостью до долга `30` и повторное открытие последнего месяца при неизменных `4` начислениях и `3` выплатах. Добавлена запись «Что нового» `0.718.0`. Проверки: focused SQLite `1/1`, focused PostgreSQL `1/1`; полный backend `1703/1703`, пропущено `0`, coverage `87,25%` строк / `70,71%` ветвей при порогах `85% / 70%`; frontend `548/548`, coverage `81,49% / 72,43% / 77,34% / 82,03%`; lint, Release-сборка, production build, bundle budget, `dotnet format`, privacy `737` файлов, JSON и UTF-8 — успешно. Незакрытых изменений EF-модели нет, idempotent SQL сформирован; на чистой PostgreSQL 17 применены все `58` migrations, повторный update не внес изменений. В логах PostgreSQL только штатные первичные проверки migration history и намеренные нарушения `CHECK`/`UNIQUE` из теста ограничений; `FATAL`, `PANIC` и ошибок прикладного расчета нет. Frontend console содержит только известное сообщение jsdom о неподдерживаемой навигации документа, падений и ошибок приложения нет.
- 17.07.2026 — продолжен Этап 2: месячная форма выплат больше не маскирует перенос одной общей суммой. Для каждой пары получатель + вид выплаты backend отдельно возвращает входящий долг, входящий аванс, стоимость и оплату месяца, исходящий долг и исходящий аванс; исходящий знак определяется после расчета `входящий долг − входящий аванс + стоимость − оплачено`, а долг и аванс отдаются неотрицательными взаимоисключающими полями. UI показывает четыре отдельные колонки вместе со стоимостью, оплатой, собранной суммой и разницей; кнопка оплаты предлагает исходящий долг с учетом истории. Добавлена запись «Что нового» `0.717.0`. Проверки: focused backend `2/2`, PostgreSQL integration `1/1`, focused frontend `3/3`; полный backend `1701/1701`, пропущено `0`, coverage `87,25%` строк / `70,71%` ветвей при порогах `85% / 70%`; frontend `548/548`, coverage `81,49% / 72,43% / 77,34% / 82,03%`; lint, Release-сборка, production build, bundle budget, `dotnet format`, privacy `737` файлов, JSON и UTF-8 — успешно. Незакрытых изменений EF-модели нет, idempotent SQL сформирован; на чистой PostgreSQL 17 применены все `58` migrations, повторный update не внес изменений, PostgreSQL-сценарий подтвердил исходящий долг после переноса. В логах PostgreSQL только штатные первичные проверки migration history и намеренные нарушения `CHECK`/`UNIQUE` из теста ограничений; `FATAL`, `PANIC` и ошибок прикладного расчета нет. Frontend console содержит только известное сообщение jsdom о неподдерживаемой навигации документа, падений и ошибок приложения нет.
- 17.07.2026 — начат Этап 2 и завершен расчет входящего остатка месячной формы выплат. Backend одним агрегирующим запросом формирует отдельные пары `поставщик/сотрудник + вид выплаты`, суммирует только активные начисления и выплаты строго до выбранного месяца и сохраняет знак остатка: положительный — долг, отрицательный — аванс. Для сотрудников используется системный вид `Зарплата`; поскольку отдельная история ставок пока не хранится, обязательство восстанавливается по действующей месячной ставке от месяца создания сотрудника либо от более раннего импортированного платежа. В UI добавлена отдельная колонка входящего долга/аванса и ее итог без смешения с текущей стоимостью и оплатой. Добавлена запись «Что нового» `0.716.0`. Проверки: focused SQLite `8/8`, отдельный трехмесячный сценарий `1/1`, PostgreSQL integration `1/1`, focused frontend `1/1`; полный backend `1701/1701`, пропущено `0`, coverage `87,23%` строк / `70,71%` ветвей при порогах `85% / 70%`; frontend `546/546`, coverage `81,45% / 72,31% / 77,28% / 81,99%`; lint, Release-сборка, production build, bundle budget, `dotnet format`, privacy `735` файлов, JSON и UTF-8 — успешно. Незакрытых изменений EF-модели нет, idempotent SQL сформирован; на чистой PostgreSQL 17 применены все `58` migrations, повторный update не внес изменений, а интеграционный запрос подтвердил новый расчет. В журнале PostgreSQL только ожидаемая первичная проверка отсутствующей migration history, намеренные нарушения ограничений из теста и два неудачных read-only запроса проверки history с неверным экранированием; ошибок прикладного расчета, `FATAL` и `PANIC` нет.
- 17.07.2026 — завершен PostgreSQL integration-test срез Этапа 1: тестовый проект создает отдельную временную базу, применяет к ней все migrations и на PostgreSQL 17 проверяет FIFO-распределение, два одновременно сохраняемых платежа и физические `CHECK`/частичное `UNIQUE`-ограничения журнала распределений. Устранена найденная гонка: операции одной пары `гараж + вид поступления` используют стабильную PostgreSQL advisory-блокировку на время перестроения и сохранения, поэтому параллельные платежи не могут дважды погасить одно начисление; разные пары не блокируют друг друга. Интеграционный тест включен в GitHub Actions через изолированный сервис PostgreSQL 17, а обычный локальный прогон без настроенной тестовой БД остается быстрым. Добавлена запись «Что нового» `0.715.0`. Проверки: focused PostgreSQL `1/1`; полный backend с включенной PostgreSQL-проверкой `1699/1699`, пропущено `0`, coverage `89,72%` строк / `71,54%` ветвей; frontend `546/546`, coverage `81,46% / 72,29% / 77,31% / 81,99%`; lint, Release-сборка backend, production build frontend, bundle budget, `dotnet format`, privacy `734` файла, отсутствие незакрытых изменений EF-модели и idempotent migration SQL — успешно. На отдельной чистой базе PostgreSQL 17 все `58` migrations применились, повторный update не внес изменений; в журнале сервера только ожидаемые отклонения `CHECK`/`UNIQUE` из теста ограничений и штатная первичная проверка отсутствующей таблицы migrations, неожиданных ошибок приложения, `PANIC` и падений сервера нет.
- 17.07.2026 — завершен оставшийся service/unit-test срез распределений Этапа 1: единый регрессионный сценарий проводит досрочный платеж до начислений, подтверждает первоначальную переплату, последующее FIFO-распределение по двум начислениям, полный и частичный остаток после изменения суммы, точное полное погашение и деактивацию всех распределений после отмены. Вместе с существующими allocator-тестами закрыты полная, частичная, досрочная и избыточная оплата, несколько начислений, изменение и отмена платежа; пункт автоматических тестов отмечен `[x]`. Focused-проверка `4/4`, полный backend `1698/1698`, coverage `89,70%` строк / `71,47%` ветвей; frontend `546/546`, coverage `81,46% / 72,29% / 77,31% / 81,99%` при порогах `78% / 69% / 74% / 79%`. Также прошли lint, Release-сборка, production build, bundle budget, `dotnet format`, privacy `731` файлов, idempotent migration SQL и проверка модели EF. На чистой PostgreSQL 17 применены все `58` migrations, повторный update не изменил history, журнал чист. Следующий незакрытый пункт Этапа 1 — автоматические PostgreSQL integration-тесты конкурентных платежей и ограничений БД. Запись «Что нового» не добавлялась: бизнес-логика и интерфейс не менялись.
- 17.07.2026 — выполнен пятый этап оптимизации frontend-тестов: общий setup теперь лениво загружает `jest-dom` и Testing Library DOM только при наличии браузерного `document`, поэтому 35 чистых Node-файлов больше не платят за неиспользуемые DOM-matchers. Автоматический guard проверяет условную загрузку и отсутствие статических DOM-import; все Node-файлы дополнительно защищены от использования браузерных matcher. Контрольный набор из `40` `.ts`-файлов (`283` теста, включая пять jsdom-сценариев) ускорился с `28,7` до `17,4` секунды (на 39%), суммарный setup — с `29,4` до `2,9` секунды; быстрый developer-suite выполняется примерно за `25,7` секунды. Полный coverage-gate прошел `546/546` за `96,5` секунды с теми же порогами и DOM-matchers. Остальные проверки: frontend coverage `81,45% / 72,29% / 77,28% / 81,99%` при порогах `78% / 69% / 74% / 79%`, backend `1697/1697` и coverage `89,74%` строк / `71,50%` ветвей, lint, Release-сборка, production build, bundle budget, `dotnet format`, privacy `731` файлов, idempotent migration SQL и отсутствие незакрытых изменений EF — успешно. На чистой PostgreSQL 17 применены все `58` migrations, повторный update не изменил history, журнал чист; временные процессы и артефакты удалены. Запись «Что нового» не добавлялась: пользовательская логика не менялась.
- 17.07.2026 — выполнен четвертый этап оптимизации frontend-тестов: профиль всех `545` проверок подтвердил, что шесть App-shard уже сбалансированы (`48–58` секунд тестового времени на shard), поэтому стандартный runner переведен с отдельных fork-процессов на изолированный thread-pool Vitest. Все сценарии, globals, моки и jsdom остаются изолированными средствами Vitest, но устраняется значительная часть повторного process/setup overhead. Штатный полный прогон на текущей машине выполняется примерно за `77–91` секунду вместо `85–95`, coverage-gate — за `85–88` секунд вместо `124–127` (примерно на 31% быстрее); быстрый suite остался около `26` секунд. Суммарно обычный полный набор ускорен с исходных `212` примерно до `91` секунды (около 57%). Автоматический guard закрепляет thread-pool в runner. Проверки: frontend `545/545`, coverage `81,45% / 72,29% / 77,28% / 81,99%` при порогах `78% / 69% / 74% / 79%`, backend `1697/1697` и coverage `89,7%` строк / `71,47%` ветвей, lint, Release-сборка, production build, bundle budget, `dotnet format`, privacy `731` файлов, idempotent migration SQL и отсутствие незакрытых изменений EF — успешно. На чистой PostgreSQL 17 применены все `58` migrations, повторный update не изменил history, журнал чист; временный профиль, процессы и артефакты удалены. Запись «Что нового» не добавлялась: бизнес-логика и интерфейс не менялись.
- 17.07.2026 — выполнен третий этап оптимизации frontend-тестов: после сравнительных полных прогонов `5 worker / 5 shard` (`107,2` секунды) и `6 / 6` (`94,9` секунды) адаптивный лимит штатного runner увеличен до шести доступных CPU. На текущей машине полный набор ускорился примерно со `141` до `95` секунд (еще на 33%, суммарно с исходных `212` секунд — примерно на 55%), coverage-gate — со `136` до `127` секунд; быстрый developer-suite остался около `26` секунд. На машинах с меньшим числом CPU runner использует только фактически доступные ядра, верхняя граница остается `6`, ручные `VITEST_MAX_WORKERS` и `VITEST_APP_SHARDS` сохранены. Автоматические тесты закрепляют бюджет для `1–8` CPU и fallback некорректного значения. Проверки: frontend `545/545`, coverage `81,45% / 72,29% / 77,28% / 81,99%` при порогах `78% / 69% / 74% / 79%`, backend `1697/1697` и coverage `89,7%` строк / `71,47%` ветвей, lint, Release-сборка, production build, bundle budget, `dotnet format`, privacy `731` файлов, idempotent migration SQL и отсутствие незакрытых изменений EF — успешно. На чистой PostgreSQL 17 применены все `58` migrations, повторный update не изменил history, журнал чист; временные процессы и артефакты удалены. Запись «Что нового» не добавлялась: пользовательская логика не менялась.
- 17.07.2026 — выполнен второй этап оптимизации frontend-тестов без сокращения сценариев и покрытия: 35 чистых файлов (`257` unit/API/структурных тестов) переведены с `jsdom` на встроенное окружение Node, а централизованный список и автоматический guard не позволяют случайно отнести туда тест с React-render или браузерным состоянием. На машине с 6 CPU быстрый developer-suite ускорился примерно с `39` до `26` секунд (еще на 33%); полный coverage-прогон, где основное время занимают 200 браузерных App-сценариев, сократился примерно со `138` до `136` секунд. Проверки: frontend `545/545`, coverage `81,45% / 72,29% / 77,28% / 81,99%` при порогах `78% / 69% / 74% / 79%`, backend `1697/1697` и coverage `89,7%` строк / `71,47%` ветвей, lint, Release-сборка, production build, bundle budget, `dotnet format`, privacy `731` файлов, idempotent migration SQL и отсутствие незакрытых изменений EF — успешно. На чистой изолированной PostgreSQL 17 применены все `58` migrations, повторный update не изменил history, журнал чист; временный сервер и артефакты удалены. Запись «Что нового» не добавлялась: интерфейс и бизнес-правила не менялись.
- 17.07.2026 — оптимизирован тестовый контур без сокращения покрытия: frontend-runner выбирает безопасный адаптивный бюджет `1–4` worker по доступным CPU и автоматически делит 200 сценариев монолитного `App.test.tsx` на такое же число сбалансированных shard; быстрый developer-suite сохраняет все модульные/компонентные тесты вне полного App workflow и использует более легкий thread pool. На машине с 6 доступными CPU штатный полный прогон ускорен примерно с `212` до `141` секунды (на 33%), быстрый — с `62` до `39` секунд (на 37%); ручные `VITEST_MAX_WORKERS` и `VITEST_APP_SHARDS` сохранены. Добавлены тесты выбора бюджета для 1–8 CPU и некорректного значения. Проверки: frontend `544/544`, coverage `81,44% / 72,25% / 77,28% / 81,99%` при порогах `78% / 69% / 74% / 79%`, backend `1697/1697`, lint, Release-сборка, production build, bundle budget, `dotnet format` и отсутствие незакрытых изменений EF — успешно. На чистой изолированной PostgreSQL 17 выполнены полный `migration up` и повторный idempotent update; журнал чист, временные сервер, coverage, build и shard-артефакты удалены. Запись «Что нового» не добавлялась: интерфейс и бизнес-правила не менялись.
- 17.07.2026 — завершено покрытие граничных дат просрочки Этапа 1: автоматические тесты подтверждают отсутствие просрочки до срока, в дату срока и в последний день льготного периода, а также ее появление точно в первый день просрочки и сохранение после него. Отдельно закреплены конец короткого месяца, 29 февраля високосного года, сбор без даты окончания и переход декабрьского начисления на следующий год. Полный прогон выявил недетерминированное ожидание фоновой синхронизации «Что нового»; синхронизация выделена в напрямую проверяемый однократный метод без изменения запуска приложения, три сценария прошли `10/10` повторных прогонов. Проверки: backend `1697/1697`, frontend `536/536`, lint, Release-сборка, production build, bundle budget, `dotnet format` и отсутствие незакрытых изменений модели EF — успешно. На изолированной PostgreSQL 17 подтверждены полный `migration up`, повторный idempotent update и результаты границ `0/0/0/1/1`; журнал PostgreSQL чист, временные сервер и файлы удалены. Запись «Что нового» не добавлялась: пользовательская логика и интерфейс не изменились, добавлена только регрессионная защита и стабилизирован тестовый контур.
- 17.07.2026 — завершен детерминированный backfill сроков исторических начислений Этапа 1: однозначные регулярные начисления и объявленные сборы получают срок из единственной подходящей настройки, а строки без однозначного источника помечаются причиной и доступны в защищенном постраничном отчете сверки. До ручного подтверждения такие строки и их распределения не увеличивают просрочку; повторное сохранение начисления подтверждает рассчитанный срок и фиксируется в audit. Добавлена запись «Что нового» `0.714.0`. Проверки: backend `1689/1689`, frontend `536/536`, lint, Release-сборка, production build, bundle budget, `dotnet format`, отсутствие незакрытых изменений модели EF и UTF-8 без BOM — успешно. На изолированной PostgreSQL 17 проверены обновление существующей схемы с однозначными, неоднозначными и неизвестными источниками, отключение активного распределения неоднозначной строки, полный `migration up` на чистой базе и повторный idempotent update; пользовательская служба PostgreSQL и VPS не изменялись.
- 17.07.2026 — завершена серверная фильтрация должников в таблице гаражей Этапа 1: режим применяется до подсчета и пагинации, сохраняется при переходе между страницами и смене сортировки, не включает архивные гаражи, погашенный долг и начисления с еще не наступившей просрочкой. Интерфейс показывает общее количество найденных должников, возвращает переключатель после ошибки, позволяет повторить запрос и не принимает устаревший ответ. Добавлена запись «Что нового» `0.713.0`. Проверки: backend `1688/1688`, frontend `535/535`, lint, production build, bundle budget, `dotnet format`, отсутствие незакрытых изменений модели EF и UTF-8 — успешно. На чистой временной PostgreSQL 17 применены все migrations; Npgsql-запрос подтвердил фильтрацию до пагинации, сортировку `300 → 100` и исключение свежего начисления, после чего временные сервер и файлы удалены.
- 17.07.2026 — завершена доступная расшифровка просрочки Этапа 1: в форме платежей выбранного гаража показаны входящий долг и только наступившие непогашенные начисления с услугой, месяцем, сроком оплаты, датой начала просрочки, исходной/оплаченной суммой и остатком. Backend отдает отдельный защищенный endpoint, платежи и переплата уменьшают строки в порядке от старых к новым; frontend использует общий skeleton, сообщает об ошибке, поддерживает повтор и отбрасывает устаревший ответ при переключении гаража. Добавлена запись «Что нового» `0.712.0`. Проверки: backend `1687/1687`, frontend `531/531`, lint, production build, bundle budget, `dotnet format`, UTF-8 без BOM — успешно. На временной локальной PostgreSQL 17 выполнен полный `migration up`; Npgsql-запрос подтвердил остаток `300` после оплаты `200` и исключение будущего начисления, после чего временные сервер и файлы удалены.
- 17.07.2026 — завершен audit перераспределений Этапа 1: при создании, изменении, отмене и восстановлении поступления или начисления фиксируется отдельное событие только если активные распределения появились либо существовали. В metadata сохраняются причина и счетчики до/после без номера гаража, документа, комментария и персональных данных; focused-тест подтверждает создание и отмену платежа.
- 17.07.2026 — migrations Этапа 1 проверены на временном изолированном PostgreSQL 17: полный `up` на чистой базе, обновление существующей схемы с частичной оплатой, переплатой и отмененными строками, повторный idempotent update, четыре целевых индекса и ограничение положительной суммы распределения. Backfill создал FIFO-распределения `500 + 300 + 400`, оставил переплату `100` нераспределенной и исключил отмененные начисление и платеж. Добавлена пользовательская запись «Что нового». Локальные службы PostgreSQL пользователя и VPS не изменялись.
- 17.07.2026 — продолжен Этап 1: введен версионный журнал распределений `платеж → начисление` с FIFO внутри пары `гараж + вид поступления`, поддержаны частичная оплата, переплата и перерасчет при изменении/отмене/восстановлении. Общий баланс отделен от просрочки: учитываются только непогашенные начисления с наступившей датой просрочки, а нераспределенный остаток уменьшает входящий долг и не создает просрочку. Добавлены migration для журнала и PostgreSQL-backfill исторических однозначных распределений; idempotent SQL успешно сформирован и проверен структурно, запуск на PostgreSQL остается отдельным незавершенным пунктом. Проверки: backend `1683/1683`, frontend `528/528`, lint, production build и bundle budget — успешно.
- 17.07.2026 — пользователь дал команду начать выполнение roadmap, тем самым приняты пять рекомендованных бизнес-решений. Начат Этап 1: добавлена доменная политика сроков для ежемесячных и годовых начислений, наружного освещения и сборов; новые начисления получают неизменяемые снимки даты оплаты и первого дня просрочки. Подготовлена EF migration с детерминированным безопасным backfill ежемесячного срока для исторических строк. Focused backend-проверка: `8/8`; сборка решения без предупреждений. Этап не завершен: далее требуется распределение платежей по начислениям и перевод агрегатов должников на непогашенные просроченные начисления.
- 17.07.2026 — создан отдельный roadmap по результатам детального аудита исходных XLSX/DOCX, кода, тестов и активных decision-checklist. Подтвержден baseline: backend `1674/1674`, frontend `528/528`, lint, production build и bundle budget. Производственный код, база, VPS, commit, push и deployment не изменялись. Следующий шаг — утвердить пять бизнес-решений и начать Этап 1 с модели срока и распределения платежей.
