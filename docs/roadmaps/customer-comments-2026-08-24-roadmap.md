# Исправления по комментариям заказчика от 17.08.2026

## Источники

- `C:\Users\User16\Downloads\КОмментарии_170826.docx` — исходные замечания заказчика.
- Подтвержденный пользователем перечень из 17 исправлений, которые можно выполнить без дополнительных бизнес-пояснений.
- Текущее поведение GarageBalance, API-контракты, миграции, автоматические тесты и пользовательская документация.

## Границы и решения

- В работу входят только 17 подтвержденных пунктов. Замечания, для которых ранее были запрошены бизнес-решения, не реализуются скрытыми предположениями.
- С 2026-08-24 в работу дополнительно включены семь расчётных замечаний, для которых пользователь разрешил использовать данные текущего VPS-стенда: состояние «Вывоз снега», открытие финансового отчёта, расчёты гаражей 101/102, нерегулярная оплата 3 000 руб., точность полной оплаты и блок просроченной задолженности.
- Данные VPS используются только для воспроизведения и сверки. Персональные данные, полные финансовые выгрузки, секреты и дампы не переносятся в репозиторий, тесты, документацию или пользовательские заметки.
- Снимки «до» фиксируют текущую версию приложения до изменений; снимки «после» фиксируют собранную и проверенную версию после изменений.
- Все пользовательские снимки выполняются в разрешении 1920×1080 и сохраняются как приемочные материалы.
- Уже реализованные пункты не дублируются: они проверяются кодом, тестами и визуальным свидетельством, а при необходимости уточняется подпись или представление.
- Для эпизодической выплаты допускается свободное имя получателя без обязательного поставщика; для обычной выплаты сохраняется выбор зарегистрированного поставщика.
- Явное подтверждение отрицательного остатка фонда разрешает банковскую выплату только при достаточном остатке банка и обязательно отражается в аудите.

## Этапы и проверяемые задачи

### 1. Контрольная среда и материалы «до»

- [x] Поднять отдельную локальную PostgreSQL-базу и текущие backend/frontend без использования данных заказчика.
- [x] Заполнить обезличенными демонстрационными данными, достаточными для всех 17 сценариев.
- [x] Зафиксировать снимки «до» для каждого пункта в 1920×1080.

### 2. Тарифы, сборы и календарь

- [x] Добавить в таблицу сборов колонку `Собрано` с серверным расчетом и форматированием суммы.
- [x] Убрать пример `1 000 000.00` из стоимости нерегулярной услуги.
- [x] Перенести переключатель `Регулярные платежи` из заголовка в логичную часть формы.
- [x] По уточнению заказчика вынести периодичность в самостоятельную колонку перед `Месяцем начисления`; обе колонки сделать только для чтения.
- [x] Добавить в `Настройки → Отображение` независимое включение колонок периодичности и месяца начисления; по умолчанию обе скрыты.
- [x] Добавить между таблицами нерегулярных платежей и объявленных сборов доступный разделитель ширины с персональным сохранением настройки.
- [x] Исключить обрезание календаря в диалоге добавления сбора.
- [x] Сделать фонд назначения сбора явно видимым в форме и таблице.
- [x] Добавить/обновить backend- и frontend-тесты всех измененных веток.

### 3. Выплаты и история операций

- [x] Заменить две колонки состояния фонда в таблице выплат одной колонкой `Текущий размер фонда`.
- [x] Скрыть пользовательские кнопки печати/повторной печати чека в истории операций.
- [x] Объединить обычную и эпизодическую выплату поставщику в одно действие `Добавить выплату`.
- [x] Сделать единую форму с выбором источника: банк/обычная и касса/эпизодическая.
- [x] Разрешить для эпизодической выплаты свободное необязательное имя получателя без карточки поставщика.
- [x] Условно показывать и валидировать поля формы в зависимости от выбранного источника.
- [x] Перекомпоновать форму добавления выплаты: источник вывести отдельной строкой без переноса, связанные поля расположить парами, а на узком экране сохранить одноколоночный вид.
- [x] Добавить/обновить backend- и frontend-тесты успешных, пустых, неверных и запрещенных веток.

### 4. Остатки, подтверждения и аудит

- [x] Разрешить банковскую выплату при достаточном остатке банка и недостаточном фонде только после явного подтверждения.
- [x] Зафиксировать отрицательный остаток фонда и специальное подтверждение в аудите операции.
- [x] Показать понятную ошибку при выплате зарплаты из недостаточного источника.
- [x] Подтвердить автоматический возврат снятой суммы фонда в нераспределенный остаток тестом и визуальным сценарием.
- [x] Переименовать `Распределено` в `Текущее значение` на странице фондов.
- [x] Сделать обычный комментарий к операции фонда необязательным, сохранив автоматически сформированную обязательную причину аудита.
- [x] Подтвердить наличие фонда расходования в карточке поставщика и защитить сценарий тестом.

### 5. Документация и приемка

- [x] Добавить пользовательскую запись `Что нового` без внутренних технических деталей.
- [x] Обновить относящуюся к изменениям пользовательскую/эксплуатационную документацию.
- [x] Выполнить backend unit/controller/integration/PostgreSQL-тесты, форматирование, privacy и migration checks.
- [x] Выполнить frontend tests, lint, production build и bundle budget.
- [x] Проверить кодировку измененных пользовательских текстов.
- [x] Зафиксировать снимки «после» для каждого пункта в 1920×1080 и составить индекс соответствия 17 пунктам.
- [x] Провести финальный аудит процессов и временных артефактов; оставить только код, документацию и запрошенные приемочные снимки.
- [x] Создать логические коммиты с русскими сообщениями без push.

### 6. Расчётные замечания по данным VPS

- [x] Проверить запись «Вывоз снега» и заменить неясное зачёркивание понятным состоянием услуги без потери доступности действий.
- [x] Устранить общий отказ открытия финансового отчёта: при недоступности определения полного периода отчёт открывается за стандартный период с возможностью изменить даты.
- [x] Исправить состав «Полного расчёта» для гаража 102: регулярные, целевые и нерегулярные строки выбранного месяца входят в одну точную сумму, а годовое обязательство не дублируется.
- [x] Исправить периодную оплату гаража 101: обычный платёж сначала закрывает регулярную строку, а не более ранний целевой сбор того же вида поступления.
- [x] Проверить привязку оплаты 3 000 руб. к нерегулярному начислению «Внеочередной вывоз мусора»: новые операции сохраняют идентификатор основания, а историческая операция без него использует остаток после регулярных начислений.
- [x] Устранить расхождения округления и состава строк в действии «Полная оплата» переводом суммы и распределения в целые копейки.
- [x] Проверить итог и расшифровку блока просроченной задолженности с учётом входящего долга, аванса, границы срока, частичных оплат, отменённых начислений и отменённых оплат.
- [x] Добавить регрессионные backend/frontend/PostgreSQL-тесты для всех найденных причин и важных пограничных веток.
- [x] Повторно проверить семь сценариев и новые входящие остатки на фактическом VPS через веб-интерфейс; публикация и три независимых веб-прохода завершены, согласованные обезличенные контрольные записи после приёмки архивированы с сохранением истории изменений.
- [x] Зафиксировать пользовательские изменения в «Что нового», документации и истории выполнения; создать логические коммиты без push.

### 7. Оставшиеся пункты исходного Word-документа

- [x] В форме нового гаража сделать редактируемыми и независимыми `Начальный баланс` и `Начальную просрочку`.
- [x] Ограничить начальную просрочку положительным начальным балансом, сохранить обратную совместимость существующих гаражей и запретить обычное изменение исходных сумм после появления финансовой истории.
- [x] Подтвердить единый справочник отделов в `Контрагенты` → `Персонал` и исключить отдельный несвязанный список отделов в тарифах.
- [x] В форме нового поставщика заменить вычисляемую задолженность редактируемым полем `Начальная задолженность`.
- [x] Построить связь основной таблицы тарифов с персоналом: строки зарплатного фонда получать по действующим отделам и рассчитывать как сумму ставок активных сотрудников.
- [x] Добавить миграцию PostgreSQL с безопасным заполнением входящей просрочки существующих гаражей и ограничением целостности.
- [x] Добавить backend/controller/frontend/PostgreSQL-регрессии для новых полей, расчёта зарплатного фонда, валидации и маршрута API.
- [x] Обновить `Что нового`, пользовательскую инструкцию и общий Word-документ; добавить под каждым пунктом снимки `До`/`После` и описание результата.
- [x] Выполнить полные локальные проверки; PostgreSQL-сценарии, требующие запущенной базы, оставить обязательными для CI/VPS и честно зафиксировать ограничение среды.

### 8. Повторная проверка проблемных расчётов и форм 26.08.2026

- [x] Повторно проверить добавление, редактирование и удаление первого, среднего и последнего порога, включая нулевую верхнюю границу и автоматический открытый последний диапазон.
- [x] Сделать применение периода финансового отчёта автоматическим, защитить форму от устаревших ответов и закрепить постоянную высоту окна во время загрузки, ошибки и отображения таблицы.
- [x] Обозначить обязательный номер гаража и подсвечивать незаполненное поле штатной валидацией браузера до отправки формы.
- [x] Разрешить кассовую эпизодическую выплату без карточки поставщика и без выбранного получателя, сохранив вид расхода и источник выплаты.
- [x] Разделить доступные остатки банка и кассы в форме выплаты; при переключении источника показывать и проверять только соответствующий остаток.
- [x] Пересчитать общий нераспределённый пул последовательным воспроизведением финансовых и фондовых операций; ручное изъятие из фонда возвращает сумму в пул даже после ранее исчерпанного нераспределённого остатка.
- [x] Защитить изменения модульными, PostgreSQL-, компонентными, stale-response, responsive- и bundle-регрессиями.
- [x] Повторно пройти шесть исправлений в веб-интерфейсе на изолированной контрольной среде без изменения рабочих данных.
- [x] Очистить временную базу, PostgreSQL-кластер, coverage, сборки и проверочные SQL; выполнить процессный аудит без остановки пользовательских и общих Codex-процессов.
- [x] Создать локальный коммит без push.

## Критерии готовности

- Все 17 подтвержденных пунктов реализованы либо доказательно подтверждены как уже реализованные.
- Каждая измененная ветка поведения защищена автоматическим тестом ближайшего практичного уровня.
- Полные обязательные проверки проходят без ошибок; ограничения среды честно отражены.
- Для каждого пункта имеется индексированный комплект реальных снимков приложения «до/после» 1920×1080.
- Изменения финансовых остатков и исключительное подтверждение отрицательного фонда отражаются в аудите.
- Временные процессы, базы, логи и промежуточные файлы удалены; приемочные снимки сохранены как заказанный результат.

## Риски и открытые вопросы

- [acceptance] Окончательная приемка внешнего вида и формулировок заказчиком выполняется по комплекту снимков после реализации.
- [x] Проверить, не потребуют ли новые поля миграции и обратной совместимости с уже существующими операциями.
- [x] Проверить поведение при конкурентных выплатах, чтобы подтверждение отрицательного фонда не обходило проверку банковского остатка.

## История выполнения

- 2026-08-24 — изучены исходные комментарии, текущий код и подтвержденные пользователем границы. Начата подготовка изолированной контрольной среды и материалов «до».
- 2026-08-24 — реализованы все 17 согласованных исправлений в API, доменной логике и интерфейсе; добавлена миграция с обратной совместимостью для существующих финансовых операций.
- 2026-08-24 — добавлены и обновлены автоматические проверки расчетов сборов, выплат из банка и кассы, отрицательного фонда с явным подтверждением, аудита, возврата средств фонда, форм и пользовательских состояний.
- 2026-08-24 — полная backend-проверка прошла: 2159 из 2159 тестов на отдельной PostgreSQL, а контрольный прогон без PostgreSQL — 2018 пройдено и 141 штатно пропущено. Миграция применена к отдельной приемочной базе.
- 2026-08-24 — полная frontend-проверка прошла: 820 из 820 тестов в 83 файлах; успешно выполнены lint, production build и bundle budget (261,3 КиБ из 262 КиБ).
- 2026-08-24 — сформирован комплект из 31 реального снимка приложения «до/после» размером 1920×1080 с индексом соответствия всем 17 пунктам.
- 2026-08-24 — остановлены приемочные API и Vite, удалены временная PostgreSQL-база и результаты production build; аудит портов и процессов подтвердил отсутствие оставшихся процессов задачи. Сохранен только заказанный комплект приемочных снимков.
- 2026-08-24 — изменения приложения, миграция, тесты, пользовательская документация и запись «Что нового» зафиксированы коммитом `7a3a33a0` (`Исправить сборы, выплаты и учет фондов`). Push не выполнялся.
- 2026-08-24 — перед публикацией повторно проверены 2018 backend-тестов без PostgreSQL (141 интеграционный тест штатно пропущен), 820 frontend-тестов с покрытием, lint, production build, bundle budget, форматирование, privacy, зависимости и генерация миграционного SQL. Полный PostgreSQL-прогон и развёртывание переданы обязательному GitHub Actions staging-конвейеру.
- 2026-08-24 — по разрешению пользователя начата вторая волна из семи расчётных замечаний с проверкой на текущих данных VPS. Прямой SSH-ключ в рабочей среде отсутствует, опубликованный стенд требует авторизации; до получения авторизованной сессии параллельно проверяются исходный Word, существующие расчётные запросы и регрессии без копирования чувствительных данных.
- 2026-08-24 — найдены две связанные причины расхождений: интерфейс исключал целевые сборы из «Полного расчёта», а серверная FIFO-разбивка обычного платежа могла погасить более раннее целевое начисление вместо регулярной строки периода. В состав полной оплаты включены регулярные, целевые и нерегулярные долги; обычные платежи получили приоритет регулярных начислений, при этом остаток исторической операции без идентификатора по-прежнему может закрыть нерегулярное начисление.
- 2026-08-24 — расчёт суммы и построчного распределения полной оплаты переведён в целые копейки. Добавлены отдельные тесты состава суммы, годовых обязательств, точности 0,10 + 0,20, передачи идентификаторов сбора/нерегулярного основания и сценария остатка 300 руб.
- 2026-08-24 — отключённые нерегулярные платежи теперь имеют явную подпись `Отключён` без зачёркивания названия. Финансовый отчёт гаража при сбое определения полного периода открывается за стандартный период вместо полного отказа.
- 2026-08-24 — подтверждены существующие серверные проверки блока просрочки: входящий долг, аванс, точная дата перехода в просрочку, частичная оплата, отменённые и требующие проверки начисления. На отдельной временной PostgreSQL 17 прошли все 2162 backend-теста без пропусков; backend coverage 89,43% строк / 72,92% ветвей. Прошли 826 frontend-тестов; frontend coverage 86,60% statements / 78,21% branches / 83,17% functions / 87,41% lines. Успешны lint, production build, bundle budget 261,5/262 КиБ, форматирование, privacy, NuGet/npm audit и генерация idempotent migration SQL.
- 2026-08-24 — код, регрессионные тесты, пользовательская документация и выпуск `1.20.0` зафиксированы коммитом `1e391304` (`Исправить полный расчет и распределение платежей`). Push и развёртывание не выполнялись. Для окончательной сверки фактических записей и снимков «после» остаётся вход пользователя в опубликованный стенд.
- 2026-08-25 — при визуальной сверке опубликованного стенда обнаружены две оставшиеся причины: интерфейс отвергал корректные PostgreSQL UUID гаража, если они не соответствовали ограниченному шаблону версий UUID, а «Полный расчёт» строился только по видимому периоду формы и мог быть меньше общего долга карточки.
- 2026-08-25 — проверка идентификатора финансового отчёта приведена к полному каноническому формату UUID. Добавлен серверный расчёт полной оплаты по всей истории гаража: входящий долг, непогашенные начисления, распределённые и ещё не распределённые поступления; интерфейс получает эту сумму до подтверждения и сохраняет точное построчное распределение.
- 2026-08-25 — сохранены реальные снимки опубликованного стенда «до»: отказ финансового отчёта и расхождение полной оплаты гаража 102. Добавлены регрессионные backend/controller/frontend/API-тесты; окончательные результаты полного прогона и снимки «после» будут внесены после публикации исправления.
- 2026-08-25 — обязательные проверки исправления завершены: 2164 из 2164 backend-тестов на отдельной PostgreSQL 17 без пропусков, покрытие 89,44% строк и 72,92% ветвей; 829 из 829 frontend-тестов, покрытие 86,53% statements / 78,06% branches / 83,14% functions / 87,34% lines. Успешны форматирование, privacy, NuGet/npm audit, lint, production build, bundle budget 261,9/262 КиБ и генерация идемпотентного migration SQL.
- 2026-08-25 — по новому уточнению заказчика периодичность отделена от месяца начисления. Обе колонки по умолчанию скрыты, независимо включаются в настройках отображения и не содержат элементов редактирования; значения меняются только в форме услуги. Настройка хранится в PostgreSQL, защищена правами и записывается в историю изменений. Добавлены серверные и интерфейсные регрессии.
- 2026-08-25 — завершена проверка уточнения: 2025 backend-тестов прошли, 142 PostgreSQL-теста штатно пропущены без локального подключения; 829 frontend-тестов прошли с покрытием 86,58% statements / 78,13% branches / 83,20% functions / 87,39% lines. Успешны lint, production build, bundle budget 262,0/262,0 КиБ, форматирование, privacy, npm/NuGet audit и генерация идемпотентного migration SQL. На локальном визуальном стенде 1920×1080 проверены настройки с двумя независимыми выключенными переключателями, таблица без колонок по умолчанию и порядок `Периодичность` → `Месяц начисления` при включении; обе ячейки отображаются как текст без элементов редактирования.
- 2026-08-25 — по уточнению заказчика переключатель «Регулярные платежи» перенесён из основной области формы добавления услуги в левую часть нижней строки; кнопки сохранения и отмены сгруппированы справа. Положение защищено компонентным тестом для компактного и раскрытого состояния формы.
- 2026-08-25 — перенос проверен визуально в реальном компоненте на размерах 1163×738 и 1555×872: переключатель находится у левого края нижней строки в выключенном и включённом состояниях, кнопки остаются справа без перекрытий. Прошли 2025 backend-тестов (142 PostgreSQL-теста штатно пропущены без локальной БД), 829 frontend-тестов, покрытие, lint, production build, bundle budget 262,0/262,0 КиБ, форматирование и аудит зависимостей.
- 2026-08-25 — между таблицами «Нерегулярные платежи» и «Объявленные сборы» добавлен вертикальный разделитель: ширина меняется указателем в пределах 25–60% и клавишами-стрелками, а настройка хранится в PostgreSQL отдельно для идентификатора вошедшего пользователя. На экранах до 1100 px разделитель скрывается, таблицы располагаются друг под другом. Новая настройка использует существующее хранилище параметров и не требует миграции.
- 2026-08-25 — завершена проверка персональной ширины таблиц: 2032 backend-теста прошли, 142 PostgreSQL-теста штатно пропущены без локального подключения; 832 frontend-теста прошли с покрытием 86,57% statements / 78,12% branches / 83,21% functions / 87,41% lines. Успешны lint, production build, bundle budget 262,0/262,0 КиБ, форматирование, privacy, npm/NuGet audit и генерация идемпотентного migration SQL. В браузере на 1742×700 подтверждены две читаемые таблицы с пропорцией 40/60; на 1000×800 подтверждены скрытие разделителя и корректное вертикальное расположение без перекрытий.
- 2026-08-25 — форма добавления выплаты перекомпонована в широкое компактное окно: источник выплаты занимает отдельную строку и имеет короткое однострочное значение, поставщик и услуга, дата и месяц, сумма и документ расположены парами, комментарий занимает всю ширину. В браузере проверены банковский и кассовый сценарии на ширине 929 px, а также одноколоночная адаптация на 620 px без горизонтального переполнения.
- 2026-08-25 — заключительная проверка формы выплаты завершена: прошли 833 frontend-теста с покрытием 86,57% statements / 78,12% branches / 83,21% functions / 87,41% lines и 2032 backend-теста; 142 PostgreSQL-теста штатно пропущены без локального подключения. Успешны lint, production build, bundle budget 262,0/262,0 КиБ, форматирование, privacy, npm/NuGet audit и генерация идемпотентного migration SQL.
- 2026-08-25 — закрыты четыре оставшихся пункта исходного Word-документа: у нового гаража разделены начальный баланс и его просроченная часть, у нового поставщика начальная задолженность вводится напрямую, справочник отделов подтверждён в `Контрагенты` → `Персонал`, а строки зарплатного фонда в тарифах теперь автоматически строятся по отделам и сумме ставок действующих сотрудников.
- 2026-08-25 — добавлена миграция входящей просрочки гаражей с заполнением существующих положительных стартовых балансов и ограничением целостности. Полный локальный backend-набор: 2034 пройдено, 143 PostgreSQL-теста штатно пропущены без запущенной базы; frontend: 833 из 833, покрытие 86,69% statements / 78,23% branches / 83,30% functions / 87,53% lines. Локальный backend coverage без PostgreSQL равен 77,76% строк / 66,17% ветвей и совпадает с базовым состоянием 77,74% / 66,15%; полный порог достигается в CI/приёмочной PostgreSQL-среде, где интеграционные сценарии не пропускаются.
- 2026-08-26 — после разрешённого входа и подтверждения отправки данных через опубликованный интерфейс созданы обезличенные контрольные записи: отдел `Приёмка Codex 260826`, сотрудник со ставкой 12 345,67 руб., гараж `ТЕСТ-260826` с начальным балансом 2 500 руб. и просрочкой 700 руб., поставщик с начальной задолженностью 1 234,56 руб. На VPS подтверждены сохранение обеих входящих сумм и автоматическая недоступная для ручного редактирования строка зарплатного фонда отдела на 12 345,67 руб.; настройки колонок включены, проверены в порядке `Периодичность` → `Месяц начисления` и возвращены в исходное выключенное состояние.
- 2026-08-26 — визуальная приёмка выявила два CSS-дефекта, которые компонентные проверки ранее не обнаруживали: из-за `align-items: start` разделитель нижних таблиц имел высоту 0 px, а позднее базовое правило `.detail-dialog` переопределяло ширину формы выплаты с 720 до 560 px. Разделителю заданы растягивание по высоте и минимальная рабочая высота, широкой форме — селектор с достаточной специфичностью; статические CSS-регрессии обновлены, в «Что нового» подготовлен выпуск 1.24.0.
- 2026-08-26 — локальная проверка CSS-исправлений успешна: 833 из 833 frontend-тестов в 84 файлах, покрытие 86,69% statements / 78,23% branches / 83,30% functions / 87,53% lines, lint, production build и bundle budget 262,0/262,0 КиБ; backend 2035 пройдено и 143 PostgreSQL-теста штатно пропущены без локального подключения. Успешны форматирование, privacy (1071 файл), npm/NuGet audit и генерация идемпотентного migration SQL размером 337 908 байт. Остаются публикация, повторная браузерная сверка двух исправлений, снимки, обновление Word и согласованное удаление контрольных записей.
- 2026-08-26 — коммит `52493dc4` опубликован и трижды успешно развёрнут на VPS: исходный push-run `32925643605` и два независимых контрольных workflow_dispatch-прогона `32928710647` и `32929316596`. В каждом дополнительном прогоне прошли 2178 backend- и 833 frontend-теста, проверки покрытия, форматирования, конфиденциальности и зависимостей, lint, production build, bundle budget, генерация миграционного SQL и полный staging-деплой. После каждого деплоя веб-интерфейс проверен повторно: настройки колонок выключены по умолчанию, зарплатный фонд контрольного отдела равен 12 345,67 руб., контрольные гараж/поставщик/сотрудник сохранены, финансовый отчёт и полный расчёт гаража 102 дают 13 105,36 руб. долга и 1 527,25 руб. просрочки, формы услуги и выплаты отображают новую компоновку. `/health` и главная страница отвечают HTTP 200, PostgreSQL отмечен как `ok`.
- 2026-08-26 — после явного подтверждения заказчика через веб-интерфейс архивированы только четыре приёмочные записи: сотрудник «Тестовый Сотрудник Приёмки», отдел «Приёмка Codex 260826», поставщик «ТЕСТ Поставщик Codex 260826» и гараж «ТЕСТ-260826». Для каждой записи проверен статус «Удалён» и отсутствие рабочих действий; все четыре операции и причины сохранены в истории изменений.
- 2026-08-26 — начата повторная проверка шести проблемных пунктов. Исправлены перестроение открытого последнего порога после удаления, нулевая верхняя граница, автоматические фильтры и постоянная геометрия финансового отчёта, обязательный номер гаража, кассовая выплата без поставщика, переключение видимого остатка банка/кассы и возврат изъятой суммы фонда в общий нераспределённый пул. Полный frontend-набор прошёл 835/835, coverage 86,76% statements / 78,16% branches / 83,54% functions / 87,59% lines; lint, production build и bundle budget 260,7/262 КиБ успешны. Полный backend-набор на изолированной PostgreSQL 17 прошёл 2181/2181 без пропусков, coverage 89,48% строк / 72,92% ветвей. Успешны format, privacy (2103 файла), npm/NuGet audit, release JSON и генерация идемпотентного migration SQL 337 908 байт. Остаются веб-проход, очистка временной среды и локальный коммит без push.
- 2026-08-26 — для визуальной проверки подготовлены отдельная демонстрационная PostgreSQL-база, локальные API и production-preview; стенд успешно открыл форму входа. Перед вводом синтетического пароля запрошено обязательное подтверждение действия в браузере, но оно не получено за три последовательных продолжения цели. Временные API, preview и PostgreSQL остановлены, демонстрационная база и кластер, coverage, промежуточные сборки и SQL удалены; порты 5080, 5173 и 55432 свободны. Общие процессы Codex и пользовательский Vite другого проекта на порту 5175 не затрагивались. Локальный коммит и push не выполнялись.
- 2026-08-26 — после подтверждения пользователя выполнен полный браузерный проход на новой изолированной PostgreSQL 17 с синтетическими данными. Подтверждены автоматическая смена периода и неизменная высота финансового отчёта 720 px, обязательность номера гаража, раздельные остатки банка 23 000,00 руб. и кассы 20 674,75 руб., кассовая выплата 10,00 руб. без поставщика и возврат изъятых 100,00 руб. из фонда в пул: фонд уменьшился с 20 000,00 до 19 900,00 руб., пул вырос с 4 944,75 до 5 044,75 руб., касса уменьшилась до 20 664,75 руб., банк не изменился, общий остаток стал 43 664,75 руб. Живой цикл порогов выявил пропущенный тестами конфликт при добавлении ступени к используемому тарифу; сохранение перенаправлено через связанную услугу и защищено регрессией. После исправления в интерфейсе успешно выполнены изменение 1 100→1 099→1 100 с пересчётом соседней нижней границы, добавление диапазона 1 701–2 000 с открытой ступенью от 2 001 и удаление с восстановлением открытого диапазона от 1 701. Повторный frontend-набор прошёл 835/835, coverage 86,76% statements / 78,17% branches / 83,54% functions / 87,59% lines; lint, production build и bundle budget 260,7/262 КиБ успешны. Синтетический стенд после проверки удалён, изменения подготовлены к локальному коммиту без push.
