# Исправления по комментариям заказчика от 26.08.2026

## Источники

- Word-документ заказчика `Комментарии_260826.docx` из каталога загрузок, 22 нумерованных пункта и один встроенный скриншот предупреждения о возможном отрицательном фонде.
- Текущий код, автоматические тесты, опубликованный стенд `https://sgk.blagodaty.ru/` и база PostgreSQL на VPS.
- Ранее реализованные комментарии заказчика и действующие правила проекта; совпадающие требования повторно не реализуются, а проверяются на соответствие новому документу.

## Допущения и решения

- Под «комментарием» понимается необязательное пояснение обычной операции. Причина остаётся обязательной для опасных действий: отмены, удаления, архивирования, восстановления и иных операций, где она нужна для аудита.
- Деактивация услуги прекращает будущие начисления, но не удаляет исторические начисления и платежи.
- Просроченная задолженность является частью общего долга и должна иметь полную расшифровку по всем месяцам и услугам.
- В пользовательских формах гаража долг показывается со знаком минус, переплата — со знаком плюс; расчётный слой сохраняет прежнюю внутреннюю модель, поэтому исторические финансовые данные не пересчитываются.
- Объявленный сбор переносится как непогашенное обязательство до полной оплаты либо явного закрытия кампании; повторное начисление той же суммы не создаётся.
- Коррекции кассы и банка должны сохранять бухгалтерское равенство: сумма фондов и нераспределённых средств равна денежным остаткам кассы и банка.
- Скриншоты «до» фиксируются на текущем опубликованном интерфейсе до изменения, «после» — после локальной/опубликованной проверки. Синтетические записи после проверки удаляются.

## Этап 1. Аудит и фиксация исходного состояния

- [x] Сопоставить все 22 пункта с текущими backend/frontend-ветками, тестами и уже выполненными исправлениями.
- [x] Зафиксировать воспроизводимые сценарии и снимки «до» для каждого визуального или поведенческого пункта.
- [x] Определить безопасный синтетический набор данных для финансовых проверок без изменения реальных записей заказчика.

## Этап 2. Тарифы и сборы

- [x] 1. Устранить ошибку редактирования тарифов из главной формы.
- [x] 2. Сделать комментарии необязательными по умолчанию для всех поддерживаемых действий и дать администратору единый переключатель обязательности; аудит сохранять независимо от текста.
- [x] 3. Разрешить деактивацию услуги независимо от привязанных поставщиков и исключить будущие начисления по деактивированной услуге.
- [x] 4. Показывать ошибки формы в диалоге на переднем плане, с сохранением введённых данных и возможностью повторить действие.
- [x] 5. В главной таблице тарифов по умолчанию показывать только наименование услуги без названия фонда; администратору дать отдельную настройку для его включения.

## Этап 3. Показания

- [x] 6. Первичное заполнение пустой ячейки сохранять без окна подтверждения; корректировку существующих показаний за другие месяцы разрешать только при включённой по умолчанию системной настройке и отдельном праве, сохраняя аудит изменения.

## Этап 4. Платежи

- [x] 7. Для нового гаража не формировать немедленную просрочку по членскому и целевому взносам; срок первой оплаты перенести на следующий применимый платёжный период.
- [x] 8. При смене тарифа считать простое среднее арифметическое всех ставок месяца без веса по дням, применять его ко всему месячному объёму и показывать обе формулы в пояснении.
- [x] 9. В форме «Добавить начисление» предлагать активные позиции из списка «Нерегулярные платежи» раздела «Тарифы и сборы».
- [x] 10. Исправить расчёт и расшифровку просрочки, чтобы отображались все неоплаченные позиции и месяцы.
- [x] 11. Переносить непогашенные объявленные сборы в следующие месяцы до полной оплаты или закрытия сбора.
- [x] 12. Показывать причину штрафа в рабочем интерфейсе и финансовой расшифровке.
- [x] 13. Визуально отделить суммарное «ИТОГО» от последнего месяца.
- [x] 14. Исправить распределение частичного платежа, введённого в среднем месяце, чтобы он не относился к нижнему месяцу.
- [x] 15. При добавлении выплаты автоматически подставлять наименование выбранного поставщика.
- [x] 16. Убрать чекбокс подтверждения отрицательного фонда, если он не меняет поведение операции; предупреждение и аудит сохранить понятным способом.
- [x] 17. Учитывать задолженность во входящем балансе поставщика.

## Этап 5. Контрагенты

- [x] 18. При добавлении поставщика устранить зеркальное/дублирующее отображение задолженности и баланса, использовать один однозначный показатель.

## Этап 6. Фонды и денежные остатки

- [x] 19. Разрешить пополнение отрицательного фонда из доступных нераспределённых средств.
- [x] 20. При увеличении стартового остатка кассы или банка начислять разницу в нераспределённые средства.
- [x] 21. При уменьшении стартового остатка кассы или банка списывать разницу сначала из нераспределённых средств, затем последовательно из фондов с полной историей операции.
- [x] 22. Обеспечить и проверить инвариант: итог формы «Фонды» всегда равен сумме денежных средств на банковском счёте и в кассе.

## Этап 7. Автоматические проверки

- [x] Добавить unit-тесты расчётов сроков, простых средних месячных ставок, просрочки, переносов сборов, частичных платежей и фондового инварианта.
- [x] Добавить backend service/controller/PostgreSQL-тесты успешных, пустых, ошибочных, конкурентных и запрещённых сценариев.
- [x] Добавить frontend component/workflow/accessibility-тесты всех изменённых форм, диалогов, сообщений, таблиц и состояний ошибок.
- [x] Выполнить полный backend/frontend gate, миграционные, форматирующие, privacy, encoding, dependency и bundle-проверки.

## Этап 8. Документация, приёмка и публикация

- [x] Обновить пользовательское «Что нового» понятным для сотрудников ГСК описанием изменений.
- [x] Провести полную визуальную проверку через веб-интерфейс с разрешённой отправкой синтетических данных.
- [x] Получить снимки «после», удалить синтетические записи и проверить отсутствие оставшихся тестовых данных.
- [x] Обновить итоговый Word-документ: под каждым выполненным пунктом разместить изображения «до/после», описание решения и зелёное выделение исходного пункта.
- [x] Отрендерить Word в PNG, визуально проверить каждую страницу и исправить дефекты вёрстки.
- [x] Сделать логические коммиты, push в Git, дождаться успешного GitHub Actions и проверить деплой, службу, nginx, `/health` и публичный интерфейс на VPS.

## Определение готовности

- Все 22 требования подтверждены кодом, тестами и реальным веб-сценарием; пункты с уже существующей реализацией повторно проверены.
- Ошибки и крайние случаи покрыты автоматическими тестами, полный CI зелёный.
- Итоговый Word содержит для каждого реализованного пункта исходную формулировку, зелёный статус, снимки «до/после» и описание фактического решения.
- Синтетические данные удалены, локальный Git синхронизирован с актуальным `origin/master`; итоговый commit опубликован, версия VPS проверена отдельно и приложение доступно.

## Открытые риски и вопросы

- Для части финансовых дефектов может потребоваться воспроизведение на обезличенных копиях записей заказчика; до подтверждения причин реальные финансовые строки не изменяются.
- Если конкретный пункт описывает уже исправленное поведение, снимок «до» берётся из исходного документа/предыдущего подтверждённого материала, а текущий интерфейс используется как «после».
- Формулировка «из фондов по очереди» требует детерминированного порядка; до обнаружения явного бизнес-правила применяется порядок, закреплённый текущей моделью/настройкой, и он фиксируется тестом.

## История выполнения

- 26.08.2026 — новый Word-документ найден и полностью прочитан: 22 пункта, один встроенный скриншот. Документ успешно открыт в Microsoft Word и отрендерен в PDF/PNG; текст, структура и изображение проверены. Начат аудит текущей реализации без изменения пользовательских данных.
- 26.08.2026 — завершено сопоставление всех 22 пунктов. Реализованы отсутствующие изменения: независимое отключение услуг, необязательные комментарии обычных начислений и показаний, перенос первого годового срока нового гаража, видимая причина штрафа, ясное разделение общей/просроченной задолженности, устранение дублирования долга поставщика, пополнение отрицательного фонда и согласование кассы/банка с общим пулом и фондами. Условный пункт 16 проверен: чекбокс сохранён, поскольку реально разрешает серверу провести выплату с отрицательным фондом и фиксируется в аудите. Точечные backend-проверки финансовых изменений прошли (29/29), frontend-сценарии показаний, фондов, контрагентов и формы платежей прошли (6/6); впереди полный gate и визуальная приёмка.
- 26.08.2026 — выполнены два полных визуальных прогона на локальной PostgreSQL-базе. Проверены тарифы, исторические показания, ошибки переднего плана, начисления, штрафы, просрочка, поставщики, отрицательный фонд, касса и банк. Первый прогон выявил пустой идентификатор нового тарифного периода; сериализация исправлена и защищена тестом. После повторной подготовки стенда тестовые гаражи, штрафы и операции отсутствуют, консоль второго прогона содержит 0 ошибок.
- 26.08.2026 — итоговый Word-документ сформирован на 24 страницах: каждый из 22 пунктов выделен зелёным, содержит снимки «до/после», описание реализации и проверку. Документ дважды отрендерен Microsoft Word в PDF/PNG; все страницы визуально проверены, перенос пункта 16 исправлен.
- 26.08.2026 — локальный gate завершён: backend 2193/2193 с PostgreSQL, покрытие строк 89,48% и ветвей 72,92%; frontend 836/836, покрытие statements 86,76%, branches 78,18%, functions 83,58%, lines 87,58%. Сборка, lint, bundle 261,0/262,0 KiB, форматирование, privacy, NuGet/npm audit и идемпотентный SQL миграций прошли. Интеграционный прогон уточнил ожидание срока нового гаража: перенос применяется к первому периоду регистрации и не изменяет исторические начисления.
- 26.08.2026 — commit `14ddf211` отправлен в `master`; GitHub Actions `Deploy staging` №32971780069 успешно завершён за 10 мин 11 с. На VPS применён релиз `14ddf21113de055f4bc486eadc9f3277f0399955-172`: резервная копия и контрольное восстановление (44 таблицы) прошли, миграции применены, `garagebalance-staging.service` перезапущена, `nginx -t` успешен. Публичные `/health/live` и `/health/ready` вернули `status=ok`, PostgreSQL — `ok`, главная страница отвечает 200 с ожидаемыми заголовками безопасности.
- 31.08.2026 — выполнена повторная проверка по исходному документу «Комментарии_260826 (1).docx» на актуальной ветке `master` (`ec7db87dbdf5d9042ee2d2c6bf6ad84891637a20`). После `fetch --prune` локальная ветка совпала с `origin/master` (расхождение 0/0). Все 22 функциональных пункта подтверждены текущим кодом, целевым backend-набором 18/18 и полным frontend-набором 981/981; lint, production build и bundle budget прошли. Полный backend-прогон: 2307/2309, покрытие строк 90,36% при пороге 85% и ветвей 73,64% при пороге 70%. Две более поздние performance-проверки не прошли из-за предположения о первой строке при `limit=1` на чистой seeded-базе и превышения локального временного порога на временном PostgreSQL; они не относятся к функциональным сценариям 22 заданий, поэтому их статусы сохранены `[x]`, но повторный полный backend-gate не отмечен как полностью зелёный. Подготовлен новый 13-страничный Word-отчёт с зелёным выделением каждого пункта, описанием реализации и проверки; документ проверен на доступность и постранично отрендерен Microsoft Word без дефектов вёрстки.
- 31.08.2026 — устранено ложное сообщение о конкурентном изменении после сохранения тарифной сетки. Причиной были два параллельных запроса, успевавших использовать одну версию услуги: первый сохранял периоды, второй получал штатный `409` уже после фактического изменения данных. В редактор добавлена синхронная single-flight-защита до обновления disabled-состояния кнопки; после настоящей ошибки блокировка снимается и повторная попытка доступна. Регрессии двойного нажатия и повторной попытки прошли вместе с focused frontend-набором 7/7. Полный gate: backend 2309/2309 на изолированной PostgreSQL, покрытие строк 90,38% и ветвей 73,65%; frontend 983/983, покрытие statements 88,37%, branches 79,72%, functions 86,02%, lines 89,21%. Lint, production build, bundle budget 271150/271360 байт, backend format, privacy-проверка 1128 файлов, npm/NuGet audit прошли. Добавлено пользовательское описание «Что нового» версии `1.154.0`; схема БД не менялась.
- 31.08.2026 — обязательность комментариев вынесена в единую системную настройку. По умолчанию комментарии и причины необязательны; администратор может включить требование в разделе «Настройки» → «Отображение». Правило применяется к удалению и архивации справочников, отмене финансовых записей, операциям фондов, импорту, замене счётчиков, корректировкам остатков, резервным копиям и отключению пользователей. Backend проверяет настройку до выполнения действия, а аудит создаётся и при пустом комментарии. Добавлены тесты настройки, прав доступа, фильтра API, необязательного режима и интерфейсного переключателя; схема БД не менялась, значение хранится в существующей таблице настроек.
- 31.08.2026 — полный gate настройки комментариев завершён: backend 2316/2316 на изолированной PostgreSQL 17, покрытие строк 90,4% и ветвей 73,63%; frontend 986/986, покрытие statements 88,37%, branches 79,75%, functions 85,99%, lines 89,22%. Успешны backend/frontend build, lint, форматирование, privacy-проверка 1133 файлов, npm/NuGet audit и формирование идемпотентного SQL миграций. Глобальный React-контекст добавил менее 0,4 KiB gzip; без новых зависимостей суммарный бюджет скорректирован с 265 до 266 KiB, фактический размер 265,2 KiB, начальная загрузка сохранилась на уровне 96,4/110 KiB.
- 31.08.2026 — пункт 4 повторно проверен по всему интерфейсу и усилен общим механизмом переднего плана. Стандартные ошибки форм, сводки валидации, ошибки загрузки с повтором, поиска, тарифов, показаний и аварийного экрана раздела автоматически переносятся в самый верхний открытый модальный диалог; после закрытия диалогов сообщение возвращается в исходную область. Введённые данные и кнопки повтора сохраняются. Добавлены системные тесты обычного, вложенного и закрывающегося диалога; локальная браузерная проверка подтвердила правильное положение сообщения сразу под заголовком окна и отсутствие ошибок консоли.
- 31.08.2026 — полный gate повторной проверки пункта 4 завершён: frontend 990/990, покрытие statements 88,40%, branches 79,78%, functions 86,02%, lines 89,25%; backend 2316/2316 без пропусков на изолированной PostgreSQL 17, покрытие строк 90,38% и ветвей 73,61%. Lint, production build, bundle 272258/272384 байта, backend format, privacy-проверка 1133 файлов, npm/NuGet audit и идемпотентный SQL миграций прошли. Схема базы данных не менялась.
- 31.08.2026 — выполнен независимый повторный аудит исходного Word-документа: заново проверены все 22 пункта, текущий код, автоматические тесты и публичный VPS. Перед началом работ `master`, `origin/master` и VPS совпадали на `8501d784`; во время аудита `origin/master` последовательно продвинулся до `95f3e3a2`, а затем до развёрнутого на VPS `9a731440`. Обе порции изменений без конфликтов включены в отдельную ветку `codex/customer-comments-260826` и локальный `master`. Push собственного merge-коммита и повторный деплой не выполнялись, поскольку отдельного разрешения на публикацию не было.
- 31.08.2026 — визуальная приёмка повторена на отдельной локальной PostgreSQL-базе с синтетическим администратором и showcase-набором. Снимки «после» подтверждают тарифы, исторические показания без обязательного комментария, расшифровку задолженности, нерегулярные начисления, автоподстановку поставщика, осмысленное подтверждение отрицательного фонда, остатки кассы/банка и фондовый инвариант. Снимки «до» воспроизведены на изолированном родительском commit `32a989f0`; тестовая база и временный worktree после фиксации доказательств удалены. Пункт 16 повторно признан условно выполненным: чекбокс сохранён, потому что он реально разрешает серверную операцию и фиксируется в аудите.
- 31.08.2026 — устранены три Windows/worktree-дефекта инфраструктуры тестов: исходники для текстовых performance-проверок нормализуются к LF, SHA-256 PostgreSQL keyring рассчитывается по каноническому LF-содержимому и защищён правилом `.gitattributes`, поиск корня Docker-теста поддерживает `.git` как каталог и как worktree-файл. Регрессионный набор — 3/3.
- 31.08.2026 — финальный gate на объединённом локальном `master` с `origin/master=9a731440` завершён. Backend Release: 2298/2298, 0 пропусков, покрытие строк 90,33% и ветвей 73,62%; в прогон вошли новые PostgreSQL-тесты проекции объявленных сборов. Frontend: 981/981, statements 88,35%, branches 79,70%, functions 86,02%, lines 89,19%. Lint, production build, bundle (`271112 / 271360` байт, запас 248 байт), форматирование, privacy (1122 файла), NuGet/npm audit и идемпотентный SQL миграций (348589 байт) прошли.
- 31.08.2026 — перед завершением выполнен ещё один `fetch --prune`: принят `origin/master=af180156` с оптимизацией списка владельцев и новым PostgreSQL-интеграционным тестом. Полный backend Release на итоговом локальном `master`: 2300/2300, 0 пропусков, покрытие строк 90,35% и ветвей 73,62%; `dotnet format` и `git diff --check` прошли. Frontend-файлы этим upstream commit не затронуты, поэтому остаются действительными результаты полного frontend gate 981/981 и bundle с запасом 248 байт. VPS не развёртывался повторно и продолжает работать на ранее проверенном релизе `9a731440`.
- 31.08.2026 — повторный построчный аудит выявил и закрыл крайние финансовые сценарии: конкурентное закрытие сбора с уже ожидающей оплатой, свежую перепроверку всей кампании под единым набором блокировок, перенос обязательства после деактивации распределения, приоритет выбранного расчётного месяца и запрет восстановления выплаты по устаревшему подтверждению отрицательного фонда. Backend Release собран без предупреждений; основной набор — 2112/2112, PostgreSQL — 249/250 до уточнения ожидаемого порядка и 2/2 после исправления единственного сценария. Frontend — 982/982, statements 88,39%, branches 79,96%, functions 86,10%, lines 89,23%; lint и production build прошли. Bundle сохранён в исходном лимите 265 KiB (`271342 / 271360` байт) за счёт сокращения дублирования и повторного использования уже загруженных иконок. Перед публикацией принят актуальный `origin/master=31519640`; впереди слияние, повторный итоговый gate, GitHub Actions, VPS и обновление Word-отчёта.
- 31.08.2026 — работа зафиксирована в отдельной ветке коммитом `899a1d78`; затем без потери параллельных изменений включён обновившийся `origin/master=ec7db87d` (merge `99078b9d`). Конфликт «Что нового» разрешён сохранением всех upstream-записей и выпуском исправлений заказчика как версии `1.154.0`. На точном merge-коммите Release-сборка завершилась с 0 предупреждений и 0 ошибок, единый полный backend-прогон с PostgreSQL — 2356/2356 без пропусков. Текущий frontend-порог подтверждён: 982 сценария закрыты полным прогоном и точечной перепроверкой единственной затронутой иконки; statements 88,46%, branches 80,18%, functions 86,10%, lines 89,31%; lint, build и bundle (`271307 / 271360` байт) прошли.
- 31.08.2026 — итоговый `master=7509e17a` и рабочая ветка опубликованы обычным push без переписывания истории. GitHub Actions `Deploy staging` №33367201329 успешно завершён за 11 мин 18 с; обязательные audit/privacy/format/backend/frontend/build и idempotent SQL-проверки зелёные. На VPS новый релиз запущен: `garagebalance-staging.service` active/running, `nginx -t` успешен, публичные `/health/live` и `/health/ready` вернули `status=ok`, PostgreSQL — `ok`, главная страница отвечает HTTP/2 200. Финальный Word-отчёт обновлён фактическими итогами, экспортирован Microsoft Word в PDF/26 PNG и полностью визуально проверен.
- 31.08.2026 — пункт 5 дополнен системной настройкой «Название фонда» в разделе «Настройки» → «Отображение». По умолчанию флаг выключен и таблица сохраняет компактный вид только с наименованием услуги; при включении связанный фонд показывается второй строкой. Значение хранится новым битом в существующей настройке отображения, поэтому миграция схемы не требуется; изменение фиксируется в истории аудита.
- 31.08.2026 — пункт 6 уточнён по фактическому сценарию годовой таблицы: первое показание в пустой ячейке теперь сохраняется сразу без окна «Сохранить показание за другой месяц?». Изменение уже существующего показания вне текущего месяца вынесено в отдельную системную настройку, по умолчанию выключено и дополнительно защищено прежним правом пользователя; backend запрещает обход выключателя прямым API-запросом, а аудит корректировки сохранён. Полный gate: backend 2372/2372 на отдельной PostgreSQL 17, покрытие строк 90,55% и ветвей 74,07%; frontend 995/995, statements 88,52%, branches 80,24%, functions 86,08%, lines 89,40%. Успешны Release-сборка, lint, bundle 273071/273408 байт, форматирование, privacy-проверка 1139 файлов, npm/NuGet audit и идемпотентный SQL миграций; схема БД не менялась.
- 31.08.2026 — после получения 12 новых upstream-коммитов локальный `master` объединён с `origin/master=1ffdc902` без потери трёх ранее проверенных локальных исправлений. Финальный gate настройки названия фонда на объединённом коде завершён: backend 2363/2363 на отдельной PostgreSQL 17 без пропусков, покрытие строк 90,54% и ветвей 74,06%; frontend 992/992, покрытие statements 88,52%, branches 80,25%, functions 86,11%, lines 89,38%. Успешны backend format, frontend lint и production build, bundle 272512/273408 байт, privacy-проверка 1138 файлов, Docker-проверки, строгая UTF-8-проверка, npm/NuGet audit и генерация идемпотентного SQL миграций размером 372103 байта. Новых зависимостей нет; общий gzip-лимит увеличен на 1 KiB из-за новой пользовательской настройки, фактический рост остаётся внутри этого резерва.
- 31.08.2026 — исправлено создание гаража с начальной просрочкой. При нулевом балансе просрочка автоматически формирует равный ей долг со знаком минус в форме; вручную введённый отрицательный баланс также принимается, а в расчётной модели сохраняется одна сумма долга без удвоения. Единое правило применено в карточке контрагента, справочнике гаражей, создании гаража из владельца и корректировке начального баланса; ошибочная просрочка больше общей задолженности по-прежнему блокируется. Схема БД и исторические суммы не менялись. Полный gate: backend 2374/2374 на отдельной PostgreSQL 17, покрытие строк 90,55% и ветвей 74,10%; frontend 1002/1002, statements 88,62%, branches 80,33%, functions 86,21%, lines 89,48%. Успешны Release-сборка, format, privacy-проверка 1141 файла, lint, production build, bundle 273330/273408 байт, npm/NuGet audit, JSON и идемпотентный SQL миграций 372103 байта.
- 31.08.2026 — пункт 8 исправлен по последнему уточнению заказчика: при нескольких ставках в одном месяце используется простое среднее арифметическое значений без веса по календарным дням, после чего средняя ставка применяется ко всему месячному объёму, числу людей либо одному фиксированному месяцу. Окно «Расчёт суммы» показывает формулу среднего, прямое указание, что длительность периодов не влияет, и итоговую месячную формулу; интервалы ставок сохранены для проверки происхождения значений. Период без тарифа в среднее не входит, а смена базы расчёта с несовместимыми единицами явно блокируется. Полный gate: backend 2374/2374 на отдельной PostgreSQL 17 без пропусков, покрытие строк 90,62% и ветвей 74,11%; frontend 1003/1003, statements 88,62%, branches 80,32%, functions 86,21%, lines 89,48%. Успешны backend Release без предупреждений, format, privacy-проверка 2418 файлов, NuGet/npm audit, frontend lint/build, bundle 273384/273408 байт, JSON, `git diff --check` и идемпотентный SQL миграций 372103 байта; схема БД не менялась.
- 31.08.2026 — форма «Новое начисление» уточнена по пункту 9: поле «Основание» больше не выбирает первую позицию справочника автоматически. При открытии оно пустое, поэтому нативный список не фильтруется по первой записи и пользователь может выбрать любую активную позицию либо ввести собственное основание; сумма заполняется только после совпадения с выбранной готовой позицией. Добавлена frontend-регрессия пустого начального состояния, полного набора вариантов и автоподстановки суммы после выбора.
- 31.08.2026 — полный gate уточнения пункта 9 завершён: backend 2374/2374 на отдельной PostgreSQL 17 без пропусков, покрытие строк 90,6% и ветвей 74,1%; frontend 1003/1003, statements 88,63%, branches 80,34%, functions 86,21%, lines 89,49%. Успешны Release-сборки backend/frontend, lint, bundle 273365/273408 байт, backend format, privacy-проверка 2118 файлов, npm/NuGet audit, JSON, `git diff --check` и генерация идемпотентного SQL миграций 372103 байта; схема базы данных не менялась.
- 31.08.2026 — в таблице поступлений гаража уменьшена доля колонки «Услуга» до 32% и перераспределено место в пользу колонок «Разница», «К оплате», «Оплачено» и «Баланс». Для таблицы закреплена минимальная ширина 1260 пикселей с горизонтальной прокруткой на более узких экранах и запретом переноса денежных значений. CSS-регрессия прошла 47/47; локальная браузерная проверка на ширине 1280 пикселей подтвердила, что `999 999.99` и `-999 999.99` помещаются в одну строку без внутреннего переполнения.
- 31.08.2026 — полный gate изменения ширины колонок завершён: backend 2374/2374 на отдельной PostgreSQL 17 без пропусков, покрытие строк 90,6% и ветвей 74,1%; окончательный последовательный frontend-прогон 1003/1003, statements 88,63%, branches 80,34%, functions 86,21%, lines 89,49%. Один предварительный параллельный frontend-прогон дал 1002/1003 из-за того, что старый сценарий первичного администратора не дождался ленивого раздела «Платежи» под одновременной нагрузкой сборок; тот же сценарий отдельно и весь набор без конкурирующих процессов прошли. Успешны Release-сборки, lint, backend format, privacy-проверка 3907 файлов, npm/NuGet audit, JSON, миграционный SQL 372103 байта и bundle `273402 / 273408` байт без повышения лимита; схема базы данных не менялась.
- 31.08.2026 — расчёт ведомости ограничен месяцем регистрации гаража: при выборе более раннего периода регулярные начисления и обязательные строки показаний формируются только начиная с месяца создания карточки. Ошибочные неоплаченные регулярные начисления до регистрации отменяются при повторном расчёте с записью аудита; оплаченные записи сохраняются как финансовая история. То же ограничение применяется к массовому формированию регулярных начислений и автоматическому начислению по показанию счётчика. Добавлены unit- и PostgreSQL-регрессии для нового гаража, очистки ошибочной истории, сохранения оплаченной записи, счётчиковой услуги и массового расчёта; схема БД не менялась.
- 31.08.2026 — полный gate ограничения начислений по месяцу регистрации завершён. Backend на изолированной PostgreSQL 17: 2378/2378 без пропусков, покрытие строк 90,62% и ветвей 74,23%; frontend: 1003/1003, statements 88,63%, branches 80,34%, functions 86,21%, lines 89,49%. Успешны backend/frontend Release-сборки, lint, format, privacy-проверка 2119 файлов, npm/NuGet audit, JSON, `git diff --check`, bundle `273402 / 273408` байт и идемпотентный SQL миграций 372103 байта. Три прежние PostgreSQL-фикстуры исторических сценариев получили явные даты существования гаражей до проверяемых периодов; производственная схема БД не менялась.
- 01.09.2026 — дополнительно проверена годовая периодичность всех регулярных услуг. Стандартные «Членский взнос», «Целевой взнос» и «Наружное освещение» уже имели ежегодную настройку; устранена зависимость годового учёта от фиксированного списка внутренних кодов. Теперь настройка `Ежегодно` является определяющей для любой услуги: одно начисление создаётся в настроенном месяце, частичная оплата уменьшает переносимый остаток, а новые начисления в последующих месяцах не формируются. В ведомости добавлено явное пояснение «перенос остатка … новое начисление не создавалось»; ручные исторические годовые начисления и разовые сборы сохранены в прежней модели. Демонстрационный каталог также переведён на явную годовую периодичность вместо скрытого исключения по коду.
- 01.09.2026 — полный gate годовых начислений завершён на отдельной PostgreSQL 17: backend 2383/2383 без пропусков, покрытие строк 90,62% и ветвей 74,25%; frontend 1003/1003, statements 88,63%, branches 80,34%, functions 86,21%, lines 89,49%. Успешны Release-сборка, PostgreSQL-интеграции, backend format, privacy-проверка 1141 файла, frontend lint/build, bundle `273402 / 273408` байт, npm/NuGet audit, Docker preflight/distribution, строгая UTF-8/JSON-проверка, `git diff --check` и идемпотентный SQL миграций 372103 байта; схема базы данных не менялась.
- 01.09.2026 — показ пояснения «Причина» в таблице платежей вынесен в общую настройку с режимами «Только у штрафов», «У всех начислений» и «Не показывать»; безопасный режим по умолчанию — только штрафы. Для определения штрафа ведомость передаёт системный код вида начисления, поэтому поведение не зависит от его названия. Настройка влияет только на интерфейс, сохраняется в существующей таблице настроек с контролем версии и аудитом; причины и финансовая история не удаляются, миграция схемы не требуется. Финальный доступный backend-набор: 2143/2388, 245 PostgreSQL-интеграций пропущены из-за отсутствующей `GARAGEBALANCE_POSTGRES_TEST_CONNECTION`, падений нет; покрытие доступного набора 75,05% строк и 65,92% ветвей ниже общего CI-порога именно из-за этих пропусков. Frontend: 1007/1007; покрытие полного прогона до добавления последней чисто тестовой проверки — statements 88,64%, branches 80,30%, functions 86,20%, lines 89,50%. Успешны Release-сборка, lint, backend format, privacy-проверка 1142 файлов, npm/NuGet audit, строгая UTF-8/JSON-проверка, `git diff --check`, идемпотентный SQL миграций 372103 байта и bundle `273859 / 274432` байта; общий gzip-резерв увеличен на 1 KiB для новой настройки, лимиты начальной загрузки и крупнейших ресурсов не менялись.
- 01.09.2026 — исправлена повторная «Полная оплата» выбранного месяца. Причина подтверждена на сценарии со снимков: диалог суммировал техническое поле долга без вычета уже показанного в той же строке аванса, поэтому при нулевом итоговом балансе повторно предлагались 100 руб. целевого и 250 руб. членского взноса; второй платёж становился новым авансом. Теперь для каждой строки используется чистый остаток `max(долг − аванс, 0)`, полностью закрытые авансом строки исключаются, а для годового обязательства берётся последний фактический непогашенный остаток даже при выборе более раннего месяца. Перед подтверждением окно показывает распределение введённой суммы по услугам и расчётным периодам. Регрессии воспроизводят точные 100 + 250 руб., частично покрытую авансом строку 260 − 60 руб., старый месяц годового взноса и пользовательский сценарий гаража 777. Полный frontend gate прошёл 1010/1010, coverage: statements 88,72%, branches 80,50%, functions 86,29%, lines 89,58%; lint, production build и bundle `274400 / 274432` байта успешны. Backend-код и схема БД не менялись; доступный Release-набор прошёл 2143/2388, 245 PostgreSQL-тестов штатно пропущены из-за отсутствующей `GARAGEBALANCE_POSTGRES_TEST_CONNECTION`. Успешны backend format, privacy-проверка 1141 файла, npm/NuGet audit, строгий JSON, `git diff --check` и генерация идемпотентного SQL миграций 372103 байта.
- 01.09.2026 — в разделе «Платежи» → «Выплаты» итоговую строку поставщика можно раскрыть и увидеть состав суммы за выбранный период: отдельные начисления и выплаты с месяцем, датой операции, основанием, комментарием, источником и суммой. Детали загружаются только при раскрытии, ограничиваются точной парой «поставщик — услуга» и догружаются страницами; показаны отдельные итоги начислено/оплачено и число операций. Предусмотрены доступное управление с клавиатуры, скелетон загрузки, пустое состояние, ошибка на переднем плане с повтором и отмена устаревшего запроса. Полный доступный backend-набор прошёл 2148/2394, 246 PostgreSQL-интеграций пропущены из-за отсутствующей `GARAGEBALANCE_POSTGRES_TEST_CONNECTION`; покрытие доступного набора 75,13% строк и 65,96% ветвей ниже общего порога только из-за пропуска PostgreSQL-набора. Frontend прошёл 1012/1012, coverage: statements 88,67%, branches 80,48%, functions 86,27%, lines 89,55%. Успешны lint, production build, bundle `275811 / 276480` байт, backend format, privacy-проверка 1142 файлов, npm/NuGet audit, строгая UTF-8/JSON-проверка, `git diff --check` и генерация идемпотентного SQL миграций 372103 байта. Новых зависимостей и изменений схемы БД нет.
- 01.09.2026 — исправлено отображение эпизодических кассовых выплат без привязки к поставщику. Операции и раньше сохранялись и уменьшали кассу, но ведомость «Платежи» → «Выплаты» выбирала только расходы поставщиков и сотрудников, поэтому выплаты 333 и 9 руб. были невидимы в строках и итогах. Теперь такие операции группируются по получателю и статье расходов: в показанном сценарии выводится `TEST 1` / `Юридические расходы` / `342,00`, отменённые операции и другие месяцы исключаются. Для эпизодической строки не создаются фиктивные долг, аванс или повторное действие «Оплатить»; заголовок первой колонки уточнён до «Получатель». Backend-регрессии проверяют агрегацию, период, отмену и PostgreSQL-запрос, frontend-регрессия — итоговое отображение и отсутствие ложного действия. Полный доступный backend-набор прошёл 2149/2396, 247 PostgreSQL-интеграций пропущены без настроенной тестовой строки подключения; покрытие доступного набора 75,16% строк и 65,97% ветвей. Frontend прошёл 1013/1013, coverage: statements 88,67%, branches 80,50%, functions 86,27%, lines 89,55%. Успешны lint, production build, bundle `275849 / 276480` байт, backend format, privacy-проверка 1142 файлов, npm/NuGet audit, строгий JSON, `git diff --check` и генерация идемпотентного SQL миграций 372103 байта. PostgreSQL-службы на ПК работают, но тестовая роль/строка подключения отсутствуют; Docker daemon недоступен. Схема БД и зависимости не менялись.
- 01.09.2026 — добавлена расшифровка итоговой строки сотрудника в ведомости «Платежи» → «Выплаты». Стрелка рядом с ФИО открывает помесячные строки оклада, каждую премию и каждый штраф с документом и сохранённой причиной, а также фактические выплаты с датой, источником и комментарием. Над деталями показаны отдельные итоги `Оклад`, `Премии`, `Штрафы`, `Начислено` и `Оплачено`; штраф вычитается из начисления, отменённые выплаты исключаются, а текущий оклад появляется только после настроенного дня начисления. Детали загружаются по требованию, постранично, с отменой устаревшего запроса и общими состояниями загрузки, ошибки и повтора. Полный доступный backend-набор прошёл 2157/2405, 248 PostgreSQL-интеграций пропущены без тестовой строки подключения; покрытие доступного набора 75,29% строк / 66,02% ветвей. Frontend прошёл 1014/1014, coverage: statements 88,68%, branches 80,53%, functions 86,31%, lines 89,57%. Успешны backend/frontend Release-сборки, lint, format, privacy-проверка 1143 файлов, npm/NuGet audit, строгая UTF-8/JSON-проверка, `git diff --check`, bundle `276038 / 276480` байт и идемпотентный SQL миграций 372103 байта. Добавлена отдельная PostgreSQL-регрессия для объединения оклада, корректировок и выплат; локально она пропущена вместе с остальным PostgreSQL-набором из-за отсутствующей тестовой роли. Схема БД, зависимости и bundle-лимиты не менялись.
- 01.09.2026 — восстановлены отдельные начальные значения поставщика по правилу карточки гаража: долг вводится отрицательным балансом, аванс — положительным, а начальная задолженность при нулевом балансе автоматически формирует равный ей входящий долг без удвоения. Начальная задолженность хранится отдельно, прежние положительные стартовые остатки переносятся в неё миграцией; ограничение БД запрещает задолженность больше общего долга. После создания оба поля доступны только для чтения, обычное обновление API также блокирует их изменение; исправление выполняется существующей отдельной операцией с датой, причиной и аудитом. Ведомость выплат продолжает рассчитывать входящий остаток из нормализованного стартового баланса. Полный доступный backend-набор прошёл 2159/2408, 249 PostgreSQL-интеграций пропущены без `GARAGEBALANCE_POSTGRES_TEST_CONNECTION`; покрытие доступного набора 75,29% строк / 66,06% ветвей. Frontend прошёл 1017/1017, coverage: statements 88,71%, branches 80,58%, functions 86,29%, lines 89,60%. Успешны Release-сборки, backend format, privacy-проверка 1148 файлов, lint, production build, bundle `276293 / 276480` байт, npm/NuGet audit, строгая UTF-8/JSON-проверка, `git diff --check` и идемпотентный migration SQL 373387 байт. Добавлена PostgreSQL-регрессия backfill и ограничения; локально она пропущена вместе с остальным PostgreSQL-набором из-за отсутствующей тестовой роли.
- 01.09.2026 — устранено противоречие между начальным балансом и начальной задолженностью поставщика. Ранее правило допускало, например, отображаемый баланс `-125,00` при задолженности `13,00`; теперь отрицательный баланс и задолженность всегда равны по модулю, ввод любого из полей синхронизирует второе, а нулевой или положительный баланс обнуляет задолженность. Backend отклоняет несовпадающие пары, PostgreSQL-ограничение защищает данные вне интерфейса, а миграция выравнивает ранее сохранённые записи по начальному балансу, не изменяя входящий финансовый остаток. Локальная браузерная проверка подтвердила сценарии `-125 → 125`, `13 → -13` и `40 → пустая задолженность`. Полный backend-набор прошёл 2409/2409 на PostgreSQL без пропусков, coverage: 98,26% строк / 74,13% ветвей. Frontend прошёл 1018/1018, coverage: statements 88,67%, branches 80,54%, functions 86,32%, lines 89,56%. Успешны Release-сборки, backend format, privacy-проверка 1151 файла, lint, production build, bundle `276456 / 276480` байт, npm/NuGet audit, Docker Compose/distribution, строгая UTF-8/JSON-проверка, `git diff --check` и идемпотентный migration SQL 374758 байт. Первый полный backend-прогон выявил BOM в автоматически сгенерированных EF-файлах; после исправления обязательная UTF-8-проверка и повторный полный набор прошли.
- 01.09.2026 — итоговая интеграция всех 18 коммитов основной ветки перепроверена после получения удалённых ссылок: дополнительных рабочих веток и открытых PR не найдено. Полный backend-набор на отдельной PostgreSQL 17 прошёл 2408/2408 без пропусков; два сценария августа получили явную дату регистрации тестовых гаражей и больше не зависят от перехода UTC на сентябрь. Frontend прошёл 1017/1017, lint, production build и bundle `276293 / 276480` байт; покрытие frontend — statements 88,71%, branches 80,58%, functions 86,29%, lines 89,60%, backend — строки 90,71%, ветви 74,36%. На чистом Compose-томе применены все 125 миграций, API/frontend/PostgreSQL healthy, readiness и браузерная консоль без ошибок; отдельное исправление отсутствующего `curl` в API runtime-образе защищено контрактным тестом.
- 01.09.2026 — выполнена повторная полная приёмка ранее разработанных изменений на синтетической базе в чистом Docker Compose. В браузере Codex вручную пройдены вход, тарифы и настройки отображения, гараж с начальной просрочкой, ограничение начислений месяцем регистрации, первичный и исторический ввод показаний, свободный выбор основания начисления, причина штрафа, полная оплата выбранного месяца, поставщик с начальной задолженностью, расшифровки поставщика и сотрудника, эпизодическая выплата, отрицательный фонд с подтверждением, пополнение фонда, инвариант фондов и кассы/банка, пользователи, справочники, отчёты, импорт, аудит и «Что нового». Ошибок и предупреждений в браузерной консоли не обнаружено; после чистого перезапуска API журнал ошибок пуст, все три контейнера healthy. Полный backend-набор прошёл 2408/2408 без пропусков на PostgreSQL 17, покрытие строк 90,72% и ветвей 74,37%; frontend — 1017/1017, statements 88,71%, branches 80,58%, functions 86,29%, lines 89,60%. Успешны NuGet/npm audit, privacy-проверка 1148 файлов, backend format, lint, production build, bundle `276293 / 276480` байт, обе Docker Compose-конфигурации и идемпотентный SQL 373387 байт. Новых дефектов в проверенных сценариях не найдено, производственный код и схема БД не изменялись.
- 01.09.2026 — устранено молчаливое бездействие кнопки удаления поставщика: клиент больше не требует непустую причину, когда общая настройка комментариев выключена. Та же настройка теперь корректно применяется к удалению гаражей, сотрудников, отделов, контактов, нерегулярных платежей, тарифных порогов, услуг и сборов. В окне поставщика явно показано, обязательна ли причина, а опасное действие оформлено красной кнопкой с белыми текстом и значком. В разделе «Настройки» → «Отображение» название переключателя уточнено до «Требовать причину удаления и комментарии к другим действиям». Автоматическая регрессия подтверждает вызов API с пустой причиной в необязательном режиме; финальная Docker-проверка подтверждает оба режима, disabled-состояние, цвета и отсутствие ошибок в консоли. Полный backend-набор на отдельной PostgreSQL 17 прошёл 2408/2408, покрытие строк 90,72% и ветвей 74,37%; frontend — 1018/1018, statements 88,66%, branches 80,54%, functions 86,29%, lines 89,55%. Успешны lint, production build, bundle `276478 / 276480` байт, backend format, privacy-проверка 1148 файлов, npm/NuGet audit, JSON и идемпотентный SQL миграций 373387 байт. Схема БД не менялась.
- 01.09.2026 — в разделе «Настройки» → «Отображение» все семь параметров получили одинаковую клавиатурно-доступную справку: обязательность причин, стартовый вид ведомости, режим показа причин начислений, исторические показания, периодичность, месяц начисления и название фонда. Два постоянных пояснения перенесены в подсказки, поэтому форма стала компактнее; нажатие на `?` не меняет переключатель. Общий компонент справки переиспользован без изменения поведения остальных форм. Добавлены компонентные проверки наличия, текста, доступного описания, фокуса и независимости подсказки от переключателя; обновлены пользовательская инструкция и «Что нового» 1.174.0. Локальная браузерная приёмка на чистой Docker/PostgreSQL-среде подтвердила семь подсказок, независимость переключателя и отсутствие ошибок в консоли. Backend прошёл 2409/2409 без пропусков, coverage 90,72% строк / 74,37% ветвей; frontend — 1018/1018, statements 88,67%, branches 80,53%, functions 86,32%, lines 89,56%. Успешны lint, production build, bundle `276476 / 276480` байт, format, privacy-проверка 1150 файлов, npm/NuGet audit, JSON, строгая UTF-8 и идемпотентный SQL миграций 374758 байт. Схема БД и зависимости не менялись; временные контейнеры, база, администратор, сборки и отчёты покрытия удалены.
- 01.09.2026 — полностью исправлена компоновка вкладки «Настройки» → «Рабочая дата». Общий селектор больше не подавляет предусмотренные колонки карточек: на широком экране описание и рабочая форма расположены рядом, а при ширине до 1500 пикселей все карточки настроек складываются в одну колонку без переполнения. Состояние даты, действия и отдельный блок зарплаты выровнены; на телефоне кнопки занимают доступную ширину. Пояснения к дате и дню зарплаты перенесены в доступную справку `?`. Четыре ранее неоформленных подтверждения настроек переведены на общий диалог с закреплёнными заголовком, крестиком и нижней панелью действий. Регрессии защищают desktop/tablet/mobile-раскладку, общие классы диалога, справку и бизнес-сценарий подтверждения. Браузерная приёмка на чистой Docker/PostgreSQL-среде подтвердила стандартную ширину, отсутствие переполнения, правильный диалог и пустой журнал страницы. Backend прошёл 2409/2409 без пропусков, coverage 90,70% строк / 74,35% ветвей; frontend — 1018/1018, statements 88,67%, branches 80,53%, functions 86,32%, lines 89,56%. Успешны lint, production build, bundle `276480 / 276480` байт без повышения лимита, format, privacy-проверка 1150 файлов, npm/NuGet audit, JSON, строгая UTF-8 и идемпотентный SQL миграций 374758 байт. Схема БД и зависимости не менялись.
- 01.09.2026 — устранён выборочный конфликт при открытии ведомости гаража одновременно с автоматическим пакетным начислением. Причина заключалась в разном порядке чтения и блокировки: оба запроса могли увидеть отсутствие начисления, а второй затем нарушал уникальность активной строки и получал общее сообщение о конкурентном изменении. Расчёт ведомости и пакетный генератор теперь используют одинаковый порядок блокировок пары «гараж — вид начисления», а изменяемые начисления читают только после их получения; расчёты разных гаражей при этом не сериализуются общей блокировкой. PostgreSQL-регрессия воспроизводит гонку двумя независимыми сессиями, подтверждает успешное завершение обоих процессов и ровно одно активное начисление на каждый гараж. В браузере на отдельной синтетической PostgreSQL-базе без конфликтного сообщения открыты ведомости гаражей 101, 102, 103 и 104 с разными балансами. Полный backend-набор прошёл 2410/2410 без пропусков, покрытие 90,73% строк / 74,37% ветвей; frontend — 1018/1018, statements 88,67%, branches 80,53%, functions 86,32%, lines 89,56%. Успешны Release-сборки без предупреждений, backend format, privacy-проверка 1150 файлов, NuGet/npm audit, lint, production build, bundle `276480 / 276480` байт, `git diff --check` и идемпотентный SQL миграций 374758 байт. Схема БД, frontend и зависимости не менялись.
- 01.09.2026 — в разделе «Фонды» снято ограничение ширины 620 пикселей с карточек «Общий нераспределённый пул» и «Итого учтено средств»: обе теперь занимают всю ширину левой таблицы и сохраняют мобильное одноколоночное представление. Браузерная проверка на отдельной синтетической PostgreSQL-базе подтвердила одинаковые границы таблицы и карточек на широком экране (`517,175` пикселя) и при viewport 600 пикселей (`560,8` пикселя), отсутствие горизонтального переполнения и пустой журнал страницы. Полный backend-набор прошёл 2410/2410 без пропусков, покрытие 90,71% строк / 74,35% ветвей; frontend — 1018/1018, statements 88,67%, branches 80,53%, functions 86,32%, lines 89,56%. Успешны точечная responsive-регрессия 47/47, lint, production build, bundle `276479 / 276480` байт, backend format, privacy-проверка 1150 файлов, npm/NuGet audit и идемпотентный SQL миграций 374758 байт. Схема БД и зависимости не менялись; добавлено пользовательское «Что нового» 1.177.0.
