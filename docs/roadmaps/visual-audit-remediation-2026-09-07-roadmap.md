# Исправление дефектов визуального аудита 6–7 сентября 2026

## Источники и границы

- [Первичный журнал и последующие уточнения](full-visual-product-audit-2026-09-06-roadmap.md): доказательства до прохода 205; подготовка 206 не считается завершённой проверкой.
- [Замечания заказчика](customer-comments-2026-09-05-roadmap.md), корневой AGENTS.md, пользовательское поручение 07.09.2026.
- Каталог пользовательских релизов, конфигурации полного backend/frontend coverage, штатный Vitest runner, CI и инфраструктурные проверки.
- Исторические Word и материалы аудита остаются локальными; персональные данные не переносятся в регрессии.

## Решения и приоритеты

Работа ведётся локально в существующей ветке. Push, deploy и очистка VPS не разрешены этим этапом. Старый журнал сохраняется без ретроспективного редактирования. Опубликованные изменения 1.183.19–20 не закрывают DEF автоматически. Невоспроизведённый симптом и ограничение среды не равны исправлению.

Порядок: DEF-11; DEF-38/39; DEF-56; DEF-58; DEF-62; DEF-05/06/30/48/52/54; даты и валидация; история; диалоги и клавиатура; адаптивность. Связанные пункты можно объединять, но доказательства закрытия отдельны.

Для DEF-11 применяется допустимый в журнале вариант: архивный фонд и его ручные операции доступны только для чтения. При архивировании остаток уже возвращён в общий резерв; историческое изменение не должно повторно распределять средства в скрытый фонд. Отказ проверяется под существующей блокировкой распределения, история сохраняется.

## Проверяемые задачи

Каждый следующий пункт включает чтение всех уточнений соответствующего DEF в журнале, воспроизведение на текущем коде, регрессионные тесты, реализацию, проверки затронутых сценариев и запись результатов в историю. Детали каталога ниже — навигация; поздние подтверждённые уточнения первичного журнала имеют приоритет.
- [ ] DEF-01. Недоступные по правам действия остаются активными в интерфейсе.
  У operator/reports_viewer открываются команды и формы создания/редактирования в нескольких разделах без права записи. Backend возвращает 403. Исправить доступность и объяснение ограничений в UI. Успешная несанкционированная запись не была доказана; не называй это подтвержденным обходом backend-авторизации.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-02. Escape закрывает сразу вложенный отчет и карточку гаража.
  Из-за этого теряется несохраненный комментарий карточки. Закрытие крестиком верхнего окна работало корректно. Обработать Escape только верхним слоем, сохранить черновик и восстановить фокус.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-03. Связанная история скрывает существующие события.
  При переходе из отчетов поставщика/сотрудника или с фильтром связанного гаража часть событий справочников не видна. После снятия фильтра они находятся. Проверить область связи и фильтрацию. Потеря самих записей истории не доказана.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-04. Некорректная обработка отрицательной ставки зарплаты.
  Значение -1 доходит до подтверждения и общей серверной ошибки без ошибки у поля. Сервер запись отклоняет. Нужна согласованная валидация формы и API.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [x] DEF-05. Полностью оплаченный сбор исчезает из детализации ведомости.
  Итоги заголовка, нижней части таблицы и карточки расходятся на сумму оплаченного сбора. Баланс, история и фонд сохраняются; потеря денег не доказана. Проверить состав строк и формулы итогов, включая фильтры и выгрузки.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [x] DEF-06. Карточка гаража показывает устаревший долг после ручного начисления/пени.
  До перезагрузки или повторного выбора гаража сумма не обновляется, хотя другие представления уже корректны.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-07. Escape в открытом списке источника выплаты закрывает всю форму.
  Первое нажатие должно закрывать список, а не терять форму.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-08. Старая ошибка зарезервированного кода остается после исправления.
  После невалидного кода salary ввод корректного кода и новое подтверждение сохраняют старое красное сообщение, хотя сохранение может пройти.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-09. Слишком тесные пустые состояния таблиц.
  В истории и позднее в общем журнале показана маленькая подсказка возле заголовка/прокрутки вместо общего оформленного пустого состояния.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [x] DEF-10. Фильтр дат истории вызывает 500 в UTC+7.
  DateTimeOffset со смещением +07 передается в Npgsql timestamptz неправильно. Исправить преобразование границ дат с учетом локального бизнес-дня. Проверить PostgreSQL. Не показывать старые строки как результат нового неуспешного фильтра.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [x] DEF-11. Нарушение финансового равенства при редактировании операции удаленного фонда.
  В аудите изменение пополнения удаленного фонда с 80 до 81 оставляло 1 в скрытом фонде и уменьшало общий резерв, тогда как сумма активных фондов его исключала. Нарушалось равенство: касса + банк = учитываемые фонды + общий резерв. Контрольное изменение затем возвращено 81 → 80. Это приоритетный дефект. Разобраться в бизнес-правиле, обеспечить согласованность расчетов, транзакций и истории. Не лечить расхождение ручной правкой остатков.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-12. Архивный фонд отсутствует в фильтре исторических отчетов.
  Его операции видны в истории, но выбрать фонд нельзя. Обеспечить работу с историческими фондами и понятное обозначение архива.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-13. UI предлагает изменение/архивацию системной записи, которую сервер запрещает.
  Воспроизводилось на системных записях воды/зарплаты. Это отдельное ограничение системной сущности, не только права роли из DEF-01.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-14. Наложение содержимого карточек интеграций.
  На 1280×720 у оператора значения/даты выходят за карточки. Проверить соответствующие представления настроек и безопасности.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-15. Пустая вкладка интеграций для reports_viewer.
  При включенном VITE_SHOW_INTEGRATION_SETTINGS отображается вкладка без доступного содержимого. Согласовать видимость вкладки с правами и состояниями доступа.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [x] DEF-16. Общий журнал принимает обратный диапазон месяцев.
  Начало позже конца приводит к нулевому результату вместо понятной валидации.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-17. Ошибка редактирования конкретного существующего ручного начисления.
  Первоначально не редактировалось историческое начисление на 75. Позднее новое ручное начисление воды 1 → 2 → отмена обработалось успешно. Этот успешный сценарий не закрывает исходный дефект. Воспроизведи точные условия старой записи.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-18. Отмененная пеня исчезает без доступного восстановления.
  Зафиксировано на гараже 904, сумма 10. Сначала проверь предусмотренный продуктом жизненный цикл. Не распространяй автоматически требование восстановления на все виды отмененных операций.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-19. Некоторые изменения пользователя обходят обязательную причину.
  Редактирование пользователя и сброс пароля администратором проходили без причины при включенном требовании комментариев. Восстановление пользователя позднее корректно требовало причину: не объявляй дефектом все пользовательские операции.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-20. В фильтре истории отсутствует раздел настроек.
  События настроек существуют, но соответствующий выбор недоступен.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-21. Финансовый фильтр истории не включает изменения настроек кассы/банка.
  Проверить классификацию событий и ожидаемую область фильтра.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-22. Порядок операций кассы/банка внутри дня не хронологический.
  Проверить сортировку с учетом времени и стабильного дополнительного ключа.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-23. Пользователю показывается техническое Failed to fetch.
  В нескольких формах и отчетах нужны понятные русские сообщения, повтор запроса и сохранение введенных данных.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-24. После закрытия уведомлений фокус уходит на BODY.
  Восстановить фокус на вызвавшем элементе в подтвержденном сценарии.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-25. История показаний содержит неверное значение расхода.
  В истории отображалось 20 при фактическом изменении 9 → 10 → 9. Финансовая сумма была корректна. Не смешивать с DEF-63.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-26. Раздел платежей создает чрезмерное горизонтальное переполнение.
  Исправить компоновку на узких экранах, сохранив работоспособность плотной таблицы.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-27. На мобильном экране обрезаются элементы рабочей даты/зарплаты.
  Проверить доступность всех полей и команд.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-28. Неудобная/обрезанная мобильная компоновка финансового отчета сотрудника.
  Проверить ширину, прокрутку, фильтры и действия.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-29. Третий тарифный порог имеет доступное имя второго.
  Исправить уникальные подписи и связь label с полем; проверить несколько порогов.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [x] DEF-30. Годовой сводный отчет учитывает только декабрь.
  Сентябрьские результаты выпадают из годового итога. Проверить полный год, границы периода, запрос, группировку и экспорт.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-31. Escape не закрывает раскрытый фильтр фондов.
  Проверить закрытие списка и возврат фокуса без закрытия родительского интерфейса.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-32. В истории удаления быстрого списка показаны внутренние коды.
  Сделать описание понятным пользователю, сохранив технические связи события.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-33. Закрытие подтверждения выхода кликом по подложке теряет фокус.
  После закрытия activeElement становится BODY.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-34. Закрытие форм пользователей/ролей по подложке теряет фокус.
  Исправить общий механизм там, где возможно, и проверить затронутые формы.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-35. Подсказка поиска пользователей обещает поиск по роли, который не работает.
  Сверить требования: реализовать обещанное поведение либо привести подсказку в соответствие реальной функции.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-36. Ошибка пагинации оставляет старые строки с новым номером страницы.
  Не показывать несогласованные данные. Проверить ошибку, повтор, смену фильтра и отмену устаревшего ответа.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-37. Нарушения клавиатуры и фокуса контекстных меню.
  В пользователях стрелки переводили фокус на TR, Tab уходил на фон; после формы фокус мог уходить на BODY. В общем журнале стрелки работали, но Tab с последнего пункта оставлял меню открытым при уходе фокуса. Не утверждай, что все клавиши не работают во всех меню. Исправляй общий компонент с проверкой конкретных вариантов.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [x] DEF-38. Скрытое право payments.read остается у роли и не снимается через UI.
  Согласовать каталог разрешений и редактор с фактически сохраненными правами. Не удалять неизвестные права без разбора их назначения.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [x] DEF-39. Устаревший редактор роли перезаписывает изменения из другой вкладки.
  Может повторно выдать только что отозванное право. Нужна защита от потерянного обновления и понятная обработка конфликта, например версия/409.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-40. Интерфейс разрешает редактирование поступления целевого взноса, сервер возвращает 400.
  Сначала установить правильное бизнес-правило, затем согласовать UI и API. Не разрешать финансовое изменение вслепую ради устранения ошибки.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-41. Закрытие вложенного подтверждения платежа теряет фокус.
  Отмена, Escape и крестик приводят к BODY. Фокус должен возвращаться в правильный родительский контекст.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-42. Причина в истории обрезается по точке.
  Примеры: сен.26 превращается в сен; 100.60 — в 100. Проверить форматирование/разбор/маскирование, не ослабляя защиту персональных данных.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-43. В структурированном поле причины отмены хранится общий текст.
  Показывается «Отмена финансовой записи» вместо введенной причины, хотя пользовательский текст может оставаться в описании. Согласовать поля и представление истории.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-44. Ошибка загрузки истории платежей выглядит как отсутствие платежей.
  Разделить состояния ошибки, загрузки, пустого результата и успешного повтора.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-45. Нулевая/отрицательная сумма при редактировании платежа молча блокирует сохранение.
  Нет понятной ошибки у поля, PUT не отправляется. Не описывать это как сохранение некорректного платежа.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-46. Пустые обязательные дата/месяц платежа доходят до подтверждения и общего 400.
  Для пустого месяца в предпросмотре появлялось «-01». Валидировать до подтверждения и запроса, сохраняя серверную защиту.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-47. История изменения месяца платежа содержит неверные «до/после».
  Вместо 09.2026 → 10.2026 показаны описание платежа и число 364. Сам месяц сохранялся правильно.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [x] DEF-48. Частичная оплата выбранного месяца подменяет общий долг гаража месячным.
  Контрольный пример: общий долг 2441,58; долг месяца 383,37; оплата 1; ожидаемый общий долг 2440,58; в карточке появлялось 382,37 до повторного выбора. Проверить семантику garageDebtAfter в ответе и обновление клиентского состояния. Это отличается от обычного устаревшего состояния DEF-06.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-49. Escape в списке периода «Оплатить все» закрывает весь диалог.
  Теряется черновик. Воспроизводилось онлайн и офлайн. Первое нажатие должно закрывать список.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-50. Вкладки отчетов с ролью tablist не поддерживают стрелки/Home/End.
  Все восемь вкладок имели tabindex=0. Tab и Enter работали. Реализовать корректную доступную модель вкладок без ложного заявления о полной недоступности с клавиатуры.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [x] DEF-51. Пустая обязательная дата отчета незаметно заменяется рабочим месяцем.
  Поле выглядит пустым, но запрос использует скрытое значение. Затрагивались фонды, касса, банк и поступления. Нужна явная валидация либо видимое эффективное значение согласно выбранному правилу.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [x] DEF-52. Сортировка долга после оплаты в отдельных строках поступлений неверная.
  Значения идут не по возрастанию/убыванию; групповой режим работал. Проверить сортировку именно отображаемого числового показателя и серверную пагинацию.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [x] DEF-53. Очистка обязательных месяцев в отчетах приводит к общему API 400.
  Сверить точные отчеты и календари с журналом. Проверить пустые границы, обратный диапазон и восстановление корректного периода.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [x] DEF-54. Загрузка/ошибка отчета по сборам показывает нулевые финансовые итоги.
  Нули отображаются как будто рассчитанный результат; повтор восстанавливает реальные значения. Использовать корректные состояния недоступных/загружаемых итогов.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-55. Закрытие создания, редактирования и удаления быстрого списка теряет фокус.
  Подтверждены Отмена и Escape в нескольких сценариях. Проверить возвращение к конкретной вызвавшей кнопке.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [x] DEF-56. Устаревший редактор быстрого списка перезаписывает состав из другой вкладки.
  В одной вкладке удален гараж, во второй изменено только имя, но сохранение возвращает старый состав. Нужна защита от потерянного обновления и понятное разрешение конфликта.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-57. Редактор сгруппированного начисления зарплаты показывает неверную заблокированную услугу.
  В строке зарплата, в редакторе «Контрольное обслуживание». Сохранение в этом сценарии не проверялось. Не утверждать, что неверная услуга записалась в БД.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [x] DEF-58. Во время запроса групповой зарплаты доступны Escape и «Закрыть без сохранения».
  Финансовый запрос при этом может завершиться сохранением; повторный 409 отображался внутри подтверждения закрытия. Согласовать состояние ожидания, закрытие, повторную отправку и идемпотентность. Не обещать отмену уже отправленной операции без реального механизма.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-59. Редактирование существующего поступления/выплаты озаглавлено как создание.
  Режим и данные существующей записи корректны, но заголовок «Новый». Дублирование записи не доказано.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-60. Отброшенное новое показание -1 появляется в следующей новой форме.
  При этом форма не обозначена измененной. POST и сохранение не выполнялись. Исправить сброс/инициализацию черновика.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-61. Контекстное меню показаний выходит ниже экрана у viewer.
  При размере 934×678 меню выходило за нижнюю границу, все четыре команды были отключены. Повторено через Shift+F10/ContextMenu. Активные команды администратора в этом конкретном сценарии не проверялись.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [x] DEF-62. Новая банковская выплата из общего журнала не дает подтвердить отрицательный остаток фонда.
  На банковском счете средств достаточно, но фонд отрицательный. В обычном добавлении выплаты нужное подтверждение есть. Обеспечить согласованное поведение обеих точек входа. Не разрешать отрицательный фонд без явного предусмотренного подтверждения; сохранить backend-проверки и аудит.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [ ] DEF-63. Комментарий автоматического начисления не обновляется после изменения/отмены показаний.
  Сумма менялась 100,60 → 201,20 → 0 корректно, расход в комментарии оставался 1. Наличие активного нулевого начисления само по себе в этом наблюдении не объявлялось дефектом. Не смешивать с неверным расходом в аудите DEF-25.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

- [x] DEF-64. Маскирование персональных данных повреждает технический UUID в истории.
  Часть идентификатора показания заменяется на «[документ скрыт]» в таблице, карточке и relatedDocumentId. Исправить обработку технических идентификаторов с учетом поля/типа данных. Не отключать защиту реальных документов и персональных данных. Поиск по полному UUID в исходном проходе не проверялся.
  Проверка: регрессия исходного сценария; успешный/пустой/невалидный/ошибочный путь, права, повтор, отмена и устаревший ответ по применимости. Для финансов — PostgreSQL, остатки, транзакции и аудит; для UI — пользовательский workflow, клавиатура и узкий экран.

## Общие этапы проверки и приёмки

Backend: тонкие контроллеры и серверные права; доменные/сервисные/API регрессии. Frontend: общие компоненты, ошибки без ложных нулей, защита черновиков и фокуса. Данные: изолированные PostgreSQL-тесты, миграции только при изменении схемы; прежние базы воспроизведения не очищаются.

Перед каждым логическим коммитом: полные suites и coverage, lint, production build, bundle budget, backend formatting, privacy, diff и строгий UTF-8; SQL/миграции при изменении данных. Документация и «Что нового» обновляются вместе с кодом. Docker-конфигурация меняется при изменении эксплуатационных требований. Публикация и проверка VPS — только по отдельному поручению.

## Определение готовности

Все 64 пункта имеют индивидуальные подтверждённые результаты и статус [x]; финансовые равенства и история сохраняются; обязательные gates пройдены без ослабления. Невоспроизведённые условия описаны явно и не объявлены закрытыми. Процессы этапа остановлены, временные материалы удалены, логические коммиты сохранены. Процент считается только по 64 пунктам выше, без истории.

## Риски и открытые вопросы

- Бизнес-правила DEF-18/40 и точные условия DEF-17 требуют сверки с кодом и поздними уточнениями; пока это открытые пункты, а не подтверждённая необходимость нового правила.
- Архивные финансовые связи требуют отдельной проверки: запрет ручного изменения операции фонда не доказывает безопасность всех поступлений/выплат.
- Бюджет frontend близок к пределу; увеличение лимита ради прохождения не допускается.
- Реальные ответы внешних сервисов и production не подменяются синтетической проверкой.
- Старые .playwright-mcp, tmp, материалы аудита и прежние процессы не принадлежат этапу.

## История выполнения

### 07.09.2026 — начало реализации, DEF-11

Сверены HEAD 1c31555e и удалённые master/рабочая ветка: совпадают. Исходные незакоммиченные каталоги .playwright-mcp и tmp не включаются в работу. Изучены все упоминания DEF-11 и соседние проходы архивирования. Подтверждена причина в коде: Update/Cancel/Restore пересчитывают архивный фонд, тогда как сверка исключает архивные фонды. Добавляется PostgreSQL-регрессия с отдельной автоматически удаляемой БД; финансовые данные аудита и VPS не меняются. Проверки и исправление ещё выполняются, закрытых DEF 0/64.
Проверка до исправления на PostgreSQL 17: тест воспроизвёл разрешённые update 80→81, update изъятия, cancel и restore после архивирования. Изменение реализовано через серверный отказ fund_archived (HTTP 409) под блокировкой распределения; DTO и обе PostgreSQL-проекции передают IsFundArchived; UI показывает режим просмотра. Добавлены три API-регрессии, сквозной PostgreSQL-тест и App workflow; обновлены проверки компактных проекций.

Первый focused frontend: 1/1 выбранный тест прошёл, 1114 остальных намеренно не запускались этим фильтром; это не полный gate. В браузере проверен настоящий компонент на синтетических ответах при 1280×720 и 390×844: обе архивные строки читаются с пояснением, активная сохраняет команды; Tab переводит фокус между активными командами. Это визуальная проверка компонента, не сквозная проверка живого API.

Ограничения среды: подключение приложения из user secrets не имеет CREATEDB; штатный локальный postgres позволяет изолированные тесты. На пользовательском сервере focused набор 95/106 прошёл, 11 ошибок из-за заполнения C:, такой прогон не считается успешным. Для повторов создан временный PostgreSQL на D:5434. Одновременные suites/quality перегрузили память/диск, возникли таймауты удаления тестовых БД; прогоны остановлены. Повтор выполняется последовательно с меньшим параллелизмом, без изменения таймаутов и assertions. Для временного кластера выбрана та же русская локаль, что у прежней локальной базы. Format verify, privacy (1385 файлов), строгий UTF-8 15 изменённых файлов и diff --check прошли; полные gates ещё выполняются.

Подготовлен следующий этап: прочитаны все упоминания DEF-38/39. В редакторе отсутствует отдельный переключатель payments.read, хотя зависимость payments.write его сохраняет; UpdateRolePermissionsRequest не передаёт версию роли. Это вывод чтения кода, не новое UI-воспроизведение или исправление; статусы этих DEF остаются открытыми.
Полный frontend gate завершён штатным runner с VITEST_MAX_WORKERS=2 и VITEST_APP_SHARDS=4: 100/100 файлов, 1115/1115 тестов, 439,33 с; statements 87,96% ≥78%, branches 80,51% ≥69%, functions 85,72% ≥74%, lines 89,16% ≥79%. Пропусков нет. Следующий полный backend-прогон ограничен двумя потоками временным xunit.runner.json в bin, без изменения исходной конфигурации проекта.

Повтор backend при fsync=on на D: снова получил таймауты DROP DATABASE: pg_stat_activity подтвердил DataFileSync и CheckpointDone даже при свободных 4,7 ГБ памяти. Прогон остановлен; временный кластер переведён в fsync=off, запущен полный повтор. Это проверяет реальные PostgreSQL-запросы, ограничения, транзакции, миграции, остатки и аудит, но не устойчивость к аварийному отключению питания. Пользовательский PostgreSQL на 5433 и его настройки не менялись.

Автоматическая проверка разрешений отклонила Remove-Item для остановленного первого кластера D:\GarageBalance-remediation-20260907\postgres: blocked by policy. Обход удаления не выполняется; этот временный материал остаётся ограничением очистки. Независимые проверки продолжаются.
Полный backend выявил дополнительный воспроизводимый дефект: FundServiceTests.DeleteFundAsync_DetachesLinkedSuppliersAndPreservesServices падает при случайном UUID, содержащем десять цифр, из-за PassportRegex в AuditTextMasker. Это та же причина, что DEF-64 (прочитан проход 205). Добавлены три детерминированных примера: до исправления два не проходят, один проходит. UUID в тесте архивирования закреплён именно на проблемном значении, утверждения не ослаблены. Маскирование полных синтаксически корректных UUID отделено от маскирования свободного текста; секреты и явно подписанные личные данные скрываются до выделения UUID. Дополнены проверки writer→DTO и frontend-карточки истории.

DEF-64 переведён в работу как связанный блокирующий тестовый gate; для его полного закрытия отдельно требуется приёмка исходного сценария создания/отмены показания в живом интерфейсе. Уже повреждённые сохранённые текстовые поля автоматически не восстанавливаются без надёжного источника. Остальные 62 DEF не начаты. DEF-11 ожидает окончательного полного прогона после этой связанной правки.
Связанный focused backend после исправления UUID: 142/142, 0 пропусков, 2м58с. Включает PostgreSQL-сценарий DEF-11, получение истории обеими проекциями, проверки HTTP 409, детерминированный проблемный UUID при архивировании фонда и проверку writer→DTO. Backend format verify на новом коде прошёл. Полные suites после последней правки ещё выполняются.
Окончательный полный frontend после проверки UUID: 100/100 файлов, 1115/1115 тестов, 0 пропусков, 590,63 с; statements 87,96%, branches 80,51% (9602/11925), functions 85,72%, lines 89,16%. Все пороги сохранены. Команда из frontend: VITEST_MAX_WORKERS=2, VITEST_APP_SHARDS=4, node scripts/run-vitest.mjs --ci --coverage.enabled=true --coverage.reportsDirectory=D:/GarageBalance-remediation-20260907/frontend-uuid. Полный backend после последней правки продолжает выполняться.
Итоговый backend обнаружил устаревшую структурную проверку BackendPerformanceGuardTests.FundOperationsAndReleaseLists_KeepNormalizedOutputBounds: она ожидала прежнюю проекцию Fund без IsArchived. Проверка обновлена и усилена требованиями передачи признака архива в обеих PostgreSQL-проекциях; ограничения Take, число SQL-материализаций и запрет лишних полей сохранены. Предыдущий прогон остановлен как уже неуспешный. Начат полный gate с пересборкой и четырьмя потоками xUnit при свободных 7,7 ГБ памяти; исходные таймауты/пороги/состав тестов не менялись.

Последний frontend lint, tsc/production build и bundle budget прошли после всех изменений UI-тестов. Размер 283810/284672 байт, запас 862; превышение рекомендованного размера отдельного Vite chunk остаётся предупреждением, установленный общий бюджет проходит без изменения лимита.
### 07.09.2026 — итог первого этапа

DEF-11 закрыт: подтверждён отказ изменения 80→81, изменения/отмены изъятия, восстановления и повторного создания операции после архивирования; на свежих PostgreSQL-контекстах сохранены сверка 200 = 0 + 200, нулевой остаток архивного фонда, все операции и количество событий аудита. История в обеих выборках передаёт состояние архива, UI показывает режим просмотра. Проверки активного жизненного цикла и финансовых связей прошли полным набором. Пользовательская запись подготовлена в версии 1.183.21.

Итоговые проверки последнего кода:
- Полный backend: 2691/2691, 0 пропусков, 12м20с; coverage lines 88,54% ≥85%, branches 74,18% ≥70%. Команда из корня: dotnet test backend/GarageBalance.Api.Tests/GarageBalance.Api.Tests.csproj --no-restore --collect:"XPlat Code Coverage" --settings backend-coverage.runsettings --results-directory D:/GarageBalance-remediation-20260907/backend-gate. Проверка verify-backend-coverage.ps1 прошла. Сборка Debug выполнена этим запуском. Временный runner ограничивал параллельность четырьмя потоками, файл удалён после тестов.
- Полный frontend после всех правок: 1115/1115, 100 файлов; statements 87,96% ≥78%, branches 80,51% ≥69%, functions 85,72% ≥74%, lines 89,16% ≥79%.
- Последние frontend lint, production build, bundle budget и backend format verify прошли. Bundle 283810/284672 байт, запас 862.
- Privacy: 1385 файлов, passed. Строгий UTF-8 и git diff --check проверяются повторно перед коммитом. Миграция схемы не нужна; PostgreSQL-запросы, существующие миграции и транзакции проверены полным набором.
- PostgreSQL 17: отдельный кластер D:5434, русская локаль, fsync=off после подтверждённых локальных задержек физической синхронизации. Устойчивость к отключению питания не проверялась. Служба и прежние базы на 5433 не изменялись. Docker build, CI, push и deploy не выполнялись.

DEF-64 остаётся [~]: причина и регрессии исправлены в общем маскировании, карточка истории защищена frontend-тестом, но приёмка исходного живого сценария показаний и оценка ранее сохранённых повреждённых полей остаются. Следующий приоритет реализации — DEF-38/39. Нуждающихся в бизнес-решении/внешне заблокированных DEF на этом этапе не назначено.

Прогресс по 64 строкам, без истории: [x] 1 (1,56%), [~] 1 (1,56%), [ ] 62 (96,88%), [!] 0, [decision] 0, [acceptance] 0. Это не процент общего покрытия продукта.

Очистка: Vite остановлен, браузерная вкладка закрыта и viewport сброшен, оба временных PostgreSQL-кластера остановлены, dotnet build-server shutdown завершён. Командный аудит не обнаружил процессов этапа и слушателей 5177/5187/5434. Временный xunit.runner.json, файлы browser preview и сгенерированные App shards удалены. Прежние .playwright-mcp, tmp и материалы аудита не трогались; существующие build-каталоги остаются для локальной разработки.

Удаление обоих остановленных кластеров и остальных логов/coverage/scratch в D:\GarageBalance-remediation-20260907 отклонено автоматической проверкой разрешений (blocked by policy). Обхода другой оболочкой или инструментом не было. Эти файлы не являются частью коммита; они остаются только из-за ограничения очистки. Состав оставшихся материалов (кластеры — синтетические тестовые данные; остальные — результаты и служебные файлы проверок):
backend, backend-complete, backend-final, backend-final-uuid, backend-gate, backend-verified, frontend, frontend-final, frontend-uuid, postgres, postgres-ru, temp, backend-complete.log, backend-final-uuid.log, backend-final.log, backend-gate.log, backend-verified.log, backend.log, build-complete.log, build-final.log, build.log, bundle-complete.log, bundle-final.log, focused-fixed.log, format-gate.log, format-uuid.log, format.log, frontend-final.log, frontend-uuid.log, frontend.log, lint-complete.log, lint-final.log, lint.log, postgres-ru.log, privacy-uuid.log, privacy.log, uuid-red.log.

Изменения сохраняются только локально в work/word-tasks-2026-09-05; публикация требует отдельного поручения.

### 07.09.2026 — права ролей, DEF-38/39 (в работе)

Повторно сверены все упоминания DEF-38/39 в исходном аудите. Для DEF-38 показаны отдельные чтение/изменение платежей и справочников; каталог матрицы покрывает все известные разрешения. Исходный unit-тест каталога красный до изменения. Workflow проверяет выдачу, снятие изменения, видимость оставшегося чтения, отзыв с клавиатуры, сохранение и повторное открытие. Первые попытки workflow выявили отсутствие ожидания загрузки в новом тесте; ожидание сделано по доступной кнопке. Один широкий ошибочный запуск остановлен; целевые проверки проходят.

Для DEF-39 два новых детерминированных теста сначала воспроизвели отсутствие конфликта. Добавлена версия роли, обязательная на HTTP-границе, проверка до сравнения наборов и EF concurrency token с миграцией. PostgreSQL проверил конфликт двух загруженных контекстов: первое сохранение остаётся, второе не меняет права, сеансы и аудит. Редактор сохраняет черновик при ошибке и позволяет явно перечитать права; проверены ошибка перечитывания, исчезнувшая роль и успешное повторное сохранение новой версии. Focused backend 39/39, focused frontend 10 успешных (остальные исключены фильтром; это не полный gate). Миграция и дополнительные проверки ещё в работе, оба DEF не закрыты.

Повторно используется прежний локальный временный PostgreSQL-кластер D:5434; пользовательская служба 5433 не меняется. Push/deploy не выполнялись. Полные gates и визуальная приёмка ожидаются.

Живая приёмка DEF-38/39 выполнена в IAB с настоящими API и PostgreSQL на новой синтетической базе garagebalance_roles_smoke_20260907 (порт 5434). Снятие изменения оставляет явно видимое чтение; после отдельного отзыва новый login возвращает dictionaries.read,reports.read и чтение платежей — 403. Два открытых редактора: первый отзывает reports.read, второй получает конфликт, черновик остаётся. После явного перечитывания добавлено audit.read без восстановления reports.read. HTTP без версии — 400, со случайной устаревшей — 409. В БД ровно пять успешных событий изменения роли, неуспешные запросы не создают аудит. Просмотрены реальные снимки 1280×720 и 390×844; диалог прокручивается, действия доступны. Выявлен и исправлен общий CSS: выбранные disabled-флажки теряли контраст галочки; добавлена регрессия стиля.

Первый полный frontend остановлен после двух падений: старое ожидаемое число колонок 12 вместо 14 и имя кнопки Сохранить вместо Сохраняем... во время загрузки в новом тесте. Точные ошибки воспроизведены focused-прогоном, проверки обновлены без ослабления поведения. Последующий focused — 4 успешных; полный frontend начат заново после последних правок CSS/тестов. Backend gate продолжается.

Дополнительная приёмка DEF-64: через настоящий API создан контрольный гараж, указано стартовое показание воды 0, создано и отменено показание 1 за 09.2026. Поиск полного UUID в живой истории возвращает два события; обе карточки и relatedDocumentId содержат полный UUID, снимки просмотрены. Первое создание без стартового значения штатно отклонено 400; после заполнения выполнено успешно. Это не восстановление ранее повреждённых полей исходной базы; DEF-64 остаётся в работе до решения этой части. Реальные персональные данные не использовались.

Полный backend последнего кода: 2698/2698, 0 пропусков, 14м42с; lines 88,54% ≥85%, branches 74,20% ≥70%. Миграция существующих ролей проверена PostgreSQL-тестом: старые права сохранены, новые версии непустые и различны. Format verify прошёл; idempotent SQL успешно получен (397613 байт), privacy passed (1387 файлов). Последние lint/build/bundle до дополнительной правки кэша: 284005/284672 байт, запас 667. Проверки UI повторяются после кэша.

При чтении повторной загрузки найден связанный путь: после сохранения поиск пользователей возвращал старый promise кэша ролей. Успешное сохранение теперь обновляет кэш вместе с версией. Workflow продолжен поиском, повторным открытием и сохранением актуальной версии; focused 1/1 проходит. Второй незавершённый полный frontend остановлен, чтобы gate соответствовал окончательному коду; начат roles-frontend-gate.

Уточнение оставшейся части DEF-64 по коду: AuditEventWriter сохраняет EntityId/RelatedDocumentId отдельными строковыми полями без маскирования, но маскирует дубли в MetadataJson. AuditService предпочитает отдельные поля для DTO, однако служебные данные берёт из старого JSON. Это позволяет исследовать безопасное отображение прежних UUID из надёжных структурированных полей без изменения исторической записи; ещё не реализовано и не проверено на старом синтетическом событии. Требования к скрытию персональных значений сохраняются.

Очистка процесса: обе вкладки закрыты, viewport сброшен; API/Vite остановлены, smoke-база удалена штатным DROP DATABASE только из временного кластера, PostgreSQL5434 остановлен, xunit.runner.json удалён, dotnet build-server shutdown выполнен. Удаление новых roles-backend, roles-frontend, roles-smoke-keys, roles-smoke-import, roles-migrations.sql отклонено автоматической проверкой (blocked by policy); повторного обхода нет. Финальный frontend ещё работает; его очистка и итоговый аудит впереди.

### 07.09.2026 — итог этапа DEF-38/39

DEF-38 и DEF-39 закрыты: реализованы и проверены видимые read-права, отдельный отзыв, зависимости, пустой набор, защита версии на HTTP и в PostgreSQL, сохранение черновика, явное перечитывание, ошибки/отмена загрузки и актуальность кэша после поиска. Исходные live-сценарии повторены на синтетических данных через настоящий API; версия 1.183.22 содержит пользовательские изменения.

Итоговый frontend: 100/100 файлов, 1118/1118 тестов, 0 пропусков, 520,14 с. Statements 87,98% ≥78%, branches 80,53% (9620/11945) ≥69%, functions 85,74% ≥74%, lines 89,19% ≥79%. Команда: VITEST_MAX_WORKERS=2, VITEST_APP_SHARDS=4, node scripts/run-vitest.mjs --ci --coverage.enabled=true --coverage.reportsDirectory=D:/GarageBalance-remediation-20260907/roles-frontend-gate. После запуска полного набора существующий workflow дополнен проверкой пустого набора прав; этот сценарий отдельно успешно перезапущен из App.test.tsx (1/1) и повторно проверен ESLint. Производственный код после старта итогового полного прогона не менялся.

Итоговый backend: 2698/2698, 0 пропусков, 14м42с; lines 88,54% ≥85%, branches 74,20% ≥70%. Полный набор использовал реальный PostgreSQL17 на 5434, включая миграцию старых ролей и атомарность конфликта. Ограничение fsync=off прежнего временного кластера сохраняется: устойчивость при отключении питания не проверялась. Пользовательская служба 5433 не изменялась. Backend format, idempotent migration SQL, privacy, строгий UTF-8 и diff-check прошли. Последние frontend lint/build/bundle прошли: 284009/284672 байт, запас 663. Пороги и таймауты не изменялись. CI, Docker build, push и deploy не выполнялись.

Прогресс только по 64 DEF, без истории: [x] 3 (4,69%), [~] 1 (1,56%; DEF-64), [ ] 60 (93,75%), [!] 0, [decision] 0, [acceptance] 0. Следующие работы: DEF-56 и оставшаяся часть DEF-64, далее исходный порядок карты. Для DEF-56 прочитаны все упоминания: у GarageReportQuickList/UpsertGarageReportQuickListRequest отсутствует версия, ReportPanel сохраняет целиком имя и garageIds; реализация ещё не начата.

Финальный командный аудит: процессов этапа 0, слушателей5177/5187/5434 — 0, сгенерированных App shards — 0; временный xunit.runner.json отсутствует. Все браузерные вкладки этапа закрыты, viewport сброшен. Автоматическая проверка отклонила также отдельное удаление двух новых остановленных frontend-каталогов (blocked by policy); это не повторная попытка удаления ранее отклонённых путей. Обход не выполнялся.

Из созданных в этом этапе файлов остаются только следующие материалы в D:\GarageBalance-remediation-20260907 из-за отказа удаления: roles-backend (успешный backend coverage), roles-frontend (незавершённый неуспешный frontend), roles-frontend-final (прерванный frontend до правки кэша), roles-frontend-gate (успешный frontend coverage), roles-migrations.sql (проверенный idempotent SQL), roles-smoke-keys (ключи удалённой синтетической smoke-базы). roles-smoke-import фактически не был создан. Прежние ограниченные очисткой материалы предыдущего этапа сохраняются, их состав указан выше. Существующие build-каталоги нужны для локальной разработки. Временные файлы и старые .playwright-mcp/tmp в Git не добавляются. Этап сохраняется локальным коммитом без публикации.

### 07.09.2026 — DEF-56 и завершение DEF-64 (в работе)

Состояние начала: c4c5bac2, только прежние .playwright-mcp/tmp вне Git; процессов этапа не было. Все уточнения DEF-56 перечитаны. Два теста сначала воспроизвели сохранение/удаление устаревшего списка без конфликта. Добавлены Guid-версия агрегата, миграция, передача версии компактной проекцией и DTO, HTTP-требование версии для PUT/DELETE и guard до изменения. PostgreSQL проверяет откат состава/названия/аудита при конфликте и сохранение данных при обновлении схемы. UI передаёт версию, сохраняет черновик при конфликте, позволяет перечитать список и обрабатывает ошибку/исчезновение списка и отменённый запрос. Версия удаления обновляется явным перечитыванием.

DEF-64: четыре теста сначала подтвердили повреждённый UUID в старом MetadataJson при целом UUID отдельного поля. Теперь канонические технические ID в отображении берутся из целого UUID того же события только при старой числовой маске. Исторический JSON не изменяется; неизвестные ключи, неполные UUID, нулевые UUID и секретные маски не восстанавливаются. Проверены список/карточка/страница/CSV и сохранение скрытия паспорта/секрета. Живая приёмка старого синтетического события ещё требуется.

Focused backend 71/71, затем дополнительные 13/13 (legacy metadata, CSV и конфликт удаления); focused frontend 8 успешных, остальные исключены фильтром. Lint/build/bundle прошли: 284293/284672 байт, запас379. Первый format verify указал только переносы строк в новом AuditServiceTests; исправляются форматтером без изменения поведения. Полные backend/frontend gates запущены. Пользовательские изменения добавлены в 1.183.23. Используется прежний временный PostgreSQL5434 с ранее оговорённым fsync=off; служба5433 не меняется.

Живая приёмка DEF-56: в двух вкладках первая убрала гараж904, вторая получила409 при переименовании со старым составом; черновик сохранён. Явное перечитывание загрузило только901, затем переименование прошло. Удаление из старой вкладки получило409; перечитывание обновило название и версию, сохранило причину, повтор завершился успешно. Проверены1280×720 и390×844. SQL: архивный список содержит1гараж, ровно4события аудита (создание, два успешных изменения, удаление); конфликтные попытки не записали изменений.

Живая приёмка DEF-64: два старых синтетических события создания/отмены с повреждённым relatedDocumentId найдены по полному UUID. В обеих карточках полный ID объекта/документа/служебного поля, паспорт и apiToken скрыты. SQL подтвердил, что прежняя маска в обоих MetadataJson сохранена. Первый контрольный SQL использовал неверные имена таблиц и ничего не изменил; исправленный read-only запрос успешен. Синтетическая smoke-база удалена, API/Vite и две вкладки остановлены. Format verify после правки пробелов успешен, строгий UTF-8 без BOM21файла, privacy1389файлов и diff-check прошли.

Полный frontend обнаружил сбой существующего сценария пользовательских окон; подробности ожидаются по завершении. До успешного полного прогона коммит и закрытие пунктов не выполняются.
Полный backend успешно завершён: 2721/2721, 0 пропусков, 13м47с; coverage lines 88,57% ≥85%, branches 74,53% ≥70%. Реальные PostgreSQL-тесты подтвердили миграцию списка, конфликт обновления/удаления и откат состава/аудита. Временный PostgreSQL остановлен, xunit.runner.json удалён, dotnet build-server shutdown выполнен.

Первый полный frontend: 1120/1121, один сбой поиска кратковременного уведомления «Пользователь отключен.» после долгого DOM-запроса. Остальные 99 файлов прошли. Полная подмена таймеров в этом длинном workflow привела к его таймауту, попытка отменена; прерван только остаток фильтрованного прогона после уже полученной ошибки. Итоговый подход наблюдает уведомление одновременно с действием и проверяет его доступный status сразу при появлении. Таймауты, длительность показа и утверждения о закрытии/фокусе/ошибках не ослабляются. Повтор focused и полного gate требуется.
Стабилизированный пользовательский workflow: 1/1, остальные 433 сценария исключены фильтром; прямой запуск App.test.tsx. Повторные lint, production build и budget успешны: 284293/284672 байт. Полный frontend повторяется отдельно от backend. Разбор следующего DEF-58 начат только чтением: закрытие общего финансового редактора не проверяет saving; поля зарплаты также активны. Код этого пункта пока не менялся.

Автоматическая проверка разрешений отклонила очистку новых остановленных материалов (blocked by policy), обход не выполнялся. Командный read-only аудит подтвердил остатки в D:\GarageBalance-remediation-20260907: quick-history-backend (успешное покрытие backend), quick-history-keys (ключи уже удалённой синтетической базы), quick-history-migrations.sql (проверенный SQL миграций). quick-history-frontend и quick-history-import фактически отсутствуют. quick-history-frontend-final пока занят повторным полным прогоном и будет проверен после завершения. Прежние отклонённые к удалению материалы не затрагивались.
Повторный frontend до дополнительной правки завершился успешно: 1121/1121, 100 файлов, 442,97с; statements 88,03%, branches 80,58%, functions 85,76%, lines 89,23%, все пороги соблюдены. Во время финального чтения найден дополнительный путь DEF-56: перечитывание обновляло редактор и версию, но отмена/повторное открытие брали прежний внешний выбор. Расширенная регрессия сначала упала (после повторного открытия два гаража вместо одного). Явное перечитывание теперь синхронизирует и внешний выбор. Focused 4/4 прошли, полный frontend повторяется после этой правки. Последние lint/build/budget успешны: 284306/284672 байт, запас 366.

Дополнительная живая приёмка: отдельная синтетическая база, редактор двух гаражей, параллельное удаление904 через настоящий API, конфликт UI, перечитывание, Отмена, повторное открытие — только901. Последующее сохранение оставило один гараж; SQL подтвердил три успешных события аудита. Снимок просмотрен inline, файл не создавался. Новая база удалена, вкладка/API/Vite/PostgreSQL остановлены, build-server shutdown повторён. Дополнительные ключи и предыдущий успешный frontend coverage также не удалены из-за нового отказа автоматической проверки (blocked by policy); это отдельные новые пути, прежние отказы не обходились.
### 07.09.2026 — DEF-56/64 закрыты после финальных проверок

Итоговый frontend после исправления повторного открытия: 1121/1121, 100 файлов, 446,81с; statements 88,03% ≥78%, branches 80,58% ≥69%, functions 85,76% ≥74%, lines 89,23% ≥79%. Команда: VITEST_MAX_WORKERS=2, VITEST_APP_SHARDS=4, node scripts/run-vitest.mjs --ci --coverage.enabled=true --coverage.reportsDirectory=D:/GarageBalance-remediation-20260907/quick-history-frontend-gate. Таймауты и пороги не менялись. Backend актуального кода 2721/2721, 0 пропусков, lines 88,57% ≥85%, branches 74,53% ≥70%; после этого менялись только frontend и документация. Последние lint/build/budget, backend format, idempotent migration SQL (398252 байта), privacy (1389 файлов), строгий UTF-8 (21 файл) и diff-check успешны. Реальный PostgreSQL 17 использован; ограничение fsync=off временного кластера сохраняется, отключение питания не проверялось.

DEF-56 закрыт: конфликт сохранения/удаления, откат транзакции, версия старых списков, повтор после перечитывания, ошибка/исчезновение списка, отмена загрузки и отмена/повторное открытие редактора защищены тестами; исходный сценарий подтверждён двумя живыми вкладками. DEF-64 закрыт: новые UUID не маскируются как документы, старые канонические значения отображаются из целого отдельного поля без изменения исторического JSON; создание/отмена, поиск полного UUID и сохранение масок личных данных проверены в живом интерфейсе. Если полного технического поля нет, восстановление не выдумывает отсутствующие данные. Пользовательские изменения включены в «Что нового» 1.183.23.

Прогресс только по 64 DEF: [x] 5 (7,81%), [~] 0 (0%), [ ] 59 (92,19%), [!] 0, [decision] 0, [acceptance] 0. Следующий приоритет — DEF-58, затем DEF-62 и остальные пункты в исходном порядке. Полная карта ещё не завершена. CI не запускался; push, deploy и действия на VPS не выполнялись.

Финальный командный аудит: процессов этапа 0; слушателей 5177/5187/5434 — 0; сгенерированных App shards — 0; временного xunit.runner.json нет. Три вкладки этапа закрыты, обе синтетические базы удалены, build-server shutdown выполнен. Пользовательские и общие службы не останавливались. Существующие bin/obj/node_modules/dist сохранены для локальной разработки, прежние .playwright-mcp/tmp не менялись и не добавляются в Git.

Автоматическая проверка разрешений отклонила также удаление последнего отчёта покрытия (blocked by policy); обходов нет. Из новых материалов остаются ровно шесть путей внутри D:\GarageBalance-remediation-20260907: quick-history-backend (backend coverage), quick-history-frontend-final (успешный frontend до дополнительной правки), quick-history-frontend-gate (окончательный успешный frontend coverage), quick-history-migrations.sql (проверенный SQL), quick-history-keys и quick-cancel-keys (ключи уже удалённых синтетических баз). Они остаются только из-за отказа очистки; не являются частью коммита. Прежние ограниченные очисткой материалы перечислены в предыдущих записях истории. Результат сохраняется логическим локальным коммитом.

### 07.09.2026 — DEF-58 (в работе)

Предыдущий этап является прогрессом: коммит 6899636e, пять DEF закрыты. Начальное состояние текущего этапа: только прежние .playwright-mcp/tmp вне Git, новых процессов нет (службы пользователя и Codex сохранены). Перечитаны проходы 191–194 и DEF-58: подтверждено закрытие формы при отправленном запросе, backend повтор ранее отклонял. Требуется регрессия ожидания/закрытия, блокировка полей и повторной отправки, успех/ошибка/повтор и проверка сохранённого аудита.


DEF-58: frontend-регрессия сначала показала два POST до обновления React. Добавлены ref-защита повторной отправки/закрытия, отключение полей и доступный индикатор. PostgreSQL-регрессия воспроизвела два успешных пакета без номера документа: уникальный индекс допускает NULL. Начисление группы за месяц теперь сериализовано advisory lock до проверки существующих строк и сохранения. Focused backend 9/9, frontend 2/2; проверены отмена ожидания, освобождение соединения и повтор после отмены сохранения. Начальное ожидание числового createdCount в тесте исправлено под фактический строковый формат metadata, проверки количества/суммы/аудита сохранены. Начат DEF-62, перечитаны проходы201–202 и правило user-guide: требуется явное согласие при превышении фонда, банковский остаток проверяется отдельно.

DEF-62: общая и обычная формы используют единый компонент согласия. Согласие сбрасывается при изменении суммы, поставщика, источника и повторном открытии, недоступно при сохранении; backend-проверки банка/кассы сохранены. Focused frontend: 4/4 App workflow (новые DEF-58/62, прежние зарплата и обычная выплата), компонент 2/2. PostgreSQL-сценарий фонда 1/1 проверяет отказ без согласия, недостаток банка/кассы, успешную выплату, проводку фонда, аудит и сверку.

Живая проверка в отдельной синтетической базе garagebalance_salary_consent_smoke_20260907, API5187/Vite5177/PostgreSQL5434. Первоначально общая ведомость была скрыта настройкой: DOM/AX-доступность не принималась за доказательство видимого входа. Затем настройка включена через Настройки → Отображение; повтор выполнен из видимой ведомости. Два участника группы, зарплата1 без документа: при удержании advisory lock поля/закрытие/отмена отключены, Escape/Enter не открывают подтверждение закрытия; после освобождения — ровно два начисления на2. Повтор после ожидания возвращает отказ в основной форме, черновик1 сохранён, поля снова доступны. Снимки1280×900 и390×844 просмотрены inline.

Живая общая выплата: фонд−10, банк2, сумма1 без согласия отклонена; изменение суммы сбрасывает согласие; сумма3 с согласием отклонена банком. Сумма1 с новым согласием прошла, появилась одна выплата. API-сверка: банк/касса1, именованные фонды−11, резерв12, расхождение0, isReconciled=true. Аудит содержит negativeFundBalanceConfirmed="True"; первоначальный регистрозависимый SQL-поиск по true дал0, чтение фактического metadata подтвердило строковый формат. Узкий экран: полный текст и кнопки доступны внутренней прокруткой, отдельный снимок проверен. Все данные синтетические; база удалена, API/Vite/вкладка/psql остановлены, viewport восстановлен.

Полный backend: 2726/2726, 0 пропусков, 15м36с; покрытие строк88,58%≥85%, ветвей74,54%≥70%. Реальный PostgreSQL17.2, русская локаль/UTF8; временный кластер fsync=off, устойчивость при отключении питания не проверялась. Схема не меняется, новые миграции не требуются; применены существующие миграции и полная PostgreSQL suite. dotnet format --verify-no-changes --no-restore прошёл. Privacy1393 файлов, strict UTF8 без BOM11 файлов, diffcheck успешны.

Первый полный frontend: 1124/1125,101 файл; один сбой accessibleStatus из-за постоянного aria-live при условном role. Исправлена разметка: только pending содержит span role="status" aria-live="polite". Тесты/пороги не ослаблены, запущен полный повтор. После исправления lint, production build и bundle gate прошли:284610/284672 байт, запас62. Что нового1.183.24 описывает обе пользовательские правки.

Автоматическая проверка разрешений отклонила объединённую команду cleanup (blocked by policy), включая удаление временного backend/GarageBalance.Api.Tests/bin/Debug/net10.0/xunit.runner.json. Удаление не повторялось другим способом. Отдельные команды pg_ctl stop и dotnet build-server shutdown прошли; пользовательский PostgreSQL5433 и службы Codex не затронуты. Финальные frontend-результаты и аудит остатков будут записаны после завершения повторного прогона. Публикаций и действий на VPS нет.

### 07.09.2026 — DEF-58/62 завершены и проверены

Окончательный frontend:1125/1125,101/101 файл,499,44с. Statements88,10%≥78%, branches80,66%≥69%, functions85,83%≥74%, lines89,30%≥79%. Backend2726/2726 и все остальные gates описаны выше; повторный прогон backend после единственной последующей JSX-правки не требовался. DEF-58 и DEF-62 отмечены[x] только после полного успешного повтора frontend и живой проверки. Миграции/провайдер проверены на настоящем локальном PostgreSQL; CI не запускался, push/deploy/VPS не выполнялись.

Прогресс только по64 DEF, без Истории выполнения: [x]7 (10,94%), [~]0 (0%), [ ]57 (89,06%), [!]0, [decision]0, [acceptance]0. Полная задача остаётся активной. Следующий блок: DEF-05/06/30/48/52/54. Предварительное чтение DEF-06/48 подтвердило две разные ветви: commitGarageAccrual/commitPenaltyAccrual обновляют строки/итоги без карточки; строковая/полная оплата подставляет garageDebtAfter в общий баланс. Новые исправления и тесты этого блока ещё не начаты.

Финальный командный аудит:0 процессов этапа,0 слушателей5177/5187/5434,0 временных App shards. Вкладка закрыта, viewport сброшен, синтетическая база удалена, API/Vite/psql/PostgreSQL остановлены, dotnet build-server shutdown успешен. Пользовательские службы и общие Codex/MCP/IDE сохранены. Существующие bin/obj/node_modules/dist остаются для локальной разработки; прежние .playwright-mcp/tmp не менялись и не добавляются в коммит.

Автоматическая проверка разрешений отклонила также очистку последнего frontend-отчёта (blocked by policy). Read-only аудит подтвердил ровно четыре новых оставшихся пути: D:\GarageBalance-remediation-20260907\salary-consent-backend (успешное backend-покрытие), D:\GarageBalance-remediation-20260907\salary-consent-frontend-final (успешное frontend-покрытие), D:\GarageBalance-remediation-20260907\salary-consent-keys (ключи удалённой синтетической базы), C:\Users\ilel\source\repos\GarageBalance\backend\GarageBalance.Api.Tests\bin\Debug\net10.0\xunit.runner.json (временное ограничение4 потоков). salary-consent-frontend и salary-consent-import отсутствуют. Остатки сохранены только из-за отказа cleanup; обходов и повторов удаления этих путей не было, в Git они не входят. Прежние отклонённые пути перечислены в предыдущей истории. Этап сохраняется отдельным локальным русским коммитом.

### 07.09.2026 — DEF-05/06/48 в работе

Предыдущий этап — подтверждённый прогресс, коммит127b41b6;7/64 закрыты. Рабочее дерево исходно содержит только прежние .playwright-mcp/tmp. Перечитаны все упоминания DEF-05/06/48, включая расширение48 на строковый платёж и отмену. Требуется согласовать детализацию оплаченного сбора и отдельно общий баланс карточки/периодный остаток, покрыть начисление, штраф, оплату, изменение/отмену, смену гаража и ошибки обновления. Старые ограничения cleanup сохраняются: отклонённые пути не удаляются повторно.

DEF-06/48: в GarageOverdueDebtDto добавлен общий Balance из уже полученных серверных агрегатов (нового запроса нет). Ответ карточки учитывает будущие начисления, аванс и отменённые операции. UI после ручного начисления/штрафа обновляет баланс и перечитывает финансовые итоги; после строковой/пакетной оплаты вычитает сумму из текущего общего состояния, не подставляет периодный garageDebtAfter. Устаревшие/отменённые фоновые ответы игнорируются; при ошибке сохранённая операция не объявляется неуспешной. Общий текст ошибки вынесен из повторяющихся литералов. Старый формат клиента без balance сохраняет локальную оценку; современный API всегда передаёт balance.

Регрессия48: сумма360 при общем долге1360 давала0 вместо1000. Серверная регрессияBalance: ожидалось1900, новый контракт до реализации возвращал0. Новые workflow ручного начисления/штрафа проверяют локальное обновление1000→1150 и подтверждённый сервером1250, сбой обновления и смену гаража. Ранее ошибочное имя кнопки в новом manual-тесте исправлено на реальное доступное имя. Focused frontend8/8 прошли до финальной оптимизации updater; backend overdue7/7. Проверка PostgreSQL будущих начислений сначала использовала пустые даты оплаты, из-за чего они считались просроченными; fixture исправлен явными будущими DueDate/OverdueFromDate, суммаBalance250 оставалась правильной.

DEF-05: полностью оплаченный сбор удалялся из rows, хотя totals сохранялись; новая проверка закрытого сбора упала на пустой детализации. В периоде с начислением/оплатой строка сохраняется с остатком; пустые периоды не заполняются ложными строками. Закрытый/погашенный сбор недоступен для повторного ввода, правило защищено общим isFeePaymentClosed и параметризованными тестами. Старые ожидания отсутствия строки в тестах закрытия/миграций заменены точными историческими суммами; существующие проверки миграций, проводок, остатков и аудита сохранены. Первый PostgreSQL focused41:37 прошли,4 не прошли (даты fixture и3 ожидания скрытой истории). Повтор соответствующих6/6 прошёл. Остальные проверки выполняются в полном gate.

Живой сценарий: изолированная garagebalance_balances_smoke_20260907, API5187/Vite5177/PostgreSQL5434. Гараж901: будущее начисление1000, сбор100; при открытии ведомости автоматически рассчитан Мусор128,69. Ручное75 и штраф25 приводят общий долг к1328,69, сентябрьский к328,69 без повторного выбора. Оплата сбора100: общий1228,69/период228,69; оплаченная строка остаётся (100/100/0), оба элемента платежа disabled. Частичная полная оплата сентября1: общий1227,69/период227,69; отмена этой операции из истории возвращает1228,69/228,69. API повторно подтвердил totals328,69/100/228,69 и строку сбора. Сверка фондов:cashAndBank100,namedFund100,reserve0,difference0,isReconciled=true. Все данные синтетические. Широкий и узкий снимки просмотрены inline, файлы не создавались. На390px сохраняется исходное горизонтальное переполнение общей страницы, отдельные открытые пункты адаптивности этим этапом не закрываются. Вкладка закрыта, viewport восстановлен.

Схема БД не меняется;139 текущих миграций применены к новой базе. dotnet format --verify-no-changes --no-restore прошёл. Первая сборка превысила общий gzip-бюджет на51байт; после устранения повторяющихся сообщений и использования текущего state итог284662/284672, запас10байт, лимит не изменён. Первый полный frontend выявил старую противоречивую fixture: входящий долг900 был задан startingBalance=-900 (аванс); исправлен знак, ожидание остатка200 после оплаты700 сохранено. После изменений updater и fixture требуется окончательный полный повтор. Что нового1.183.25 и user-guide обновлены. Полные backend/frontend пока выполняются; отметки[x] не ставятся.

Повторная сверка: API/Vite остановлены, синтетическая база удалена (pg_database count=0). Privacy1394 файлов, strict UTF8 без BOM14 файлов, lint и diffcheck прошли. Первый полный backend2726/2727,0 пропусков: единственный сбой CreateFullGaragePaymentAsync_CampaignLineReachesTargetClosesSettlesAndRetriesIdempotently также требовал скрытия оплаченного сбора. Ожидание заменено точной строкой500/500/0, нулевым остатком цели; прежние проверки идемпотентности/проводок/итогов сохранены. Focused1/1 прошёл; запущен окончательный полный backend. Первый frontend-прогон завершился с ранее зарегистрированным сбоем fixture; окончательный полный frontend выполняется с текущим кодом и исправленной fixture.

Предварительно изучен следующий блок без изменения его статусов: все упоминания DEF-30/52/54 сверены с исходным журналом. В ReportPanel годовой запрос явно содержит limit:1 без пагинации. В EfIncomeReportQuery PostgreSQL payment rows сортируются по debt=-income_amount, а отображаемый DebtAfterPayment рассчитывается после отбора страницы; исправление должно сортировать реальный показанный остаток в PostgreSQL до OFFSET/LIMIT, с проверкой обоих режимов и экспорта. Панель итогов сбора использует ??0 при отсутствии отчёта; требуются loading/error/empty/retry/stale-response проверки. Реализация следующего блока ещё не начата.
Окончательный frontend1135/1135,103/103 файла (6 автоматически созданных App shards),292,99с; покрытие statements88,11%≥78%, branches80,69%≥69%, functions85,84%≥74%, lines89,31%≥79%. Все пороги пройдены. Повторный dotnet format --verify-no-changes --no-restore также прошёл после последней правки теста. Временных App shards0. Автоматическая проверка разрешений отклонила удаление двух новых путей garage-balance-frontend-final (успешное покрытие) и garage-balance-keys (ключи удалённой синтетической базы), причина blocked by policy; обходов/повторов нет. garage-balance-frontend и garage-balance-import отсутствуют. Окончательный полный backend ещё выполняется.
### 07.09.2026 — DEF-05/06/48 завершены и проверены

Окончательный полный backend2727/2727,0 пропусков,12м16с; строки88,58%≥85%, ветви74,54%≥70%. Реальный PostgreSQL17.2 с русской локалью/UTF8; существующие миграции и все PostgreSQL integration tests прошли. Новая схема не требуется. Окончательный frontend1135/1135,103/103 файла: statements88,11%≥78%, branches80,69%≥69%, functions85,84%≥74%, lines89,31%≥79%. Lint, production build, bundle284662/284672, dotnet format, privacy1394, strict UTF8 без BOM14 файлов, diffcheck успешны. Живые сценарии и ограничения узкого экрана описаны выше. DEF-05/06/48 отмечены[x] после всех обязательных проверок; другие пункты не закрываются по смежности.

Прогресс по64 DEF, без Истории выполнения: [x]10 (15,63%), [~]0 (0%), [ ]54 (84,38%), [!]0, [decision]0, [acceptance]0. Полная задача остаётся активной. Следующий блок DEF-30/52/54: причины предварительно установлены, реализация ещё не начата. CI не запускался, push/deploy/VPS не выполнялись.

Финальный командный аудит:0 процессов этапа,0 слушателей5177/5187/5434,0 App shards. Синтетическая база удалена; API/Vite/psql/PostgreSQL остановлены, dotnet build-server shutdown прошёл. Вкладка закрыта и viewport сброшен ранее. Общие Codex/MCP/IDE и пользовательские службы не затронуты. bin/obj/node_modules/dist сохранены для локальной разработки; прежние .playwright-mcp/tmp не менялись и не включаются в коммит.

Автоматическая проверка разрешений отклонила удаление обоих backend-отчётов с причиной blocked by policy. Финальный read-only аудит подтвердил четыре новых оставшихся каталога: D:\GarageBalance-remediation-20260907\garage-balance-backend (покрытие первого прогона с одним старым ожиданием), D:\GarageBalance-remediation-20260907\garage-balance-backend-final (успешное backend-покрытие), D:\GarageBalance-remediation-20260907\garage-balance-frontend-final (успешное frontend-покрытие), D:\GarageBalance-remediation-20260907\garage-balance-keys (ключи удалённой синтетической базы). garage-balance-frontend и garage-balance-import отсутствуют. Остатки сохраняются только из-за отказа cleanup, обходов/повторов удаления нет, в Git не входят. Прежние отклонённые пути, включая xunit.runner.json, перечислены в предыдущей истории и повторно не удалялись. Этап сохраняется отдельным локальным русским коммитом.

### 07.09.2026 — DEF-30/52/54 в работе

Предыдущий этап — подтверждённый прогресс, коммит a99e665f;10/64 закрыты. Исходное рабочее дерево содержит только прежние .playwright-mcp/tmp. Продолжается блок отчётов по ранее сверенным уточнениям: годовая полнота, сортировка показанного долга до пагинации, отсутствие ложных нулей при загрузке/ошибке. Прежние отклонённые cleanup-пути повторно не удаляются.

DEF-30/54: frontend-регрессии воспроизвели одну строку вместо12 и три ложных нуля при pending. Запрос консолидированного отчёта теперь ограничен120 строками — общим максимальным числом месяцев допустимого периода; год содержит12 месяцев, вспомогательная выборка гаражей тоже остаётся ограниченной. У сборов используется общий skeleton итогов, при недоступности — прочерки, при ошибке обновления ранее полученные суммы явно помечаются. Проверены начальная загрузка, сортировка, отказ, retry, настоящий пустой нулевой отчёт и сохранённые итоги. Focused frontend2/2, затем7/7 (включая права, пустые состояния, отмену/устаревшие ответы и сортировки) прошли до последней оптимизации разметки; окончательный полный gate ещё предстоит.

DEF-52: новые PostgreSQL/SQLite регрессии дали порядок600/900/-30/500/550 вместо-30/500/550/600/900. Исправлено поле сортировки отдельных/сгруппированных платежей; видимый остаток вычисляется до OFFSET/LIMIT. Проверяются оба направления, каждый однострочный page и выход за конец, фильтр вида поступления с учётом истории других видов, аванс, будущие начисления, отменённые операции и cancellation. Смешанный режим all сохраняет сортировку по своей колонке Debt. Первые ошибки компиляции новой fixture исправлены использованием SqliteTestDatabase и явных HashSet для IReadOnlySet; они не считались дефектами продукта. Focused2/2, расширенный backend16/16 (включая годовой PostgreSQL запрос, XLSX/PDF и существующие SQL/pagination проверки) прошли до оптимизации SQL.

Производительность DEF-52: production не материализует историю для сортировки в приложении. Только при сортировке по остатку PostgreSQL считает накопительную сумму оконной функцией для действующих поступлений выбранных гаражей до конца периода; это заменяет повторное суммирование истории каждого платежа. Начисления агрегируются по гаражу/месяцу, результат страницы ограничен на сервере. Существующие индексы GarageId и GarageId/AccountingMonth используются по применимости. Полная материализация до сортировки разрешена только SQLite fallback тестов и только payment/debt. Остальные сортировки сохраняют прежний путь; порядок/число SQL-запросов дополнительно проверяются. Схема не меняется.

Что нового1.183.26 и руководство обновлены. Промежуточный bundle gate выявил небольшой выход за278KiB. В окончательной реализации сохранена разметка итогов, выделены общие функции отображения отсутствующей суммы и ошибки загрузки; консолидированный отчёт и сборы используют существующий scheduleDebouncedRequest для отмены и защиты от устаревших ответов. Порог не повышался. Проверка fallback ошибки без Error добавлена для всех восьми отчётов.

### 07.09.2026 — DEF-30/52/54 завершены и проверены

Окончательный focused backend16/16 прошёл после оптимизации оконного SQL и усиления проверки двух запросов. Полный backend2732/2732,0 пропусков,17м37с: строки88,60%≥85%, ветви74,61%≥70%. Настоящий локальный PostgreSQL17.2, русская локаль/UTF8;139 существующих миграций применены в синтетической базе, полный набор PostgreSQL integration tests прошёл. Новая схема не требуется. Временный кластер fsync=off: отказоустойчивость при отключении питания здесь не проверяется.

Первый полный frontend1137/1138 выявил гонку существующей проверки кратковременного уведомления отключения пользователя: ожидание начиналось до завершения дорогого userEvent и истекало под нагрузкой. Тест теперь наблюдает фактическое появление role=status через MutationObserver, установленный до нажатия; текст, API, форма и итоговые утверждения сохранены, тайм-ауты и пороги не менялись. Окончательный полный frontend1138/1138,103/103 файла,326,26с: statements88,11%≥78%, branches80,72%≥69%, functions85,92%≥74%, lines89,30%≥79%. Lint, production build и bundle284660/284672 байт прошли; запас12 байт остаётся риском следующих изменений. dotnet format --verify-no-changes --no-restore, privacy1395 файлов, strict UTF8 без BOM10 изменённых файлов и diffcheck прошли.

Живая проверка выполнена через UI с отдельными синтетическими гаражами901/902 в garagebalance_reports_smoke_20260907, API5187/Vite5177/PostgreSQL5434. Год2026 содержит12 строк: январь10, сентябрь550, итог560; предыдущий2025 после сортировки тоже показывает все12 месяцев. В поступлениях сентября показанный долг возрастает550/890/900 для сгруппированных и550/600/890/900 для отдельных платежей; убывание даёт900/890/600/550, общий итог550 сохраняется. XLSX/PDF за год и порядок долга дополнительно проверены автоматическими тестами.

DEF-54: при короткой блокировке financial_operations в тестовой базе показаны скелетоны отчёта и итогов, без нулей; после остановки только тестового API новая сортировка показывает три прочерка и кнопку повтора. После восстановления API повтор возвращает1000/110/890 и сохраняет сортировку/раскрытых должников. Собрано110 включает январский платёж10 того же вида поступления; это фактическое правило текущего отчёта, данный этап меняет доступность итогов, не состав платежей. Снимки1280×900 просмотрены; на390×844 существующая тесная адаптивная компоновка остаётся отдельной задачей, её закрытие не заявляется. Английское Failed to fetch при сетевом отказе также остаётся в отдельном DEF-23. Данные исключительно синтетические.

Прогресс по64 DEF без Истории выполнения: [x]13 (20,31%), [~]0 (0%), [ ]51 (79,69%), [!]0 (0%), [decision]0 (0%), [acceptance]0 (0%). Следующий блок по принятому порядку — даты и валидация. Полная задача остаётся активной. CI не запускался, push/deploy/VPS не выполнялись.

Финальный командный аудит:0 процессов этапа,0 слушателей5177/5187/5434,0 App shards. Синтетическая база удалена; API/Vite/psql/PostgreSQL остановлены, dotnet build-server shutdown успешен. Вкладка закрыта, viewport сброшен. Общие Codex/MCP/IDE и пользовательские службы сохранены. bin/obj/node_modules/dist остаются для локальной разработки; прежние .playwright-mcp/tmp не менялись и не включаются в коммит.

Автоматическая проверка разрешений отклонила очистку временных каталогов с причиной blocked by policy. Финальный read-only аудит подтвердил три новых оставшихся пути: D:\GarageBalance-remediation-20260907\reports-backend (успешное backend-покрытие), D:\GarageBalance-remediation-20260907\reports-frontend-final (успешное frontend-покрытие), D:\GarageBalance-remediation-20260907\reports-keys (ключи удалённой синтетической базы). reports-frontend и reports-import отсутствуют. Остатки сохраняются только из-за отказа cleanup, обходов/повторов удаления нет, в Git они не входят. Прежние отклонённые пути, включая xunit.runner.json, перечислены в предыдущей истории и повторно не удалялись. Этап сохраняется отдельным локальным русским коммитом.

### 07.09.2026 — DEF-10 в работе

Предыдущий этап — прогресс, коммит4d09bdb8,13/64 DEF закрыты. Рабочее дерево перед началом содержит только прежние .playwright-mcp/tmp. Прочитаны все упоминания DEF-10 в исходном журнале: Npgsql отклоняет смещение+07, старые строки при ошибке не являются результатом фильтра. Текущий IQueryable сравнивает DateTimeOffset без UTC-нормализации; frontend передаёт календарные даты без явных границ локальных суток. Проверяются открытые и замкнутые интервалы, начало/конец дня, CSV/XLSX, отказ/retry и устаревшие ответы. Прежние отклонённые cleanup-пути не удаляются повторно. Публикаций нет.

Новая PostgreSQL-регрессия воспроизвела исходный ArgumentException для+07. Репозиторий переводит обе границы в UTC перед формированием SQL; поддержаны также отрицательное смещение и отсутствие каждой границы. Клиент явно задаёт начало и последнюю микросекунду выбранных местных суток для списка, страницы, CSV и XLSX; временные метки с явным смещением не подменяются. Focused PostgreSQL1/1, API-клиент15/15, четыре App сценария прошли. Регрессия интерфейса сначала обнаружила старое событие под новым периодом; теперь загруженные строки привязаны к фильтрам, ошибочный новый запрос не показывает их как свой результат, отменённый поздний ответ игнорируется. Сохраняется прежнее поведение ожидания следующей страницы в рамках того же фильтра; DEF-36 этим этапом не закрывается.

Часовые пояса проверяются отдельными завершёнными Node-процессами с настоящим TZ при старте, включая23- и25-часовые дни Нью-Йорка. Первоначальная установка TZ внутри Vitest threads не меняла часовую зону и дала три ложных падения; тесты перенесены на запуск собранного production API-клиента в памяти дочернего процесса, без копирования алгоритма и без временных файлов. Первоначальная команда quick пропустила App сценарии и не считалась успешной проверкой; затем штатные shards проверили нужные сценарии. Пороги и тайм-ауты не ослаблялись.

Окончательная реализация устраняет дублирование фильтров страницы/выгрузки и одинаковых ветвей URL-параметров, сохраняя весь набор фильтров отдельной регрессией. Заголовки таблицы используют ту же разметку через общий список. Пробные объединения разметки карточки, обработчиков экспорта, смена загрузчика и переназначение chunks не дали выигрыша и полностью отменены. JSON-ключ фильтров сохраняет совместимость React Compiler; проверка ссылкой на объект не вошла в результат. Lint и production build прошли, bundle284662/284672 байт, запас10; конфигурация chunks и все лимиты сохранены. Что нового1.183.27 и руководство обновлены.

Полный backend2733/2733,0 пропусков,13м34с: строки88,61%≥85%, ветви74,64%≥70%. PostgreSQL17.2 с русской локалью/UTF8,139 существующих миграций, новая схема не требуется. dotnet format --verify-no-changes --no-restore прошёл. Первый полный frontend1143/1143,104/104 файла,286,74с: statements88,09%≥78%, branches80,68%≥69%, functions85,93%≥74%, lines89,29%≥79%. После окончательной правки ключа фильтра запущен полный повтор; результаты первого прогона не подменяют финальный gate. Privacy1397 файлов прошёл.

Живая проверка: отдельная синтетическая база garagebalance_audit_dates_smoke_20260907, API5187/Vite5177/PostgreSQL5434. Созданы пять событий Boundary до начала, в00:00, в12:00, в23:59:59.999999 и на начало следующих суток. Первая вставка русских подписей через psql отклонена кодировкой аргумента, строк не создала; повтор с ASCII-подписями создал ровно5 событий. Это ограничение тестовой команды, не дефект приложения. UI за06.09.2026 и живые API/CSV/XLSX с+07 возвращают ровно3 внутренних события. При остановке тестового API смена начала на05.09 скрывает старые строки и счётчик; повтор после восстановления возвращает4 события, сохраняя поиск и даты. Английское Failed to fetch остаётся отдельным DEF-23.

После обновления исходников Vite сбросил фильтры, поэтому снимок с23 событиями не использовался как доказательство выбранного дня; период задан повторно и снимок1280×900 с3 событиями просмотрен. Попытка пустого fill не применила очистку в браузере; подтверждённая очистка выполнена штатной кнопкой календаря. На390×844 календарь помещается по ширине; последовательная очистка обоих календарей проверена по пустым DOM-значениям и5 строкам. Клавиатурные ветви общего календаря покрываются существующими тестами; новые утверждения о полном мобильном аудите не делаются. Финансовые операции не менялись. Вкладка закрыта, viewport сброшен, синтетическая база удалена, API/Vite/psql/PostgreSQL остановлены, dotnet build-server shutdown выполнен. Cleanup покрытия и финальный аудит будут выполнены после полного frontend gate.

Следующий блок предварительно сверяется без изменения статусов: DEF-16 (FinancePanel, financeFilter общей ведомости), DEF-51 (пустые обязательные даты четырёх отчётов), DEF-53 (пустые месяцы трёх отчётов). Прочитаны последующие уточнения, включая календари и восстановление периода. В shared/validation уже есть функции проверки месячного и дневного периода, но ReportPanel использует преобразование пустого месяца в -01, а дневные фильтры передаются пустыми и допускают серверную подстановку. Реализация следующего блока ещё не начата.

### 07.09.2026 — DEF-10 завершён и проверен

Окончательный полный frontend1143/1143,104/104 файла,225,20с; statements88,09%≥78%, branches80,68%≥69%, functions85,93%≥74%, lines89,29%≥79%. Полный backend2733/2733 и покрытие88,61%/74,64%, локальный PostgreSQL, форматирование, lint, production build, bundle284662/284672 и privacy1397 описаны выше. Strict UTF8 без BOM10 изменённых файлов и diffcheck прошли. DEF-10 отмечен[x] только после окончательного повтора и живой проверки. CI не запускался, push/deploy/VPS не выполнялись.

Прогресс по64 DEF без Истории выполнения: [x]14 (21,88%), [~]0 (0%), [ ]50 (78,13%), [!]0 (0%), [decision]0 (0%), [acceptance]0 (0%). Проценты округлены независимо. Полная задача остаётся активной; далее DEF-16/51/53 и остальные пункты в принятом порядке.

Финальный командный аудит подтвердил0 процессов этапа,0 слушателей5177/5187/5434,0 App shards. Вкладка закрыта, viewport сброшен, синтетическая база удалена; API/Vite/psql/PostgreSQL и build servers остановлены. Общие Codex/MCP/IDE и пользовательские службы сохранены. bin/obj/node_modules/dist остаются для локальной разработки; прежние .playwright-mcp/tmp не менялись и не включаются в коммит.

Автоматическая проверка разрешений отклонила очистку с причиной blocked by policy. Read-only аудит подтвердил четыре новых оставшихся пути: D:\GarageBalance-remediation-20260907\audit-dates-backend (успешное backend-покрытие), D:\GarageBalance-remediation-20260907\audit-dates-frontend (первый успешный frontend-прогон), D:\GarageBalance-remediation-20260907\audit-dates-frontend-final (окончательное frontend-покрытие), D:\GarageBalance-remediation-20260907\audit-dates-keys (ключи удалённой синтетической базы). audit-dates-import отсутствует. Остатки сохранены только из-за отказа cleanup, обходов/повторов удаления нет, в Git они не входят. Прежние ограниченные очисткой пути, включая xunit.runner.json, перечислены в предыдущей истории. Этап сохраняется отдельным локальным русским коммитом.

### 07.09.2026 — DEF-16/51/53 в работе

Прочитаны все последующие уточнения трёх DEF: общая FinancePanel, четыре дневных и три месячных отчёта, очистка календарей и восстановление периода. DEF-51 исправляется обязательностью обеих видимых дат, разрешённой исходным аудитом; серверные значения по умолчанию не меняются. DEF-16 сохраняет допустимые открытые интервалы. Три пункта [~], закрытие только после полного gate.

Семь новых регрессий отчётов сначала воспроизвели отсутствие валидации. Исправлены клиентские проверки перед загрузкой/экспортом, связанное с полями сообщение, скрытие таблиц при неверном периоде, отмена прежних запросов. Первое применение месячного валидатора с yyyy-MM вместо yyyy-MM-01 исправлено после focused-падения. Focused 10/10 (семь отчётов, платежи, доступность общего календаря и быстрые периоды) прошли; дополнительная регрессия отменённого позднего ответа и перехода ошибка→пустая дата→исправление также 1/1. Новая запись «Что нового»1.183.28 и руководство подготовлены.

Production build и lint прошли до оптимизации; bundle превысил неизменный лимит на520 байт, после объединения дневного/месячного фильтра — на392. Выполняется устранение дублирования экспорта с повтором всех проверок; превышение не считается пройденным gate. Полный backend запущен с локальным PostgreSQL17.2/5434. Публикаций и обращений к VPS нет.
Инцидент локальной проверки: первая команда createdb использовала склеенный аргумент -h127.0.0.1, который PowerShell/psql обработали как host127; она завершилась таймаутом и базу не создала. Преждевременный запуск API использовал неверный ключ ConnectionStrings__GarageBalance вместо DefaultConnection; из user-secrets было взято garagebalance_local на127.0.0.1:5433. Лог подтвердил применение90 недостающих миграций; read-only проверка после остановки процесса9012 показала139 миграций и650 записей app_releases. Синхронизация каталога сообщала конфликт уникальной версии. Исходный снимок состояния не создан, поэтому безопасный автоматический откат не выполнялся, влияние на данные полностью не установлено. Пользователю сообщено сразу после обнаружения. Служба PostgreSQL5433 не останавливалась; VPS не затронут. Это ошибка запуска проверки, её нельзя выдавать за штатный результат тестового стенда.

Для продолжения отдельная база создана с раздельными аргументами; перед повторным API проверены current_database()=garagebalance_period_validation_smoke_20260907 и inet_server_port()=5434. Новый процесс получает правильный ConnectionStrings__DefaultConnection. Применяются139 миграций в новой синтетической базе. Объединение экспортных обработчиков не уменьшило gzip и отменено. Дневные/месячные фильтры объединены, шесть ручных циклов загрузки используют уже принятый scheduleDebouncedRequest; focused12/12 после этого прошёл. Общая валидация проверяет даты, месяцы предварительно переводятся в первое число; отдельный неиспользовавшийся в production месячный валидатор не включается в bundle.
Полный backend2733/2733,0 пропусков,12м47с; PostgreSQL17.2 с139 миграциями в отдельной синтетической базе, покрытие строк88,61%≥85%, ветвей74,62%≥70%. Форматирование backend --verify-no-changes прошло, privacy1397 прошёл. Первый полный frontend1152 успешных/1 падение,104 файла,295,93с: статическая проверка accessibleStatus ожидала две старые реализации фильтра. Она обновлена под общий компонент, сохраняя запрет нативных date/month, оба режима и быстрые периоды. Поведенческие проверки обеих границ и восстановления для всех семи отчётов остаются; focused11/11 после перехода общего календаря на стандартные aria-invalid/aria-describedby прошёл. Регрессия платежей дополнена отменённым поздним ответом и отсутствием старого счётчика операций.

Второй полный frontend1153/1153,104/104 файла,272,22с: statements88,12%≥78%, branches80,79%≥69%, functions86,24%≥74%, lines89,27%≥79%. Это ещё не окончательный gate: во время прогона завершены сокращения проверок/параметров и текста ошибки; запущен повтор на зафиксированном неизменном коде. Итоговый bundle284659/284672 байт, запас13, production build и lint прошли. Лимиты и chunks не менялись. В итог вошли общие состояния загрузки/ошибок, разметка пары экспортных кнопок и фильтра, одиночные итоги; объединение всех экспортных обработчиков, отдельный useReportState и общий рендер тройных итогов отвергнуты по замерам. Пороги не повышались, функциональные тесты не ослаблялись.

Живые сценарии: очистка конечного месяца консолидированного отчёта штатным календарём показала обязательность поля и отключила XLSX/PDF; быстрый месяц восстановил загрузку. В фондах очистка дневной границы скрыла строки и итоги, обратный01.10–30.09 показал ошибку. На390px полный снимок подтвердил перенос полей и читаемое сообщение; это не полная приёмка мобильного интерфейса. В общей ведомости включена настройка только в отдельной базе;09.2026–08.2026 скрывает суммы, таблицу, пагинацию и счётчик в заголовке, при переходе к счётчикам ошибка сохраняется.09.2026–09.2026 возвращает таблицу; последовательная очистка обеих границ разрешает весь период. Окончательный текст «Начало периода позже конца.» просмотрен на1280×900. Снимки только inline, локальных файлов нет. HMR один раз сбросил отчёт; проверка повторена по свежему дереву, сброшенные фильтры не использованы как доказательство.

Остановлены API17892/Vite29208, закрыта вкладка11, viewport сброшен; синтетическая база удалена, PostgreSQL5434 остановлен штатно, dotnet build-server shutdown выполнен. Пользовательская служба5433 сохранена. Read-only проверка затронутой garagebalance_local:139 миграций,650 релизов,0 пользователей с синтетическим email. Исходного снимка нет, последствия90 миграций для её данных полностью не установлены. Ключи D:\GarageBalance-remediation-20260907\period-validation-keys намеренно сохраняются для возможного восстановления после ошибочного запуска; удалять их до разбора нельзя. В дальнейшем каждый локальный запуск обязан явно задавать ConnectionStrings__DefaultConnection и до API проверять фактические current_database()/inet_server_port(); база из user-secrets не используется для стенда.

Автоматическая проверка разрешений отклонила Remove-Item двух завершённых каталогов покрытия с причиной blocked by policy: D:\GarageBalance-remediation-20260907\period-validation-backend и D:\GarageBalance-remediation-20260907\period-validation-frontend-final. Повторных или обходных удалений нет. Каталог первого неуспешного frontend отсутствует, period-validation-import не создан. Окончательное покрытие period-validation-frontend-verified пока используется текущим прогоном; аудит и попытка его очистки выполняются после завершения. Все прежние ограниченные очисткой пути остаются в предыдущей истории; .playwright-mcp/tmp не менялись и не включаются в коммит.
### 07.09.2026 — DEF-16/51/53 завершены и проверены

Окончательный неизменный frontend1153/1153,104/104 файла,235,54с; statements88,12%≥78%, branches80,82%≥69%, functions86,24%≥74%, lines89,26%≥79%. Backend2733/2733 с PostgreSQL и покрытием88,61%/74,62%, форматирование, lint, production build, bundle284659/284672, privacy1397 и strict UTF8 без BOM9 файлов прошли. DEF-16/51/53 отмечены[x] после итогового прогона и живых проверок. CI не запускался; push/deploy/VPS не выполнялись.

Прогресс64 DEF без Истории выполнения: [x]17 (26,56%), [~]0 (0%), [ ]47 (73,44%), [!]0 (0%), [decision]0 (0%), [acceptance]0 (0%). Общая задача остаётся активной, оставшиеся47 пунктов не объявляются исправленными.

Финальный командный аудит после тестов:0 процессов стенда,0 слушателей5177/5187/5434,0 App shards. Синтетическая база удалена, вкладка закрыта и viewport сброшен. Общие Codex/MCP/IDE и PostgreSQL5433 сохранены. bin/obj/node_modules/dist остаются для локальной разработки. После проверки абсолютного пути попытка очистить D:\GarageBalance-remediation-20260907\period-validation-frontend-verified также отклонена автоматической проверкой разрешений: blocked by policy; обходов и повторов нет. Таким образом, из новых одноразовых материалов остаются три каталога покрытия (backend, frontend-final, frontend-verified) с префиксом period-validation в D:\GarageBalance-remediation-20260907. period-validation-keys намеренно сохранён для разбора инцидента локальной базы. Прежние ограниченные очисткой пути перечислены выше; в Git ни они, ни .playwright-mcp/tmp не входят.

Последствия ошибочного применения90 миграций к garagebalance_local остаются отдельным незавершённым вопросом, описанным выше; закрытие трёх UI-дефектов не означает восстановления исходного состояния этой базы. Перед любыми дальнейшими локальными проверками обязательна явная проверка целевой базы. Этап сохраняется отдельным русским локальным коммитом после дополнительной проверки актуального каталога релизов и повторного аудита процессов.

Актуальный исходный каталог релизов дополнительно проверен AppReleaseServiceTests:18/18,0 пропусков. Повторный dotnet build-server shutdown и командный аудит подтвердили0 процессов,0 тестовых слушателей и0 App shards; остались ровно четыре новых пути, перечисленные выше.
