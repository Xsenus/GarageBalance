# Исправления по комментариям заказчика от 22.07.2026

## Источники

- Документ заказчика `C:\Users\User16\Downloads\Комментарии Илье.docx`: 6 страниц, 11 скриншотов.
- Текущая реализация backend, frontend, миграций, тестов, пользовательской документации и `backend/GarageBalance.Api/AppReleases/releases.json`.
- Правила проекта из `AGENTS.md`.

## Допущения и решения

- Работа выполняется строго сверху вниз по замечаниям документа.
- Каждое законченное исправление получает собственный логический коммит на русском языке; push выполняется только по отдельному поручению пользователя.
- Обычное сохранение можно упростить, но подтверждения удаления, отмены, восстановления и других существенных финансовых действий сохраняются.
- Уже существующая функциональность не переписывается: она проверяется тестами и при необходимости доводится до требования.
- Для упрощённой парольной политики сохранена минимальная длина 8 символов; обязательные требования к регистру букв и наличию цифр удалены.
- Пункты с недостаточным бизнес-описанием помечаются `[decision]`; до решения можно выполнять последующие независимые пункты.
- Для каждого изменения обязательны ближайшие автоматические тесты, полный backend/frontend gate, обновление пользовательской документации и «Что нового», если изменение видно пользователю.

## Веха 1. Пользователи, вход и права

- [x] При истечении срока активной сессии очистить авторизацию и принудительно открыть форму входа.
- [x] Полностью скрывать недоступные разделы из боковой навигации, главного экрана и программных переходов, сохраняя backend-авторизацию.
- [x] Упростить парольную политику: сохранить минимум 8 символов, убрать требования к регистру и цифрам.
- [x] Проверить и закрепить название раздела `Тарифы и сборы` во всех пользовательских поверхностях.
- [x] Убрать подтверждение обычного изменения прав и иных обычных сохранений, сохранив аудит и подтверждения опасных действий.

## Веха 2. Тарифы и сборы

- [x] Исключить попадание нерегулярной услуги в регулярные и наследование признаков счетчика/порогов.
- [x] Уточнить и унифицировать поля `Вид поступления`, `Способ расчета` и `Тариф` для регулярной услуги.
- [x] Заменить неясную периодичность на явный выбор `Ежемесячно`/`Ежегодно` и связать срок оплаты с выбранным режимом.
- [x] Проверить добавление, сохранение и удаление пользовательского порога тарифа.
- [x] Автоматически рассчитывать плановую сумму сбора по взносу и составу участников.
- [x] Показывать созданный активный сбор в рабочем списке и отчете по устойчивой связи со сбором.
- [x] Автоматизировать начисление действующих сборов; ручное дозаполнение оставить административным действием с понятным текстом.
- [x] Обеспечить полноценное редактирование услуги в основной форме.
- [x] Выводить и валидировать единицу измерения из способа расчета.
- [x] Показывать признаки счетчика и порогов один раз на услугу, а не на каждой технической строке.
- [x] Исправить обнуление стоимости добавленной услуги и сохранять услугу со ставкой атомарно.
- [x] Сразу показывать пороги при включении пороговой тарификации.

## Веха 3. Контрагенты и персонал

- [x] Убрать успешную подсказку DaData после выбора адреса, оставив только ошибки и доступный статус.
- [x] Ввести единый формат и серверную нормализацию телефона.
- [x] Скрыть кнопку финансового отчета у новой несохраненной записи.
- [x] Убрать переключатель должников у поставщиков.
- [x] Проверить поле стартового баланса поставщика и включить его в единый расчет отчетов.
- [x] Устранить расхождение задолженности поставщика между таблицей и финансовым отчетом.
- [x] Считать входящий остаток финансового отчета с учетом всей предшествующей истории.
- [x] По умолчанию открывать финансовый отчет за весь период обслуживания.
- [x] Исправить обрезание календаря и стабильность геометрии финансового отчета во время загрузки.
- [x] Упростить подтверждение изменения поставщика.
- [x] Исправить сохранение телефона и почты поставщика и устранить конкурирующие источники контакта.
- [x] Убрать лишнюю подсказку об audit-праве из финансового отчета.
- [x] Закрывать выбор отдела по клику вне списка и закрепить сценарий тестом.
- [ ] Убрать колонку/поле `Активен` из рабочего списка отделов.

## Веха 4. Счетчики

- [ ] Запретить создание и изменение показаний будущего месяца во frontend и backend.
- [ ] Явно отделить демонстрационные показания с 2021 года от данных заказчика и исключить demo-seed в рабочей базе.

## Веха 5. Платежи и выплаты

- [ ] Очищать поиск, выбранные гаражи и локальную ведомость при новом открытии платежей.
- [ ] Проверить и закрепить множественный выбор гаражей за один раз.
- [ ] Ранжировать поиск гаражей: точное совпадение, начало номера, остальные совпадения, естественный порядок.
- [ ] Объяснимо показывать общий баланс и просроченный долг с разбивкой по услугам.
- [ ] Гарантировать применение действующих ставок воды и электричества при начислении по показаниям.
- [ ] Автоматически применять все действующие объявленные сборы вместе с тарифами.
- [ ] Перестроить шапку формы платежей: поиск, выбор, сведения, действия.
- [ ] Ограничивать распределение оплаты остатком начисления, сохраняя избыток как аванс.
- [ ] Исключить ошибку отсутствующего вида поступления для системной услуги.
- [ ] Упростить изменение комментария платежа без второго подтверждения.
- [ ] Убрать ручную кнопку переноса задолженности из обычного интерфейса.
- [ ] Убрать ручную кнопку формирования начислений из обычного интерфейса.
- [ ] Скрывать общую историю платежей по умолчанию и открывать отдельным действием.
- [ ] Добавить произвольное начисление штрафа владельцу с причиной и аудитом.
- [ ] Формировать единую квитанцию по пакету платежа и его распределениям.
- [ ] Убрать поиск гаража из режима выплат.
- [ ] Исправить обрезание календаря выбора месяца в выплатах.
- [ ] Переносить неиспользованную собранную сумму в следующий месяц как остаток назначения/фонда.
- [ ] Для архивных месяцев убрать оперативные колонки и действия.
- [ ] Объединить пары долг/аванс в знаковую колонку `Баланс`.
- [ ] Подсвечивать только `Собрано`: красным ниже обязательства, зеленым при достаточной сумме.
- [ ] Строго закрепить связку услуга–контрагент во frontend и backend.
- [ ] Разделить услугу/статью расхода и тип выплаты `с чеком`/`без чека`.
- [ ] Добавить премию и штраф сотруднику поверх автоматического месячного оклада.
- [ ] Проводить сдачу денег в банк как отдельный перевод `Касса → Банк`, а не операцию фонда.

## Веха 6. Фонды

- [ ] Исправить рассинхронизацию выбранного фонда и операции распределения на скриншоте заказчика.
- [ ] Объединить допустимые поступления в единый нераспределенный пул без двойного распределения.
- [ ] В рабочей истории фондов показывать ручные распределения, а автоматические пополнения оставить в общем аудите/отчетах.
- [ ] Реализовать создание и переименование фондов.
- [ ] Показывать в карточке фонда привязанные услуги без редактирования из списка.
- [ ] Реализовать безопасное удаление фонда только после переназначения услуг.
- [ ] При удалении фонда переводить его остаток в нераспределенную сумму транзакционно и с аудитом.

## Веха 7. Отчеты

- [decision] Согласовать точный макет и контрольный пример консолидированного отчета.
- [ ] В отчете по гаражам поменять местами `Услуга` и `Начисления` и добавить требуемый комментарий.
- [ ] Унифицировать множественный выбор гаражей с формой платежей.
- [ ] Исправить и проверить XLSX/PDF-выгрузки отчета по гаражам на совпадение с экраном.
- [ ] Объединять начисление и выплату одного месяца в одной строке отчета по выплатам.
- [ ] Использовать множественный выбор через флажки во всех применимых отчетах.
- [ ] Показывать стартовый баланс поставщика только как входящий остаток первого периода.
- [ ] По умолчанию группировать платежи в отчете по поступлениям.
- [ ] Воспроизвести и исправить неверный итог `414872`, исключив повторное суммирование.
- [ ] Сделать отчет владельца агрегированным по умолчанию с явным переключателем детализации.
- [ ] Гарантировать появление созданного сбора в отчете по сборам.
- [ ] Переименовать колонку отчета по фондам в `Изменение, руб.` на экране и в выгрузках.

## Backend, данные и миграции

- [ ] Для каждого измененного endpoint/service/query добавить success, empty, invalid, failure, permission-denied и важные граничные тесты.
- [ ] Все финансовые изменения выполнять транзакционно, с decimal-округлением, audit и cancellation token.
- [ ] Изменения схемы выполнять EF Core migrations; проверить SQL и PostgreSQL-интеграционные сценарии.
- [ ] Не допускать чувствительные данные заказчика в тесты, документацию и release notes.

## Frontend и доступность

- [ ] Для каждой измененной формы/диалога/таблицы покрыть loading, empty, error, validation, permission и keyboard-сценарии.
- [ ] Сохранять общие компоненты календаря, select, пагинации, context menu и create-action-button.
- [ ] Проверить узкие экраны, `prefers-reduced-motion`, роли, имена и восстановление фокуса.

## Общий тестовый и выпускной gate

- [ ] После каждого исправления выполнить focused tests.
- [ ] Перед каждым коммитом выполнить полный backend suite и frontend suite.
- [ ] Перед каждым коммитом выполнить backend format/privacy/migration checks, frontend lint/build/bundle/coverage gates.
- [ ] Проверить локальный PostgreSQL или честно зафиксировать ближайшую безопасную замену.
- [ ] Обновлять пользовательское руководство и `Что нового` для видимых изменений.
- [ ] Перед каждым коммитом остановить task-owned процессы, выполнить `dotnet build-server shutdown` и аудит временных артефактов.

## Definition of Done

- Все пункты документа получили статус `[x]`, `[decision]` или `[acceptance]` с понятной причиной.
- Экранные формы, API, расчеты, миграции, отчеты и выгрузки согласованы между собой и покрыты автоматическими тестами.
- Полные локальные проверки и CI успешны; пользовательские изменения отражены в `Что нового`.
- Выполнена ручная приемка на копии реальных данных без публикации персональной и финансовой информации.

## Риски и вопросы

- Точный состав консолидированного отчета отсутствует в документе заказчика.
- Единая квитанция, тип выплаты и новая модель фондов затрагивают существующую финансовую историю и потребуют миграционного плана.
- Некоторые замечания сняты скриншотами до последних изменений; такие пункты необходимо проверять на актуальной сборке, а не реализовывать повторно.

## История выполнения

- 2026-07-23: завершён тридцатый пункт. В форме нового сотрудника общий `SelectControl` не получал всплывающий `mousedown` при клике по соседнему полю, потому что модальное окно останавливает распространение события; поэтому список отделов оставался открытым. Обработчик клика вне списка переведён в capture-фазу: он закрывает список до локального `stopPropagation`, не меняет выбранный отдел и сохраняет обычный выбор опции внутри списка. Добавлены регрессии общего компонента для внешнего поля с остановкой всплытия и реального сценария формы сотрудника `список отделов → клик по ФИО → список закрыт → повторное открытие и выбор отдела`; обновлены пользовательское руководство, месячный чек-лист и «Что нового» 0.790.0. Backend, схема БД и финансовые правила не менялись. Проверено: focused frontend 15/15 и 1/1, полный frontend 638/638 с coverage 82,33% statements / 74,22% branches / 78,23% functions / 82,95% lines, backend Release 1574/1574 на PostgreSQL 17 с русской локалью; backend coverage 90,42% строк и 73,25% ветвей; lint, TypeScript production build, bundle budget 241,7 KiB из 260 KiB, backend format, privacy/ignore, Docker Compose и distribution, локальная установка без Docker с PostgreSQL preflight и idempotent migration SQL — успешно.
- 2026-07-23: завершён двадцать девятый пункт. По скриншоту заказчика найден нерабочий блок `История изменений` в финансовом отчёте контрагента: пользователь без `audit.read` видел заголовок и служебную подсказку о недоступном праве, но не мог выполнить действие. Для такого пользователя теперь скрывается весь блок, поэтому отчёт не занимает место бесполезным сообщением; для пользователя с `audit.read` сохранены кнопка и переход в общий журнал с фильтром по поставщику или сотруднику. Добавлена отдельная permission-регрессия отсутствия заголовка, подсказки и кнопки без права; существующий сценарий подтверждает доступный переход и правильный фильтр при наличии права. Обновлены пользовательская инструкция, месячный чек-лист и «Что нового» 0.789.0; backend и схема БД не менялись. Проверено: focused frontend 2/2, полный frontend 637/637 с coverage 82,32% statements / 74,21% branches / 78,23% functions / 82,95% lines, backend Release 1574/1574 на PostgreSQL 17 с русской локалью; backend coverage 90,41% строк и 73,23% ветвей; lint, TypeScript production build, bundle budget 241,7 KiB из 260 KiB, backend format, privacy/ignore, Docker Compose и distribution, локальная установка без Docker, PostgreSQL preflight и migration SQL — успешно.
- 2026-07-23: завершён двадцать восьмой пункт. Замечание заказчика `Телефон и почта не отредактировались после кнопки Сохранить` сопоставлено с двумя конкурирующими местами ввода в основной карточке поставщика: отдельными полями внизу формы и полями строки контакта. При наличии контакта нормализация молча возвращала его прежние значения, поэтому изменения нижних полей исчезали после повторного открытия. Единственным редактируемым источником сделана таблица `Контакты`; дублирующие нижние поля удалены. Первый действующий контакт синхронизируется в совместимые поля поставщика для API и общего списка, а при удалении всех заведённых контактов старые телефон, email и ФИО очищаются и больше не отображаются из устаревшего источника. Workflow-регрессия проверяет создание, изменение обоих значений, запросы поставщика и контакта, таблицу, повторное открытие и удаление последнего контакта; адаптивная геометрия формы также закреплена тестом. Обновлены пользовательская инструкция, месячный чек-лист и «Что нового» 0.788.0; backend и схема БД не менялись. Проверено: focused frontend 2/2, полный frontend 636/636 с coverage 82,29% statements / 74,20% branches / 78,23% functions / 82,92% lines, backend Release 1574/1574 на PostgreSQL 17 с русской локалью; backend coverage 90,41% строк и 73,23% ветвей; lint, TypeScript production build, bundle budget 241,8 KiB из 260 KiB, backend format, privacy/ignore, Docker Compose и distribution, локальная установка без Docker, PostgreSQL preflight и migration SQL — успешно.
- 2026-07-23: завершён двадцать седьмой пункт. По скриншоту заказчика подтверждено, что после обычного сохранения карточки поставщика открывалось второе громоздкое окно с плоским сравнением всех полей и контактов. Это повторное подтверждение убрано только из основной карточки поставщика: изменённые реквизиты, стартовый баланс и контакты сохраняются непосредственно по кнопке `Сохранить`, а карточка без изменений закрывается без API-вызова. Отдельные опасные действия не упрощались: удаление и восстановление контакта по-прежнему требуют подтверждения, причина удаления обязательна и передаётся в audit. Обновлены workflow-регрессии, пользовательская инструкция, месячный чек-лист и «Что нового» 0.787.0; backend и схема БД не менялись. Проверено: focused frontend 2/2, полный frontend 636/636 с coverage 82,30% statements / 74,15% branches / 78,24% functions / 82,92% lines, backend Release 1574/1574 на PostgreSQL 17 с русской локалью; backend coverage 90,41% строк и 73,23% ветвей; один существующий backend-тест при первом повторном запуске нестабильно завершился 1573/1574, затем прошёл изолированно 1/1 и в полном повторном наборе 1574/1574. Lint, TypeScript production build, bundle budget 241,8 KiB из 260 KiB, backend format, privacy/ignore, Docker Compose и distribution, локальная установка без Docker, PostgreSQL preflight и migration SQL — успешно.
- 2026-07-23: завершён двадцать шестой пункт. По скриншоту заказчика воспроизведено состояние медленного первоначального открытия финансового отчёта: до прихода данных область итогов и таблицы отсутствовала, диалог схлопывался до фильтров, а раскрытый календарь месяца обрезался нижней границей прокрутки. Для отчётов гаража, поставщика и сотрудника добавлен общий каркас загрузки из шести строк, сохраняющий рабочую высоту и форму будущего содержимого; диалог сообщает `aria-busy`, не показывает преждевременное пустое состояние, а заблокированная кнопка использует компактный индикатор с отключением анимации при `prefers-reduced-motion`. Календарь остаётся доступным во время загрузки. Добавлены workflow-регрессия с управляемым медленным ответом, CSS-регрессия геометрии, пользовательская документация и «Что нового» 0.786.0; backend и схема БД не менялись. Проверено: focused frontend 31/31, полный frontend 636/636 с coverage 82,30% statements / 74,19% branches / 78,22% functions / 82,92% lines, backend Release 1574/1574 на PostgreSQL 17 с русской локалью; backend coverage 90,40% строк и 73,22% ветвей; lint, TypeScript production build, bundle budget 241,9 KiB из 260 KiB, backend format, privacy/ignore, Docker Compose и distribution, локальная установка без Docker, PostgreSQL preflight и idempotent migration SQL — успешно.
- 2026-07-23: завершён двадцать пятый пункт. Финансовые отчёты гаража, поставщика и сотрудника теперь при открытии автоматически охватывают весь период обслуживания: сервер определяет первый и последний месяц активных начислений и операций выбранной записи, обязательно включает текущий рабочий месяц и учитывает будущие активные записи; отменённые записи границы периода не расширяют. Если финансовой истории ещё нет, открывается текущий месяц. Пользователь по-прежнему может вручную изменить обе границы. Для этого добавлены ограниченный PostgreSQL-запрос, сервисный и HTTP-контракты, обработка неверной/отсутствующей цели, клиентская интеграция во все три точки открытия, backend/controller/PostgreSQL/frontend/API/workflow-регрессии, пользовательская документация и «Что нового» 0.785.0. Допустимый диапазон истории расширен до 600 месяцев, сохранив защитный предел; схема БД не менялась. Проверено: focused frontend 15/15, focused backend 81/81, отдельная PostgreSQL-регрессия 1/1, полный frontend 634/634 с coverage 82,31% statements / 74,15% branches / 78,25% functions / 82,92% lines, backend Release 1574/1574 на PostgreSQL 17 с русской локалью; backend coverage 97,43% строк и 73,09% ветвей; lint, TypeScript production build, bundle budget 241,7 KiB из 260 KiB, backend format, privacy/ignore, Docker Compose и distribution, локальная установка без Docker, PostgreSQL preflight, idempotent migration SQL, JSON, строгий UTF-8 и `git diff --check` — успешно.
- 2026-07-23: завершён двадцать четвёртый пункт. Финансовый отчёт поставщика теперь начинает выбранный период с серверного входящего остатка: стартовый баланс плюс все активные начисления до периода минус все активные выплаты до периода. Отменённые и операции самого выбранного периода в перенос не входят. PostgreSQL выполняет ограниченный агрегат, поэтому браузер не загружает прежнюю историю. В отчёте добавлены карточка и отдельная строка `Входящий остаток`; она задаёт начальное сальдо, но не увеличивает показатели `Начислено` и `Оплачено` выбранного периода. Для поставщика без предшествующих операций сохраняется понятная строка `Стартовый баланс`, а нулевой остаток после взаимного погашения истории остаётся видимым. Добавлены сервисный и HTTP-контракты, 404 для отсутствующего поставщика, backend/controller/PostgreSQL/frontend/API/workflow-регрессии, пользовательская документация и «Что нового» 0.784.0; схема БД не менялась. Проверено: focused frontend 13/13, focused backend 94/94, отдельная PostgreSQL-регрессия 1/1, полный frontend 632/632 с coverage 82,30% statements / 74,14% branches / 78,24% functions / 82,92% lines, backend Release 1568/1568 на PostgreSQL 17 с русской локалью; backend coverage 97,43% строк и 73,04% ветвей; lint, TypeScript production build, bundle budget 241,5 KiB из 260 KiB, backend format, privacy/ignore, Docker Compose и distribution, локальная установка без Docker, PostgreSQL preflight, idempotent migration SQL, JSON, строгий UTF-8 и `git diff --check` — успешно.
- 2026-07-23: завершён двадцать третий пункт. Устранено расхождение, возникавшее сразу после изменения карточки поставщика: успешный ответ `UpdateSupplier` раньше подставлял в `Debt` только стартовый баланс, хотя серверный список и финансовый отчёт рассчитывали `стартовый баланс + активные начисления − активные выплаты`. Оба успешных пути обновления — нормализованный no-op и фактическое изменение с audit — теперь возвращают полный долг через единый серверный агрегат; frontend сразу сохраняет это значение в строке таблицы. Регрессии проверяют пример `100 + 900 − 250 = 750` до изменения и `200 + 900 − 250 = 850` после него, а также отсутствие ошибочного значения `200` в колонке. Следующий пункт про входящий остаток предыдущих периодов не смешивался с этим исправлением. Обновлены ежемесячный чек-лист и «Что нового»; схема БД не менялась. Проверено: focused frontend 1/1, focused backend 1/1, полный frontend 631/631 с coverage 82,31% statements / 74,14% branches / 78,20% functions / 82,93% lines, backend Release 1563/1563 на PostgreSQL 17 с русской локалью; backend coverage 90,35% строк и 73,18% ветвей; lint, TypeScript production build, bundle budget, backend format, privacy, Docker distribution, idempotent migration SQL, JSON, строгий UTF-8 и `git diff --check` — успешно.
- 2026-07-23: дополнительная проверка двадцать второго пункта выявила, что поле `Стартовый баланс` было доступно в справочнике и API, но отсутствовало в основной карточке `Контрагенты → Поставщики`. Поле добавлено в адаптивный финансовый ряд карточки для создания и редактирования; изменение включено в окно сравнения, сохраняется существующим backend-контрактом и остаётся при повторном открытии. Запись 0.782.0 в «Что нового» и руководство дополнены. Проверено: focused frontend 30/30, полный frontend 631/631 с coverage 82,31% statements / 74,14% branches / 78,20% functions / 82,93% lines, backend Release 1563/1563 на PostgreSQL 17 с русской локалью; backend coverage 90,35% строк и 73,18% ветвей; lint, TypeScript production build, bundle budget, backend format, privacy, Docker distribution и idempotent migration SQL — успешно.
- 2026-07-23: завершён двадцать второй пункт. Подтверждено, что поле `Стартовый баланс` уже сохраняется в карточке поставщика и PostgreSQL, а серверный отчёт по выплатам уже включает его отдельным начальным обязательством. Устранён оставшийся разрыв в финансовом отчёте карточки поставщика: ненулевой стартовый баланс показывается отдельной строкой на начало выбранного периода, входит в `Начислено` и последовательный остаток; положительное значение увеличивает задолженность, отрицательное учитывается как аванс, нулевое не создаёт пустую строку. Отчёт сотрудника не изменён. Обновлены руководство, ежемесячный чек-лист и «Что нового»; backend и схема БД не менялись. Первый полный frontend-запуск был остановлен внешним двухминутным лимитом команды без падения тестов и повторён с достаточным лимитом без ослабления тестовых тайм-аутов. Проверено: focused frontend 4/4, полный frontend 630/630 с coverage 82,30% statements / 74,10% branches / 78,19% functions / 82,92% lines, backend Release 1562/1562 на PostgreSQL 17 с русской локалью и дополнительно релизы 23/23; backend coverage 90,35% строк и 73,15% ветвей; lint, TypeScript production build, bundle budget, backend format, privacy, Docker distribution, idempotent migration SQL, JSON, строгий UTF-8 и `git diff --check` — успешно.
- 2026-07-23: завершён двадцать первый пункт. Во вкладке поставщиков удалены кнопки `Показать должников` / `Показать всех поставщиков` и связанное клиентское состояние, которое фильтровало только загруженную страницу. Поставщики всегда остаются в общем серверно-пагинированном списке, а задолженность можно анализировать штатной сортировкой колонки. Фильтр должников гаражей, его серверный запрос, состояния загрузки и восстановления после ошибки не менялись. Проверено: focused frontend 2/2, полный frontend 627/627 с coverage 82,29% statements / 74,13% branches / 78,19% functions / 82,91% lines, backend Release 1562/1562 на PostgreSQL 17 с русской локалью; backend coverage 90,34% строк и 73,14% ветвей; lint, TypeScript production build, bundle budget, backend format, privacy, Docker distribution, idempotent migration SQL, JSON, строгий UTF-8 и `git diff --check` — успешно.
- 2026-07-23: завершён двадцатый пункт. В формах создания гаража, поставщика и сотрудника скрыта кнопка `Открыть фин. отчет`, поскольку до первого сохранения у записи нет серверного идентификатора и отчёт сформировать невозможно. В карточках уже сохранённых записей, строках таблиц и контекстных меню действие осталось доступным. Новый отдельный сквозной тест проверяет все три формы создания, а существующие проверки подтверждают кнопку во всех трёх формах редактирования. Backend, финансовые расчёты и схема БД не менялись. Первый полный frontend-прогон показал, что дополнительные шаги в старом крупном сценарии довели его до штатного истечения тестовой сессии; проверка вынесена в отдельный быстрый сценарий без увеличения тайм-аута, после чего полный gate повторён. Проверено: focused frontend 1/1, полный frontend 627/627 с coverage 82,31% statements / 74,18% branches / 78,23% functions / 82,93% lines, backend Release 1562/1562 на PostgreSQL 17 с русской локалью; backend coverage 90,35% строк и 73,15% ветвей; lint, TypeScript production build, bundle budget, backend format, privacy, Docker distribution, idempotent migration SQL, JSON, строгий UTF-8 и `git diff --check` — успешно.
- 2026-07-23: завершён девятнадцатый пункт. Во всех формах владельцев, гаражей, поставщиков и контактов поставщиков введён общий телефонный контрол с прогрессивной маской `+7 (999) 123-45-67`; разрешён ввод через `8`, `+7` и десять национальных цифр, пустое значение остаётся допустимым, неполные номера, текст и неподходящие коды отклоняются. Backend независимо нормализует владельца, поставщика и контакт до записи и возвращает `phone_invalid` при ошибке. Data-migration на PostgreSQL приводит распознаваемые старые значения в трёх таблицах к каноническому виду без изменения цифр, а нестандартные значения сохраняет для ручной проверки. Миграционный PostgreSQL-тест проверяет повторное выполнение конкретной миграции на реальных старых значениях. Полные gate-прогоны выявили и устранили имена snake_case-таблиц в SQL, BOM в файлах EF и устаревшие фикстуры с невозможными кодами `111`/`222`; после исправлений проверки повторены с нуля. Проверено: focused frontend 23/23 и сквозные сценарии 6/6, focused backend 121/121, PostgreSQL migration 1/1, полный frontend 626/626 с coverage 82,29% statements / 74,09% branches / 78,16% functions / 82,91% lines, backend Release 1562/1562 на PostgreSQL 17 с русской локалью; backend coverage 90,35% строк и 73,15% ветвей; lint, TypeScript production build, bundle budget, backend format, privacy, Docker distribution и idempotent migration SQL — успешно.
- 2026-07-23: завершён восемнадцатый пункт. После выбора адреса из DaData в формах гаража, поставщика и владельца больше не показывается отдельная успешная строка; уведомление сохранено как визуально скрытый `role="status"` для экранных дикторов. Сообщения поиска, отсутствия результатов и ошибок остаются видимыми. Обновлены пользовательское руководство, ежемесячный чек-лист и «Что нового»; backend и схема БД не менялись. Проверено: focused frontend 4/4, полный frontend 613/613 с coverage 82,23% statements / 74,01% branches / 78,11% functions / 82,85% lines, backend Release 1546/1546 на временном PostgreSQL 17 с русской локалью; backend coverage 90,34% строк и 73,00% ветвей; lint, TypeScript production build, bundle budget, backend format, privacy, Docker distribution и idempotent migration SQL — успешно.
- 2026-07-22: разобраны все 6 страниц и 11 скриншотов документа заказчика, замечания сопоставлены с текущим кодом. Создана дорожная карта; начата работа над принудительным возвратом на форму входа при истечении активной сессии.
- 2026-07-22: завершён первый пункт. Интерфейс ставит таймер по `expiresAtUtc`, при срабатывании очищает сохранённую авторизацию и кэш справочников, закрывает рабочие разделы и показывает форму входа. Добавлен регрессионный UI-тест; обновлены руководство пользователя и «Что нового». Проверено: frontend 593/593, backend 1522/1522 на временном PostgreSQL 17 с системной русской локалью, backend coverage 90,28% строк и 72,34% ветвей, frontend coverage 82,11% statements / 73,75% branches / 78,00% functions / 82,71% lines, lint, production build, bundle budget, backend format, privacy и idempotent migration SQL — успешно.
- 2026-07-22: завершён второй пункт. Введена единая матрица доступа к рабочим разделам; недоступные пункты исключаются из главного экрана и боковой панели, а внутренние и audit-переходы проверяют полный набор прав. Для `Платежей` и `Отчётов` закреплено одновременное требование профильного права и чтения справочников. Добавлены 13 unit-сценариев матрицы и обновлены интеграционные UI-тесты скрытия. Проверено: frontend 606/606, backend 1522/1522 на временном PostgreSQL 17 с русской локалью, backend coverage 90,29% строк и 72,35% ветвей, frontend coverage 82,09% statements / 73,70% branches / 77,99% functions / 82,69% lines, lint, TypeScript production build, bundle budget, backend format, privacy и idempotent migration SQL — успешно.
- 2026-07-22: завершён третий пункт. Сохранён минимальный пароль длиной 8 символов, удалены обязательные требования к заглавным и строчным буквам и цифрам. Правило синхронизировано между backend, входом, первичной настройкой, сменой собственного пароля и управлением пользователями; пустые и состоящие только из пробелов значения отклоняются. Обновлены подсказки, руководства и «Что нового». Проверено: focused backend 51/51, focused frontend 38/38 и 6/6 сквозных сценариев, полный frontend 606/606, backend 1523/1523 на временном PostgreSQL 17 с русской локалью, backend coverage 90,29% строк и 72,34% ветвей, frontend coverage 82,10% statements / 73,69% branches / 78,02% functions / 82,69% lines, lint, TypeScript production build, bundle budget, backend format, privacy и idempotent migration SQL — успешно.
- 2026-07-22: завершён четвёртый пункт. По исходному комментарию `Тарифы -> Тарифы и сборы` проверены боковая навигация, плитка главного экрана, заголовок и доступное имя страницы, сообщение об ограничении доступа и переход из истории изменений. Основные поверхности уже использовали правильное название; оставшееся сокращённое название исправлено в пользовательской матрице прав, руководстве и ежемесячном чек-листе. Подгруппа справочника `Тарифы` сохранена как название отдельного списка ставок. Добавлены точные регрессионные проверки документации и обновлён существующий UI-тест. Проверено: focused frontend 4/4 и 1/1 сквозной сценарий, focused backend 2/2, backend 1523/1523 на временном PostgreSQL 17 с русской локалью, backend coverage 90,29% строк и 72,34% ветвей; первый параллельный frontend-прогон дал один невоспроизводимый сбой старого сценария контрагентов под нагрузкой, этот сценарий отдельно прошёл 1/1, затем полный frontend без параллельной нагрузки прошёл 606/606 с coverage 82,09% statements / 73,65% branches / 77,99% functions / 82,69% lines. Lint, TypeScript production build, bundle budget, backend format, privacy и idempotent migration SQL — успешно.
- 2026-07-22: завершён пятый пункт. По скриншоту заказчика удалено дополнительное окно сравнения прав роли; обычное изменение пользователя также сохраняется одним нажатием. Серверный аудит сохранён. Ошибка API оставляет исходную форму открытой и позволяет повторить сохранение. Для опасного отключения пользователя оставлено отдельное подтверждение; подтверждения удаления и восстановления не менялись. Обновлены сквозные тесты успешного сохранения, no-op, ошибки и повторной попытки, отмены и подтверждения отключения, пользовательское руководство и «Что нового». Проверено: focused frontend 2/2, focused backend 1/1, полный frontend 606/606 с coverage 82,16% statements / 73,75% branches / 78,06% functions / 82,77% lines, backend 1523/1523 на временном PostgreSQL 17 с русской локалью, backend coverage 90,29% строк и 72,34% ветвей, lint, TypeScript production build, bundle budget, backend format, privacy и idempotent migration SQL — успешно.
- 2026-07-22: завершён шестой пункт. В форме добавления услуги разделены маршруты сохранения: услуга с выключенным признаком регулярных платежей создаётся в справочнике нерегулярных платежей с введённой стоимостью и сразу отображается в соответствующей таблице. Скрытые признаки счётчика и пороговой тарификации в нерегулярную запись не передаются; backend общего каталога дополнительно обнуляет эти признаки для нерегулярных служебных записей, а старые нерегулярные записи общего каталога не выводятся среди регулярных тарифов и услуг. Обновлены пользовательское руководство и «Что нового». Проверено: focused frontend 1/1, затронутые старые frontend-сценарии 2/2, focused backend 99/99, полный frontend 607/607 с coverage 82,18% statements / 73,77% branches / 78,07% functions / 82,79% lines, backend Release 1524/1524 на временном PostgreSQL 17 с русской локалью, backend coverage 90,29% строк и 72,35% ветвей, lint, TypeScript production build, bundle budget, backend format, privacy, idempotent migration SQL, JSON и строгий UTF-8 — успешно.
- 2026-07-22: завершён седьмой пункт. В форме регулярной услуги термин `Вид начисления` заменён на единый `Вид поступления` и дополнен пояснением назначения. Отдельное заблокированное поле `Способ расчёта` автоматически показывает базу выбранного совместимого тарифа; в поле `Тариф` вместе с названием видна применяемая ставка. Смена вида поступления сразу обновляет совместимый тариф и способ расчёта. Те же термины закреплены в backend-валидации и названиях полей audit. Обновлены адаптивная раскладка, руководство пользователя и «Что нового». Проверено: focused frontend 1/1, focused backend 2/2, полный frontend 607/607 с coverage 82,21% statements / 73,80% branches / 78,09% functions / 82,82% lines, backend Release 1524/1524 на временном PostgreSQL 17 с русской локалью, backend coverage 90,29% строк и 72,51% ветвей; lint, TypeScript production build, bundle budget, backend format, privacy, Docker distribution, idempotent migration SQL, JSON и строгий UTF-8 — успешно.
- 2026-07-22: завершён восьмой пункт. Числовая периодичность регулярной услуги заменена единым выбором `Ежемесячно`/`Ежегодно` в форме создания и рабочей таблице. Для ежемесячного режима показывается только день оплаты с явным правилом `следующего месяца`, месяц оплаты не передаётся и нормализуется backend в `null`; для ежегодного режима показываются месяц начисления и полная дата оплаты в том же учетном году. Backend отклоняет произвольные интервалы и неполные сроки. Изменение схемы и EF migration не требуются, поскольку существующие nullable-поля сохраняют внутренние значения `1/12`. Первый полный backend-прогон выявил и предотвратил ошибочное расширение правила переноса годового срока на следующий год: сохранено проверенное поведение, при котором позднее годовое начисление может сразу стать просроченным. Обновлены модель данных, руководство и «Что нового». Проверено: focused frontend 1/1, focused backend 15/15, полный frontend 607/607 с coverage 82,18% statements / 73,86% branches / 77,98% functions / 82,81% lines, backend Release 1525/1525 на временном PostgreSQL 17 с русской локалью, backend coverage 90,31% строк и 72,59% ветвей; lint, TypeScript production build, bundle budget, backend format, privacy, Docker distribution, idempotent migration SQL, JSON и строгий UTF-8 — успешно.
- 2026-07-22: завершён девятый пункт. Причина замечания `Нельзя добавить новый порог` устранена на уровне данных: добавление открывает отдельную форму названия, возрастающей верхней границы и ставки, конфигурация от 2 до 20 ступеней сохраняется атомарно в PostgreSQL `jsonb`, возвращается со стабильными идентификаторами и используется прогрессивным расчётом начислений. Пользовательский порог остаётся после ответа API и повторной загрузки; удаление пересохраняет тариф и фиксирует обязательную причину в audit. Старые трехступенчатые тарифы читаются без ручной миграции данных и переводятся в новую конфигурацию при первом изменении. Первый полный frontend gate выявил обычный decimal-input границы и потребовал общий компонент числовых показаний; первый полный backend gate выявил BOM исходников EF migration и ложное audit-изменение пустого списка ступеней — все три отклонения исправлены, их регрессии прошли. Обновлены модель данных, руководство и «Что нового». Проверено: focused frontend 2/2 и дополнительная gate-регрессия 2/2, focused backend 7/7 и повторная проверка исправлений 5/5, полный frontend 607/607 с coverage 82,06% statements / 73,78% branches / 77,92% functions / 82,69% lines, backend Release 1529/1529 на временном PostgreSQL 17 с русской локалью, backend coverage 90,28% строк и 72,93% ветвей; lint, TypeScript production build, bundle budget, backend format, privacy, Docker distribution и idempotent migration SQL — успешно.
- 2026-07-22: завершён десятый пункт. В форме сбора поле `Сумма сбора` стало неизменяемым и сразу показывает формулу `взнос × количество участников`; при переключении между всеми гаражами и выбранным составом результат обновляется. Для общего сбора frontend получает полный серверный счётчик активных гаражей, а backend независимо пересчитывает план по фактическому активному или проверенному выбранному составу и не доверяет присланной клиентом сумме. Изменение взноса либо допустимое изменение состава пересчитывает план и попадает в существующий audit; схема БД не менялась. Проверено: focused frontend 3/3, focused backend 3/3, полный frontend 607/607 с coverage 82,09% statements / 73,83% branches / 77,96% functions / 82,71% lines, backend Release 1529/1529 на временном PostgreSQL 17 с русской локалью, backend coverage 90,28% строк и 72,93% ветвей; lint, TypeScript production build, bundle budget, backend format, privacy, Docker distribution, JSON, строгий UTF-8, `git diff --check` и idempotent migration SQL — успешно.
- 2026-07-22: завершён одиннадцатый пункт. Устранена гонка, при которой сохранённый сбор сначала появлялся в `Объявленных сборах`, а запоздавший ответ первоначальной загрузки заменял список старым снимком; успешное создание теперь немедленно завершает состояние загрузки, а поздний снимок объединяется с локально сохранённой записью. Ограниченный backend-список отдаёт сначала активные сборы от новых к старым, поэтому архивные и старые записи не вытесняют новый сбор. Отчёт не изменялся: существующая устойчивая связь по `FeeCampaignId` отдельно подтверждена регрессионным тестом. Проверено: focused frontend 1/1, focused backend 2/2, полный frontend 608/608 с coverage 82,10% statements / 73,87% branches / 77,96% functions / 82,73% lines, backend Release 1530/1530 на временном PostgreSQL 17 с русской локалью, backend coverage 90,28% строк и 72,93% ветвей; lint, TypeScript production build, bundle budget, backend format, privacy, Docker distribution, JSON, строгий UTF-8, `git diff --check` и idempotent migration SQL — успешно.
- 2026-07-22: завершён двенадцатый пункт. Ежемесячная фоновая обработка теперь вместе с регулярными услугами выбирает действующие объявленные сборы и идемпотентно создаёт начисления только отсутствующим участникам; сбор, начавшийся в середине месяца, относится к этому же учетному месяцу. Выборка ограничена 500 сборами с явной ошибкой при превышении, а ошибки отдельных сборов переводят фоновую задачу на ускоренный повтор. Ручная кнопка переименована в `Доначислить`: форма объясняет автоматический режим, отсутствие дублей, назначение выбранного месяца и необязательного комментария. Все финансовые изменения сохраняют существующий audit по `FeeCampaignId`. Проверено: focused frontend 1/1, focused backend 7/7, полный frontend 608/608 с coverage 82,10% statements / 73,87% branches / 77,96% functions / 82,73% lines, backend Release 1536/1536 на временном PostgreSQL 17 с русской локалью, включая отдельную PostgreSQL-регрессию; backend coverage 90,33% строк и 72,97% ветвей; lint, TypeScript production build, bundle budget, backend format, privacy, Docker distribution, JSON, строгий UTF-8, `git diff --check` и idempotent migration SQL — успешно.
- 2026-07-23: завершён тринадцатый пункт. В основной строке действующей регулярной услуги появилась кнопка с карандашом, открывающая единую предзаполненную форму редактирования названия, вида поступления, тарифа, расписания, срока оплаты, переноса долга, единицы измерения и признаков расчёта. Тип услуги после создания защищён от смены; успешный атомарный ответ обновляет основную и все технические строки, а ошибка API оставляет форму и введённые данные для повторного сохранения. Схема БД и backend-контракт не менялись. Проверено: focused frontend 1/1 и соседние сценарии услуг 2/2, focused backend 5/5, полный frontend 609/609 с coverage 82,18% statements / 73,98% branches / 78,10% functions / 82,80% lines, backend Release 1536/1536 на временном PostgreSQL 17 с русской локалью; backend coverage 90,33% строк и 72,96% ветвей; lint, TypeScript production build, bundle budget, backend format, privacy, Docker/ignore 8/8, JSON, строгий UTF-8, `git diff --check` и idempotent migration SQL — успешно.
- 2026-07-23: завершён четырнадцатый пункт. Единица измерения теперь однозначно выводится из способа расчёта выбранного тарифа: `руб.` для фиксированной суммы, `чел.` для расчёта по людям, `м³` для воды и `кВт·ч` для электроэнергии. Форма обновляет вычисленное read-only поле при смене тарифа, рабочая таблица показывает и блокирует ту же единицу, а inline-сохранение старой записи отправляет вычисленное значение вместо устаревшего. Backend повторно сверяет единицу с базой тарифа и отклоняет подмену до записи и audit; схема БД не менялась. Проверено: focused frontend 5/5, focused backend 7/7, полный frontend 610/610 с coverage 82,21% statements / 73,96% branches / 78,09% functions / 82,82% lines, backend Release 1541/1541 на временном PostgreSQL 17 с русской локалью; backend coverage 90,34% строк и 73,00% ветвей; lint, TypeScript production build, bundle budget, backend format, privacy, Docker/ignore 8/8, JSON, строгий UTF-8, `git diff --check` и idempotent migration SQL — успешно.
- 2026-07-23: завершён пятнадцатый пункт. Признаки `Пороговая тарификация` и `По счётчику` теперь показываются только в основной строке услуги; ячейки строк периодичности, срока оплаты, переноса долга и отдельных тарифных порогов остаются пустыми. Для услуг, совпадающих со встроенными строками таблицы, основная строка явно связана с серверной настройкой, поэтому признаки сохраняются через единый договор услуги и доступна общая форма редактирования. Схема БД и backend-контракт не менялись. Проверено: focused frontend 2/2, полный frontend 610/610 с coverage 82,20% statements / 73,97% branches / 78,12% functions / 82,80% lines, backend Release 1541/1541 на временном PostgreSQL 17 с русской локалью; backend coverage 90,33% строк и 72,99% ветвей; lint, TypeScript production build, bundle budget, backend format, privacy, Docker distribution и idempotent migration SQL — успешно.
- 2026-07-23: завершён шестнадцатый пункт. В форме создания регулярной услуги добавлено обязательное поле стоимости, автоматически подставляющее ставку выбранного тарифа и позволяющее задать собственное значение. Новый атомарный backend-контракт за один `SaveChanges` создаёт отдельный тариф, услугу, устойчивую связь между ними и две audit-записи; имитация сбоя вставки услуги подтвердила откат тарифа и audit. Основная строка сразу показывает сохранённую стоимость, а последующее inline-изменение обновляет связанный тариф, не меняя название услуги и не обнуляя сумму. Такой же атомарный маршрут закреплён за кнопкой добавления услуги в разделе поставщиков: там форма создаёт регулярную услугу, сохраняет введённую ставку и сразу добавляет запись в список выбора поставщика. Схема БД не менялась. Первый полный backend gate выявил один устаревший architecture-тест со ссылкой на прежний метод создания; контракт обновлён и отдельно прошёл 1/1. Дополнительная проверка общего диалога выявила прежний маршрут в разделе поставщиков; он исправлен, и после этого полный gate повторён с нуля. Проверено: focused frontend 2/2 и API-клиент 7/7, focused backend 5/5 и architecture 1/1, полный frontend 611/611 с coverage 82,22% statements / 73,97% branches / 78,09% functions / 82,84% lines, backend Release 1546/1546 на временном PostgreSQL 17 с русской локалью; backend coverage 90,34% строк и 73,00% ветвей; lint, TypeScript production build, bundle budget, backend format, privacy, Docker distribution, idempotent migration SQL, JSON и строгий UTF-8 — успешно.
- 2026-07-23: завершён семнадцатый пункт. При включении `Пороговой тарификации` форма услуги сразу показывает фактические ступени выбранного электрического тарифа: названия, верхние границы, отсутствие границы у последней ступени и ставки. Пустые несвязанные поля и неработающая кнопка добавления из формы услуги удалены; значения доступны только для чтения и редактируются в строках самого тарифа. Для тарифа без настроенных порогов выводится доступное статусное пояснение, при отключении признака блок сразу скрывается. Схема БД и backend-контракты не менялись. Первый полный frontend gate выявил устаревшее ожидание пустого поля и нарушение общего правила доступных live-region; оба отклонения исправлены, отдельно прошли 3/3, после чего полный gate повторён с нуля. Проверено: focused frontend 4/4 и gate-регрессии 3/3, полный frontend 612/612 с coverage 82,23% statements / 74,00% branches / 78,11% functions / 82,85% lines, backend Release 1546/1546 на временном PostgreSQL 17 с русской локалью; backend coverage 90,33% строк и 73,02% ветвей; lint, TypeScript production build, bundle budget, backend format, privacy, Docker distribution, idempotent migration SQL, JSON, строгий UTF-8 и `git diff --check` — успешно.
