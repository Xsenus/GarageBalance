# Дополнительные замечания заказчика от 27.07.2026

## Источники

- Новый документ заказчика `C:\Users\User16\Downloads\Комментарии Илье (1).docx`: 11 страниц.
- Добавленный блок документа на страницах 7–11: 37 нумерованных замечаний и один пустой пункт `38`.
- Ранее завершённая дорожная карта `docs/roadmaps/customer-comments-2026-07-22-roadmap.md`.
- Текущая реализация backend, frontend, EF Core migrations, автоматические тесты, пользовательская документация и `backend/GarageBalance.Api/AppReleases/releases.json`.
- Правила проекта из `AGENTS.md`.

## Допущения и решения

- Первые шесть страниц нового DOCX повторяют задание от 22.07.2026 и не открывают заново уже проверенные 77 замечаний.
- Эта дорожная карта охватывает только новый блок 1–37 со страниц 7–11.
- Работа выполняется сверху вниз. Независимые пункты после `[decision]` можно выполнять, не дожидаясь решения по заблокированному требованию.
- Новая формулировка имеет приоритет над старой только после явного бизнес-решения, если требования противоречат друг другу.
- Удаление в пользовательских формулировках трактуется как безопасное архивирование, пока заказчик явно не потребует необратимого физического удаления.
- Денежные корректировки кассы и банка выполняются отдельными финансовыми операциями с бизнес-датой, причиной, правом доступа, транзакцией и аудитом; сохранённый итог нельзя менять прямой перезаписью без истории.
- Реальные персональные и финансовые данные заказчика не копируются в тесты, документацию, release notes или диагностические артефакты.
- Пункт 27 нельзя реализовывать по догадке: на скриншоте не определено, какую кнопку обозначает комментарий `Зачем??`.

## Веха 1. Настройки, тарифы и услуги

- [x] 1. Добавить в настройки стартовые остатки кассы и банковского счёта, а также отдельные действия пополнения и списания с датой, причиной, правами и аудитом.
- [x] 2. При включении пороговой тарификации сразу показывать пороги выбранного тарифа; существующая реализация и регрессии проверены в предыдущей дорожной карте.
- [x] 3. При включении расчёта по счётчику автоматически выбирать совместимый счётчиковый тариф и показывать ставку вместо сохранения фиксированного способа расчёта.
- [x] 4. Устранить рассинхронизацию между карточкой услуги и основной таблицей: название, тариф, ставка, единица, признаки расчёта и связанные справочники должны иметь один источник.
- [decision] 5. Определить, какие единицы измерения разрешено редактировать вручную и как исключить несовместимость с формулой тарифа; текущее проверенное правило вычисляет единицу из способа расчёта.
- [x] 6. Перенести колонку `Ед.` перед значением/ставкой и проверить смысловую связь заголовков на широком и узком экране.
- [decision] 7. Подтвердить единицу электроэнергии: документ требует `кВт`, но расчёт потребления и показания электросчётчика сейчас используют `кВт·ч`.
- [x] 8. Перенести настройку перехода долга в просроченный из повторяющейся технической строки в компактную колонку или поле основной строки услуги.
- [x] 9. Добавить понятное пользовательское объяснение деактивации услуги и её последствий для начислений, платежей, истории и восстановления.
- [x] 10. Убрать архивные услуги из рабочего списка в отдельный режим `Удалённые услуги`.
- [x] 11. Исправить frontend-восстановление услуги: после возврата она не зачёркнута, редактируема и доступна при начислении.
- [x] 12. Привязывать поступления по услугам к фонду через вид поступления, а не к поставщику; backend-проверка обязательного действующего фонда уже реализована и покрыта тестами.
- [x] 13. Добавить бизнес-действие `Закрыть сбор`: обычное закрытие после полной оплаты и досрочное закрытие с обязательным комментарием; после закрытия новые начисления не создаются.

## Веха 2. Контрагенты и счётчики

- [x] 14. Связать услугу поставщика с фондом расходования и учитывать эту связь при начислениях, выплатах, доступном остатке и отчётах.
- [x] 15. Вернуть телефон и почту в карточку поставщика, сохранив единый источник контактов и корректное отображение на реальных данных.
- [x] 16. Исправить вёрстку таблицы и карточки поставщика по скриншоту: колонки, переносы, выравнивание и адаптивность.
- [x] 17. Описать и проверить работу показаний счётчиков: действующий тариф, автоматическое начисление, перерасчёт, историческая корректировка и запрет будущего месяца уже реализованы и документированы.

## Веха 3. Поступления владельцев

- [x] 18. По Enter выбирать первый результат поиска гаража; существующий keyboard-сценарий покрыт тестом.
- [decision] 19. Подтвердить переход от ранее согласованного множественного выбора гаражей к работе только с одной карточкой и удалению блока `Выбрано`.
- [x] 20. Начислять сбор выбранным участникам и повторять в следующем месяце только неплательщикам до оплаты либо закрытия сбора.
- [x] 21. Убрать подсказку `ещё не просрочено`, сохранив серверное правило первоочередного погашения старейшей просроченной задолженности.
- [x] 22. Перестроить шапку карточки гаража по смысловым группам: гараж, владелец, финансы.
- [x] 23. Заменить отдельные поля долга и аванса единым знаковым балансом во всех сводках и таблицах карточки.
- [x] 24. Загружать доступный период карточки с начала финансовой истории гаража, включая задолженность до текущего ограниченного диапазона.
- [x] 25. По умолчанию открывать период с первого непогашенного начисления до текущего месяца.
- [x] 26. Воспроизвести на синтетическом PostgreSQL-сценарии несохранение частичной оплаты 360 руб., устранить причину и проверить исчезновение погашенной просрочки после обновления страницы.
- [x] 27. Убрать обязательный комментарий при создании и изменении ручного начисления поставщику; для корректировки автоматического начисления сохранить обязательную причину.

## Веха 4. Начисления и выплаты

- [x] 28. Исправить вёрстку форм нового начисления и премии: единая сетка полей, сообщения валидации, кнопки, узкий экран и отсутствие наложений.
- [x] 29. Убрать ручную кнопку начисления зарплаты, добавить автоматическое ежемесячное начисление активным сотрудникам и настройку дня начисления.
- [x] 30. Заменить закрытое поле `Услуга` в разовом начислении на `Основание` с выбором существующего значения или вводом произвольного текста; сохранить отдельный сценарий штрафа.
- [x] 31. Разделить действия выплат: регулярный поставщик со счёта, эпизодический поставщик из кассы с чеком/без чека, сотрудник с выплатой оклада/премии/штрафа.

## Веха 5. Отчёты

- [ ] 32. Добавить быстрые периоды `Текущий месяц`, `Текущий год`, `Предыдущий год` ко всем применимым отчётам.
- [ ] 33. Воспроизвести дефект PDF консолидированного отчёта, исправить геометрию и проверить многострочные данные, несколько страниц и кириллицу.
- [ ] 34. Доработать фильтр отчёта по гаражам: `Все`, выпадающий список и создание именованных быстрых списков.
- [x] 35. Использовать выпадающий множественный выбор с сокращением результатов по мере ввода; общий компонент уже применяется в отчётах.
- [ ] 36. Исправить ранжирование гаражей в отчёте по поступлениям: точное совпадение, начало номера, остальные совпадения, естественный порядок.
- [ ] 37. Воспроизвести неработающую группировку платежей на реальном API-контракте и устранить рассинхронизацию кнопки, запроса, таблицы и выгрузок.

## Backend, данные и миграции

- [ ] Для каждого нового или изменённого endpoint/service/query покрыть success, empty, invalid, failure, cancellation, permission-denied и важные границы.
- [ ] Все финансовые изменения выполнять транзакционно, с `decimal`, явным округлением, audit и cancellation token.
- [ ] Добавить EF Core migrations для новых настроек, связей, состояний сборов, быстрых списков и финансовых операций; production-код не должен выполнять DDL.
- [ ] Проверить индексы и серверное ограничение выборок для истории гаража, непогашенных начислений, сборов и фильтров отчётов.
- [ ] Проверить миграционный SQL и двойное применение на PostgreSQL с русской ICU-локалью.
- [ ] Сохранить privacy/ignore gate для Access-файлов, дампов, секретов и данных заказчика.

## Frontend и доступность

- [ ] Для каждой изменённой формы, таблицы и диалога покрыть loading, empty, error, validation, stale response, permission и keyboard-сценарии.
- [ ] Использовать общие `LocalizedDatePicker`, `SelectControl`, `TablePagination`, context menu и `create-action-button`.
- [ ] Проверить узкие экраны, масштабирование текста, `prefers-reduced-motion`, доступные имена, live-region и восстановление фокуса.
- [ ] Не показывать пустое состояние до завершения загрузки; использовать общие skeleton/status-компоненты.

## Документация, выпуск и развёртывание

- [ ] После каждого видимого изменения обновлять `docs/user-guide.md`, применимые рабочие checklist и `Что нового`.
- [x] Устранить текущее расхождение документации и кода по автоматическому начислению зарплаты.
- [ ] При изменении переменных окружения, миграций или startup-поведения проверить локальную установку, Docker Compose и автономный дистрибутив.
- [ ] Не включать в пользовательские release notes имена файлов, миграций, коммитов и внутренние технические детали.

## Общий тестовый и выпускной gate

- [ ] После каждого пункта выполнить focused backend/frontend tests.
- [ ] Перед каждым коммитом выполнить полный backend suite и frontend suite с coverage gates.
- [ ] Выполнить backend build/format/privacy/migration/model checks и frontend lint/production build/bundle budget.
- [ ] Выполнить npm/NuGet audit, JSON, строгий UTF-8 и `git diff --check`.
- [ ] Проверить PostgreSQL-поведение; при недоступности честно зафиксировать безопасную замену.
- [ ] Перед каждым коммитом остановить task-owned процессы, выполнить `dotnet build-server shutdown` и удалить временные артефакты.
- [ ] Выполнить ручную браузерную приёмку изменённого сценария на синтетических данных.

## Приёмка на данных заказчика

- [acceptance] Проверить исправление контактов поставщика, частичной оплаты, PDF и группировки платежей на рабочей копии данных без переноса чувствительных данных в репозиторий.
- [acceptance] Согласовать единицы измерения, одиночный/множественный выбор гаражей и неидентифицированную кнопку из пункта 27.
- [acceptance] После завершения всех пунктов провести итоговый месячный сценарий: тарифы, сборы, показания, поступления, выплаты, зарплата, фонды и отчёты.

## Определение готовности

- Все пункты 1–37 имеют статус `[x]` либо явно приняты заказчиком как неприменимые.
- Для каждого изменения существуют ближайшие автоматические регрессии и успешно выполнен полный обязательный gate.
- Финансовые операции, новые связи и изменения прав проверены на PostgreSQL, транзакционность и audit.
- Пользовательская документация и `Что нового` соответствуют фактическому поведению.
- Нет незакоммиченных изменений, временных артефактов и task-owned процессов.
- Изменения закоммичены логическими коммитами на русском языке; push выполняется только по отдельному поручению.

## Открытые риски и вопросы

- Ручное редактирование единиц может сделать тариф несовместимым с формулой начисления.
- `кВт` и `кВт·ч` имеют разный физический смысл; неверное решение исказит показания и отчёты.
- Переход к одной карточке гаража отменяет ранее реализованный и принятый множественный выбор.
- Повторное начисление сбора только неплательщикам требует точного правила частичной оплаты и распределения аванса.
- Стартовые остатки кассы/банка нельзя вводить прямым изменением вычисленного итога без отдельного неизменяемого финансового следа.
- Реальные дефекты пунктов 15, 26, 33 и 37 могут зависеть от конкретной истории данных и требуют приёмки на рабочей копии.

## История выполнения

- 2026-07-28: завершён пункт 31 по исходному DOCX. В форме выплат вместо смешанного действия появились отдельные кнопки `Регулярный поставщик`, `Эпизодический поставщик`, `Выплатить оклад`, `Начислить премию` и `Начислить штраф`. Регулярная выплата использует банковский счёт и настройки услуги/фонда из карточки поставщика; эпизодическая использует кассу, выбранные статью и фонд и атомарно создаёт стоимость с оплатой. Наличие чека теперь хранится независимо от источника денег: кассовая выплата поддерживает оба типа `С чеком` и `Без чека`. Источник сохраняется отдельным полем, участвует в расчёте кассы/банка, изменении, отмене, восстановлении, audit и отображается в общей таблице; миграция заполняет старые расходы по прежнему правилу для сохранения исторических итогов. Обновлены руководство, месячный checklist, модель данных и `Что нового` 0.860.0. Focused проверки: backend 328/328 и PostgreSQL 11/11, frontend 34/34 и два полных UI-сценария 2/2. Полный backend gate на PostgreSQL 17.2 с ICU `ru-RU`: 1718/1718 без пропусков; coverage 90,85% строк / 74,26% ветвей. Полный frontend: 693/693; coverage 82,92% statements / 75,03% branches / 78,89% functions / 83,55% lines. Успешны Release build без предупреждений, backend format, frontend lint/production build, bundle budget 252,6 KiB из 260 KiB, privacy/ignore, npm/NuGet audit без уязвимостей, Docker Compose/distribution, локальная установка без Docker, JSON, строгий UTF-8 и `git diff --check`. Idempotent SQL размером 258093 байта дважды применён к чистой базе; подтверждены 82 миграции, новая колонка, заполнение старых значений, ограничение `bank/cash` и отсутствие pending model changes. Ручная браузерная проверка production-сборки подтвердила пять отдельных действий, фиксированные банковские реквизиты регулярной выплаты, выбираемые статью/фонд и оба типа чека кассовой выплаты, отдельные кадровые диалоги и пустую консоль; API health вернул `ok`, прикладных ошибок в логах нет. Временные процессы, PostgreSQL-кластер, синтетические данные, coverage, SQL и логи удалены перед коммитом.
- 2026-07-28: завершён пункт 30. В диалоге разового начисления закрытый выбор `Услуга` заменён на редактируемое поле `Основание`: бухгалтер может выбрать существующее значение с автоматически подставленной суммой или ввести произвольный текст и сумму. Backend доверяет справочнику при точном выборе, а пользовательские основание и сумма хранятся отдельно для произвольного сценария; повторы ограничиваются только для выбранной справочной записи. Сценарий штрафа остался отдельным действием и диалогом. Миграция добавляет основание к начислению, заполняет его для ранее созданных разовых начислений и корректирует уникальный индекс. Ручная браузерная проверка выявила и устранила дополнительный дефект: после перезагрузки сводная форма теряла основание и показывала `Прочие оплаты`; группировка и распределения теперь сохраняют фактическое основание, а полностью распределённый платёж не создаёт лишнюю общую строку. Добавлены service-, controller-, query-, PostgreSQL- и React-регрессии; обновлены модель данных, пользовательская инструкция, месячный checklist и `Что нового` 0.859.0. Полный backend gate на PostgreSQL 17 с ICU `ru-RU`: 1717/1717 без пропусков, coverage 97,20% строк / 73,19% ветвей; полный frontend: 693/693, coverage 83,03% statements / 75,20% branches / 79,09% functions / 83,66% lines. Успешны Release build без предупреждений, backend format, frontend lint/production build, bundle budget 251,1 KiB из 260 KiB, privacy/ignore, npm/NuGet audit без уязвимостей, Docker Compose/distribution, локальная установка без Docker, JSON, строгий UTF-8, отсутствие pending model changes и `git diff --check`. Idempotent migration SQL размером 256503 байт дважды применён к чистой базе; отдельно проверен переход с предыдущей миграции и заполнение основания у существующей записи. Браузерная проверка после полной перезагрузки подтвердила строку `Замена пульта ворот`, сумму, баланс, отдельную кнопку штрафа и отсутствие ошибок/предупреждений в консоли. Временные серверы, PostgreSQL-кластер, синтетические данные, coverage, SQL и логи удалены перед коммитом.
- 2026-07-28: завершён пункт 29. Из формы выплат удалены ручная кнопка и диалог `Начислить зарплату`, которые ошибочно создавали начисления группе поставщиков; отдельный серверный групповой сценарий поставщиков не изменён. Оклад активного сотрудника по-прежнему рассчитывается из его ставки без дублирующей финансовой таблицы, но теперь включается в ведомость автоматически только с настраиваемого дня текущего месяца: прошлые месяцы остаются начисленными, будущие заранее не показываются. Администратор выбирает день 1–28 во вкладке `Настройки → Рабочая дата`; параметр хранится в PostgreSQL, недопустимые значения отклоняются backend, изменение фиксируется в audit. Добавлены service-, controller-, schedule-, SQLite-, PostgreSQL-, API- и React-регрессии; обновлены модель данных, руководство, месячный checklist и `Что нового` 0.858.0, устранено расхождение документации и кода. Focused backend 31/31 и PostgreSQL 1/1; финальный полный backend Release 1714/1714 без пропусков на PostgreSQL 17 с ICU `ru-RU`; полный coverage-прогон до добавления двух заключительных HTTP-регрессий прошёл 1712/1712 и подтвердил 97,80% строк / 74,07% ветвей. Полный frontend 693/693, coverage 83,01% statements / 75,13% branches / 79,06% functions / 83,65% lines; один старый длинный сценарий карточки сотрудника дал невоспроизводимую потерю фокуса только при первом параллельном запуске под нагрузкой, отдельно прошёл 1/1, повторный полный прогон без конкурирующего backend-набора прошёл полностью без ослабления таймаутов. Успешны backend format и Release publish, frontend lint/production build, bundle budget 251,0 KiB из 260 KiB, privacy/ignore (2409 файлов), npm/NuGet audit без уязвимостей, Docker Compose/distribution, JSON, строгий UTF-8, отсутствие pending model changes и `git diff --check`. Idempotent migration SQL размером 254961 байт дважды применён к чистой PostgreSQL-базе; подтверждены 80 миграций и колонка целого типа для настройки. Проверка локальной установки без Docker прошла; preflight существующей пользовательской БД честно помечен `Blocked` из-за отсутствия пароля, при этом новая схема и логика отдельно полностью проверены на изолированном PostgreSQL. Ручная браузерная проверка подтвердила сохранение 15-го дня, одну audit-запись, отсутствие ручной кнопки зарплаты при сохранённых действиях премии и штрафа, отсутствие ошибок и предупреждений в консоли. Все временные серверы, базы, кластер, тестовый администратор, coverage, migration SQL и логи удалены.
- 2026-07-28: завершён пункт 28 по исходному скриншоту заказчика. Причиной смещения формы премии и аналогичной формы начисления поставщику была самостоятельная строка-подсказка в общей grid-сетке: после неё подписи и поля занимали соседние неправильные ячейки, а сообщение валидации попадало в колонку подписей. Каждое поле теперь является цельной двухколоночной строкой, подсказка об окладе или фонде находится внутри связанного поля, ошибка закреплена под колонкой ввода, действия занимают всю ширину; на узком экране поля, подсказки, ошибки и кнопки переходят в одну колонку. Добавлены компонентные проверки связи подсказок с нужными полями и исполняемые CSS-регрессии широкого и узкого режимов; обновлены руководство пользователя и `Что нового` 0.857.0. Focused frontend: CSS-контракт 34/34 и полный workflow форм 1/1. Полный frontend: 691/691, coverage 83,02% statements / 75,06% branches / 79,14% functions / 83,66% lines. Backend подтверждён полным набором из 1701 сценария: 1612 обычных тестов и 89 PostgreSQL-интеграций; отдельный PostgreSQL-фильтр прошёл 90/90 без пропусков, включая один общий сценарий, coverage 90,82% строк / 74,18% ветвей. Успешны Release build без предупреждений, backend format, frontend lint/production build, bundle budget 251,1 KiB из 260 KiB, privacy/ignore (2707 файлов), npm/NuGet audit, Docker Compose/distribution, локальная установка без Docker, JSON, строгий UTF-8, отсутствие pending model changes и `git diff --check`. Idempotent migration SQL размером 254386 байт дважды применён к чистой PostgreSQL 17.2 с ICU `ru-RU`; подтверждены 79 миграций. Ручная браузерная проверка production-сборки при ширине 1280 px подтвердила ровные подписи и поля, подсказки под связанными комбобоксами, ошибки под полями, непересекающиеся кнопки и отсутствие внутреннего и общего горизонтального переполнения; консоль браузера пуста. Узкая одноколоночная компоновка подтверждена отдельной исполняемой responsive-регрессией. Временные серверы, изолированный кластер, тестовый администратор, синтетические данные, логи, скриншоты и проверочные артефакты удалены перед коммитом.
- 2026-07-28: завершён пункт 27 после повторной проверки оригинального DOCX. Пометка `Зачем??` относится к скриншоту формы `Новое начисление` с сообщением `Для ручного начисления поставщику нужен комментарий`, а не к неидентифицированной кнопке. Комментарий при создании и изменении ручного начисления поставщику теперь необязателен во frontend и backend; пустое значение нормализуется в `null`, само начисление и audit-событие сохраняются. Для изменения автоматически созданного начисления обязательный комментарий оставлен как причина корректировки. Обновлены service-, validation- и React-регрессии, руководство пользователя, месячный checklist и `Что нового` 0.856.0. Полный gate на чистом PostgreSQL 17: backend 1701/1701 без пропусков, coverage 90,83% строк / 74,20% ветвей; frontend 691/691, coverage 83,03% statements / 75,09% branches / 79,17% functions / 83,66% lines. Успешны Release build, backend format, frontend lint/production build, bundle budget 251,1 KiB из 260 KiB, privacy/ignore (1817 файлов), npm/NuGet audit без уязвимостей, JSON/UTF-8 и отсутствие pending model changes.
- 2026-07-28: завершён пункт 26 по точному сценарию замечания: гараж 101, вывоз мусора, апрель 2026, сумма 360 руб. PostgreSQL-воспроизведение подтвердило, что финансовая операция и распределение оплаты сохранялись корректно, а остаток просрочки на сервере становился нулевым; дефект находился в карточке, которая после сохранения оставляла прежние баланс и расшифровку до перезагрузки. После успешной записи frontend теперь сразу обновляет баланс, получает фактическую просрочку с backend, перечитывает ведомость и историю при её открытом состоянии; полностью погашенная строка исчезает без перезагрузки. Ошибка последующего обновления явно сообщает, что платёж уже сохранён, поэтому пользователь не создаёт дубль. Рядом с суммой добавлена доступная кнопка сохранения с индикатором; Enter сохранён. Добавлены React-регрессии точного сценария, явной кнопки, канонического повторного чтения и ошибки обновления, скорректированы существующие серверные имитации после платежа, добавлена PostgreSQL-регрессия сохранности операции и распределения после нового контекста. Обновлены руководство пользователя, месячный checklist и `Что нового` 0.855.0. Полный gate: backend 1701/1701 без пропусков, coverage 90,84% строк / 74,23% ветвей; frontend 691/691, coverage 83,03% statements / 75,10% branches / 79,14% functions / 83,66% lines. Успешны Release build, backend format, frontend lint/production build, bundle budget 251,0 KiB из 260 KiB, privacy/ignore (2407 файлов), npm/NuGet audit без уязвимостей, Docker Compose, distribution и локальная установка без Docker. Схема не менялась, pending model changes нет; idempotent migration SQL размером 254386 байт дважды применён к чистой локальной PostgreSQL-базе, подтверждены 79 миграций. Браузерная проверка на синтетическом гараже 104 подтвердила баланс `-360.00` и просрочку `360.00` до оплаты, нулевые показатели и исчезновение строки сразу после сохранения, а также сохранность результата после полной перезагрузки; ошибок и предупреждений в консоли нет, серверные логи содержат только ожидаемое предупреждение локального HTTP-запуска об отсутствии HTTPS-порта.
- 2026-07-28: завершён пункт 25. Серверная граница финансового периода гаража теперь отдельно возвращает полный доступный диапазон и рабочий диапазон по умолчанию: начало определяется самым ранним активным начислением с непогашенным остатком после учёта только действующих распределений платежей, конец соответствует текущему месяцу. Полностью оплаченные, отменённые и будущие начисления не сдвигают рабочее начало; при отсутствии текущего долга карточка открывается на текущем месяце. Frontend сразу запрашивает этот ограниченный рабочий период без промежуточной загрузки всей истории, но сохраняет в полях выбора непрерывный полный диапазон. Добавлены SQLite service-, PostgreSQL query- и React workflow-регрессии для полностью погашенного старого начисления, первого непогашенного месяца, пустой истории, будущего долга, stale-response и полного списка месяцев; обновлены руководство пользователя, месячный checklist и `Что нового` 0.854.0. Финальный полный gate выполнен на PostgreSQL 17 с русской локалью: backend 1700/1700 без пропусков, coverage 97,77% строк / 74,07% ветвей; frontend 689/689, coverage 83,01% statements / 75,07% branches / 79,10% functions / 83,65% lines. Успешны frontend lint/production build, bundle budget 250,7 KiB из 260 KiB, backend format, privacy/ignore (835 файлов), npm/NuGet audit без уязвимостей, Docker Compose и distribution. Схема не менялась, pending model changes нет; idempotent migration SQL размером 254386 байт дважды применён к чистой локальной базе. Браузерная проверка на синтетическом гараже с полностью погашенным начислением 02.2024 и непогашенными начислениями 05.2024 и 07.2026 подтвердила выбранный период 05.2024–07.2026, наличие раннего 02.2024 и промежуточного 03.2024 в списке, итоговый долг 600,00 руб. и отсутствие ошибок/предупреждений в консоли; в серверных логах только ожидаемое предупреждение локального HTTP-запуска об отсутствии HTTPS-порта. Временные серверы, PostgreSQL-кластер, тестовый администратор, синтетические данные, логи и проверочные артефакты удалены перед коммитом.
- 2026-07-28: завершён пункт 24. При открытии карточки гаража интерфейс сначала получает доступные границы его финансового отчёта, затем загружает ведомость от первого месяца финансовой истории до последнего доступного месяца; список месяцев строится непрерывно, поэтому можно выбрать любой промежуточный период. Ошибка определения периода не запускает частичную загрузку, а запоздавший ответ от ранее выбранного гаража не перезаписывает новую карточку. Обновлены React workflow-регрессии для успешной загрузки, ошибки, непрерывного списка месяцев и stale-response, руководство пользователя, месячный checklist и `Что нового` 0.853.0. Полный gate выполнен на PostgreSQL 17 с русской локалью: backend 1698/1698 без пропусков, coverage 90,83% строк / 74,19% ветвей; frontend 689/689, coverage 83,00% statements / 75,06% branches / 79,10% functions / 83,64% lines. Успешны frontend lint/production build, bundle budget 250,7 KiB из 260 KiB, backend format, privacy/ignore (2405 файлов), npm/NuGet audit без уязвимостей, Docker Compose и distribution. Схема не менялась; idempotent migration SQL размером 254386 байт дважды применён к чистой локальной базе. Браузерная проверка на синтетическом гараже подтвердила период 02.2024–07.2026, наличие промежуточного месяца 03.2024, начальный баланс `-400.00`, начисления `600.00`, конечный баланс `-1 000.00`, историческую и текущую строки, а также отсутствие ошибок и предупреждений в консоли; в серверных логах только ожидаемое предупреждение локального HTTP-запуска об отсутствии HTTPS-порта. Временные серверы, PostgreSQL-кластер, пользователь, синтетические данные, логи и проверочные артефакты удалены перед коммитом.
- 2026-07-28: завершён пункт 23. В выбранных гаражах, группе `Финансы`, итогах периода и ведомости поступлений теперь используется единый показатель `Баланс`: долг отображается со знаком минус и расходным цветом, аванс — со знаком плюс и цветом поступления, нулевой остаток остаётся нейтральным. В таблице две колонки `Аванс`/`Задолженность` заменены одной знаковой колонкой для строк, месячных групп и общего итога; просроченная задолженность сохранена отдельным показателем и не смешивается с балансом. Backend-контракт ведомости дополнен точными балансами на начало и конец периода, чтобы начальный аванс не терялся; оптимистичное добавление начисления и платежа обновляет тот же баланс без перезагрузки. Добавлены unit-, service-, controller-, PostgreSQL-, React workflow- и responsive-регрессии; обновлены руководство пользователя, месячный checklist и `Что нового` 0.852.0. Полный gate выполнен на PostgreSQL 17 с русской локалью: backend 1698/1698 без пропусков, coverage 90,80% строк / 74,16% ветвей; frontend 687/687, coverage 82,97% statements / 75,02% branches / 79,09% functions / 83,61% lines. Успешны frontend lint/production build, bundle budget 250,5 KiB из 260 KiB, backend format, privacy/ignore (2404 файла), Docker Compose и distribution. Схема не менялась; idempotent migration SQL размером 254386 байт дважды применён к чистой локальной базе. Браузерная проверка на синтетическом гараже подтвердила баланс `-1 850.00`, начальный баланс `-1 500.00`, строку начисления `-350.00`, одну колонку `Баланс`, отдельную просрочку `1 500.00` и отсутствие ошибок/предупреждений в консоли и логах. Временные серверы, PostgreSQL-кластер, пользователь, синтетические данные, логи и проверочные артефакты удалены перед коммитом.
- 2026-07-28: завершён пункт 22. Шапка активной карточки разделена на три доступно именованные смысловые группы: `Гараж` содержит номер, количество людей и этажей, `Владелец` — ФИО и телефон, `Финансы` — общий баланс и просроченную задолженность; рабочие действия вынесены в отдельную строку. Разметка переведена на смысловые заголовки и списки терминов/значений, порядок групп закреплён React-регрессией, а широкая, двухколоночная и одноколоночная компоновки — CSS-регрессией. Ручная проверка выявила и устранила скрытое растягивание карточки открытой таблицей просрочки: теперь на ширине 375 px карточка занимает 298 px, группы — по 272 px, таблица прокручивается внутри 242 px, документ не переполняется; на ширине 1440 px три группы располагаются в одной строке. Обновлены руководство пользователя и `Что нового` 0.851.0. Полный gate выполнен на PostgreSQL 17 с ICU `ru-RU`: backend 1698/1698 без пропусков, coverage 97,12% строк / 73,11% ветвей; финальный frontend-прогон 685/685, coverage 82,96% statements / 75,03% branches / 79,07% functions / 83,61% lines. Успешны backend Release build без предупреждений, format, privacy/ignore (1811 файлов), NuGet/npm audit, frontend lint/production build, bundle budget 250,4 KiB из 260 KiB, Docker Compose/distribution и локальная установка без Docker. Pending model changes нет; idempotent migration SQL размером 254386 байт дважды применён к чистой базе с 79 миграциями, схема не менялась. Браузерная production-проверка выполнена на синтетическом владельце и гараже с долгом 1500 руб.; доступны все три группы, ошибок и предупреждений в консоли нет, `/health` отвечает штатно.
- 2026-07-28: завершён пункт 21. Из карточки выбранного гаража удалены отдельный расчёт и текст `ещё не просрочено`; при частичной просрочке интерфейс оставляет только общий долг, сумму просроченной части и открытую подробную таблицу. Неиспользуемое поле непросроченной части удалено из frontend-модели представления. Серверное распределение поступлений не менялось: unit- и service-регрессии подтверждают сортировку начислений по сроку оплаты от старых к новым, а полный PostgreSQL-набор сохраняет тот же порядок на реальной БД. Обновлены руководство пользователя, месячный checklist и `Что нового` 0.850.0. Полный gate выполнен на PostgreSQL 17 с ICU `ru-RU`: backend 1698/1698 без пропусков, coverage 97,11% строк / 73,11% ветвей; frontend 685/685, coverage 82,96% statements / 75,03% branches / 79,07% functions / 83,61% lines. Успешны backend Release build без предупреждений, format, privacy/ignore (2404 файла), NuGet/npm audit, frontend lint/production build, bundle budget 250,3 KiB из 260 KiB, Docker Compose/distribution и локальная установка без Docker. Pending model changes нет; idempotent migration SQL размером 254386 байт дважды применён к чистой базе с 79 миграциями. Ручная браузерная приёмка production-сборки на синтетическом гараже подтвердила общий долг 1500,00 руб., просрочку 500,00 руб., отсутствие удалённой подсказки, сохранность июньской строки расшифровки, отображение версии 0.850.0 и отсутствие ошибок/предупреждений в консоли; при ширине 360 px документ не получил горизонтального переполнения. Временный HTTP-хост записал только ожидаемое для локального запуска без TLS предупреждение HTTPS-redirection. Временные серверы, база, тестовый пользователь, синтетические гараж и начисления, логи и проверочные артефакты удалены перед коммитом.
- 2026-07-27: завершён пункт 20. Первое начисление действующего сбора по-прежнему создаётся только выбранным участникам, а перед каждым следующим месяцем backend агрегирует в PostgreSQL все предыдущие активные начисления и распределённые оплаты по каждому гаражу. Полностью оплативший участник исключается из новых начислений; отсутствие оплаты и частичная оплата оставляют его в следующем месяце до погашения накопившихся начислений или закрытия сбора. Проверка не загружает финансовую историю целиком и сохраняет постоянное число SQL-запросов при росте числа гаражей; повторный запуск одного месяца не создаёт дубли, отменённые начисления и неактивные распределения не учитываются. В форме ручного дозаполнения правило объяснено пользователю, а итог теперь показывает общее число пропущенных участников. Обновлены руководство пользователя, месячный checklist и `Что нового` 0.849.0. Полный gate выполнен на PostgreSQL 17 с ICU `ru-RU`: backend 1698/1698 без пропусков, coverage 97,12% строк / 73,11% ветвей; frontend 685/685, coverage 82,96% statements / 75,04% branches / 79,07% functions / 83,62% lines. Успешны backend Release build без предупреждений, format, privacy/ignore (2406 файлов), NuGet/npm audit, frontend lint/production build, bundle budget 250,4 KiB из 260 KiB, Docker Compose/distribution и локальная установка без Docker. Pending model changes нет; idempotent migration SQL размером 254386 байт дважды применён к чистой базе с 79 миграциями. Отдельные SQLite- и PostgreSQL-сценарии подтвердили выборочный состав, полную, частичную и отсутствующую оплату, накопительное погашение, игнорирование неактивного распределения и штатный пустой автоматический месяц после полной оплаты всех участников. Ручная браузерная приёмка production-сборки подтвердила новый текст правила, создание одного начисления на 500 руб., итог `пропущено: 0`, отображение версии 0.849.0 и отсутствие ошибок/предупреждений в консоли; при ширине 360 px документ не переполняется, окно занимает 319 px, поля — 291 px. Временный HTTP-хост записал только ожидаемое для локального запуска без TLS предупреждение HTTPS-redirection. Временные серверы, база, тестовый пользователь, синтетические гараж и сбор, логи и проверочные артефакты удалены перед коммитом.
- 2026-07-27: завершён пункт 16. По исходному скриншоту из документа заказчика перераспределены минимальные и стартовые ширины колонок таблицы поставщиков: длинное название и почта переносятся только внутри своих ячеек, телефон и денежная задолженность сохраняются целиком, заголовок задолженности больше не разрывается посередине слова, а горизонтальная прокрутка остаётся внутри таблицы. Для ячеек и заголовков добавлены смысловые классы, закреплённые компонентными и CSS-регрессиями; карточка поставщика сохраняет одноколоночную адаптивную компоновку. Обновлены руководство пользователя, месячный checklist и `Что нового` 0.848.0. Полный gate выполнен на PostgreSQL 17 с ICU `ru-RU`: backend 1695/1695 без пропусков, coverage 90,80% строк / 74,16% ветвей; frontend 685/685, coverage 82,96% statements / 75,01% branches / 79,07% functions / 83,62% lines. Успешны backend format, privacy/ignore (2424 файла), NuGet/npm audit, frontend lint/production build, bundle budget 250,3 KiB из 260 KiB, Docker Compose/distribution и локальная установка без Docker. Pending model changes нет; idempotent migration SQL размером 254386 байт дважды применён к чистой базе. Ручная браузерная приёмка production-сборки подтвердила отсутствие переполнения документа, внутреннюю прокрутку таблицы и сохранение значений в своих колонках; при ширине 360 px карточка занимает 319 px без внутреннего переполнения, а все группы полей перестраиваются в одну колонку шириной 291 px. Ошибок и предупреждений в консоли браузера нет; временный HTTP-хост записал только ожидаемое для локального запуска без TLS предупреждение HTTPS-redirection. Исходный DOCX не изменялся: из-за отсутствия LibreOffice встроенный скриншот был извлечён и проверен напрямую. Временные серверы, база, синтетические данные, логи и проверочные артефакты удалены перед коммитом.
- 2026-07-27: завершён пункт 15. Телефон и почта снова показаны в верхней части карточки поставщика, но не создают второй источник данных: оба поля редактируют первую действующую строку таблицы контактов, а изменение этой строки сразу обновляет верхние поля и сводку поставщика. При открытии сохранённой карточки контакты теперь запрашиваются отдельно по идентификатору поставщика, поэтому нужный контакт не теряется за общим ограничением большого справочника; ошибка оставляет карточку закрытой и допускает повторную попытку, а устаревший ответ не открывает уже неактуальную запись. Пустая необязательная почта передаётся как отсутствие значения, поэтому новая карточка и удаление последнего контакта не получают ложную ошибку формата email. Обновлены руководство пользователя, месячный checklist и `Что нового` 0.847.0. Полный gate выполнен на PostgreSQL 17 с ICU `ru-RU`: backend 1695/1695 без пропусков, coverage 90,80% строк / 74,17% ветвей; frontend 684/684, coverage 82,96% statements / 75,04% branches / 79,07% functions / 83,62% lines. Успешны backend format, privacy/ignore (2393 файла), NuGet/npm audit, frontend lint/production build, bundle budget 250,1 KiB из 260 KiB, Docker Compose/distribution и локальная установка без Docker. Pending model changes нет; idempotent migration SQL размером 254386 байт дважды применён к чистой базе. Ручная браузерная приёмка на production-сборке подтвердила загрузку исходного контакта, двустороннее обновление телефона и почты, сохранение в PostgreSQL, повторное открытие, одноколоночную компоновку без переполнения при ширине 360 px и отсутствие ошибок/предупреждений в консоли браузера. Временный HTTP-хост записал только ожидаемое для локального запуска без TLS предупреждение HTTPS-redirection; ошибок приложения и необработанных исключений после штатного запуска не было. Временные серверы, база, тестовый пользователь, синтетические данные и проверочные артефакты удалены перед коммитом.
- 2026-07-27: завершён пункт 14. Для регулярной услуги поставщика добавлен прямой обязательный выбор действующего фонда расходования; связь показывается в карточке поставщика и сохраняется снимком в начислении и финансовой операции. Выплата поставщику транзакционно создаёт связанное списание фонда, изменение суммы корректирует его, отмена возвращает остаток, восстановление повторно списывает средства, а недостаточный остаток блокирует операцию до сохранения. Ведомость выплат использует прямую связь без догадок по названию, показывает фонд и его доступный остаток; итог по общему фонду не дублируется между услугами. Миграция переносит прежние назначения, создаёт исторические связанные списания и пересчитывает цепочки остатков; проверены чистое применение, откат к предыдущей миграции, повторный подъём и двойное применение idempotent SQL на PostgreSQL 17.2 с ICU `ru-RU`. Обновлены руководство пользователя и `Что нового` 0.846.0. Полный gate: backend 1695/1695 без пропусков, coverage 90,83% строк / 74,19% ветвей; frontend 684/684, coverage 82,97% statements / 75,06% branches / 79,13% functions / 83,63% lines. Успешны frontend lint/production build, bundle budget 249,5 KiB из 260 KiB, backend Release build без предупреждений, format, privacy/ignore (835 файлов), npm/NuGet audit, Docker Compose/distribution, проверка отсутствия pending model changes, JSON, строгий UTF-8 и `git diff --check`. Ручная браузерная приёмка на production-сборке и синтетической PostgreSQL-базе подтвердила обязательность фонда, его список с остатками, успешное сохранение услуги и сохранность связи после повторного открытия; ошибок и предупреждений в консоли и API-логах нет. Временные серверы, база, тестовый пользователь, синтетические данные, логи и проверочные артефакты удалены перед коммитом.
- 2026-07-27: завершён пункт 13. В строке действующего объявленного сбора появилось отдельное действие `Закрыть сбор`: backend сравнивает план с суммой реально распределённых неотменённых оплат, разрешает обычное закрытие выполненного плана без обязательного комментария и требует комментарий при досрочном закрытии. Закрытое состояние, исполнитель, вид закрытия и комментарий сохраняются миграцией и audit; строка показывает статус, больше не допускает редактирование и ручное доначисление, а автоматическая ежемесячная выборка исключает закрытые сборы. Прежние начисления, оплаты и отчёты сохраняются; восстановление архивной закрытой записи не возобновляет начисления. Обновлены руководство пользователя и `Что нового` 0.845.0. Полный gate выполнен на PostgreSQL 17.2 с ICU `ru-RU`: backend 1694/1694 без пропусков, coverage 90,87% строк / 74,32% ветвей; frontend 684/684, coverage 83,04% statements / 75,11% branches / 79,16% functions / 83,71% lines. Успешны frontend lint/build, bundle budget 249,1 KiB из 260 KiB, backend Release build без предупреждений, format, privacy/ignore (831 файл), Docker Compose/distribution, локальная установка без Docker и отсутствие pending model changes. Idempotent migration SQL применён дважды к чистой PostgreSQL-базе; применены 78 миграций, новые четыре поля состояния присутствуют. Ручная браузерная приёмка на синтетических данных подтвердила серверный отказ при досрочном закрытии без комментария, успешное закрытие с причиной, удаление действий редактирования/доначисления, отсутствие горизонтального переполнения при ширине 375 px и отсутствие ошибок/предупреждений в консоли и логах.

- 2026-07-27: завершён пункт 11. Во вкладке `Удалённые услуги` в основной строке архивной услуги появилось доступное действие восстановления; без права `tariffs.manage` оно заблокировано. Подтверждение напоминает о будущих начислениях, сохранении проведённых операций и отсутствии автоматического пересчёта прошлого. Ошибка API оставляет диалог и архивную строку на месте для повторной попытки; Escape закрывает окно и возвращает фокус на действие. После успешного backend-ответа интерфейс пересобирает строки из активной настройки, снимает зачёркивание и блокировку, обновляет счётчик архива, сбрасывает пагинацию и автоматически открывает действующий список, где услугу снова можно редактировать и деактивировать. Существующий backend снимает `IsArchived`, возвращает запись в активную выборку для будущих начислений, проверяет назначенный действующий фонд и пишет audit; его сервисные и controller-регрессии подтверждены отдельно 3/3. Добавлены frontend-регрессии успеха, ошибки, повторной попытки, permission-denied, фокуса и доступности полей; обновлены руководство и `Что нового` 0.844.0. Первый полный frontend-прогон выявил гонку прежнего теста разделения списков: строка проверялась до завершения загрузки; ожидание привязано к появлению фактических данных без увеличения тайм-аута, focused-повтор и полный gate прошли. Проверено: frontend 683/683, coverage 83,04% statements / 75,13% branches / 79,16% functions / 83,70% lines; backend 1688/1688 без пропусков, coverage 90,86% строк / 74,30% ветвей. Успешны frontend lint/build, bundle budget 248,4 KiB из 260 KiB, npm/NuGet audit, backend Release build без предупреждений, format, privacy/ignore (829 файлов), отсутствие pending model changes, Docker Compose/distribution и локальная установка без Docker. Idempotent migration SQL размером 244948 байт дважды применён к чистой PostgreSQL 17 с ICU `ru-RU`; схема не менялась. Ручная браузерная приёмка на production-сборке сначала подтвердила понятный серверный отказ при отсутствующем фонде, затем после назначения синтетического фонда — успешный возврат `Наружного освещения` в действующие услуги, доступность редактирования и деактивации, счётчик `Удалённые услуги (0)`, адаптивность при ширине 375 px и отсутствие ошибок/предупреждений в консоли и логах. Временные серверы, база, тестовый пользователь, синтетические данные, логи и проверочные артефакты удалены.
- 2026-07-27: завершён пункт 10. Основная таблица раздела `Тарифы и сборы` по умолчанию показывает только действующие тарифы и услуги, а деактивированные записи вынесены в отдельную доступную вкладку `Удалённые услуги` со счётчиком. После успешной деактивации услуга сразу исчезает из рабочего списка и появляется в удалённых; все её строки и поля там доступны только для чтения, действия изменения, повторной деактивации и преждевременного восстановления не показываются. Переключение режимов сбрасывает пагинацию, использует общий компонент таблицы и собственные доступные имена, а пустой архив имеет отдельное состояние. Пункт 11 о восстановлении намеренно не смешивался с этой задачей. Добавлены workflow-регрессии разделения активных/архивных записей, загрузки архива, изменения счётчика после деактивации, readonly-режима, возврата к рабочему списку и пустого состояния; обновлены руководство и `Что нового` 0.843.0. Проверено: frontend 682/682, coverage 82,87% statements / 75,06% branches / 79,00% functions / 83,54% lines; backend 1688/1688 без пропусков, coverage 90,86% строк / 74,30% ветвей. Успешны frontend lint/build, bundle budget 248,4 KiB из 260 KiB, npm/NuGet audit без известных уязвимостей, backend Release build без предупреждений, format, privacy/ignore (829 файлов), отсутствие pending model changes, Docker Compose/distribution и локальная установка без Docker. Idempotent migration SQL размером 244948 байт дважды применён к чистой PostgreSQL 17 с ICU `ru-RU`; схема не менялась. Ручная браузерная приёмка на production-сборке и синтетической локальной БД подтвердила перенос `Наружного освещения` из действующих в `Удалённые услуги (1)`, блокировку всех полей, адаптивную компоновку вкладок при ширине 375 px и отсутствие ошибок/предупреждений в консоли. Временные серверы, база, тестовый пользователь, логи и проверочные артефакты удалены.
- 2026-07-27: завершён пункт 9. В основной строке действующей услуги появилось отдельное действие `Деактивировать` с понятной иконкой и обязательной причиной. Подтверждение до изменения объясняет все последствия: новые регулярные начисления прекращаются, уже созданные начисления и связанные платежи сохраняются, задолженность можно погашать, операции и причина остаются в отчётах и истории, а восстановление включает услугу только для будущих начислений без автоматического пересчёта прошлых периодов. Тот же смысл закреплён в подтверждении восстановления. Ошибка API не закрывает окно и не стирает причину; после успешного ответа строка блокируется, а без права `tariffs.manage` действие недоступно. Обновлены руководство пользователя и `Что нового` 0.842.0. Первый полный frontend-прогон выявил устаревшее ожидание скрытого действия у пользователя с `tariffs.manage`; регрессия приведена в соответствие новому сценарию, повторный gate прошёл полностью. Проверено: frontend 681/681, coverage 82,86% statements / 75,02% branches / 78,98% functions / 83,52% lines; backend 1688/1688 без пропусков, coverage 90,86% строк / 74,30% ветвей. Успешны frontend lint/build, bundle budget 248,2 KiB из 260 KiB, npm/NuGet audit без известных уязвимостей, backend Release build без предупреждений, format, privacy/ignore (829 файлов), отсутствие pending model changes, Docker Compose/distribution и локальная установка без Docker. Idempotent migration SQL размером 244948 байт дважды применён к чистой PostgreSQL 17.2 с ICU `ru-RU`; применены 77 миграций, схема не менялась. Ручная браузерная приёмка на production-сборке подтвердила обязательную причину, полный текст последствий, успешную деактивацию с audit-записью, блокировку строки, возврат фокуса и отсутствие ошибок/предупреждений в консоли; при ширине 375 px окно занимает 336 px, внутреннего и общего горизонтального переполнения нет.
- 2026-07-27: завершён пункт 8. Повторяющиеся строки `Перенос долга в просроченный` исключены из рабочей таблицы и пагинации, а срок перенесён в компактную колонку `Просрочка, дн.` основной строки каждой услуги; подтверждение, валидация, сохранение в настройке услуги и обработка ошибки сохранены. Обнаруженный ручной проверкой дубль поля в строке даты оплаты `Наружного освещения` устранён отдельным условием и регрессией. Обновлены руководство пользователя и `Что нового` 0.841.0. Полный gate выполнен на PostgreSQL 17.2 с ICU `ru-RU`: backend 1688/1688 без пропусков, coverage 90,86% строк / 74,30% ветвей; frontend 679/679, coverage 82,69% statements / 74,86% branches / 78,83% functions / 83,33% lines. Успешны backend Release build без предупреждений, format, privacy/ignore (829 файлов), NuGet/npm audit, frontend lint/build, bundle budget 247,8 KiB из 260 KiB, Docker Compose/distribution и локальная установка без Docker. Idempotent migration SQL размером 244948 байт дважды применён к чистой PostgreSQL-базе; pending model changes нет, схема не менялась. Ручная браузерная приёмка подтвердила шесть согласованных колонок, отсутствие отдельных и повторных строк просрочки, подтверждение изменения, штатную ошибку бизнес-валидации при отсутствии назначенного фонда, внутреннюю прокрутку таблицы без переполнения документа при ширине 375 px и отсутствие ошибок/предупреждений в консоли.
- 2026-07-27: завершён пункт 6. В таблице тарифов колонка `Ед.` перенесена перед колонкой `Значение / ставка`; тот же семантический порядок сохранён в каждой строке и закреплён React-регрессией по доступным заголовкам и DOM-последовательности полей. Обновлены руководство пользователя и `Что нового` 0.840.0. Полный gate выполнен на PostgreSQL 17.2 с ICU `ru-RU`: backend 1688/1688 без пропусков, coverage 90,85% строк / 74,29% ветвей; frontend 678/678, coverage 82,57% statements / 74,63% branches / 78,75% functions / 83,21% lines. Успешны backend Release build без предупреждений, format, privacy/ignore (829 файлов), NuGet/npm audit, frontend lint/build, bundle budget 247,6 KiB из 260 KiB, Docker Compose/distribution и локальная установка без Docker. Idempotent migration SQL размером 244948 байт дважды применён к чистой PostgreSQL-базе; учтены 77 миграций, pending model changes нет, схема не менялась. Ручная браузерная приёмка на синтетических данных подтвердила порядок `Основание` → `Ед.` → `Значение / ставка` при ширине 1440 и 375 px, внутреннюю горизонтальную прокрутку таблицы без переполнения документа и отсутствие ошибок/предупреждений в консоли.
- 2026-07-27: устранено замедление тестового VPS вне очереди пользовательских пунктов. Диагностика исключила нехватку CPU, RAM, блокировки PostgreSQL и медленные SQL-команды: база занимает 27 МБ, за 7 дней максимальная длительность DB-команды составила 117 мс, незавершённых блокировок не было. В публичном маршруте через Cloudflare воспроизведены нестабильные загрузки статических ресурсов до 20 секунд при локальном ответе nginx/API за 5–12 мс. На VPS выполнен `VACUUM (ANALYZE)`, для nginx явно закреплены HTTP/2 и повторное использование upstream-соединений, а подробное журналирование каждой успешной EF Core DB-команды понижено до `Warning`; конфигурации предварительно сохранены для отката. После исправления 20 последовательных внешних загрузок завершились без зависаний за 0,69–0,75 с, 10 health-запросов — за 0,55–0,85 с; API, nginx и PostgreSQL активны, предупреждений и информационных SQL-команд после финального перезапуска нет. В проект добавлена защищающая конфигурационная регрессия и обновлена VPS-инструкция; `Что нового` не менялось, поскольку изменение инфраструктурное. На отдельном PostgreSQL 17.2 с ICU `ru-RU` прошли backend 1688/1688 без пропусков и повторное применение idempotent migration SQL (244948 байт, 77 миграций, pending model changes нет); backend coverage — 90,86% строк / 74,30% ветвей. Frontend прошёл 678/678, coverage 82,57% statements / 74,59% branches / 78,75% functions / 83,21% lines, lint/build и bundle budget 247,6 KiB из 260 KiB. Также успешны Release build без предупреждений, format, privacy (829 файлов), NuGet/npm audit, Docker Compose/distribution, локальная установка без Docker, JSON, строгий UTF-8 и `git diff --check`.
- 2026-07-27: завершён пункт 4. Основная строка услуги теперь строится из сохранённой настройки услуги и связанного с ней тарифного DTO: отдельно показывает название услуги и название тарифа, а ставку, единицу, способ расчёта и признаки получает из той же пары записей. После создания, редактирования, изменения ставки, архивации и восстановления таблица полностью пересобирается из подтверждённых backend-ответов; название тарифа больше не записывается как название услуги. Пользовательский счётчиковый тариф исключён из несвязанной системной категории, а после переименования системной услуги прежняя прототипная строка не остаётся дубликатом. Добавлены React workflow-регрессии создания, переименования, ошибки и повтора сохранения, смены тарифа и отсутствия чужой/устаревшей строки; обновлены руководство и `Что нового` 0.839.0. Проверено на отдельном PostgreSQL 17.2 с ICU `ru-RU`: backend 1687/1687 без пропусков, coverage 90,86% строк / 74,30% ветвей; frontend 678/678, coverage 82,57% statements / 74,59% branches / 78,75% functions / 83,21% lines. Успешны backend Release build без предупреждений, format, privacy/ignore (828 файлов), NuGet/npm audit, frontend lint/build, bundle budget 247,6 KiB из 260 KiB, Docker Compose/distribution и локальная установка без Docker. Idempotent migration SQL размером 244948 байт дважды применён к чистой PostgreSQL-базе; схема не менялась. Ручная браузерная приёмка на синтетических данных подтвердила переименование `Наружное освещение` в `Освещение территории` при сохранении отдельного имени тарифа, ставки 300,00 руб., единицы и признаков; после обновления страницы осталась одна строка, карточка совпала с таблицей, ошибок в консоли и горизонтального переполнения при ширине 375 px нет. Временные серверы, база и проверочные артефакты удалены перед коммитом.
- 2026-07-27: завершён пункт 3. Переключатель `По счётчику` в форме и основной таблице теперь подбирает совместимый тариф воды или электроэнергии, одновременно меняет ставку, единицу и ссылку настройки услуги на тариф; обратное переключение требует совместимого несчётчикового тарифа. В карточке счётчиковой услуги отдельно показана действующая ставка с единицей. Backend отклоняет обе несовместимые комбинации: включённый счётчик с фиксированным тарифом и счётчиковый тариф с выключенным признаком. Устранён обнаруженный при ручной приёмке оптимистичный показ несохранённого состояния: при ошибке API строка остаётся в прежнем состоянии и допускает повтор. Добавлены unit, service и React workflow-регрессии для автоподбора, отсутствия подходящего тарифа, серверной валидации, успешного сохранения, ошибки и повтора; обновлены руководство пользователя и `Что нового` 0.838.0. Проверено на отдельном PostgreSQL 17.2 с ICU `ru-RU`: backend 1687/1687 без пропусков, coverage 88,70% строк / 73,29% ветвей; frontend 677/677, coverage 82,56% statements / 74,68% branches / 78,76% functions / 83,20% lines. Успешны backend Release build без предупреждений, format, отсутствие pending model changes, privacy/ignore (828 файлов), NuGet/npm audit, frontend lint/build, bundle budget 247,5 KiB из 260 KiB, Docker Compose/distribution, локальная установка без Docker, JSON, строгий UTF-8 и `git diff --check`. Idempotent migration SQL размером 244948 байт дважды применён к чистой базе; схема не менялась. Ручная браузерная приёмка на синтетических данных подтвердила замену фиксированных 300,00 руб. на счётчиковый тариф 100,60 руб./м³, отдельное поле ставки, отсутствие ошибок в консоли и горизонтального переполнения при ширине 375 px. Временные серверы, база, конфигурация, логи и проверочные артефакты удалены перед коммитом.
- 2026-07-27: завершён пункт 1. В административных настройках добавлена вкладка `Касса и счёт`: стартовые остатки кассы и банковского счёта сохраняются неизменяемыми дельта-операциями, а отдельные пополнения и списания требуют бизнес-дату и причину. Списание сверх доступного остатка и уменьшение старта, делающее текущий остаток отрицательным, блокируются; все изменения выполняются под PostgreSQL advisory lock, сохраняются одной транзакцией и создают audit с исполнителем. Текущие суммы включены в финансовый журнал и ведомость выплат без дополнительной неограниченной загрузки; старые коды кассовых расходов используют общий классификатор. История интерфейса ограничена 50 последними операциями, доступна только администратору и имеет loading, empty, validation, error и responsive-состояния. Добавлены EF Core migration, индексы, ограничения БД, backend/controller/PostgreSQL/frontend/API/workflow-регрессии, пользовательские инструкции и `Что нового` 0.837.0. Во время полного gate устранены расхождение классификатора старых кассовых расходов, применение неунифицированных денежных input, хрупкость долгого auth-теста и новая high-уязвимость транзитивного `postcss`; lock-файл обновлён совместимо, повторный audit не находит уязвимостей. Проверено на отдельном PostgreSQL 17.2 с ICU `ru-RU`: focused backend 248/248, полный backend 1686/1686 без пропусков, coverage 90,86% строк / 74,28% ветвей; полный frontend 677/677, coverage 82,45% statements / 74,63% branches / 78,58% functions / 83,12% lines. Успешны backend build без предупреждений, format, отсутствие pending model changes, privacy/ignore (828 файлов), NuGet/npm audit, frontend lint/build, bundle budget 247,0 KiB из 260 KiB, Docker Compose/distribution, локальная установка без Docker, JSON, строгий UTF-8 и `git diff --check`. Idempotent migration SQL размером 244948 байт дважды применён к чистой PostgreSQL-базе. Ручная браузерная приёмка на синтетических данных подтвердила стартовые остатки 1500/7000, пополнение кассы на 250,50, итог 1750,50, историю и отсутствие ошибок в консоли/логах; при ширине 375 px документ не имеет горизонтального переполнения. Временные серверы, база, логи и проверочные артефакты удалены перед коммитом.
- 2026-07-27: изучен новый DOCX целиком и сопоставлен с завершённой дорожной картой от 22.07.2026. Установлено, что страницы 7–11 содержат 37 дополнительных замечаний. Пункты 2, 12, 17, 18 и 35 подтверждены существующим кодом, тестами и документацией; противоречащие прежним решениям пункты 5, 7 и 19, а также неидентифицированный пункт 27 отмечены `[decision]`. Начата реализация пункта 1.
