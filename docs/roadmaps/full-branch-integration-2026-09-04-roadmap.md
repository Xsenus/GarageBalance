# Полная интеграция рабочих веток 04.09.2026

## Источники

- требование заказчика объединить все актуальные локальные и удалённые рабочие ветки в основную ветку;
- локальные ветки `master`, `xsenus/full-system-acceptance-audit` и их история;
- удалённые ветки `origin/master`, `origin/codex/ci-test-performance` после `fetch --all --prune`;
- открытые pull request, GitHub Actions, конфигурация автодеплоя и опубликованный VPS.

## Предположения и решения

- Основная ветка определена по `origin/HEAD`: `master`.
- Интеграция сначала собирается в `xsenus/full-branch-integration`; `master` обновляется только после полного локального gate.
- `origin/codex/ci-test-performance` является актуальной рабочей веткой: она содержит ускорение PostgreSQL-тестов/CI и безопасный первый запуск Docker, которых нет в `master`.
- Локальная `xsenus/full-system-acceptance-audit` содержит завершённый проверочный набор и исправление демонстрационной зарплатной выплаты.
- Производственная база не используется для разрушительных проверок; миграции и финансовые сценарии проверяются на чистой локальной PostgreSQL-базе.
- Рабочие ветки удаляются только после успешного push, CI/CD, проверки VPS и подтверждения достижимости их последних коммитов из `master`.

## Этап 1. Инвентаризация и точки восстановления

- [x] Проверить рабочее дерево, активную и основную ветки, remotes и `origin/HEAD`.
- [x] Выполнить `fetch --all --prune` и зафиксировать вершины всех веток.
- [x] Проверить открытые pull request и назначение обнаруженных веток.
- [x] Сравнить уникальные коммиты и файлы каждой рабочей ветки с `master`.

## Этап 2. Сборка интеграции

- [x] Объединить `origin/codex/ci-test-performance` с проверенной локальной приёмкой.
- [x] Объединить появившуюся после повторного fetch ветку `origin/codex/full-system-clean-db-audit`.
- [x] Разрешить конфликты по смыслу и проверить сохранность обеих реализаций.
- [x] Проверить миграции, release notes, документацию, CI и Docker-конфигурацию после слияния.
- [x] Оформить необходимые интеграционные исправления отдельными коммитами на русском языке.

## Этап 3. Локальный обязательный gate

- [x] Запустить полный backend-набор на PostgreSQL и проверить пороги покрытия.
- [x] Запустить backend format, privacy/security, зависимости и migration gate.
- [x] Создать чистую PostgreSQL-базу, применить миграции и выполнить ключевые финансовые сверки.
- [x] Запустить полный frontend-набор, coverage, lint, production build и bundle budget.
- [x] Проверить Docker Compose/образы, health-check и основные локальные сценарии штатным способом.
- [x] Проверить кодировку, русский текст, итоговый diff, процессы и временные артефакты.

## Этап 4. Основная ветка, CI/CD и VPS

- [x] Обновить локальную `master` проверенной интеграцией без переписывания истории.
- [ ] Отправить `master` в `origin` и дождаться всех обязательных GitHub Actions.
- [ ] При ошибках CI исправить причину, повторить локальный gate и дождаться зелёного повторного запуска.
- [ ] Проверить развернувшийся commit, сервис, nginx, `/health`, frontend, API и журналы VPS.
- [ ] Выполнить smoke-проверку основных пользовательских сценариев опубликованной версии.

## Этап 5. Очистка веток и финальная сверка

- [ ] Подтвердить достижимость вершин завершённых рабочих веток из `master`.
- [ ] Удалить полностью влитые рабочие ветки локально и на GitHub, не затрагивая постоянные ветки.
- [ ] Выполнить повторный `fetch --prune`, проверить список веток и синхронизацию `master`/`origin/master`.
- [ ] Подтвердить чистое рабочее дерево и отсутствие оставленных задачей процессов/временных файлов.

## Definition of done

- Все актуальные уникальные изменения рабочих веток присутствуют в `master`, без дублирования и потерь.
- Полные backend/frontend, migration, security/privacy, Docker и production-build проверки проходят.
- GitHub Actions и автодеплой завершаются успешно; опубликованный VPS обслуживает commit `origin/master` и проходит smoke-check.
- Влитые рабочие ветки безопасно удалены локально и удалённо, а неоднозначные/постоянные ветки сохранены.
- Документация и «Что нового» соответствуют объединённому состоянию; рабочее дерево чистое.

## Риски и открытые вопросы

- [acceptance] Пользовательский Docker ZIP по-прежнему требует отдельной финальной приёмки на чистом Windows 10/11 с Docker Desktop.
- [decision] Поддержка Windows ARM64 остаётся отдельным будущим решением и не входит в эту интеграцию.

## История выполнения

- 04.09.2026 — `origin/HEAD` подтвердил основную ветку `master`. Зафиксированы вершины: `origin/master` — `b1be7c37`, локальная `master` — `98bb3527`, локальная приёмка — `a05224f7`, удалённая ветка ускорения — `7787257a`. Рабочее дерево было чистым, открытых pull request не обнаружено.
- 04.09.2026 — создана интеграционная ветка `xsenus/full-branch-integration` от проверенной вершины `a05224f7`; производственная ветка и VPS до завершения локальной проверки не изменяются.
- 04.09.2026 — удалённая ветка `origin/codex/ci-test-performance` объединена. Единственный конфликт возник в `releases.json`: сохранены обе цепочки записей, Docker-записи присвоена уникальная версия `1.182.17`. Целевой набор из 47 тестов обнаружил потерю UTF-8 BOM в двух Windows-скриптах; кодировка восстановлена, повторный прогон прошёл 47/47 на PostgreSQL 17.
- 04.09.2026 — полный локальный gate объединённого дерева прошёл: backend 2520/2520 за 5 мин 42 с, покрытие 90,82% строк и 74,58% ветвей; frontend 1044/1044, покрытие 88,87% строк и 79,96% ветвей; успешны format, lint, privacy (1193 файла), NuGet/npm audit без уязвимостей, production build и bundle 283723/284672 байта.
- 04.09.2026 — SQL размером 383225 байт с 131 миграцией дважды применён к чистой PostgreSQL-базе. Showcase prepare/audit подтвердил 10 гаражей, 67 начислений, 11 операций, 36 показаний, 3 сбора, 3 поставщика и 3 сотрудника, а также долг/аванс, просрочку, уникальность годовых начислений, фиксированный состав участников и равенство фондов.
- 04.09.2026 — production Docker-образы собраны и подняты на изолированных портах. PostgreSQL, API и frontend стали healthy, прямой и проксированный health вернули 200, 131 миграция применена, администратор создан ровно один раз и сохранился после повторного старта. В браузере проверены вход и объединённые записи «Что нового»; консоль пуста.
- 04.09.2026 — локальные Docker-контейнеры, volumes и тестовые image-теги удалены; временная PostgreSQL-база, coverage, production build, migration SQL, bind-каталоги и журналы очищены. PostgreSQL-кластер и запущенный задачей Docker Desktop остановлены, порты 5081/5174/55432/55433 свободны, `dotnet build-server` завершён, рабочее дерево чистое.
- 04.09.2026 — повторный fetch перед публикацией обнаружил новую вершину `origin/codex/full-system-clean-db-audit` — `7285f52e`. Из неё сохранено исправление маршрута «объявленный сбор → выбранное назначение → фонд», фиксация рабочей даты showcase и две независимые PostgreSQL-регрессии. Конфликты контрольных наборов объединены: 67 начислений, расширенные поставщики/персонал/фонды и зафиксированная дата проверяются вместе; затронутый набор прошёл 55/55.
- 04.09.2026 — после объединения всех обнаруженных веток повторно пройден полный обязательный gate: backend 2523/2523 за 6 мин 26 с, покрытие 90,84% строк и 74,65% ветвей; frontend 1044/1044, покрытие 88,87% строк и 79,96% ветвей. Повторно успешны format, lint, privacy (1194 файла), NuGet/npm audit без уязвимостей, production build и bundle 283723/284672 байта. Idempotent SQL из 131 миграции дважды применён к чистой PostgreSQL-базе; объединённый showcase подтвердил 10 гаражей, 67 начислений, 11 операций, 36 показаний, 3 сбора, 3 поставщика, 3 сотрудника, финансовые сверки и зафиксированную рабочую дату.
- 04.09.2026 — итоговые production Docker-образы повторно собраны уже после второго слияния. На чистом volume PostgreSQL, API и frontend стали healthy; прямой и проксированный `/health` и frontend вернули 200, применена 131 миграция, вход начального администратора работает. После перезапуска API/frontend данные и вход сохранились, повторная учётная запись не появилась, критических и необработанных ошибок в журнале нет.
- 04.09.2026 — проверенная интеграция без переписывания истории перенесена fast-forward в локальную `master`; вершина перед публикацией — `a70db455`.
