# Исправления по комментариям от 8 сентября 2026 года

## Назначение

Дорожная карта охватывает полный разбор и реализацию замечаний из документа `Комментарии_080926 (1).docx`: тарифные периоды, назначение сборов, начисления по счётчикам, отображение и расчёт задолженности, нерегулярные начисления и штрафы, удаление поставщиков, обновление расчёта, целостность связей услуги и фонда, а также выбор фонда расходования в выплатах.

## Исходные материалы

- документ заказчика `C:\Users\ilel\Desktop\Комментарии_080926 (1).docx`;
- 18 абзацев документа, включая 14 отдельных замечаний;
- встроенный снимок таблицы полного расчёта с перенесённой задолженностью 5 000 руб.;
- текущие backend, frontend, миграции, тесты, записи «Что нового» и опубликованная staging-версия GarageBalance.

Исходный документ и снимок могут содержать рабочий контекст и не добавляются в Git. Для регрессий используются синтетические данные.

## Предположения и решения

- [x] Для удаления периода тарифа сохранить непрерывность сетки: после удаления внутреннего периода соседние периоды должны сомкнуться без дня, оставшегося без тарифа.
- [x] В форме назначения сбора оставить выбор фонда вместо технического вида поступления; остальные бизнес-поля самого сбора сохраняются.
- [x] Требование счётчика определяется услугой и её расчётными настройками, а не текстом названия.
- [x] Начисление по воде создаётся из сохранённой разницы показаний и действующего тарифа за расчётный месяц.
- [x] Несколько ручных начислений одного основания за период складываются; уникальность сохраняется для регулярных начислений.
- [x] Остаток долга после платежа считается от долга до операции с учётом перенесённой задолженности и в истории показывается без отрицательного значения при авансе.
- [x] Для нерегулярного начисления и штрафа форма показывает бизнес-основание; фонд остаётся внутренним бухгалтерским назначением.
- [x] Архивация поставщика не требует архивации контактных лиц: контакты сохраняются как исторические данные.
- [x] Связь услуга → фонд проверяется и используется одинаково при отображении и проведении оплаты.
- [x] В выплатах фонд расходования выбирается явно; начальное значение — общий нераспределённый остаток.

## Этап 1 Анализ и воспроизведение

- [x] Сопоставить все 14 замечаний с компонентами, endpoint, прикладными сервисами, сущностями и миграциями.
- [x] Воспроизвести каждый дефект на синтетической локальной базе или существующей автоматической регрессией.
- [x] Зафиксировать предоставленный снимок «до» расчёта перенесённой задолженности без персональных данных.
- [x] Проверить текущую модель связей услуг, поставщиков, фондов, начислений, штрафов и выплат.

## Этап 2 Тарифы и сборы

- [x] Исправить удаление внутреннего периода тарифа без разрыва календарной сетки.
- [x] Покрыть удаление первого, внутреннего и последнего периода и пустые границы; существующие тесты диалога защищают отмену сохранения.
- [x] Заменить технический выбор назначения поступления на прямой выбор фонда.
- [x] Добавить регрессии формы, сохранения, ошибок API и прав доступа.

## Этап 3 Счётчики и начисления

- [x] Устранить запрос счётчика у услуги вывоза мусора.
- [x] Проверить создание начисления по воде после расчёта разницы показаний и сохранить корректную ветку расчёта.
- [x] Обновлять платёжную форму сразу после добавления начисления без перезаписи оптимистичного результата запоздавшим ответом.
- [x] Разрешить два и более ручных начисления за один период и корректно суммировать их.
- [x] Покрыть отсутствие тарифа, нулевой расход, повторное сохранение, параллельное обновление и ошибки API.

## Этап 4 Платежи и задолженность

- [x] Добавить слово «Сбор» в строки и поля платежей, относящиеся к сборам.
- [x] Исправить остаток задолженности в истории платежей.
- [x] Учитывать перенесённую задолженность как начальное значение полного расчёта.
- [x] Для нерегулярного начисления показывать основание, для штрафа — причину штрафа.
- [x] Удалить кнопку «Обновить расчёт» из формы полного расчёта и сохранить автоматическое обновление.
- [x] Покрыть частичную, полную и превышающую долг оплату, отменённые операции и несколько начислений периода.

## Этап 5 Поставщики и целостность фондов

- [x] Убрать требование архивировать контакты при архивации поставщика.
- [x] Исправить несогласованность отображаемого и фактически списываемого фонда.
- [x] Сохранить backend-проверку несовместимой связи услуги и фонда.
- [x] Подготовить безопасную миграцию данных для поставщика «Чистый город» без привязки к конкретному UUID.
- [x] Проверить аудит изменений финансовых связей и исправления данных.

## Этап 6 Выплаты

- [x] Добавить в формы выплаты с чеком и без чека поле «Фонд расходования».
- [x] По умолчанию выбирать общий нераспределённый остаток.
- [x] Передавать выбор через существующий DTO и controller в application service и отражать его в аудите.
- [x] Проверить права, недостаточный остаток, отрицательный фонд, повторную отправку и ошибки API.

## Этап 7 Пользовательский интерфейс и доказательства

- [x] Подготовить снимки «после» для визуально наблюдаемых исправлений.
- [x] Для расчётных исправлений подготовить снимки результата в платёжной таблице и полном расчёте.
- [x] Проверить узкое рабочее разрешение, клавиатурно доступные поля, загрузку, пустые и ошибочные состояния автоматическими тестами.
- [x] Проверить живые сценарии через браузер без необработанных ошибок API и интерфейса.

## Этап 8 Полная проверка и публикация

- [x] Запустить целевые backend и frontend тесты во время разработки.
- [x] Проверить миграции на чистой PostgreSQL и поверх актуальной схемы.
- [x] Запустить 100% backend и frontend suites с порогами покрытия.
- [x] Запустить lint, production build, bundle budget, форматирование, privacy и проверку UTF-8.
- [x] Обновить `backend/GarageBalance.Api/AppReleases/releases.json` понятными пользователю изменениями.
- [x] Проверить Docker-конфигурацию доступным штатным способом: PostgreSQL 17 service-container и Linux release package прошли в GitHub Actions; локальный Docker CLI отсутствует.
- [x] Остановить все процессы задачи и удалить временные файлы, кроме явно запрошенных доказательных снимков.
- [x] Создать логичные коммиты на русском языке.
- [x] Объединить проверенную ветку с `master` без потери параллельных изменений.
- [x] Отправить `master` и дождаться зелёного GitHub Actions.
- [x] Проверить backup, restore check, миграции, nginx, сервис, health, frontend и защищённый API на VPS.

## Критерии готовности

- все 14 замечаний имеют установленную причину и проверенное исправление;
- финансовые расчёты, связи услуг и фондов защищены backend-проверками и аудитом;
- визуальные изменения подтверждены снимками «до/после»;
- полные тесты и обязательные quality gates проходят;
- миграции безопасны для существующих данных и чистой базы;
- `master`, GitHub и VPS содержат одну проверенную версию;
- временные процессы остановлены, рабочее дерево Git чистое.

## Открытые риски и вопросы

- [x] Формулировка «оставить только фонд» сверена с фактической формой: техническое назначение поступления заменено выбором фонда, а бизнес-поля сбора сохранены.
- [x] Для «Чистого города» исправлены обе части: миграция данных и единый backend-контроль фонда при выплате.
- [x] Снимки сделаны только на синтетических демонстрационных данных; исходный документ не публикуется.
- [x] Локальный Docker CLI недоступен; Linux-упаковка и PostgreSQL-контейнер проверены GitHub Actions, VPS — deploy-журналом и внешними health-запросами.

## Повторная проверка документа в ветке аудита

Источник повторной проверки — предоставленный пользователем `Комментарии_080926.docx` от 08.09.2026: те же 14 замечаний и один снимок. Пользователь разрешил коммит и push в `audit/full-check-2026-09-08`. Историческая публикация в `master` и на VPS ниже относится к предыдущей реализации; текущая доработка публикуется в ветке аудита.

- [x] Разрешить несколько ручных начислений по одному каталожному основанию за месяц во всех путях создания, редактирования, восстановления и оплаты; сохранить уникальность автоматических начислений.
- [x] Сохранять основание и идентификатор начисления при немедленном обновлении платёжной формы; сверять её с сохранённым расчётом без потери результата из-за старого ответа.
- [x] Не показывать выдуманный нулевой остаток фонда; проверить загрузку списка фондов, повтор после ошибки и явное подтверждение отрицательного остатка.
- [x] Проверить миграцию «Чистого города» с реальной синтетической историей начислений, платежей и движений фондов.
- [x] Повторить полные тесты, покрытие, сборки, бюджет, миграции, форматирование и проверки данных после новых исправлений.
- [x] Проверить изменённые формы в живом браузере на разных разрешениях и обновить пользовательские заметки релиза.
- [ ] Отправить проверенные исправления в ту же ветку и дождаться успешного CI.
- [x] Независимо проверить текущий VPS без изменения рабочих данных: сервис активен, nginx корректен, readiness с PostgreSQL возвращает 200, защищённый endpoint — 401; установлен релиз 1.183.46.

## История выполнения

### 08 сентября 2026 года Начало работ

Документ заказчика прочитан напрямую из DOCX: извлечены все 18 абзацев, 14 замечаний и единственный встроенный снимок полного расчёта. В Word-комментариях дополнительных требований нет. Исходные материалы не копировались в репозиторий. Создана отдельная ветка `feature/comments-080926`; начато сопоставление замечаний с текущей реализацией.

### 08 сентября 2026 года Реализация и локальная проверка

Все 14 замечаний реализованы и защищены регрессиями. Добавлены миграции для восстановления связи «Чистого города» с услугой вывоза мусора и для разрешения нескольких ручных начислений одного периода при сохранении уникальности регулярных начислений. Исправлены непрерывность тарифной сетки, определение счётчика, подписи сборов и оснований, долг после платежа, полный расчёт с перенесённой задолженностью, немедленное отображение начисления, архивация поставщика и выбор фонда расходования.

Проверка выполнена на отдельной PostgreSQL 17: целевая миграционная регрессия прошла, незаписанных изменений модели EF Core нет, сформирован идемпотентный SQL. Полный backend-набор: 2819 из 2819 тестов, покрытие строк 91,17%, ветвей 76,17%. Полный frontend-набор: 1186 из 1186 тестов, покрытие строк 89,81%, ветвей 81,36%. После последней доработки выбора фонда повторно пройдены lint, production build и bundle budget: 278,5 из 279,0 КиБ. `dotnet format`, проверка уязвимостей NuGet и privacy-проверка прошли. Локальный Docker CLI отсутствует; контейнерная проверка оставлена штатному CI.

В `artifacts/comments-080926/` подготовлен подробный отчёт и доказательные снимки на синтетических данных. Каталог намеренно исключён из Git и сохраняется локально как запрошенный результат задачи.

### 08 сентября 2026 года Публикация и VPS

Коммит `37a9f273` объединён с `master` быстрым перемещением и отправлен в GitHub. Workflow `Deploy staging` №34224901163 завершился успешно за 7 минут 34 секунды: все backend/frontend тесты, dependency audit, privacy, format, lint, production build, bundle, PostgreSQL 17, идемпотентный migration SQL, Linux publish и упаковка прошли.

На VPS применён релиз `37a9f2734f504f32ba7546b60fbba8af276dd213-295`. Перед обновлением создан backup `garagebalance_20260908-152101_37a9f2734f504f32ba7546b60fbba8af276dd213-295.pgdump`; контрольное восстановление подтвердило 50 таблиц. Миграции завершены, обе проверки `nginx -t` успешны, `garagebalance-staging.service` перезапущен, deploy завершился со статусом `ok`. Независимая внешняя проверка подтвердила HTTP 200 для `/health/ready` с PostgreSQL `ok`, главной страницы и опубликованного entry-asset; защищённый `/api/funds/options` без токена вернул штатный 401.

### 09 сентября 2026 года Повторная регрессия перед публикацией

После первого CI-прогона исправлены два остаточных сценария: запаздывающий ответ worksheet больше не затирает оптимистично добавленное начисление в платёжной форме; свободные начисления и каталоговые начисления разделяются по `IrregularPaymentId` при суммировании долга, оплаты и оснований. Запрос worksheet сохранён одним bounded SQL-запросом, а ручные начисления с одноимённым основанием сбора сохранили прежнее отображение.

Финальная локальная проверка на PostgreSQL 17: 2 855/2 855 backend-тестов, строки 91,21% (минимум 85%), ветви 76,24% (минимум 70%); frontend 1 224/1 224, строки 88,75%, ветви 81,49%. Дополнительно прошли 94 целевых frontend-теста, lint, production build, bundle budget (278,9 KiB из 279,0 KiB), `dotnet format`, dependency audit, privacy, отсутствие pending model changes и генерация непустого idempotent migration SQL. Локальный Docker CLI и bash/WSL отсутствуют; соответствующие Linux/Docker проверки остаются за GitHub Actions.
