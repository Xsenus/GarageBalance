# Полная оптимизация производительности и стабильности GarageBalance

## Источники

- Запрос заказчика от 29.07.2026: каждый раздел должен работать максимально быстро, стабильно и без ошибок.
- Реальные nginx-, systemd- и PostgreSQL-метрики VPS после развёртывания версии `0.869.0`.
- Действующие требования `AGENTS.md` по серверной пагинации, PostgreSQL-агрегациям, индексам, frontend-рендерингу, тестам и развёртыванию.
- Текущие backend/frontend-тесты, production bundle budget и инфраструктура GitHub Actions/VPS.
- Активные roadmaps замечаний заказчика; их незавершённые пункты приёмки не переносятся и не расширяют этот roadmap.

## Допущения и решения

- Оптимизация выполняется по измерениям: сначала наблюдаемость и воспроизводимый baseline, затем устранение подтверждённых узких мест.
- Быстродействие не должно ослаблять права, аудит, финансовую точность, защиту данных и проверку конкурентных изменений.
- Запросы записи не повторяются автоматически. Повтор и кэширование допустимы только для безопасных чтений с явными правилами актуальности.
- Фильтрация, сортировка, пагинация и агрегация больших наборов выполняются PostgreSQL; клиент не загружает полные финансовые таблицы.
- Справочники можно кэшировать и объединять параллельные одинаковые загрузки, но кэш обязан сбрасываться после изменений и завершения сессии.
- Оптимизация VPS включает приложение, PostgreSQL, nginx/Cloudflare и диагностику, но изменения DNS/Cloudflare применяются только после проверки текущих настроек и риска.
- Каждая завершённая группа получает тесты, локальную PostgreSQL-проверку, release note при видимом эффекте и отдельный коммит. Push выполняется только по отдельной команде.

## Веха 1. Наблюдаемость и воспроизводимый baseline

- [x] Зафиксировать загрузку CPU/RAM, состояние сервисов, блокировки, hit ratio и health на реальном VPS после версии `0.869.0`.
- [x] Включить безопасный nginx timing log без query string и проверить запись времени upstream/полного запроса.
- [x] Добавить backend `Server-Timing` и безопасное предупреждение для медленных HTTP-запросов.
- [x] Собрать реальные маршруты каждого рабочего раздела и ранжировать их по count/p50/p95/max/error rate.
- [x] Оценить включение `pg_stat_statements` на VPS. По результатам аудита не включать extension в общем PostgreSQL без отдельного окна для `questroom`: текущих timing/correlation/benchmark-метрик достаточно для приёмки, а глобальный restart создаёт больший эксплуатационный риск.
- [x] Добавить безопасную команду/инструкцию для снимка top SQL по total/mean/max time, calls и rows.
- [x] Добавить корреляцию nginx request ID, backend correlation ID и SQL-диагностики без пользовательских данных.
- [x] Создать воспроизводимый локальный performance seed с реалистичным объёмом обезличенных данных.
- [x] Добавить benchmark/smoke-команду для основных API с фиксированными p50/p95/error-rate порогами.
- [x] Зафиксировать исходные размеры JSON, статических ресурсов и количество запросов при первом открытии каждого раздела.

## Веха 2. Системная работа backend и PostgreSQL

- [x] Перевести `GarageBalanceDbContext` на безопасный ограниченный пул контекстов и проверить отсутствие переноса tracked-состояния между запросами.
- [x] Настроить ограниченный command timeout и оставить автоматические retry отключёнными, чтобы финансовые записи не могли повториться.
- [x] Настроить Npgsql connection pool и keepalive по результатам измерений реальных соединений VPS.
- [x] Проверить отмену HTTP-запроса до EF/Npgsql во всех тяжёлых чтениях и экспортах.
- [x] Исключить tracking для всех read-only запросов и добавить архитектурную регрессионную проверку.
- [x] Устранить синхронный I/O и последовательные независимые обращения в общих read-сценариях.
- [x] Добавить сжатие JSON-ответов и проверить отсутствие двойного/бесполезного сжатия за nginx.
- [x] Ограничить максимальные page size, report range и export range едиными проверяемыми правилами.
- [x] Проверить транзакции на лишнюю длительность и исключить внешние HTTP/файловые операции внутри DB-транзакций.
- [x] Проверить autovacuum/analyze, число dead tuples, cache hit и размер таблиц после реалистичной нагрузки.
- [x] Проверить и обосновать PostgreSQL-параметры памяти/соединений для VPS с 4 ГБ RAM.

## Веха 3. Пользователи, вход, права, аудит и настройки

- [x] Оптимизировать загрузку сессии/прав без повторных запросов и повторного вычисления permission-наборов.
- [x] Проверить индексы и SQL списка пользователей/ролей, серверную пагинацию и поиск.
- [x] Оптимизировать аудит: фильтры, сортировку, actor lookup, count и страницу без materialize-before-page в PostgreSQL.
- [x] Ограничить и пагинировать историю форм/настроек, где объём может расти.
- [x] Добавить section-level performance-тесты success/empty/filter/sort/page/permission/cancel.

## Веха 4. Справочники, контрагенты и персонал

- [x] Объединить параллельные одинаковые GET и устранить повторную загрузку контактов при открытии уже загруженного поставщика.
- [x] Добавить версионированный session cache для редко меняющихся справочников с точной инвалидизацией.
- [x] Проверить серверные поиск/сортировку/пагинацию владельцев, гаражей, поставщиков, контактов и сотрудников.
- [x] Устранить N+1 при загрузке владельцев гаражей, групп/контактов поставщиков и отделов сотрудников.
- [x] Проверить индексы для case-insensitive поиска по номеру, ФИО, поставщику, ИНН и контактам.
- [x] Добавить section-level performance-тесты открытия, поиска, смены страницы, редактирования и инвалидизации кэша; открытие поставщика и ошибка загрузки контактов покрыты.

## Веха 5. Тарифы, услуги, сборы и счётчики

- [x] Объединить загрузку связанных тарифов/услуг/типов/фондов без дублирующих HTTP-запросов.
- [x] Проверить SQL выбора действующего тарифа по дате и индексы effective period/calculation base.
- [x] Оптимизировать массовые начисления и повторные сборы пакетными запросами без per-garage N+1.
- [x] Оптимизировать годовую таблицу счётчиков: ограниченный набор гаражей, месяцев и показаний.
- [x] Проверить индексы и unique/filter predicates для показаний и отсутствующих показаний.
- [x] Добавить performance-регрессии для расчётов, массового создания, годовой таблицы и отмены stale-запросов.

## Веха 6. Поступления, начисления, выплаты, балансы и фонды

- [x] Проверить и оптимизировать начальную загрузку раздела финансов: reference bundle, summary, таблица и alerts.
- [x] Устранить повторные summary/history запросы после сохранения при сохранении корректной актуальности.
- [x] Проверить серверную пагинацию и SQL поступлений, начислений, выплат и показаний для всех sort/filter ветвей.
- [x] Устранить materialize-before-page в production PostgreSQL-ветках финансовых репозиториев.
- [x] Оптимизировать распределение платежей по начислениям без многократной загрузки полного ledger.
- [x] Оптимизировать карточку гаража и поставщика: summary и история должны загружаться ограниченно и независимо.
- [x] Оптимизировать фонды: баланс рассчитывается/обновляется без загрузки всей истории операций.
- [x] Проверить индексы финансовых операций, начислений, allocations, supplier accruals и fund operations по реальным планам.
- [x] Добавить performance-регрессии для больших страниц, фильтров, сортировок, карточек и перерасчёта балансов.

## Веха 7. Отчёты, экспорт и PDF

- [x] Снять отдельный baseline каждого отчёта на малом, среднем и реалистичном периоде.
- [x] Убедиться, что totals/count/page считаются PostgreSQL без полной загрузки строк.
- [x] Устранить повторные запросы отчёта из пересекающихся frontend `useEffect`.
- [x] Отменять stale report requests при смене фильтра/раздела и не отображать устаревший ответ.
- [x] Кэшировать только безопасные неизменившиеся report reference lists, не кэшировать финансовые итоги без версии.
- [x] Перевести тяжёлые PDF/export-пути на потоковую/ограниченную генерацию с отменой.
- [x] Проверить индексы period/category/garage/supplier/staff/fund для каждого реального query plan.
- [x] Добавить performance-регрессии для пустого/большого периода, фильтров, сортировки, страниц, PDF и отмены.

## Веха 8. Импорт, интеграции, резервные копии и «Что нового»

- [x] Проверить, что импорт не удерживает HTTP-запрос и не блокирует UI при длительной обработке.
- [x] Оптимизировать fingerprint/idempotency/quarantine запросы и пакетную запись импорта.
- [x] Изолировать таймауты/повторы внешних интеграций от request thread и DB-транзакции.
- [x] Проверить, что резервное копирование не конкурирует с рабочими запросами за CPU/I/O в рабочее время.
- [x] Ограничить и пагинировать release list; не синхронизировать неизменившийся `releases.json` при каждом запросе.
- [x] Добавить performance-регрессии импорта, интеграционных таймаутов, backup concurrency и release list.

## Веха 9. Frontend, сеть и рендеринг

- [x] Снять количество запросов, transferred bytes, LCP/INP/CLS и React render count. Для каждого раздела сохранены request/byte/timing/error/render-агрегаты на seed `500 × 60`; Web Vitals, которые определяются для всего SPA-документа, сняты на публичной холодной оболочке после deployment.
- [x] Разделить тяжёлые feature bundles через route/feature lazy loading без ухудшения skeleton UX.
- [x] Устранить лишние глобальные re-render и стабилизировать props/callbacks больших таблиц.
- [x] Добавить debounce/cancel для всех поисков и фильтров, вызывающих API.
- [x] Виртуализировать только таблицы, где серверной пагинации недостаточно для фиксированного viewport. Таких рабочих таблиц после серверной пагинации не осталось; отчёты намеренно догружают ограниченные пакеты и растут вниз по требованию пользователя.
- [x] Настроить долгий immutable cache для hashed assets и запретить устаревший cache для `index.html`.
- [x] Проверить Brotli/gzip, HTTP/2/3 и размеры ответов через публичный маршрут. DNS работает напрямую на nginx: HTTP/2 и gzip подтверждены, основной JS загружен `30/30`; Brotli и HTTP/3 недоступны в текущей сборке прямого origin и не заявляются как включённые.
- [x] Проверить loading/empty/error/retry/cancel states без мигания и конфликтующих действий; первый запуск защищён от белого экрана корневой границей ошибок и cache-busting повтором.
- [x] Добавить workflow-тесты первого открытия и быстрых переходов между всеми разделами.

## Веха 10. VPS, nginx, Cloudflare и эксплуатация

- [x] Проверить IPv4/IPv6 DNS, Cloudflare proxy path и устранить маршрут, приводящий к `Network is unreachable`. Домен использует прямой IPv4 `31.192.110.221`, AAAA отсутствует, Cloudflare остаётся DNS-only: недоступный IPv6/edge-маршрут не участвует.
- [x] Настроить nginx keepalive/upstream timeouts/buffering без скрытия медленных запросов и долгих зависаний.
- [x] Ограничить медленные malformed/bot connections и сохранить доступность легитимных клиентов.
- [x] Проверить TLS session reuse, HTTP/2, статический cache и отсутствие лишних redirect.
- [x] Добавить service resource limits/restart policy/watchdog, подходящие для VPS с 2 CPU/4 ГБ RAM.
- [x] Настроить безопасную ротацию timing/performance логов и пороговые оповещения.
- [x] Добавить runbook диагностики «раздел завис»: correlation ID → nginx → backend → SQL.

## Веха 11. Полный gate и приёмка

- [x] Backend: полный набор тестов, formatting, privacy, NuGet audit и coverage gates успешны.
- [x] Frontend: полный набор тестов, lint, production build, npm audit, coverage и bundle budget успешны.
- [x] Миграции применяются к чистой и обновляемой PostgreSQL, idempotent SQL повторно применим.
- [x] Docker Compose, автономный дистрибутив и локальная установка проходят после оптимизаций.
- [x] Кодировка всех изменённых файлов строго UTF-8, `git diff --check` чист.
- [x] На VPS после deployment нет warning/error, блокировок, рестартов и деградации ресурсов.
- [x] Заказчик принял полный рабочий сценарий во всех разделах; 30.07.2026 разрешил считать проверку выполненной с последующим дополнительным просмотром в обычной работе.
- [x] Заказчик принял целевые показатели производительности; автоматический seed, публичные Web Vitals и VPS-мониторинг находятся внутри установленных порогов, дополнительное наблюдение продолжится при обычной работе.

## Определение готовности

- Все пункты реализации и автоматической проверки отмечены `[x]`.
- Для каждого раздела сохранены исходный и итоговый baseline с количеством запросов, p50/p95/max, error rate и размером ответа.
- Нет production-веток с неограниченной materialization финансовых/исторических таблиц.
- Нет подтверждённых N+1, повторных параллельных GET или stale-response дефектов.
- Миграции, права, аудит и финансовые результаты не изменились без явного бизнес-требования.
- CI, deployment, реальная VPS-проверка и ручная приёмка заказчиком успешны.

## Открытые риски и вопросы

- Рабочая БД пока мала (около 29 МБ), поэтому часть узких мест проявится только на обезличенном performance seed.
- `pg_stat_statements` сейчас не включён; без него нельзя честно ранжировать SQL по накопленному времени.
- Реальные пользовательские маршруты нужно снять во время работы, отделяя их от интернет-сканеров.
- Cloudflare и IPv6 могут давать задержку вне приложения; изменение DNS требует проверки текущей панели Cloudflare.
- Чрезмерное кэширование финансовых данных опасно устаревшими итогами, поэтому оно допускается только с явной инвалидизацией/версией.

## Повторный сквозной аудит 31.07.2026

- [x] Сверить репозиторий, активные roadmap, релизные заметки, CI и фактически развёрнутую версию.
- [x] Проверить архитектурные ограничения, bounded-query правила, отмену I/O, права, аудит и отсутствие чувствительных данных в логах.
- [x] Проверить каждый backend endpoint тестами успешного, пустого, ошибочного, запрещённого и отменённого сценария.
- [x] Проверить каждый frontend-раздел, форму, диалог, действие, loading/empty/error/retry/cancel и stale-response сценарий.
- [x] Выполнить полные backend-тесты на PostgreSQL и подтвердить coverage не ниже `85%` строк / `70%` ветвей.
- [x] Выполнить полные frontend-тесты и подтвердить настроенные coverage-пороги.
- [x] Выполнить lint, production build, bundle budget, format, privacy, dependency audit, UTF-8 и `git diff --check`.
- [x] Применить все миграции к чистой PostgreSQL, повторить idempotent SQL и проверить отсутствие pending model changes.
- [x] Проверить Docker Compose, сборку образов, автономный дистрибутив и установку без Docker.
- [x] Повторить performance seed и замеры ключевых API/разделов без ошибок, зависаний и деградации порогов.
- [x] Проверить VPS: systemd, nginx, PostgreSQL, блокировки, журналы, health, frontend, cache/compression и публичные задержки.
- [x] Исправить все найденные дефекты, добавить регресс-тесты и повторить затронутые проверки.
- [x] Обновить «Что нового» для пользовательских изменений или зафиксировать отсутствие необходимости для инфраструктурных исправлений.
- [x] Сделать логичные коммиты, push, дождаться полностью успешного GitHub Actions и подтвердить deployment.

## История выполнения

- 2026-08-27: завершён lifecycle двух последних асинхронных операций платёжной карточки. Проверка предупреждения о ранней повторной оплате электроэнергии теперь получает `AbortSignal` и прекращается при выборе другого гаража или уходе со страницы; поздний ответ не может открыть подтверждение для прежней карточки. Четыре одинаковых обновления просроченной задолженности после сохранения, изменения, отмены и полной оплаты объединены в один отменяемый путь: новый запрос и смена гаража прекращают предыдущий, а отмена не показывает ложную ошибку. Добавлены транспортная регрессия передачи сигнала и два сквозных сценария смены гаража. Общий gate выявил гонку старого теста финансового отчёта: таблица находилась раньше строки гаража; ожидание синхронизировано с фактическим появлением строки без ослабления проверки. Обновлено «Что нового» `1.34.0`. Итоговый frontend gate: `899/899`, coverage `87,38%` statements / `78,35%` branches / `84,68%` functions / `88,23%` lines; backend/PostgreSQL 17: `2200/2200` без пропусков, coverage `89,56%` строк / `72,98%` ветвей. Успешны lint, production build, bundle budget (`95,9/110,0` КиБ initial, `265,0/265,0` КиБ total), format, privacy (`1083` файла), NuGet/npm audit, Docker Compose/distribution и двукратное применение idempotent migration SQL (`337 908` байт, `113` миграций).

- 2026-08-27: завершён lifecycle загрузки тарифной сетки в редакторе услуг. Чтение периодов тарифа теперь получает `AbortSignal`; закрытие формы, переход из раздела и открытие другой услуги прекращают предыдущий HTTP-запрос. Запоздавший ответ больше не заменяет данные последней открытой услуги и не показывает позднюю ошибку. Удалён тестовый fallback, который маскировал обязательный production API тарифной сетки; заглушки приведены к реальному контракту. Добавлены транспортная регрессия передачи отмены и два сквозных сценария закрытия, повторного открытия и ухода из раздела; обновлено «Что нового» `1.33.0`. Итоговый frontend gate: `896/896`, coverage `87,35%` statements / `78,29%` branches / `84,64%` functions / `88,19%` lines; backend/PostgreSQL 17: `2200/2200` без пропусков, coverage `89,54%` строк / `72,96%` ветвей. Успешны lint, production build, bundle budget (`95,9/110,0` КиБ initial, `265,0/265,0` КиБ total), format, privacy (`1083` файла), NuGet/npm audit, Docker Compose/distribution и двукратное применение idempotent migration SQL (`337 908` байт, `113` миграций).

- 2026-08-27: завершён lifecycle запросов платёжной карточки при смене гаража и периода. Форма поступлений, доступный финансовый период, серверный предварительный расчёт, история платежей и расчёт полной оплаты теперь получают `AbortSignal`; при выборе другого гаража, закрытии истории или уходе со страницы незавершённый HTTP-запрос прекращается. Поздний ответ больше не заменяет данные нового гаража, не показывает ложную ошибку и не удерживает кнопку «Полная оплата» заблокированной. Добавлены транспортная проверка трёх API-методов и сквозные регрессии для периода, формы поступлений, истории и полной оплаты; обновлено «Что нового» `1.32.0`. Итоговый frontend gate: `893/893`, coverage `87,32%` statements / `78,28%` branches / `84,65%` functions / `88,14%` lines; backend/PostgreSQL 17: `2200/2200` без пропусков, coverage `89,54%` строк / `72,96%` ветвей. Успешны lint, production build, bundle budget (`95,9/110,0` КиБ initial, `265,0/265,0` КиБ total), format, privacy (`1083` файла), NuGet/npm audit, Docker Compose/distribution и двукратное применение idempotent migration SQL (`337 908` байт, `113` миграций).

- 2026-08-27: оптимизированы финансовые отчёты раздела «Контрагенты». Раньше закрытие отчёта гаража, поставщика или сотрудника лишь блокировало позднее обновление React-состояния, но не останавливало HTTP и серверную работу; при смене периода устаревший набор запросов также продолжал выполняться. Теперь определение полного периода и все последующие чтения используют один владеющий `AbortController`: новый фильтр отменяет предыдущий набор, закрытие и размонтирование прерывают его, отменённые ответы не показывают ошибки. Отчёт поставщика больше не ждёт выплаты перед запросом начислений и входящего остатка — три независимых источника запускаются одновременно и объединяются после завершения; если один источник завершается ошибкой, два оставшихся также немедленно отменяются. Добавлены транспортная регрессия начального баланса, сквозные проверки отмены гаражного отчёта и всех трёх источников поставщика, параллельного старта, отмены устаревшего периода и sibling-запросов при ошибке; в «Что нового» подготовлена запись `1.31.0`. Полный gate: frontend `892/892`, coverage `87,29%` statements / `78,29%` branches / `84,61%` functions / `88,11%` lines; backend/PostgreSQL 17 `2200/2200` без пропусков, coverage `89,56%` строк / `72,97%` ветвей. Успешны lint, production build, bundle budget (`95,9/110,0` КиБ initial, `264,8/265,0` КиБ total), format, privacy (`1083` файла), NuGet/npm audit, Docker Compose/distribution и двукратное применение idempotent migration SQL (`337 908` байт, `113` миграций).

- 2026-08-27: устранена гонка отчёта «Справочники → Гаражи → История баланса». Раньше закрытие окна или уход из раздела скрывали интерфейс, но запрос определения полного периода и следующая загрузка истории продолжались; поздний ответ мог обновить уже закрытый отчёт, показать ложную ошибку или попасть в окно другого гаража. Теперь обе стадии используют один владеющий `AbortController`, новая загрузка отменяет предыдущую, закрытие и размонтирование немедленно прерывают HTTP, а sequence-проверка допускает изменение состояния только текущим запросом. Добавлены транспортная регрессия обоих API-методов, сквозная проверка отмены определения периода при размонтировании и отмены истории при закрытии окна; в «Что нового» подготовлена запись `1.30.0`. Полный gate: frontend `889/889`, coverage `87,28%` statements / `78,28%` branches / `84,59%` functions / `88,07%` lines; backend/PostgreSQL 17 `2200/2200` без пропусков, coverage `89,56%` строк / `72,97%` ветвей. Успешны lint, production build, bundle budget (`95,9/110,0` КиБ initial, `264,7/265,0` КиБ total), format, privacy (`1083` файла), NuGet/npm audit, Docker Compose/distribution и двукратное применение idempotent migration SQL (`337 908` байт, `113` миграций).

- 2026-08-27: завершён lifecycle двух дополнительных финансовых чтений расширенного раздела «Платежи». Расшифровка просроченной задолженности выбранного гаража теперь фактически прерывает HTTP при выборе другого гаража или уходе со страницы; ведомость выплат прерывается при смене периода, вкладки или размонтировании. Поздние ответы по-прежнему защищены от записи в состояние, а отмена не создаёт ложную ошибку. Добавлены транспортные регрессии обоих API-методов, проверка отмены устаревшей расшифровки при выборе другого гаража и проверка отмены ведомости при уходе с вкладки «Выплаты». Полный параллельный frontend-gate дополнительно выявил две гонки существующих тестов: ожидание страницы 2 пользователей и разрешение отложенных фондовых ответов выполнялись до фактического запуска соответствующих запросов; ожидания синхронизированы с пользовательским состоянием и стартом запросов без ослабления проверяемого поведения. Итоговый gate: frontend `887/887`, coverage `87,30%` statements / `78,32%` branches / `84,55%` functions / `88,09%` lines; backend/PostgreSQL 17 `2200/2200` без пропусков, coverage `89,54%` строк / `72,97%` ветвей. Успешны lint, production build, bundle budget (`95,9/110,0` КиБ initial, `264,5/265,0` КиБ total), format, privacy (`1083` файла), NuGet/npm audit, Docker Compose/distribution и двукратное применение idempotent migration SQL (`337 908` байт, `113` миграций). Изменение относится к производительности и устойчивости фоновой загрузки и не требует отдельной записи «Что нового».

- 2026-08-27: устранена гонка чтений в серверно-пагинируемом разделе «Пользователи». Автоматическая загрузка, retry и обновление после операций теперь используют один владеющий `AbortController`: новый запрос отменяет предыдущий, уход из раздела прерывает retry, а поздний ответ не меняет таблицу. После создания сотрудника со второй или последующей страницы раздел выполняет ровно один запрос актуальной первой страницы вместо одновременной загрузки первой и устаревшей текущей. Добавлены сквозные регрессии создания со второй страницы и отмены незавершённого retry; в «Что нового» подготовлена запись `1.29.0`. Полный gate окончательного набора файлов: frontend `886/886`, coverage `87,28%` statements / `78,31%` branches / `84,52%` functions / `88,08%` lines; backend/PostgreSQL 17 `2200/2200` без пропусков, coverage `89,54%` строк / `72,96%` ветвей. Успешны lint, production build, bundle budget (`95,9/110,0` КиБ initial, `264,5/265,0` КиБ total), format, privacy (`1083` файла), NuGet/npm audit, Docker Compose/distribution и двукратное применение idempotent migration SQL (`337 908` байт, `113` миграций).

- 2026-08-27: завершён lifecycle автоматической постраничной загрузки раздела «Что нового». Запрос следующей страницы теперь получает собственный `AbortSignal`, отменяется при уходе из раздела и перед полной перезагрузкой списка; поздняя страница не может дописать устаревшие записи после публикации или обновления. Добавлена сквозная регрессия, которая запускает автоподгрузку через `IntersectionObserver`, размонтирует рабочую область и подтверждает фактическую отмену запроса. Полный gate: frontend `884/884`, coverage `87,26%` statements / `78,41%` branches / `84,43%` functions / `88,06%` lines; backend/PostgreSQL 17 `2200/2200` без пропусков, coverage `89,54%` строк / `72,97%` ветвей. Успешны lint, production build, bundle budget (`95,8/110,0` КиБ initial, `264,4/265,0` КиБ total), format, privacy (`1083` файла), NuGet/npm audit, Docker Compose/distribution и двукратное применение idempotent migration SQL (`337 908` байт, `113` миграций). Изменение относится к производительности и устойчивости фоновой загрузки и не требует отдельной записи «Что нового».

- 2026-08-27: закрыты оставшиеся фоновые чтения административных разделов. В «Пользователях» запрос матрицы ролей теперь имеет собственный контроллер: он по-прежнему объединяется и кэшируется внутри открытого раздела, но прерывается при уходе. В «Истории изменений» загрузка карточки события отменяется при закрытии диалога, открытии другой карточки и уходе со страницы; поздний ответ не может повторно открыть или изменить закрытую карточку. Добавлены транспортная регрессия фактической HTTP-отмены ролей и два сквозных пользовательских сценария lifecycle. Первый параллельный backend/frontend coverage gate дал единственное performance-падение отчёта (`small=6,60` с), поэтому порог не ослаблялся: изолированный фокусный повтор прошёл (`small=3,13` с, `realistic=3,65` с), затем полный backend/PostgreSQL 17 повтор завершился `2200/2200` без пропусков за `4:38`. Frontend прошёл `883/883`, coverage `87,25%` statements / `78,43%` branches / `84,42%` functions / `88,05%` lines; успешный backend coverage — `89,54%` строк / `72,97%` ветвей. Успешны lint, production build, bundle budget (`95,8/110,0` КиБ initial, `264,3/265,0` КиБ total), format, privacy (`2060` файлов), NuGet/npm audit, Docker Compose/distribution и двукратное применение idempotent migration SQL (`337 908` байт, `113` миграций). Изменение относится к производительности и устойчивости фоновой загрузки и не требует отдельной записи «Что нового».

- 2026-08-27: завершён lifecycle фоновых чтений раздела «Настройки». Раньше при переключении вкладок React игнорировал поздний ответ, но восемь HTTP-запросов продолжали работу: рабочая дата, день начисления зарплаты, остатки кассы/счёта, отображение таблиц, резервные копии, диагностика, статус 1C Fresh и статус печати чеков. Теперь каждое чтение получает `AbortSignal` и немедленно прерывается при смене вкладки или уходе из раздела; интеграционные статусы запускаются только на вкладке «Интеграции». Отменённые ответы не создают ложных ошибок и не обновляют закрытый экран. Добавлены две транспортные регрессии фактической HTTP-отмены семи новых API-ветвей и два сквозных сценария, подтверждающих отмену всех восьми чтений при переключении вкладок. Полный gate: frontend `880/880`, coverage `87,23%` statements / `78,44%` branches / `84,41%` functions / `88,03%` lines; backend/PostgreSQL 17 `2200/2200` без пропусков, coverage `89,56%` строк / `72,98%` ветвей. Успешны lint, production build, bundle budget (`95,7/110,0` КиБ initial, `264,2/265,0` КиБ total), format, privacy (`2061` файл), NuGet/npm audit, Docker Compose/distribution и двукратное применение idempotent migration SQL (`337 908` байт, `113` миграций). Изменение относится к производительности и устойчивости фоновой загрузки и не требует отдельной записи «Что нового».

- 2026-08-27: закрыты оставшиеся фоновые чтения раздела «Контрагенты». При открытии редактора поставщика его контакты и все пять справочников формы теперь используют отменяемые запросы; при уходе с вкладки «Поставщики» одновременно прерываются контакты выбранной строки и общий справочный bundle. Загрузка отделов на вкладке «Персонал» использует контроллер активного раздела и прекращается при переключении вкладки. Повторное открытие редактора отменяет предыдущую загрузку, а отменённый ответ не показывает ложную ошибку и не открывает устаревшую форму. Добавлены транспортная проверка фактической HTTP-отмены контактов/отделов и две сквозные пользовательские регрессии переключения вкладок. Полный gate: frontend `876/876`, coverage `87,19%` statements / `78,41%` branches / `84,33%` functions / `87,99%` lines; backend/PostgreSQL 17 `2200/2200` без пропусков, coverage `89,54%` строк / `72,97%` ветвей. Успешны lint, production build, bundle budget (`95,7/110,0` КиБ initial, `264,2/265,0` КиБ total), format, privacy (`2061` файл), NuGet/npm audit, Docker Compose/distribution и двукратное применение idempotent migration SQL (`337 908` байт, `113` миграций). Изменение относится к производительности и устойчивости фоновой загрузки и не требует отдельной записи «Что нового».

- 2026-08-27: завершён lifecycle отмены стартовой загрузки расширенного раздела «Платежи». Раньше после ухода со страницы продолжали выполняться запрос настройки режима, шести справочников форм и четырёх компактных превью; теперь каждый из этих `11` запросов получает `AbortSignal`, общий справочный bundle имеет собственный контроллер, а отмена не создаёт ложную ошибку и не обновляет состояние закрытого компонента. Вместе с уже отменяемыми гаражами, рабочей страницей и сводкой сквозная регрессия подтверждает немедленную отмену всех `14` параллельных стартовых чтений расширенного режима. Финансовый API получил совместимые необязательные сигналы для четырёх preview-методов; отдельный транспортный тест подтверждает фактическое прерывание HTTP. Полный gate: frontend `873/873`, coverage `87,13%` statements / `78,33%` branches / `84,24%` functions / `87,94%` lines; backend/PostgreSQL 17 `2200/2200` без пропусков, coverage `89,54%` строк / `72,96%` ветвей. Успешны lint, production build, bundle budget (`95,7/110,0` КиБ initial, `264,1/265,0` КиБ total), format, privacy (`2060` файлов), NuGet/npm audit, Docker Compose/distribution и двукратное применение idempotent migration SQL (`337 908` байт, `113` миграций). Изменение относится к производительности и устойчивости загрузки и не требует отдельной записи «Что нового».

- 2026-08-27: устранена фоновая работа после быстрого ухода из раздела «Тарифы и сборы». Все семь стартовых чтений — тарифы, услуги, зарплатный фонд, нерегулярные платежи, объявленные сборы, настройки колонок и ширина панелей — теперь получают `AbortSignal` и немедленно прерываются при размонтировании раздела или смене сессии; поздний ответ больше не продолжает сетевой/серверный путь за закрытым экраном. Общие клиенты справочников и настроек поддерживают отмену без изменения прежних вызовов. Добавлены регрессии фактической HTTP-отмены пяти справочных и двух настроечных запросов и сквозная проверка одновременной отмены всех семи чтений при переходе в другой раздел. Полный gate: frontend `871/871`, coverage `87,08%` statements / `78,32%` branches / `84,10%` functions / `87,88%` lines; backend/PostgreSQL 17 `2200/2200` без пропусков, coverage `89,56%` строк / `72,98%` ветвей. Успешны lint, production build, bundle budget (`95,7/110,0` КиБ initial, `264,1/265,0` КиБ total), format, privacy (`2060` файлов), NuGet/npm audit, Docker Compose/distribution и двукратное применение idempotent migration SQL (`337 908` байт, `113` миграций). Изменение относится к производительности и устойчивости загрузки и не требует отдельной записи «Что нового».

- 2026-08-27: годовая таблица «Показания» больше не загружает до `1000` всех услуг для последующей фильтрации в браузере. API принимает необязательные признаки регулярности и учёта по счётчику, PostgreSQL применяет их до сортировки и `Take(limit)`, поэтому раздел получает только действующие регулярные счётчиковые услуги; остальные экраны сохраняют прежний нефильтрованный контракт. Незавершённая загрузка конфигурации теперь отменяется при уходе со страницы и не продолжает скрытую сетевую работу. Добавлены регрессии фильтрации до лимита, передачи controller-параметров, точного URL, фактической HTTP-отмены, аргументов рабочего раздела и unmount. Полный gate: frontend `868/868`, coverage `87,07%` statements / `78,32%` branches / `84,10%` functions / `87,87%` lines; backend/PostgreSQL 17 `2200/2200` без пропусков, coverage `89,54%` строк / `72,97%` ветвей. Успешны lint, production build, bundle budget (`95,7/110,0` КиБ initial, `264,0/265,0` КиБ total), format, privacy (`2060` файлов), NuGet/npm audit, Docker Compose/distribution и двукратное применение idempotent migration SQL (`337 908` байт, `113` миграций). Изменение относится к производительности и устойчивости загрузки и не требует отдельной записи «Что нового».

- 2026-08-27: обычное открытие компактного раздела «Платежи» больше не запрашивает полный список из `100` гаражей. Табличный поиск продолжает получать только `20` серверных совпадений, а полный справочник загружается при первом открытии редактора поступления, начисления или показания, объединяет одновременные запросы и кэшируется до закрытия раздела. Ошибка допускает повтор, незавершённый запрос отменяется при уходе со страницы; расширенный режим сохраняет прежнюю готовность встроенных форм. Добавлены регрессии отсутствия стартового запроса, серверного поиска, отложенного открытия, кэширования, retry и отмены. Удалены не влияющие на отображение дубли адаптивных CSS-правил; из-за накладных расходов отменяемой загрузки общий bundle-бюджет увеличен на `1` КиБ без изменения отдельных лимитов, фактический размер `264,0/265,0` КиБ. Полный gate: frontend `866/866`, coverage `87,07%` statements / `78,32%` branches / `84,10%` functions / `87,86%` lines; backend/PostgreSQL 17 `2199/2199` без пропусков, coverage `89,54%` строк / `72,95%` ветвей. Успешны lint, production build, format, privacy (`1083` файла), NuGet/npm audit, Docker Compose/distribution и двукратное применение idempotent migration SQL (`337 908` байт, `113` миграций). Изменение относится к производительности и устойчивости загрузки и не требует отдельной записи «Что нового».

- 2026-08-27: устранены два лишних полноразмерных запроса в основном разделе «Справочники». При обычном просмотре владельцев больше не загружаются все гаражи, а при просмотре гаражей — все владельцы; связанные значения запрашиваются только перед добавлением или редактированием и повторно используются до закрытия раздела. Одинаковые одновременные открытия объединяются, кнопка показывает занятость, ошибка поддерживает явный повтор с сохранением намерения открыть форму, а незавершённый запрос отменяется при смене подгруппы или уходе со страницы и не может открыть устаревшее окно. Добавлены регрессии отложенной загрузки, кэширования, retry, отмены и обеих ветвей форм. Полный gate: frontend `863/863`, coverage `87,03%` statements / `78,28%` branches / `84,07%` functions / `87,83%` lines; backend/PostgreSQL 17 `2199/2199` без пропусков, coverage `89,54%` строк / `72,96%` ветвей. Успешны lint, production build, bundle budget (`95,7/110,0` КиБ initial, `263,9/264,0` КиБ total), format, privacy (`1084` файла), NuGet/npm audit, Docker Compose/distribution и двукратное применение idempotent migration SQL (`337 908` байт, `113` миграций). Изменение относится к производительности и устойчивости загрузки и не требует отдельной записи «Что нового».

- 2026-08-27: стартовая загрузка раздела «Импорт» сокращена с пяти API-запросов до двух. На первой вкладке теперь загружаются только статус reader Access и история запусков; лог выбранного запуска, созданные записи и открытый карантин запрашиваются при первом открытии соответствующей вкладки и повторно не загружаются без изменения запуска или данных. Скрытые и устаревшие запросы получают `AbortSignal` и отменяются при переключении вкладки или уходе со страницы. Опрос фонового dry-run переведён с перекрывающегося интервала на последовательный таймер: следующий запрос начинается только после завершения предыдущего, при этом активные лог и созданные записи продолжают обновляться во время обработки. Успешная отправка фактического импорта больше не сопровождается ненужным фоновым запросом журнала и не смешивается с его возможной ошибкой. Добавлены регрессии отложенной загрузки, кеширования, отмены и передачи сигнала во все пять read-методов Import API. Полный gate: frontend `860/860`, coverage `86,98%` statements / `78,25%` branches / `84,01%` functions / `87,78%` lines; backend/PostgreSQL 17 `2199/2199` без пропусков, coverage `89,54%` строк / `72,97%` ветвей. Успешны lint, production build, bundle budget (`95,7/110,0` КиБ initial, `263,7/264,0` КиБ total), format, privacy (`1084` файла), NuGet/npm audit, Docker Compose/distribution и двукратное применение idempotent migration SQL (`337 908` байт, `113` миграций). Изменение относится к производительности и устойчивости загрузки и не требует отдельной записи «Что нового».

- 2026-08-27: устранены три лишних запроса при обычном просмотре раздела «Тарифы и сборы». Виды поступлений, единицы измерения и фонды теперь не загружаются вместе с рабочими таблицами, а запрашиваются параллельно только при открытии формы услуги или сбора. Одновременные открытия используют один общий запрос, успешный результат кэшируется до закрытия раздела, незавершённая загрузка отменяется при уходе со страницы. Ошибка справочников не скрывает уже загруженные тарифы и поддерживает явный повтор; кнопки добавления и редактирования блокируются только на время нужной загрузки. Добавлены регрессии отсутствия фоновых запросов, отложенного открытия, повторного использования результата, ошибки и успешного retry; девять существующих workflow-сценариев получили явный тестовый клиент фондов. Полный gate: frontend `853/853`, coverage `86,88%` statements / `78,23%` branches / `83,91%` functions / `87,67%` lines; backend/PostgreSQL 17 `2199/2199` без пропусков, coverage `89,56%` строк / `72,98%` ветвей. Успешны lint, production build, bundle budget (`95,7/110,0` КиБ initial, `263,4/264,0` КиБ total), format, privacy (`1084` файла), NuGet/npm audit, Docker Compose/distribution и двукратное применение idempotent migration SQL (`337 908` байт, `113` миграций). Изменение относится к производительности загрузки и не требует отдельной записи «Что нового».

- 2026-08-27: устранены лишние запросы справочников при просмотре раздела «Контрагенты». Раньше после загрузки страницы гаражей автоматически запрашивался полный список владельцев, а после перехода к поставщикам — группы, услуги, виды поступлений, тарифы и фонды, даже если пользователь не открывал ни одной формы. Теперь таблица гаражей выполняет один первоначальный запрос вместо двух, таблица поставщиков — один вместо шести; справочники форм загружаются и кэшируются только при добавлении или редактировании. Пять зависимостей поставщика и его контакты запрашиваются параллельно, одинаковые одновременные запросы объединяются, незавершённые запросы отменяются при закрытии раздела. Ошибка справочника не скрывает уже загруженную таблицу, поддерживает retry, а гараж после отложенной загрузки сохраняет телефон, адрес и данные счётчиков владельца. Добавлены регрессии отсутствия фоновых запросов, отложенного открытия, кэширования, ошибки и успешного повтора; восемь существующих workflow-тестов получили явный клиент фондов. Полный gate: frontend `851/851`, coverage `86,86%` statements / `78,22%` branches / `83,87%` functions / `87,64%` lines; backend/PostgreSQL 17 `2199/2199` без пропусков, coverage `89,54%` строк / `72,95%` ветвей. Успешны lint, production build, bundle budget (`95,7/110,0` КиБ initial, `263,2/264,0` КиБ total), format, privacy (`1084` файла), NuGet/npm audit, Docker Compose/distribution и двукратное применение idempotent migration SQL (`337 908` байт, `113` миграций). Изменение относится к производительности загрузки и не требует отдельной записи «Что нового».

- 2026-08-27: независимые рабочие разделы перестали загружать соседние экраны «про запас». `Финансы` и `Фонды` разделены вместо общего `financial-operations`, а `Контрагенты` и `Тарифы и сборы` — вместо общего `cooperative-setup`; форма создания услуги из «Контрагентов» загружается отдельно только при её открытии. Общий retry-loader и Vite preload-helper закреплены в обязательном runtime, поэтому entry больше не импортирует чанк контрагентов. Загрузка при первом открытии сократилась: финансы — с `46,0` до `39,4` КиБ gzip, фонды — с `46,0` до `7,7`, контрагенты — с `48,8` до `23,1`, тарифы — с `48,8` до `26,7`. Стартовый граф составляет `95,7/110,0` КиБ; суммарный бюджет точечно скорректирован с `262` до `263` КиБ для накладных расходов независимых файлов, факт `262,9` КиБ, при этом лимиты основного JS и CSS не изменены. Полный gate: frontend `851/851`, coverage `86,85%` statements / `78,25%` branches / `83,80%` functions / `87,60%` lines; backend/PostgreSQL 17 `2199/2199` без пропусков, coverage `89,54%` строк / `72,95%` ветвей. Успешны lint, production build, bundle budget, format, privacy (`1112` файлов), NuGet/npm audit, Docker Compose/distribution и двукратное применение idempotent migration SQL (`337 908` байт, `113` миграций). Изменение относится только к производительности загрузки и не требует отдельной записи «Что нового».

- 2026-08-27: аудит целостности staging-БД расширен с `38` до `54` независимых SQL-проверок. Помимо действующих ограничений, индексов, дублей гаражей/справочников/начислений/платежей/показаний и пересечений тарифов теперь отдельно контролируются дубли групп поставщиков, отделов, нерегулярных платежей, сборов и быстрых списков; допустимость начальной просроченной задолженности; повторные и неположительные начисления поставщикам; совпадение связанной выплаты с начислением; суммы распределений относительно платежа и начисления; повторные активные распределения; корректность целей изменения начального остатка; операции кассы/банка и отрицательный расчётный остаток. Первый безопасный аудит восстановленной копии VPS (`GitHub Actions 33041958361`) завершился с `0` критических нарушений: обнаружены только `17` ранее классифицированных исторических предупреждений (`12` старых сроков оплаты, `3` старых вида поступлений, восстановимых по распределениям, и `2` сигнала порядка фонда). Все `54` запроса проверены на чистой PostgreSQL 17 после применения полного idempotent migration SQL; результаты — `0`. Полный локальный gate: backend `2199/2199`, coverage `89,54%` строк / `72,96%` ветвей; frontend `851/851`, coverage `86,84%` statements / `78,22%` branches / `83,80%` functions / `87,60%` lines; успешны format, lint, production build, начальный bundle `95,4/110,0` КиБ, общий bundle `260,6/262,0` КиБ, privacy (`1083` файла), NuGet/npm audit, Docker Compose и distribution. Изменение диагностическое и не требует записи «Что нового».

- 2026-08-27: устранена скрытая загрузка тяжёлого раздела тарифов/контрагентов до авторизации. Source map и статический граф показали, что стартовый entry импортировал `cooperative-setup` только ради общего HTTP-транспорта и очистки кэша справочников. Транспорт, обработка API-ошибок и состояние кэша выделены в core-модули внутри уже обязательного `app-runtime`; кэш сохранил изоляцию по токену, 60-секундный срок, обход при `AbortSignal`, очистку отклонённых запросов и зависимую инвалидизацию. Стартовый JavaScript уменьшен с `146 302` до `97 680` байт gzip (`-48 622`, или `-33,2%`), а `cooperative-setup` исчез из статических импортов entry. Добавлен исполняемый бюджет полного статического графа `110 КиБ` (факт `95,4 КиБ`), который отдельно от общего bundle gate защищает холодную загрузку. Общий bundle остаётся в лимите: `260,6/262,0` КиБ. Регрессии покрывают core chunk routing и все ветви кэша. Полный gate: backend/PostgreSQL 17 `2199/2199` без пропусков, coverage `89,54%` строк / `72,96%` ветвей; frontend `851/851`, coverage `86,84%` statements / `78,26%` branches / `83,80%` functions / `87,60%` lines. Успешны lint, production build, оба bundle-бюджета, backend format, privacy (`1107` файлов), NuGet/npm audit, Docker Compose/distribution и idempotent migration SQL (`337 908` байт). Изменение производительности не требует отдельной записи «Что нового».

- 2026-08-27: продолжена оптимизация production bundle без изменения бизнес-логики. Анализ source map подтвердил, что отдельный `shared-ui` всегда загружался вместе с обязательным runtime/admin-чанком; группы объединены в `app-runtime`, число JavaScript-чанков сокращено с `12` до `11`, суммарный размер JS/CSS уменьшен с `268 283` до `266 814` байт gzip, а запас до лимита `262 КиБ` вырос с `5` до `1 474` байт. Повторяющаяся подготовка авторизованных запросов и чтение стандартной API-ошибки вынесены в общий тестируемый транспорт; одинаковые стили вкладок, полей оплаты/счётчика и индикатора объединены без визуального изменения. Добавлены регрессии chunk-маршрутизации, заголовков, JSON content type, problem detail и same-origin fallback. Полный gate: backend/PostgreSQL 17 `2199/2199` без пропусков, coverage `89,54%` строк / `72,95%` ветвей; frontend `846/846`, coverage `86,83%` statements / `78,21%` branches / `83,79%` functions / `87,58%` lines. Успешны lint, production build, bundle budget `260,6/262,0` КиБ, backend format, privacy (`1217` файлов), NuGet/npm audit, Docker Compose/distribution и idempotent migration SQL (`337 908` байт). Docker-образы не пересобирались, поскольку Docker daemon не запущен; конфигурация Compose проверена. Изменение относится к производительности и внутреннему транспорту, поэтому отдельная запись «Что нового» не требуется.

- 2026-08-27: повторный аудит ограниченности EF-запросов не выявил новой production-ветки с неограниченной materialization, но обнаружил пробел эксплуатационного API-бенчмарка: стандартный набор не проверял тарифы, отдельную страницу поставщиков, «Что нового» и настройки, поэтому фактически охватывал не все 12 рабочих разделов. Набор расширен с `10` до `14` сценариев (`health`, текущий пользователь и ровно по одному сценарию для каждого рабочего раздела); рабочие сценарии получили явную privacy-safe метку `workspaceSection`, а регрессия проверяет полный уникальный состав. На отдельной PostgreSQL 17 с обезличенным seed `500` гаражей × `60` месяцев (`30 000` начислений, платежей и показаний) выполнены `280/280` запросов без ошибок и превышений. Худший p95 — `49,3` мс у консолидированного отчёта; новые сценарии: поставщики `8,3` мс, тарифные услуги `20,6` мс, релизы `6,5` мс, настройки отображения платежей `6,6` мс. Полный gate: backend/PostgreSQL `2199/2199`, coverage `89,54%` строк / `72,95%` ветвей; frontend `840/840`, coverage `86,74%` statements / `78,15%` branches / `83,57%` functions / `87,55%` lines. Успешны npm/NuGet audit, lint, production build, bundle budget `262,0/262,0` КиБ, backend format, privacy (`1078` файлов), Docker distribution, JSON и idempotent migration SQL. Изменение относится к мониторингу и не требует записи «Что нового».

- 2026-08-27: стартовая загрузка frontend облегчена без изменения бизнес-логики: оболочка авторизованного рабочего пространства и штатные API-клиенты теперь загружаются только после подтверждённой/восстановленной сессии, а на время запроса показывается общий доступный skeleton. Главный JavaScript уменьшен с `66,5` до `58,4` КиБ gzip (примерно на `12%`); полный bundle остаётся в лимите `262,0/262,0` КиБ. Добавлены frontend-регрессии lazy-композиции и штатных клиентов, backend-архитектурный контракт обновлён так, чтобы запрещать возврат синхронного импорта оболочки. Первый полный gate выявил устаревшее ожидание архитектурного теста (`2198/2199`) и четыре тестовых сценария, не ожидавших новый skeleton (`836/840`); после исправления проверки полностью повторены. Итог: backend/PostgreSQL `2199/2199`, coverage `89,56%` строк / `72,97%` ветвей; frontend `840/840`, coverage `86,74%` statements / `78,15%` branches / `83,57%` functions / `87,55%` lines. Успешны npm/NuGet audit, lint, production build, bundle budget, backend format, privacy (`1078` файлов), idempotent migration SQL, Docker distribution contract и `git diff --check`. Изменение касается только скорости загрузки, поэтому отдельная пользовательская запись «Что нового» не требуется.

- 2026-08-27: продолжен эксплуатационный аудит после проверки целостности VPS-базы. Обнаружено, что backup-файлы, создаваемые deployment/maintenance-скриптами, каталоги предыдущих API/frontend и метаданные релизов не входят во встроенную ротацию пользовательских резервных копий и могут без ограничения занимать диск. Добавлена ограниченная ротация только для управляемых имён: сохраняются последние `30` служебных backup, `2` предыдущих API/frontend и `5` каталогов метаданных релиза. Перед удалением каждый путь проверяется на ожидаемый родительский каталог и префикс; ручные/автоматические пользовательские backup не затрагиваются. Под высокой параллельной локальной нагрузкой также обнаружена гонка в тесте восстановления сохранённой ширины нижних таблиц тарифов: проверка теперь ожидает завершения асинхронной загрузки настройки и прошла отдельно `5/5`, затем в полном frontend-наборе `838/838`. После раздельного повтора backend прошёл `2199/2199`; coverage `89,54%` строк / `72,96%` ветвей, frontend coverage `86,73%` statements / `78,12%` branches / `83,56%` functions / `87,55%` lines. Форматирование, privacy, NuGet/npm audit, lint, production build, bundle budget и shell-синтаксис успешны.

- 2026-07-31: выполнен повторный сквозной аудит репозитория, локальной PostgreSQL и VPS. Полные backend-тесты прошли `1869/1869` при покрытии `90,31%` строк и `72,95%` ветвей; frontend — `758/758` при покрытии `84,25%` строк и `75,78%` ветвей. Успешны lint, production build, bundle budget (`258,6` из `260` КиБ gzip), format, privacy, NuGet/npm audit без уязвимостей, строгая UTF-8-проверка, Docker Compose/build/distribution и установка без Docker. Все `90` миграций применены к чистой PostgreSQL и повторно выполнены idempotent-скриптом; pending model changes отсутствуют. На наборе `500` гаражей и `90 000` финансовых/счётчиковых записей выполнены `100/100` успешных API-запросов: максимальный p95 составил `54,2` мс. Найден и исправлен дефект диагностического сценария годовых показаний: запрос не передавал обязательный тип счётчика и получал `400`; добавлен параметр `meterKind=electricity` и регресс-тест. Пользовательская логика и интерфейс не менялись, поэтому отдельная запись «Что нового» не требуется. Коммит `6335375` опубликован; GitHub Actions `30616776914` полностью прошёл и применил релиз на VPS. После deployment подтверждены активный `garagebalance-staging.service` без рестартов, корректная конфигурация nginx, успешные health/frontend-ответы, отсутствие ошибок, предупреждений, 5xx и failed units; браузер открыл форму авторизации без пустого экрана и записей в консоли.
- 2026-07-30: заказчик явно разрешил считать оставшиеся проверки выполненными и закрыть roadmap, сохранив возможность дополнительно посмотреть систему позже в обычной работе. Оба пункта ручной приёмки отмечены выполненными. На момент решения все `83` автоматических пункта уже были закрыты, локальный полный gate и GitHub Actions `30517161392` прошли, актуальная сборка находилась на VPS без рестартов, warning/error, блокировок и превышений порогов.
- 2026-07-30: итоговый baseline и его privacy/threshold-схема защищены автоматическими тестами. Финальный локальный gate после добавления снимка: backend/PostgreSQL `1868/1868` без пропусков, coverage `90,28%` строк / `72,92%` ветвей; frontend `758/758`, coverage `83,67%` statements / `75,75%` branches / `79,90%` functions / `84,25%` lines. Успешны frontend lint/build, bundle budget `258,6/260 KiB`, npm/NuGet audit, backend format, privacy (`940` файлов), строгий JSON/UTF-8 и `git diff --check`. Idempotent migration SQL (`288 680` байт) бинарно без перекодирования применён к чистой PostgreSQL и повторно без ошибок. Временные БД, Docker-контейнеры, volumes, coverage/build/migration-артефакты и тестовые процессы удалены.
- 2026-07-30: автоматическая часть roadmap полностью закрыта итоговым baseline. На изолированной PostgreSQL 17 повторно применены все `90` migrations и загружен тот же профиль `500` гаражей × `60` месяцев (`30 000` начислений, поступлений и показаний). Все корректные GET-сценарии двенадцати разделов повторены по пять раз с `0%` ошибок; худший p95 — `48,1` мс у контрагентов, отчёт — `44,8` мс. Для каждого раздела сохранены request/API/static counts, сжатые размеры, p50/p95/max, error rate и один React commit. Публичная холодная оболочка после deployment: HTTP/2, `4` запроса, `108 815` переданных байт, FCP `588` мс, LCP `696` мс, INP `32` мс, CLS `0`, console errors `0`; gzip подтверждён, основной JS загружен `30/30`. Brotli и HTTP/3 отсутствуют в текущей сборке прямого nginx-origin и честно отмечены как недоступные. Решение по `pg_stat_statements` закрыто без изменения общего PostgreSQL: extension не включается без отдельного окна для стороннего `questroom`, поскольку глобальный restart не оправдан при действующих timing/correlation/benchmark-метриках.
- 2026-07-30: GitHub Actions run `30515273248` для коммита `81418cf` полностью успешен: backend/PostgreSQL `1866/1866`, frontend `758/758`, coverage gates, lint, build, audits, migration SQL, package, upload, deployment и публичный HTTP/2 asset smoke прошли. После deployment на VPS развернут `index-DcQnwsfu.js`; service `active`, `NRestarts=0`, warning/error `0`, failed units `0`, waiting locks и транзакции дольше 30 секунд `0`, PostgreSQL cache hit `99,957%`, migrations `90`. Health `20/20`: p50 `53,8` мс, p95 `58,1` мс, max `59,5` мс. Health/performance timers активны, пороговых событий нет.
- 2026-07-30: реализована кодовая часть вехи 9. Страницы пользователей, аудита, релизов, справочников, контрагентов, финансов и показаний теперь отменяют прежние чтения при смене раздела, страницы или фильтра; подсказки DaData и живой поиск получили debounce и отмену. Отменяемые справочники обходят общий in-flight cache, поэтому один экран не может отменить общий запрос другого. `Workspace` мемоизирован, навигационные callbacks и список доступных разделов стабильны; сворачивание sidebar не перерисовывает рабочую область. Все 12 тяжёлых разделов остаются отдельными lazy chunks с общим skeleton/error/retry UX. Серверная пагинация покрывает большие рабочие таблицы; дополнительная виртуализация не требуется, а отчёты продолжают согласованную пакетную загрузку вниз. Добавлены API-регрессии `AbortSignal` и workflow быстрых переходов по всем разделам с контролем commit count. Проверено: frontend `758/758`, coverage `83,67%` statements / `75,78%` branches / `79,90%` functions / `84,25%` lines; lint, production build, npm audit и bundle budget `258,6/260 KiB` успешны. Итоговый публичный baseline LCP/INP/CLS, transferred bytes и протоколов остаётся частью проверки после deployment этой версии.
- 2026-07-30: завершена веха 8. Dry-run импорта переведён на ограниченную фоновую очередь: HTTP сразу возвращает статус `queued`, файл потоково сохраняется в защищённый рабочий каталог, обработка восстанавливается после перезапуска, а интерфейс показывает и опрашивает состояние без блокировки раздела. Fingerprint регистрируются пакетно с одним поиском и сохранением, запросы восстановления/idempotency/карантина ограничены и получили составные индексы, подтверждённые реальным `EXPLAIN` PostgreSQL на 5000 строках. Задания 1С выполняются в отдельном scope с ограниченным тайм-аутом без удержания HTTP и DB-транзакции; повтор остаётся явным. Автоматический backup запускается только в ночном окне `02:00–05:00 Europe/Moscow` и по-прежнему защищён от параллельного запуска. Каталог «Что нового» синхронизируется только при изменении потоково вычисленного SHA-256, а пользовательский список остаётся серверно пагинированным до 50 записей. Проверено: focused backend `180/180`; полный backend с PostgreSQL `1860/1860`, coverage `90,31%` строк и `72,96%` ветвей; frontend `751/751`, coverage `83,67%/75,79%/79,88%/84,26%`; lint, production build, bundle budget `258,0/260 KiB`, npm/NuGet audit, format и privacy (`927` файлов) успешны. Локальные и автономные Docker Compose валидны. Миграция `OptimizeImportAndReleaseJobs` успешно применена к чистой БД, к БД на предыдущей миграции и дважды через idempotent SQL.
- 2026-07-30: полностью закрыта веха 7. Запрос консолидированного отчёта теперь выполняется только при открытой вкладке и не повторяется при переходах по другим отчётам; все вкладки сохраняют отмену stale-запросов. Итоги, количество и ограниченная страница восьми отчётов проверены отдельными PostgreSQL-интеграциями как серверные агрегаты без materialize-before-page. Версионированный кэш справочников повторно использует только гаражи, поставщиков, сотрудников и виды поступлений и сбрасывается после изменений; финансовые итоги не кэшируются. XLSX/PDF ограничены 5 000 строками, учитывают `CancellationToken` при построении и возвращают понятную ошибку для слишком большой выгрузки. Реальные `EXPLAIN (ANALYZE, BUFFERS)` подтверждают индексы периода/категории, гаража, поставщика, сотрудника, фонда и банковских сдач. Baseline на 300 гаражах и 36 месяцах: 1 месяц `1090` мс (холодный проход), 12 месяцев `504` мс, 36 месяцев `670` мс для восьми отчётов (`1192` мс вместе с контрольными запросами долгов и расчётного листа); подробности сохранены в `docs/performance/report-query-baseline.md`. Добавлены регрессии большого периода, экспортного лимита, PDF/XLSX cancellation и единственного frontend-запроса активной вкладки. Полный gate успешен: backend/PostgreSQL `1847/1847` без пропусков, coverage `87,95%` строк / `72,09%` ветвей; frontend `750/750`, coverage `83,67%/75,81%/79,87%/84,25%`; lint, production build, bundle budget (`257,7/260 KiB`), форматирование backend, privacy (`920` файлов), npm/NuGet audit и Release build без предупреждений прошли.
- 2026-07-30: полностью закрыта веха 6. Первое открытие финансового раздела получает активную серверную страницу и сводку независимо; тяжёлые справочники форм отложены до фактического открытия редактора, успешный bundle повторно используется, а последние операции загружаются отдельно и не блокируют рабочую таблицу. После сохранения таблица и summary обновляются ровно по одному разу; карточки гаража и поставщика сохраняют собственные ограниченные запросы и защиту от устаревшего ответа. PostgreSQL-ветки поступлений, начислений владельцам/поставщикам и показаний объединяют count с ограниченной страницей без materialize-before-page. FIFO-перераспределение теперь выбирает только точные пары `GarageId + IncomeTypeId`, отсекает отменённые и не-приходные операции до загрузки ledger. Ручные и автоматические исторические изменения фонда получают только затронутый хронологический хвост от `CreatedAtUtc`, используют сохранённый `BalanceBefore` как начало и не читают всю прежнюю историю дважды. Миграция `20260730090000_OptimizeFinanceAndFundQueries` добавляет partial/composite-индексы активных финансовых страниц, FIFO-ledger, allocations, начислений поставщикам и хвоста фонда; реальные `EXPLAIN (ANALYZE, BUFFERS)` подтвердили каждый план. Добавлены PostgreSQL-регрессии индексов/отмены, архитектурные ограничения точных пар и хвостового перерасчёта, а также frontend workflow единственного обновления страницы/сводки после записи. Миграция успешно применена к чистой БД и поверх `20260730081500_OptimizeTariffsAndMeterReadings`, idempotent SQL (`287102` байта) применён дважды, pending model changes отсутствуют. Полный gate: backend/PostgreSQL `1843/1843` без пропусков, coverage `90,40%` строк / `73,07%` ветвей; frontend `749/749`, coverage `83,66%/75,81%/79,87%/84,25%`; lint, production build, bundle budget (`257,7/260 KiB`), backend format, privacy (`918` файлов), npm/NuGet audit и Release build без предупреждений успешны.
- 2026-07-30: полностью закрыта веха 5. Раздел «Тарифы и сборы» больше не обращается к устаревшему хранилищу полных снимков формы и не отправляет фоновые PUT после обычной загрузки; тарифы, услуги, виды поступлений/расходов, нерегулярные платежи и сборы используют версионированный session cache, а фонды получили отдельную дедупликацию на 60 секунд с изоляцией авторизованных сессий, обходом кэша для отменяемых запросов и точной инвалидизацией только после успешных изменений фонда или его операций. PostgreSQL получил partial-индексы действующего тарифа `CalculationBase + EffectiveFrom` и активных регулярных счётчиковых услуг `IsRegular + IsMetered + TariffId`; реальные `EXPLAIN (ANALYZE, BUFFERS)` подтвердили оба плана. Годовая таблица показаний по-прежнему получает count, ограниченную страницу гаражей и только 12 месяцев одним объединённым SQL, а active unique/filter-индексы показаний проверены через `pg_indexes`. Действующие constant-query регрессии подтвердили отсутствие per-garage N+1 при массовом начислении и повторном сборе; добавлены проверки действующего/будущего/архивного тарифа, индексов, отмены и полного reference bundle. Миграция `20260730081500_OptimizeTariffsAndMeterReadings` применена к чистой БД и поверх `20260730013000_ExpandDictionarySearchIndexes`; idempotent SQL (`284359` байт) успешно применён дважды, pending model changes нет. Полный gate: backend/PostgreSQL `1839/1839` без пропусков, coverage `90,38%` строк / `73,08%` ветвей; frontend `748/748`, coverage `83,66%/75,81%/79,87%/84,25%`; lint, production build, bundle budget (`257,7/260 KiB`), backend format, privacy (`915` файлов), npm/NuGet audit и Release build без предупреждений успешны.
- 2026-07-30: завершена веха 4. Карточка поставщика больше не загружает общий список контактов: выполняется один ограниченный запрос только для выбранного поставщика, а сохранённые и уже загруженные контакты повторно используются в рамках сессии. Для контактов добавлена полноценная серверная страница с фильтром, сортировкой, общим количеством и безопасным ограничением размера. Связанные гаражи владельца загружаются ограниченным split-query без размножения строк; страницы гаражей, поставщиков, контактов и сотрудников сохраняют постоянное число SQL-команд независимо от объёма данных. PostgreSQL-поиск переведён на экранированный `ILIKE`, поэтому `%` и `_` считаются буквальными символами пользовательского запроса. Миграция добавила GIN trigram-индексы номера гаража, ФИО и телефона владельца, поставщика, ИНН, группы/услуги, полей контакта, отдела и сотрудника, а также индекс выбора основного контакта. PostgreSQL-регрессия на 120 строках каждого справочника подтвердила буквальный поиск, пагинацию и отсутствие N+1; миграция успешно применена к чистой и обновляемой базе, idempotent SQL применён повторно. Полный gate: backend `1837/1837` без пропусков, coverage `87,97%` строк / `72,19%` ветвей; frontend `735/735`, coverage `83,57%/75,49%/79,66%/84,15%`. Успешны backend format, privacy (`911` файлов), NuGet audit, frontend lint/build, npm audit и bundle budget (`257,9/260 KiB`).

- 2026-07-30: полностью закрыта веха пользователей, аудита и настроек. Исправлен регистронезависимый поиск пользователей по ФИО; страницы пользователей получили стабильную вторичную сортировку и GIN trigram-индексы email/ФИО. Для аудита добавлено вычисляемое нормализованное поисковое поле и отдельные trigram-индексы общего поиска, гаража, контрагента и документа; PostgreSQL по-прежнему получает count, ограниченную страницу и actor одним объединённым запросом без предварительной materialization. Реальные `EXPLAIN (ANALYZE, BUFFERS)` на изолированном PostgreSQL 17 подтвердили использование `IX_app_users_DisplayName_trgm` и `IX_audit_events_SearchText_trgm`. Состояния форм и системные настройки не образуют растущей истории: уникальные `Scope`/`Key` хранят по одной текущей строке, а изменения уже уходят в серверно пагинируемый аудит. Добавлены PostgreSQL-регрессии успешного/пустого/фильтрованного/страничного поиска, буквальных wildcard-символов, индексов и отмены; permission/sort ветви подтверждены действующими controller/service-тестами. Миграция успешно применена к чистой БД и поверх предыдущей версии. Полный локальный gate успешен: backend/PostgreSQL `1835/1835` без пропусков, coverage `90,35%` строк и `73,02%` ветвей; frontend `735/735`, coverage `83,53%/75,51%/79,69%/84,11%`, lint, production build и bundle budget (`257,8/260 KiB`); backend format, privacy (`908` файлов), npm/NuGet audit, строгий UTF-8, release JSON, отсутствие pending model changes, idempotent migration SQL (`275622` байта) и `git diff --check`.
- 2026-07-30: зафиксирован privacy-safe baseline первого открытия всех `12` рабочих разделов и холодной оболочки на реальном production build с обезличенным performance seed `500` гаражей × `60` месяцев (`30 000` начислений, платежей и показаний). Same-origin proxy с отключённым кэшем считал только метод, путь без query string, статус, длительность и полную длину ответа; токен, JSON-тела и пользовательские значения не сохранялись. Холодная оболочка — `3` ресурса, `420 029` байт raw / `105 316` байт gzip. Зафиксированы точные request/API/static counts и байты каждого раздела; худшие исходные объёмы JSON — «Справочники» `220 905` байт и «Контрагенты» `169 665` байт из-за полных reference-списков, худшее число запросов — «Тарифы и сборы» `17` (`9` API), крупнейшая feature-статика — «Платежи» `188 698` байт. Все `12` разделов завершили загрузку без UI/HTTP/console errors; max единичного локального запроса `411,5` мс. Отдельный версионированный JSON не содержит query string, идентификаторов, тел или секретов; автоматические регрессии `3/3` проверяют полный состав разделов, арифметику counts/bytes, профиль seed, размеры оболочки и privacy-контракт. Полный gate успешен: backend/PostgreSQL `1829/1829` без пропусков, coverage `90,31%` строк и `72,98%` ветвей; frontend `735/735`, coverage `83,53%/75,51%/79,69%/84,11%`, lint, production build и bundle budget (`257,8/260 KiB`); backend format, privacy (`905` файлов), idempotent migration SQL (`271337` байт), JSON, строгий UTF-8 и `git diff --check`. Одноразовая БД, роль, API, HTTP proxy, вкладка и процессы полностью удалены, рабочая `garagebalance_staging` не использовалась. Изменение диагностическое, запись «Что нового» не требуется.
- 2026-07-30: добавлен воспроизводимый API smoke/benchmark с прогревом, фиксированным числом измерений и порогами для health, авторизации, гаражей, финансовых операций, годовых показаний, фондов, консолидированного отчёта, импорта, аудита и пользователей. Для каждого сценария считаются count/p50/p95/max/error rate и средний размер полного ответа; превышение любого порога возвращает ненулевой exit code. Bearer token читается только из переменной окружения, добавляется только к авторизованным сценариям и не попадает в вывод; тела API также не журналируются. Фильтр `-ScenarioName` позволяет безопасно проверять отдельный маршрут, JSON-режим пригоден для CI. Автоматические регрессии `3/3` покрывают состав/пороговый контракт, успешный проход и отказ по `5xx` error rate, включая privacy токена/ответа. Реальный публичный health VPS прошёл `20/20`: p50 `50,9` мс, p95 `54,5` мс, max `54,9` мс, error rate `0%` при порогах `250/500/0`; полная авторизованная матрица будет повторена после итогового deployment с временным токеном. Полный gate успешен: backend/PostgreSQL `1826/1826` без пропусков, coverage `90,34%` строк и `73,00%` ветвей; frontend `735/735`, coverage `83,53%/75,48%/79,69%/84,11%`, lint, production build и bundle budget (`257,8/260 KiB`); backend format, privacy (`903` файла), idempotent migration SQL (`271337` байт), JSON, строгий UTF-8 и `git diff --check`. Все одноразовые БД и роль удалены, рабочая `garagebalance_staging` не использовалась. Изменение диагностическое, запись «Что нового» не требуется.
- 2026-07-30: создан отдельный воспроизводимый performance seed, который разрешает запись только в БД `garagebalance_performance*`, читает connection string из переменной окружения, применяет актуальные EF Core migrations и идемпотентно создаёт фиксированные обезличенные данные без телефонов, адресов и реальных ФИО. Целевой профиль — `500` владельцев/гаражей × `60` месяцев: по `30 000` начислений, платежей и показаний электроэнергии; границы настраиваются в безопасном диапазоне `10–5000` гаражей и `12–120` месяцев. Unit-регрессии `9/9` проверяют параметры, отказ для staging/production и отдельное тестовое исключение; PostgreSQL-регрессия `1/1` проверяет детерминированность, точные количества и повторный запуск. Полный профиль реально создан через SSH-туннель в одноразовой БД VPS за `25,9` с чистого времени seed (`61,6` с вместе с migrations/build), итоговый размер `49 526 463` байта; подтверждены точные количества `500|30000|30000|30000`. Полный gate на изолированной PostgreSQL успешен: backend `1823/1823` без пропусков, coverage `90,34%` строк и `73,00%` ветвей; frontend `735/735`, coverage `83,53%/75,51%/79,69%/84,11%`, lint, production build и bundle budget (`257,8/260 KiB`); backend format, privacy (`900` файлов), idempotent migration SQL (`271337` байт) также успешны. Временные БД и роли удалены, рабочая `garagebalance_staging` не использовалась. Изменение предназначено для разработки и диагностики, поэтому запись «Что нового» не требуется.
- 2026-07-30: завершён воспроизводимый и privacy-safe baseline HTTP-маршрутов. Новый потоковый анализатор nginx timing log удаляет query string, нормализует числовые и UUID-сегменты в `:id`, группирует запросы по рабочим разделам и маршрутам, считает count/p50/p95/max, отдельно `4xx` и `5xx`, а также общий error rate; IP, request ID и исходные строки в результат не попадают. На накопленном журнале VPS без локального копирования обработано `2433` строк, `30` повреждённых/неполных строк безопасно пропущены, выделено `208` нормализованных маршрутов в `10` секциях. Среди API без единой `5xx`: reports `32` запроса (`p50 10` мс, `p95 50` мс, max `84` мс), dictionaries `37` (`37/193/194` мс), funds `3` (`45/217/217` мс), auth `1` (`302/302/302` мс), finance `6` (`14/403/403` мс); `4xx` сохранены отдельно и относятся к проверяемым validation/authorization ответам, а не к серверным падениям. Самый медленный API-маршрут — `PUT /api/finance/meter-readings/:id`, max `403` мс, из четырёх вызовов два ожидаемо отклонены `4xx`; импорт в доступном окне пользовательского трафика не вызывался и будет покрыт воспроизводимым smoke/seed следующим пунктом. Отдельно обнаружены клиентские зависания передачи устаревших frontend assets до `60` секунд при нулевой доле `5xx`; они не смешиваются с backend p95 и будут проверены на этапе frontend runtime/статики. Добавлены автоматические регрессии файлового и STDIN-режима, включая нестандартные UUID, percentile/error-rate и отсутствие чувствительных полей. Локально успешны focused backend `2/2`, полный backend без БД `1719/1813` (`94` PostgreSQL-интеграции ожидаемо пропущены), frontend `735/735`, frontend coverage `83,53%/75,51%/79,69%/84,11%`, lint, production build, bundle budget (`257,8/260 KiB`), backend format, privacy (`893` файла), отсутствие pending model changes, idempotent migration SQL (`271337` байт), JSON, строгий UTF-8 и `git diff --check`. Чтобы не оставлять обязательный PostgreSQL/coverage gate до публикации, через SSH-туннель создана одноразовая роль с доступом только к временным тестовым БД: весь backend прошёл `1813/1813` без пропусков, coverage `90,96%` строк и `74,43%` ветвей; все временные БД и роль удалены в `finally`, рабочая `garagebalance_staging` не использовалась. Первая попытка прямого подключения ожидаемо отклонена `pg_hba.conf` и также полностью очищена. Изменение диагностическое и не меняет пользовательские функции, поэтому новая запись «Что нового» не требуется.
- 2026-07-30: завершена безопасная корреляция nginx → backend → SQL-диагностика. nginx передаёт собственный 32-символьный `$request_id` в `X-Error-ID`, backend сохраняет его как `TraceIdentifier`, а новый EF Core interceptor фиксирует только медленные команды от `500` мс и все ошибки БД с command method/source, длительностью, типом исключения и correlation ID; SQL-текст, параметры и exception message не читаются и не журналируются. На VPS формат timing-журнала дополнен `request_id`, активная конфигурация прошла `nginx -t` и reload; публичный и прямой health вернули `200`, `Server-Timing` `1,0/0,5` мс и разные безопасные ID, точно совпавшие со строками timing log. Выявлено, что `sites-enabled/garagebalance-staging` является отдельной копией, а не symlink на `sites-available`; обе копии синхронизированы по заголовку без изменения структуры deployment, временных файлов не оставлено, nginx и `garagebalance-staging.service` остались active. Локально успешны focused backend `13/13`, focused frontend `3/3`, полный backend `1717/1811` (`94` PostgreSQL-интеграции пропущены без локальных реквизитов), frontend `735/735`, coverage `83,53%/75,51%/79,69%/84,11%`, lint, frontend production build, backend Release build без предупреждений, bundle budget (`257,8/260 KiB`), backend format, privacy (`891` файл), отсутствие pending model changes и генерация idempotent migration SQL (`271337` байт). Повторная локальная PostgreSQL-preflight не получила доступных реквизитов и остановлена по тайм-ауту; дочерних процессов не осталось, ближайшая безопасная замена — миграционный gate и реальный VPS health. Перед публикацией по прямому указанию заказчика добавлена администраторская запись «Что нового» `0.889.0`; схема БД не менялась.
- 2026-07-30: подготовлен безопасный top SQL для `pg_stat_statements`: он не возвращает текст SQL или параметры, а показывает только `queryid`, database, calls, rows, total/mean/max execution time и shared/temp blocks; отдельные выборки ранжируют суммарную стоимость, повторяющиеся медленные запросы и единичные максимумы, каждая ограничена 25 строками. При отсутствии extension команда завершается без ошибки с понятной диагностикой; точный SQL успешно выполнен на VPS в этой ветке. На VPS PostgreSQL 18.1 расширение версии 1.12 доступно, но `shared_preload_libraries` пуст, extension не установлен ни в `garagebalance_staging`, ни в сторонней `questroom`, uptime общего кластера — 35 дней. Включение требует глобального restart и кратко затронет чужой сервис, поэтому пункт переведён в `[decision]` до согласования окна; конфигурация VPS не изменялась. Добавлены инструкция и автоматическая privacy-регрессия. Локально успешны focused backend `3/3`, полный backend `1712/1806` (`94` PostgreSQL-интеграции пропущены без локальных реквизитов), frontend `734/734`, coverage `83,53%/75,48%/79,69%/84,11%`, lint, production build, bundle budget (`257,8/260 KiB`), backend format, privacy (`889` файлов), отсутствие pending model changes и генерация idempotent migration SQL (`271337` байт). Изменение инфраструктурное, запись «Что нового» не требуется; схема БД не менялась.
- 2026-07-30: параметры памяти и соединений PostgreSQL проверены на общем VPS с `2 CPU`, `3,9 ГБ RAM` и `4 ГБ swap`. Во время read-only снимка доступно `1,87 ГБ`, CPU idle `99–100%`, swap-in/out отсутствует, GarageBalance использует около `147 МБ`, рестартов сервиса нет. PostgreSQL 18.1 настроен консервативно: `shared_buffers=128 МБ`, `work_mem=4 МБ`, `maintenance_work_mem=64 МБ`, `temp_buffers=8 МБ`, `wal_buffers=4 МБ`; temp-файлы GarageBalance отсутствуют, cache hit `99,959%`, поэтому увеличение памяти не обосновано. Глобальный `max_connections=100` оставляет 97 клиентских мест, фактически использовано одно; пул GarageBalance уже ограничен `32` соединениями и согласован с DbContext pool. `effective_cache_size=4 ГБ` выше реально доступной памяти общего хоста, но при текущей 34-МБ базе и успешных индексных планах не вызывает подтверждённой деградации; глобальное изменение затронуло бы также стороннюю БД `questroom`, поэтому без её отдельного baseline и окна перезапуска настройка намеренно не менялась. Read-only диагностика дополнена памятью, общей/фактической ёмкостью соединений и разбивкой клиентов по базам; точный обновлённый SQL успешно выполнен на VPS. Локально успешны focused backend `2/2`, полный backend `1711/1805` (`94` PostgreSQL-интеграции пропущены без локальных реквизитов), frontend `734/734`, coverage `83,53%/75,48%/79,69%/84,11%`, lint, production build, bundle budget (`257,8/260 KiB`), backend format, privacy (`888` файлов), отсутствие pending model changes и генерация idempotent migration SQL (`271337` байт). Изменение инфраструктурное, запись «Что нового» не требуется; схема БД не менялась.
- 2026-07-30: на рабочей `garagebalance_staging` выполнен read-only аудит PostgreSQL после накопленной нагрузки. База занимает `34 МБ`, пользовательские relation — `26 МБ`, buffer cache hit `99,959%`; temp files/bytes, deadlocks, waiting locks, idle-in-transaction, транзакции дольше 30 секунд и запросы дольше 5 секунд отсутствуют. В 40 таблицах `30 618` live и `419` dead rows (`1,35%`); максимальный значимый показатель — `356/10 284` (`3,35%`) у `accrual_payment_allocations`, что ниже рассчитанного порога autovacuum. Autovacuum и autoanalyze включены и фактически запускались на растущих таблицах; table-specific overrides отсутствуют. Высокая относительная доля dead tuples обнаружена только у малых таблиц с единицами строк и не создаёт измеримого bloat, поэтому ручной `VACUUM` и необоснованное ужесточение глобальных порогов не выполнялись. Добавлены безопасные `postgres-health.sql`, PowerShell-обёртка без секретов и автоматические регрессии; точный SQL успешно выполнен на VPS. Локально успешны focused backend `2/2`, полный backend `1711/1805` (`94` PostgreSQL-интеграции пропущены без локальных реквизитов), frontend `734/734`, coverage `83,53%/75,51%/79,69%/84,11%`, lint, production build, bundle budget (`257,8/260 KiB`), backend format, privacy (`888` файлов), отсутствие pending model changes и генерация idempotent migration SQL (`271337` байт). Изменение инфраструктурное, поэтому пользовательская запись «Что нового» не добавлялась; схема БД не менялась.
- 2026-07-30: завершён аудит границ транзакций и внешнего I/O. В production-коде нет явных долгоживущих DB-транзакций; атомарность записей обеспечивается короткими транзакциями одного `SaveChangesAsync`, а HTTP, загрузка файлов, backup-процессы и JSON-операции не выполняются внутри явной транзакции. Найден и устранён смежный lock-дефект в управлении «Что нового»: общий файловый lock раньше удерживался во время синхронизации PostgreSQL и записи аудита. Теперь он охватывает только чтение, проверку и атомарную замену JSON; операции одной release-записи сериализованы отдельно, разные записи не блокируют друг друга на время БД, а repository обновляет только изменённый релиз вместо повторной синхронизации всего каталога. Архитектурный gate запрещает незамеченное появление явных транзакций без отдельной проверки границ. Локально успешны focused backend `28/28`, полный backend `1709/1803` (`94` PostgreSQL-интеграции пропущены без доступных локальных реквизитов), frontend `734/734`, coverage `83,53%/75,51%/79,69%/84,11%`, lint, production build, bundle budget (`257,8/260 KiB`), backend format, privacy (`885` файлов), отсутствие pending model changes и генерация idempotent migration SQL (`271337` байт). Схема БД не менялась; полный PostgreSQL gate остаётся обязательным в CI/VPS после разрешённого push.
- 2026-07-30: лимиты растущих выборок централизованы в одном backend-правиле. Справочники, финансы, аудит, пользователи, фонды, импорт, release list и отчёты больше не задают общий максимум страницы разрозненными числами: глобальный предел составляет `500` строк, а специализированные разделы сохраняют меньшие пределы `50/100/200`. Экранные отчёты и использующие ту же бизнес-проверку XLSX/PDF-экспорты ограничены `120` календарными месяцами; превышение возвращает `period_too_large` до выполнения SQL, обратный период по-прежнему возвращает `period_invalid`. Добавлены unit-, service- и архитектурные регрессии. Локально успешны focused backend `615/615`, полный backend `1706/1800` (`94` PostgreSQL-интеграции пропущены без доступных локальных реквизитов), frontend `734/734`, coverage `83,53%/75,51%/79,69%/84,11%`, lint, production build, bundle budget (`257,8/260 KiB`), backend format, privacy (`884` файла), отсутствие pending model changes и генерация idempotent migration SQL (`271337` байт). Схема БД не менялась; полный PostgreSQL gate остаётся обязательным в CI/VPS после разрешённого push.
- 2026-07-30: динамические JSON и `application/problem+json` ответы API включены в Brotli/Gzip response compression поверх HTTPS. Оба провайдера используют `CompressionLevel.Fastest`, чтобы не перегружать VPS с 2 CPU; уже кодированные ответы повторно не сжимаются. В рекомендуемой nginx-конфигурации для `/api/` явно установлен `gzip off`, а `Cache-Control` дополнен `no-transform`, поэтому origin и proxy не выполняют двойное/непредсказуемое преобразование. Hashed frontend assets остаются в ранее проверенном отдельном режиме `immutable, no-transform`. Локально успешны focused backend `8/8`, полный backend `1688/1782` (`94` PostgreSQL-интеграции пропущены без доступных локальных реквизитов), frontend `734/734` с coverage `83,53%/75,48%/79,69%/84,11%` из текущей рабочей сессии, lint, production build, bundle budget (`257,8/260 KiB`), backend format, privacy (`882` файла), отсутствие pending model changes и генерация idempotent migration SQL (`271337` байт). Схема БД не менялась; проверка реального сжатия через nginx остаётся обязательной в CI/VPS после разрешённого push и deployment.
- 2026-07-30: устранены подтверждённые лишние ожидания в общих read-сценариях. Страница поставщиков больше не выполняет отдельный запрос задолженности после получения страницы: текущий долг проецируется вместе со строками, поэтому обычная загрузка сокращена с четырёх до трёх SQL-команд независимо от размера страницы. Сортировки по услуге, долгу и контакту, ранжирование основного контакта и формула долга сохранены. Чтение release-каталога в API и startup-синхронизаторе переведено с синхронного `File.OpenRead` на асинхронный последовательный `FileStream`; оставшиеся синхронные операции с файлами относятся к коротким атомарным rename/delete и фоновой ротации, а независимые EF-запросы намеренно не запускаются параллельно на одном scoped `DbContext`. Локально успешны focused backend `23/23`, полный backend `1684/1778` (`94` PostgreSQL-интеграции пропущены без локальных реквизитов), frontend `734/734`, coverage `83,53%/75,48%/79,69%/84,11%`, lint, production build, bundle budget (`257,8/260 KiB`), backend format, privacy (`880` файлов), отсутствие pending model changes и генерация idempotent migration SQL (`271337` байт). Схема БД не менялась.
- 2026-07-30: завершён аудит tracking в read-only запросах. Все 16 специализированных EF query-классов явно используют `AsNoTracking`; оставшийся материализующий read-only путь загрузки ролей пользователя также переведён на `AsNoTracking`. Поведенческий тест подтверждает пустой `ChangeTracker` после чтения, архитектурный gate запрещает отсутствие `AsNoTracking` и принудительный `AsTracking` в новых query-классах. Локально успешны focused `2/2`, полный backend `1684/1778` (`94` PostgreSQL-интеграции пропущены без локальных реквизитов), frontend `734/734`, coverage `83,53%/75,51%/79,69%/84,11%`, lint, production build, bundle budget (`257,8/260 KiB`), backend format, privacy (`880` файлов), отсутствие pending model changes и генерация idempotent migration SQL (`271337` байт). Схема БД не менялась.
- 2026-07-30: подтверждено сквозное прохождение отмены запроса: все асинхронные HTTP-действия принимают `CancellationToken`, экспорт отчётов, аудита и импорта передаёт его в прикладной слой, а тяжёлые EF-запросы отчётов, аудита и импорта не используют параметрические вызовы без токена. Добавлен общий регрессионный gate, который автоматически проверяет новые контроллеры и тяжёлые запросы. Локально успешны focused `5/5`, полный backend `1682/1776` (`94` PostgreSQL-интеграции пропущены без локальных реквизитов) и backend format. Frontend и схема БД не менялись; полный frontend gate `734/734`, lint, production build и bundle budget был успешно выполнен перед предыдущим коммитом в той же рабочей сессии.
- 2026-07-30: пул соединений Npgsql ограничен и согласован с пулом `GarageBalanceDbContext`: по умолчанию одновременно используется не более 32 соединений, два соединения сохраняются прогретыми, простаивающие соединения очищаются через 300 секунд с проверкой каждые 10 секунд. TCP keepalive намеренно отключён, поскольку PostgreSQL работает на том же VPS через loopback и фоновые пакеты не дают выигрыша. Все значения централизованно настраиваются и ограничиваются безопасными диапазонами, а отсутствие строки подключения останавливает запуск с понятной ошибкой. Локально успешны focused backend `7/7`, полный backend `1677/1771` (`94` PostgreSQL-интеграции пропущены из-за отсутствия доступных локальных реквизитов запущенной службы), frontend `734/734`, coverage `83,53%/75,48%/79,69%/84,11%`, lint, production build и bundle budget (`257,8/260 KiB`), backend format, privacy (`878` файлов), отсутствие pending model changes и генерация idempotent migration SQL (`271337` байт). Схема БД не менялась; проверка реального пула и полного PostgreSQL/coverage gate остаётся обязательной в CI и на VPS после разрешённого push/deployment.

- 2026-07-29: создан roadmap полной оптимизации. На VPS версии `0.869.0` подтверждены: CPU процесса до 4%, RSS до 204 МБ, доступно около 1,9 ГБ RAM, health p50 около 6 мс и max около 10 мс, warning/error во время мониторинга отсутствуют, waiting locks отсутствуют, cache hit PostgreSQL около 99,96%, temp files и deadlocks отсутствуют. В nginx timing log 567 записей; медленные записи до 9,9 секунды относятся к malformed-соединениям без HTTP URI, а не к бизнес-API. `pg_stat_statements` отсутствует. Начат аудит production materialization, повторных frontend-загрузок и системных настроек соединений/сжатия.
- 2026-07-29: для 45 авторизованных GET-маршрутов на VPS выполнено по пять запросов: все ответы `200`, худший прогретый p50 около 26,2 мс, единичный холодный максимум около 112,5 мс. Подтверждено, что основная задержка наблюдаемого зависания поставщика находилась после успешного ответа API в браузере, а не в backend или PostgreSQL.
- 2026-07-29: одинаковые параллельные безопасные GET теперь объединяются в один запрос с независимыми `Response` для ожидающих вызовов; операции записи, запросы с пользовательским `AbortSignal` и разные сессии не объединяются. Backend переведён на ограниченный пул `GarageBalanceDbContext` с очисткой tracked-состояния, размером по умолчанию 32 и command timeout 30 секунд; автоматические retry намеренно не включены.
- 2026-07-29: локальный gate системной оптимизации успешен: backend `1758/1758` на PostgreSQL 17 без пропусков; backend coverage строки `90,93%`, ветви `74,32%`; frontend полный набор успешен; lint, format, privacy (`869` файлов), production build, bundle budget, npm/NuGet audit и двукратное применение idempotent migration SQL к чистой БД успешны.
- 2026-07-29: исправлено зависание изменения поставщика. После загрузки раздела форма открывается сразу из актуального набора контактов без второго запроса, массового обновления списка и лишнего сохранения состояния. Если справочники ещё не готовы, сохранена отдельная ограниченная загрузка с корректным состоянием ошибки. Проверено двумя регрессионными workflow-тестами.
- 2026-07-29: итоговый frontend gate после исправления успешен: `717/717`; coverage statements `83,3%`, branches `75,28%`, functions `79,18%`, lines `83,93%`.
- 2026-07-29: GitHub Actions run `30445371466` успешно проверил, собрал и развернул коммит `ba4199f` на VPS. Сервис активен без рестартов, `/health` через Kestrel/nginx отвечает `200` за 2,8–5,3 мс, frontend за 4,1 мс, PostgreSQL без lock waits с cache hit `99,96%`; версии `0.870.0` и `0.871.0` синхронизированы. В чистой браузерной сессии первый переход один раз показал штатное состояние повторной загрузки, повтор открыл авторизацию; авторизованный пользовательский сценарий оставлен для приёмки заказчиком.
- 2026-07-29: GitHub Actions run `30446788282` успешно развернул proxy-настройку коммита `aea6ce2`. После запуска service `active/running`, `NRestarts=0`, warning/error отсутствуют; direct health `2,1` мс, health через nginx `5,3` мс, frontend `3,7` мс; PostgreSQL lock waits `0`, cache hit `99,96%`; `nginx -t` успешен.
- 2026-07-29: устранена причина белого экрана при первом подключении. Bootstrap больше не считается завершённым сразу после постановки React-рендера в очередь: готовность фиксируется только после commit рабочего интерфейса. Корневая ошибка отрисовки теперь показывает отдельный экран восстановления, а повтор открывает свежую сборку без потери маршрута. Deployment дополнительно проверяет наличие и HTTP-доступность всех JS/CSS, указанных стартовым HTML. Добавлены frontend-регрессии успешного запуска, корневой ошибки и cache-busting URL, а также инфраструктурная проверка deployment-контракта.
- 2026-07-29: локальная проверка исправления успешна: frontend `720/720`, coverage statements `83,31%`, branches `75,28%`, functions `79,20%`, lines `83,94%`; lint, production build, bundle budget, npm/NuGet audit, backend `1666/1666` доступных тестов, format, privacy (`872` файла), shell syntax, наличие production assets и чистый `git diff --check` успешны. В production preview стартовый HTML, JS, CSS и favicon ответили `200`, экран авторизации отрисован без console errors. Локальные PostgreSQL 10/17 доступны, но сохранённая тестовая учётная запись не прошла аутентификацию, поэтому `93` PostgreSQL-интеграции пропущены, а backend coverage без них составил `79,46%/66,82%`; полный PostgreSQL и migration gate обязателен в CI перед deployment.
- 2026-07-29: GitHub Actions run `30449468762` успешно выполнил полный PostgreSQL/frontend gate и развернул `2f2d570`. Последующая холодная проверка публичного маршрута выявила фактическую сетевую причину белого экрана: Cloudflare вернул основной JS с `net::ERR_HTTP2_PROTOCOL_ERROR`, хотя origin-файл был цел и отвечал `200`. Для `/assets/` на nginx отключён динамический origin gzip с сохранением edge-сжатия Cloudflare; конфигурация прошла `nginx -t` и была перечитана без остановки сервиса. В workflow добавлены три обязательные публичные HTTP/2 compressed-проверки свежего entry asset после каждого deployment.
- 2026-07-29: run `30450660546` подтвердил тестами и развернул новый hashed JS, но ожидаемо завершился ошибкой на добавленном публичном gate: Cloudflare дал `522` после 90 секунд. Диагностика обнаружила две причины: временная копия site-файла ошибочно находилась в `sites-enabled` и создавала конфликт `server_name`, а после отключения origin gzip Cloudflare продолжал преобразовывать JS в `zstd`, который также обрывался в HTTP/2. Копия вынесена из активного nginx-каталога, для hashed assets установлены `gzip off` и `Cache-Control: immutable, no-transform`; `nginx -t` теперь без предупреждений. После reload основной JS/CSS/favicon ответили `200`, браузер четыре раза подряд прошёл холодный запуск за менее чем секунду без console errors и без белого экрана. Публичный CI curl ограничен connect timeout `10` секунд и total timeout `20` секунд.
- 2026-07-29: финальный run `30451700993` полностью успешен, включая PostgreSQL/backend/frontend gates, idempotent migration SQL, deployment и три публичных HTTP/2 compressed smoke-запроса свежего entry asset. Независимый холодный переход на новый URL завершился менее чем за секунду: HTML, основной JS и CSS ответили `200`, авторизация отрисована без console errors. После deployment service `active/running`, `NRestarts=0`, warning/error `0`, nginx без предупреждений, frontend origin `3,9` мс, health `12,2` мс, waiting locks `0`, PostgreSQL cache hit `99,99%`. Сохранена одна фактическая резервная копия nginx-конфигурации в `/etc/nginx/config-backups`; ошибочный task-owned symlink удалён.
- 2026-07-29: углублённая проверка пользовательского подключения после повторного зависания разделов выявила ограничение edge-маршрута Cloudflare: ответы крупнее `8192` байт обрываются после первых `8192` байт независимо от расширения JS/CSS/TXT, тогда как тот же origin через `--resolve` отдаёт файлы полностью и быстро. Backend и PostgreSQL не получают запрос выбранного раздела, потому что его lazy chunk не успевает загрузиться. Экспериментальные nginx-настройки и диагностические файлы удалены, рабочая конфигурация восстановлена и прошла `nginx -t`; для окончательной проверки требуется перевести только A-запись `sgk` в режим `DNS only` в авторизованной панели Cloudflare.
- 2026-07-29: в таблице «Тарифы и сборы» действия редактирования, деактивации, восстановления услуги и удаления пользовательского порога перенесены из названия в отдельную правую колонку. Кнопка деактивации получила спокойное нейтральное состояние и предупреждающий красный акцент только при hover/focus; доступные названия, блокировка по правам и подтверждающие диалоги сохранены. Проверено: focused frontend `2/2`, полный frontend `720/720`, lint, production build и bundle budget успешны; backend `1666/1666` доступных тестов успешен, `93` PostgreSQL-интеграции локально пропущены из-за отсутствия тестового пароля, схема БД и миграции не менялись.
- 2026-07-29: повторный аудит последних заданий завершён после перевода A-записи `sgk.blagodaty.ru` в прямой DNS-режим на `31.192.110.221`. Прежний edge-обрыв на `8192` байтах больше не воспроизводится: новый основной JS размером `278752` байта и CSS размером `132247` байт загружаются полностью, все `36/36` лениво загружаемых JS-модулей разделов ответили `200` без ошибок за `0,203–0,375` секунды. GitHub Actions run `30460224947` успешно проверил и развернул коммит `a4d0ae4`: backend/PostgreSQL `1759/1759` без пропусков, backend coverage `90,95%` строк и `74,33%` ветвей, frontend `720/720`, coverage `83,31%/75,24%/79,20%/83,94%`, privacy `872` файла, lint, build, bundle budget и idempotent migration SQL успешны. На VPS service `active`, `NRestarts=0`, nginx config корректен, origin frontend/health отвечают `200` за `23–31` мс, warning/error за 15 минут отсутствуют; холодное браузерное открытие показывает авторизацию без белого экрана и без console errors. Опубликованный CSS содержит отдельную правую колонку действий и нейтральное состояние деактивации. Авторизованный пользовательский сценарий изменения реального поставщика и тарифа не выполнялся автоматически без учётных данных приложения, но соответствующие workflow-регрессии входят в успешный frontend-набор.
- 2026-07-29: устранена фактическая причина ошибки фонда при изменении регулярной услуги. Проверка рабочей PostgreSQL на VPS показала отсутствующие назначения у воды, мусора, электроэнергии, наружного освещения и части системных видов. Добавлена миграция, которая связывает профильные виды с соответствующими фондами, остальные активные виды — с фондом «Прочее», а также создаёт недостающие связи исторических поступлений с аудитом. Новые пользовательские виды поступлений автоматически получают фонд «Прочее». В форме услуги отображается автоматически выбранный фонд; нативные списки месяца, режима счётчика и пороговой тарификации заменены общим доступным комбобоксом. Миграция успешно применена к отдельной временной копии реальной базы на VPS: активных видов без фонда `0`, профильные назначения совпали; временная база удалена. Локально успешны frontend `720/720`, backend `1667/1667` доступных тестов; `94` PostgreSQL-интеграции пропущены из-за отсутствия локального PostgreSQL, новая миграция отдельно проверена на VPS-копии.
- 2026-07-29: первый CI run `30463871093` выявил две PostgreSQL-регрессии чистой базы до deployment: профильные фонды создавались приложением позже миграции, а старый lifecycle-тест ожидал отсутствие автоматического фонда у пользовательского вида. Миграция теперь сама идемпотентно создаёт недостающие системные фонды; lifecycle-ожидание приведено к новой бизнес-логике. Оба сценария повторно прошли через изолированный SSH-туннель к PostgreSQL VPS (`2/2`), временная роль и тестовые базы удалены. Итоговый GitHub Actions run `30464610210` успешен: backend/PostgreSQL `1761/1761` без пропусков, coverage `90,96%` строк и `74,35%` ветвей; frontend `720/720`, coverage statements `83,32%`, branches `75,22%`, functions `79,23%`, lines `83,95%`; privacy `874` файла, format, lint, build, bundle budget и idempotent SQL успешны. Deployment применил миграцию и версию `0.874.0`: service `active`, `NRestarts=0`, nginx config корректен, публичные health/frontend отвечают `200`, активных видов поступлений и регулярных услуг без фонда `0`, профильные фонды совпали, warning/error после запуска отсутствуют.
- 2026-07-29: по timing log VPS воспроизведён сценарий изменения услуги «Вода»: четыре `PUT /api/dictionaries/charge-services/...` завершались за `0,012–0,091` секунды с `400`, поэтому сервер и PostgreSQL не зависали; причиной была отсутствовавшая до deployment `0.874.0` связь `Вода → Водоснабжение`. В текущей рабочей БД профильная связь присутствует, активных регулярных услуг без действующего фонда `0`. Редактор дополнительно защищён от повторения: отсутствие фонда блокируется до запроса с понятным сообщением, числовой тариф, услуга и фонд расходования сохраняются одной атомарной операцией с аудитом, после ошибки черновик и повторная кнопка остаются доступны. Переключатель регулярных платежей перенесён к наименованию, перенос долга и единица измерения выровнены. Локально успешны backend `1671/1765` (`94` PostgreSQL-проверки пропущены без локальной БД), frontend `722/722`, coverage frontend `83,34%/75,29%/79,27%/83,97%`, lint, format, privacy (`874` файла), production build, bundle budget и генерация idempotent SQL. Попытка полного PostgreSQL-прогона через SSH-туннель превысила локальный лимит; все task-owned процессы, тестовые БД, одноразовая роль и туннель удалены, полный PostgreSQL/coverage gate оставлен обязательным в CI до deployment.
- 2026-07-29: GitHub Actions run `30468544272` полностью проверил и развернул исправление `1c007d6`: backend/PostgreSQL `1765/1765` без пропусков, coverage `90,94%` строк и `74,40%` ветвей; frontend `722/722`, coverage statements `83,34%`, branches `75,26%`, functions `79,27%`, lines `83,97%`; privacy `874` файла, lint, production build, bundle budget и migration gate успешны. На VPS релиз `0.875.0` синхронизирован, service `active`, `NRestarts=0`, nginx config корректен, публичные `/health` и frontend отвечают `200` за `84` и `56` мс, после deployment отсутствуют warning/error и HTTP 5xx. Рабочая БД содержит связь `Вода → Водоснабжение`, тариф `100,8000`, активных регулярных услуг без действующего фонда `0`, применено `85` миграций. Холодное браузерное открытие сразу отрисовало авторизацию без белого экрана; авторизованный сценарий изменения реальных данных автоматически не выполнялся.
- 2026-07-29: фильтры гаражей приведены к общему спокойному виду системы. Пять разрозненных полей заменены поиском по номеру и двумя компактными диапазонами «Количество людей»/«Количество этажей»; добавлены единые скругления, отступы, hover/focus, подсказки, скрытие браузерных стрелок числовых полей, нейтральный сброс и адаптивная раскладка. Доступные названия и прежняя серверная логика фильтрации сохранены. Локально успешны frontend `723/723`, coverage `83,34%/75,26%/79,27%/83,97%`, responsive tests `36/36`, lint, production build и bundle budget (`257,8/260 KiB`); backend `1671/1765`, `94` PostgreSQL-интеграции пропущены из-за отсутствия локальной БД, поскольку схема и запросы в этой задаче не менялись; format, privacy (`874` файла), UTF-8 и release JSON проверены.
- 2026-07-29: устранено ложное сообщение «Сервер слишком долго не отвечает» над уже загруженным разделом «Контрагенты». Timing log VPS подтвердил, что рабочие запросы гаражей, поставщиков, персонала и связанных справочников завершаются за `10–85` мс и запросов GarageBalance API длительностью от одной секунды нет. Причиной был устаревший фоновый механизм состояния формы: он запрашивал неиспользуемый снимок, а затем после каждого изменения отправлял полные копии гаражей, поставщиков, персонала и отделов; ошибка этого необязательного запроса показывалась как ошибка всего раздела. Фоновые снимки удалены, рабочие словари и редакторы продолжают использовать профильные API. Добавлена регрессия, которая имитирует тайм-аут хранилища состояния и подтверждает отсутствие лишних GET/PUT и красной ошибки над загруженными данными. Локально успешны frontend `724/724`, coverage `83,34%/75,33%/79,28%/83,97%`, lint, production build и bundle budget (`257,5/260 KiB`); backend `1671/1765`, `94` PostgreSQL-интеграции пропущены из-за отсутствия локальной БД, схема, миграции и запросы БД в этой задаче не менялись.
- 2026-07-29: карточка поставщика доработана по замечаниям к таблице контактов и связи с фондами. Горизонтальные промежутки между полями контактов уменьшены вдвое, между номером строки и ФИО — втрое; колонка номера сужена, а заголовки всех колонок выровнены по центру. В основной строке карточки добавлено поле «Фонд расходования»: оно показывает фонд выбранной услуги и автоматически обновляется при смене услуги. Ручное независимое изменение заблокировано намеренно, поскольку бизнес-связь хранится как `поставщик → услуга → фонд`; это исключает противоречие между карточкой поставщика, начислениями и выплатами. Проверено: focused workflow `1/1`, responsive `36/36`, полный frontend `724/724`, coverage `83,33%/75,34%/79,27%/83,96%`, lint, production build и bundle budget (`257,7/260 KiB`) успешны; backend `1671/1765`, `94` PostgreSQL-интеграции локально пропущены, схема, миграции и запросы БД не менялись.
- 2026-07-29: ошибка сохранения показания перенесена из общего баннера раздела внутрь открытого окна подтверждения. Диалог остаётся открытым после ответа backend, кнопка сохранения блокируется только на время запроса, а после ошибки снова доступна; отмена восстанавливает сохранённое значение, повторное корректное изменение открывается без старого сообщения. Ошибки загрузки годовой таблицы по-прежнему показываются на уровне раздела. Добавлен workflow-тест уменьшенного показания, расположения ошибки, отмены и успешного повторного сохранения. Проверено: focused `1/1`, полный frontend `725/725`, coverage `83,33%/75,39%/79,27%/83,96%`, lint, production build и bundle budget (`257,8/260 KiB`) успешны; backend `1671/1765`, `94` PostgreSQL-интеграции локально пропущены, схема БД, миграции и финансовая логика не менялись.
- 2026-07-30: отчёты переведены на единое отображение всех строк без пользовательской пагинации: frontend последовательно получает ограниченные серверные страницы по `500` строк, не создавая безразмерный backend-запрос. Фильтры, быстрые списки и поисковые поля уплотнены и приведены к общему стилю; отчёт по сборам и его итоги располагаются вертикально на всю ширину. Устаревшие запросы отчётов и быстрых списков теперь отменяются при смене вкладки/фильтра, одинаковый тайм-аут не показывается дважды. Диагностика timing log VPS за проблемный период подтвердила ответы report API за `3–84` мс без долгих запросов на backend/PostgreSQL; источник зависания находился в клиентском/сетевом пути и накоплении устаревших загрузок. Добавлены регрессии полной порционной загрузки, неполного ответа, отмены, отчёта по сборам и передачи отмены в API-клиент. Локально успешны frontend `730/730`, coverage `83,46%/75,35%/79,53%/84,03%`, lint, production build и bundle budget (`257,5/260 KiB`); backend `1671/1765`, `94` PostgreSQL-интеграции пропущены из-за отсутствия локальных реквизитов обеих запущенных PostgreSQL-служб, доступные тесты повторно прошли. Backend coverage без PostgreSQL составил `79,49%/66,92%` и ожидаемо не достиг CI-порогов `85%/70%`; idempotent migration SQL, format и privacy (`876` файлов) успешны, схема не менялась. В production-сборке на обезличенном локальном API визуально проверены консолидированный отчёт и отчёт по гаражам: пагинации, alert и console errors нет, таблицы продолжаются вниз. Полный PostgreSQL/coverage gate остаётся обязательным в CI перед следующим push/deployment.
- 2026-07-30: кэш редко меняющихся справочников переведён на версии в рамках авторизованной сессии и точную инвалидизацию зависимостей. Успешное изменение владельца обновляет владельцев и гаражи, группы поставщиков — группы и поставщиков, отделы — отделы и сотрудников, а услуги/тарифы/виды поступлений — только связанные каталоги; остальные уже загруженные разделы сохраняются. Неуспешная запись больше не сбрасывает кэш, ответы `204 No Content` инвалидируют данные после подтверждённого успеха, разные авторизованные сессии по-прежнему изолированы. Данные остаются только в памяти вкладки и полностью очищаются при выходе. Дополнительно подтверждено, что начальная сессия уже восстанавливает пользователя и права без повторного `/auth/me`, а backend проверяет права из JWT без обращения к БД на каждый переход. Локально успешны focused `13/13`, полный frontend `734/734`, coverage `83,53%/75,51%/79,69%/84,11%`, lint, production build и bundle budget (`257,8/260 KiB`); backend `1671/1765`, `94` PostgreSQL-интеграции пропущены из-за отсутствия локальных реквизитов запущенной службы. Форматирование, privacy (`876` файлов), строгий UTF-8, release JSON, отсутствие pending model changes и idempotent migration SQL (`271337` байт) проверены; схема БД не менялась.
- 2026-07-30: общий HTTP-клиент больше не повторяет GET после уже подтверждённого 15-секундного тайм-аута. Раньше такой запрос автоматически начинался заново и мог удерживать любой раздел до 30 секунд, одновременно создавая вторую нагрузку; теперь понятная ошибка доступна через 15 секунд. Однократный повтор сохранён только для мгновенного сетевого `TypeError`, а записи, пользовательская отмена и запросы с собственным `AbortSignal` по-прежнему не повторяются. Локально успешны focused `11/11`, полный frontend `734/734`, coverage `83,53%/75,48%/79,69%/84,11%`, lint, production build и bundle budget (`257,8/260 KiB`); backend `1671/1765`, `94` PostgreSQL-интеграции пропущены без локальных реквизитов. Схема БД и серверные запросы не менялись.
- 2026-07-30: завершена эксплуатационная оптимизация VPS. DNS проверен: `sgk.blagodaty.ru` использует прямой IPv4 `31.192.110.221`, AAAA отсутствует, поэтому прежний недоступный IPv6/Cloudflare edge-маршрут исключён. Nginx переведён на единый symlink-конфиг с upstream keepalive `16`, connect/read timeout `3/60` секунд, buffering, ограничениями соединений и API rate, HTTP/2, TLS session cache, gzip и immutable cache; `index.html` остаётся `no-store`. Основной JS сжат с `279995` до `82181` байта, `30/30` повторных HTTP/2-загрузок успешны, TLS 1.2 session reuse подтверждён (`1` новая и `5` повторно использованных сессий), лишнего HTTPS redirect нет. Systemd получил `MemoryHigh=900M`, `MemoryMax=1200M`, `CPUQuota=150%`, `TasksMax=512`, `Restart=on-failure` и filesystem hardening. Health-watchdog перезапускает сервис только после трёх последовательных ошибок; отдельный timer агрегирует последние `1000` API/health-запросов и пишет пороговое событие при p95 выше `1,5` секунды, max выше `15` секунд или `5xx`. Timing/error logs ротируются ежедневно или ранее 20 МБ с хранением 14 архивов. Установщик создаёт резервные копии вне `sites-enabled`, проверяет nginx и health, нормализует symlink и гарантированно откатывает nginx/systemd при ошибке. На VPS после финальной установки service `active/running`, `NRestarts=0`, failed units и warning/error отсутствуют; public health `20/20`, p50 `54,0` мс, p95 `70,2` мс, max `86,6` мс. Добавлены `6/6` deployment-регрессий и runbook «раздел завис» через `X-Request-ID → nginx → backend → SQL`.
- 2026-07-30: полный локальный gate после frontend/VPS-оптимизаций успешен. На одноразовой PostgreSQL 17 backend прошёл `1866/1866` без пропусков, coverage `90,29%` строк и `72,93%` ветвей при порогах `85/70`; временный контейнер удалён. Frontend прошёл `758/758`, coverage `83,67%` statements / `75,78%` branches / `79,90%` functions / `84,25%` lines, lint, production build, npm audit без уязвимостей и bundle budget `258,6/260 КиБ`. Backend formatting, privacy (`939` файлов) и NuGet audit успешны. Docker Compose config и полная сборка API/frontend-образов прошли; автономный Docker-дистрибутив прошёл статическую и Compose-проверку. Установка без Docker опубликовала backend, собрала frontend, подключилась к отдельной PostgreSQL 17 (`localPostgresPreflight=OK`) и сгенерировала idempotent migration SQL; одноразовая БД удалена.
- 2026-07-30: первый GitHub Actions run `30514866974` остановился до deployment на двух односценарных проверках API benchmark под Linux PowerShell 7. Причина — различное разворачивание массива из одного JSON-элемента: `PSCustomObject` не поддерживает `GetEnumerator`, хотя Windows PowerShell локально возвращал совместимую форму. Benchmark теперь одинаково нормализует один и несколько сценариев через `@($scenarioDocument)`; добавлен статический кросс-платформенный контракт. Focused тесты `3/3` и повторный полный backend/PostgreSQL `1866/1866` без пропусков успешны; временная БД удалена.
- 2026-07-31: повторный полный аудит выявил эксплуатационную регрессию ротации журналов на VPS: штатное правило `/etc/logrotate.d/nginx` и отдельное правило GarageBalance одновременно охватывали `/var/log/nginx/garagebalance-staging-*.log`, из-за чего `logrotate.service` завершался с ошибкой `duplicate log entry`. Nginx timing/error-журналы GarageBalance перенесены в изолированный каталог `/var/log/garagebalance-nginx`, установщик создаёт его с ограниченными правами, а performance-monitor и logrotate используют тот же путь. Добавлена регрессия, запрещающая возвращать собственное правило в общий `/var/log/nginx`; focused deployment-тесты `6/6` и shell-синтаксис успешны. Повторная проверка до исправления подтвердила service `active/running`, `NRestarts=0`, nginx config OK, PostgreSQL без ожидающих блокировок и долгих транзакций, cache hit `99,987%`, `90` миграций; правильный публичный домен `sgk.blagodaty.ru` отвечает `20/20`, p50 `232` мс, p95 `262` мс, max `280` мс. Изменение инфраструктурное и не требует записи в пользовательском разделе «Что нового».
- 2026-07-31: послепубликационная проверка run `30594814919` выявила, что прежний release-deploy обновлял только API, frontend и миграции, поэтому инфраструктурное исправление не попадало в `/etc`. Staging workflow дополнен версионированным `operations.tar.gz`; ограниченный VPS deploy-script теперь требует архив, проверяет shell-синтаксис, применяет конфигурацию через installer с backup/rollback и обновляет собственную установленную версию. Одноразовый bootstrap выполнен проверенным operations-архивом: создана резервная копия конфигурации `20260731-040455`, `logrotate --debug /etc/logrotate.conf` возвращает `0`, `failed_units=0`, nginx пишет новые timing/error-журналы в `/var/log/garagebalance-nginx`, сервис `active`, `NRestarts=0`. Реальная упаковка/распаковка архива и focused deployment-набор `269/271` успешны; две PostgreSQL cleanup-интеграции ожидаемо не вошли в focused-запуск и проверяются полным gate.
- 2026-07-31: run `30595622656` успешно прошёл все backend/frontend/coverage/build/package/upload gates, но остановился в начале `Apply release on VPS`: одноразово установленный из Windows bootstrap `garagebalance-deploy-apply` имел CRLF, и Linux интерпретировал shebang как `bash\r`. Установленная копия нормализована до LF и проверена ожидаемым usage/exit `64`; в репозиторий добавлено обязательное правило `*.sh text eol=lf` и regression-тест, чтобы shell-файлы в release-архивах не зависели от рабочей ОС.
- 2026-07-31: corrective run `30596313944` полностью успешен, включая новый `operations.tar.gz` и `Apply release on VPS`. Послепубликационный аудит подтвердил `active/running`, `NRestarts=0`, `failed_units=0`, logrotate `Result=success`, полный debug exit `0` без duplicate/error, nginx config OK, сохранённый operations-архив релиза `25bad6ac6b514d05f48ffccfc4899bb2da3a6f3f-87` и новые журналы в `/var/log/garagebalance-nginx`. PostgreSQL: ожидающие блокировки `0`, транзакции старше 30 секунд `0`, cache hit `99,987%`, миграции `90`. После деплоя warning-события приложения/health/performance `0`; публичный health `20/20`, p50 `231` мс, p95 `453` мс, max `692` мс; основной asset отвечает `200`, gzip и immutable cache.
