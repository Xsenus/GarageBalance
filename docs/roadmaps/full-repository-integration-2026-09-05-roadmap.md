# Полная интеграция репозитория — 05.09.2026

## Источники

- Запрос заказчика на объединение всех актуальных рабочих веток, полную локальную проверку, публикацию основной ветки, ожидание CI/CD, проверку VPS и удаление только полностью интегрированных веток.
- Текущее состояние локального Git-репозитория, `origin`, GitHub pull requests и GitHub Actions.
- `AGENTS.md`, действующая документация запуска, тестирования, Docker и VPS.
- Результаты независимой проверки чистой базы и визуальной приёмки из ветки `codex/full-system-clean-db-audit`.

## Зафиксированное исходное состояние

- Основная ветка репозитория: `master`.
- `origin/master`: `8a8582fbaae2cbfab091fd5883961a40e71b8ed8`.
- Локальная `master`: `a646fe20b3a52561cd4d0d562b89611cc5bbf562` (отставала от `origin/master` на 18 коммитов).
- `codex/full-system-clean-db-audit`: `dbb39797fbf5857ecf2c111132ce18ed644d80d6` (один уникальный визуальный коммит относительно `origin/master`).
- `codex/ci-test-performance`: `7787257a0e9ad6975807dd54f62bda039101db6a` (уникальных коммитов относительно `origin/master` нет; удалённая ветка уже отсутствовала).
- Открытых и недавно объединённых pull request через GitHub API не найдено; ветка `master` не защищена.

## Предположения и решения

- Коммиты чистой приёмки и ускорения тестов/Docker уже достижимы из `origin/master`; повторное слияние этих изменений не выполняется.
- Рабочая ветка сначала актуализируется относительно `origin/master`, затем полностью проверяется и объединяется в `master` штатным merge-коммитом.
- Удалённый в `origin/master` завершённый roadmap чистой приёмки не восстанавливается; история текущей работы хранится здесь до завершения интеграции.
- Дублирующая версия release note `1.182.16` визуального исправления заменяется на следующую свободную `1.182.19`.
- Production-данные не используются для миграционных или разрушительных проверок; создаются отдельные локальные базы.
- Завершённые рабочие ветки удаляются только после успешного CI/CD и подтверждения, что их вершины достижимы из опубликованной `master`.

## Этап 1. Инвентаризация и безопасное объединение

- [x] Проверить чистоту рабочего дерева, активную ветку, remotes и default branch.
- [x] Выполнить `fetch --all --prune` и зафиксировать хэши локальных и удалённых веток.
- [x] Проверить открытые pull request и назначение постоянных веток.
- [x] Сравнить рабочие ветки с `origin/master` по истории, patch-id и составу файлов.
- [~] Актуализировать `codex/full-system-clean-db-audit` относительно `origin/master` и осмысленно разрешить конфликты.
- [ ] Проверить итоговый diff на дубли, потерянные миграции, API-контракты, UI и документацию.
- [ ] Объединить проверенную рабочую ветку с локальной `master` без переписывания истории.

## Этап 2. Backend и база данных

- [ ] Восстановить зависимости и выполнить полный backend test gate с PostgreSQL без пропусков.
- [ ] Проверить backend coverage и установленные пороги.
- [ ] Выполнить Release build без ошибок и предупреждений.
- [ ] Выполнить `dotnet format`, privacy/security и NuGet audit.
- [ ] Проверить отсутствие pending EF model changes.
- [ ] Создать чистую PostgreSQL-базу и применить все миграции в правильном порядке.
- [ ] Повторно применить idempotent migration SQL и сверить число миграций/таблиц.
- [ ] Запустить Production API на тестовой базе и проверить health, авторизацию, фоновые процессы и серверные логи.

## Этап 3. Frontend и пользовательские сценарии

- [ ] Установить frontend-зависимости из lock-файла.
- [ ] Выполнить полный frontend test gate с покрытием.
- [ ] Выполнить lint, production build и bundle budget.
- [ ] Запустить приложение и провести браузерный smoke основных разделов.
- [ ] Проверить адаптивность, русский текст, консоль браузера и взаимодействие объединённых функций.

## Этап 4. Docker, документация и публикация

- [ ] Проверить Docker Compose, Docker-дистрибутив и сценарий первого запуска с тарифами, платежами и администратором.
- [ ] Выполнить фактическую Docker-сборку/запуск, если Engine доступен; иначе явно зафиксировать ограничение.
- [ ] Сверить README, инструкции, переменные окружения, release notes и активные roadmap с кодом.
- [ ] Проверить UTF-8, локальные Markdown-ссылки, JSON и `git diff --check`.
- [ ] Обновить и отправить `master` без force push.
- [ ] Дождаться всех GitHub Actions и исправить каждый сбой до зелёного состояния.
- [ ] Проверить автодеплой, systemd/nginx, health и основные сценарии на VPS.

## Этап 5. Завершение и очистка

- [ ] Убедиться, что вершины завершённых рабочих веток достижимы из `master`.
- [ ] Удалить только полностью интегрированные локальные и удалённые рабочие ветки.
- [ ] Выполнить повторный `fetch --prune` и проверить итоговый список веток.
- [ ] Остановить task-owned процессы и удалить только временные базы, логи, coverage, build и кэши.
- [ ] Проверить чистое рабочее дерево и совпадение локальной/удалённой `master`.
- [ ] Перенести значимую историю в release notes/коммиты и удалить этот завершённый roadmap.

## Definition of done

- Все уникальные актуальные изменения достижимы из опубликованной `master`, без дублирования и потери функциональности.
- Полные backend/frontend gates, миграции, production build и доступные Docker-проверки успешно завершены.
- GitHub Actions и автодеплой зелёные; опубликованное приложение отвечает и прошло smoke-проверку.
- Завершённые рабочие ветки безопасно удалены, временные данные и процессы очищены, Git чист и синхронизирован.

## Открытые риски и вопросы

- Доступность локального Docker Engine ещё не подтверждена.
- Доступность GitHub Actions secrets и VPS проверяется только фактическим запуском workflow.
- Удаление веток допускается только после успешной публикации и проверки достижимости их последних коммитов.

## История выполнения

- 05.09.2026 — выполнена первичная инвентаризация. `origin/master` уже содержит merge-коммиты независимой проверки чистой базы и ускорения тестов/Docker; единственным уникальным изменением остался визуальный коммит `dbb39797`. Рабочее дерево до интеграции было чистым.
- 05.09.2026 — начато обновление рабочей ветки из `origin/master`. Единственный содержательный конфликт — удалённый в основной ветке завершённый roadmap чистой приёмки; принято решение сохранить удаление и вести текущую интеграцию в отдельном активном roadmap.
