# Исправления по комментариям заказчика от 02.09.2026

## Источники

- Документ заказчика `Комментарии_020926.docx`: 10 содержательных замечаний в разделах «Тарифы и сборы», «Платежи», «Фонды» и общее требование по редактированию транзакций.
- Расчёт заказчика `Дата смены тарифа.xlsx`, лист `Лист1`, диапазон `A1:E10`: смена тарифа 02.09.2026, 1 день по прежним ставкам и 29 дней по новым ставкам, итог 17 755,00 руб.
- Текущий `master`, commit `cefc7ff5`, существующие backend/frontend/PostgreSQL-тесты и завершённая дорожная карта комментариев от 26.08.2026.
- Результаты финансового аудита от 02.09.2026: подтверждённая ошибка фиксации разрешённого отрицательного остатка фонда и риск конкурентного назначения штрафов сотруднику.

## Разбор требований

1. Комментарий всё ещё ошибочно требуется при изменении/удалении порога электрического тарифа.
2. В разделе тарифов и сборов требуется единая типографика по образцу нерегулярных платежей и удаление постоянных нижних пояснений в формах сборов.
3. Дата смены тарифа должна сохраняться, редактироваться и участвовать в расчёте начисления.
4. Для гаража 103 требуется объяснить и исправить появление просрочки по членским и целевым взносам раньше корректного срока.
5. Общие итоги финансов не совпадают с суммой итогов отдельных месяцев.
6. По сотруднику Петрову не совпадают задолженность в выплатах и сумма финансового отчёта.
7. Задолженность перед сотрудником должна возникать с даты добавления сотрудника в систему.
8. Финансовые отчёты должны учитывать все относящиеся к сотруднику премии и штрафы.
9. Итог денег в фондах должен согласовываться с кассой и банковским счётом.
10. Все поддерживаемые операции с контрагентами должны быть видны в едином журнале и безопасно редактироваться с сохранением аудита.

## Допущения и зафиксированные решения

- Новый расчёт из Excel заменяет прежнее правило простого среднего ставок, зафиксированное 26.08.2026. Для смены 02.09.2026 прежний тариф действует по 01.09.2026 включительно, новый — с 02.09.2026 включительно.
- Электроэнергия рассчитывается прогрессивно по диапазонам потребления: в примере 1 100 кВт·ч по первой ступени, следующие 600 кВт·ч по второй и следующие 300 кВт·ч по третьей.
- Для каждой ступени ставка месяца взвешивается по календарным дням действия версии тарифа. Контрольный пример: `7,5 × 1/30 + 10 × 29/30 = 9,916666…`; сумма второй ступени `600 × 9,916666… = 5 950,00` руб.
- Контрольный итог Excel должен воспроизводиться без ручной корректировки: `8 250 + 5 950 + 3 555 = 17 755,00` руб.
- Существующий редактор тарифных периодов и его исправление в `cefc7ff5` сначала повторно проверяются; уже работающая часть не реализуется заново.
- Обычное изменение конфигурации тарифа может сохраняться без комментария, если общая настройка обязательности комментариев выключена; аудит до/после создаётся независимо от текста комментария.
- Реальные финансовые записи не удаляются «для очистки» вслепую. Сначала выполняются резервная копия, диагностический запрос и точное определение ошибочных записей; исправление данных должно быть повторяемым и проверяемым.
- Редактирование финансовой операции не переписывает историю: сохраняются старые и новые значения, исполнитель, время и причина, а производные остатки и распределения пересчитываются транзакционно.
- Закрытые, импортированные или автоматически созданные операции редактируются только через безопасный поддерживаемый сценарий; если прямое изменение нарушает бухгалтерский след, интерфейс должен предлагать корректировку/сторно.
- Для первого неполного месяца сотрудника сохраняется действующее и единое для ведомости и отчётов правило: полная месячная ставка начиная с месяца добавления. Изменение на пропорцию календарных дней возможно только отдельным новым бизнес-решением.
- Пользовательская сумма «деньги в фондах» включает именованные фонды и строку нераспределённых средств; именно этот состав сверяется с кассой и банковским счётом.
- Общий gzip-лимит production JS/CSS установлен в 278 КиБ для нового журнала и безопасного перерасчёта; отдельные строгие лимиты initial JS, крупнейшего JS и CSS не изменены.

## Этап 0. Фиксация исходного состояния и защита данных

- [x] Полностью извлечь текст DOCX, проверить отсутствие скрытых комментариев, изображений и исправлений Word.
- [x] Проверить весь XLSX: значения, формулы, дату, итог, отсутствие ошибок формул и визуальное представление листа.
- [x] Сопоставить 10 замечаний с текущими модулями, API и существующими тестами; зафиксировать конфликт нового расчёта с прежним простым средним.
- [x] Воспроизвести каждый пункт на актуальном `master` и опубликованном стенде, не создавая и не изменяя реальные финансовые записи.
- [acceptance] Зафиксировать обезличенные контрольные данные и ожидаемые бухгалтерские равенства для гаража 103 и сотрудника Петрова. Серверный аудит подтвердил сотрудника и целостность его историй, но не нашёл ровно одну активную запись гаража с номером `103`; заказчик должен указать нужную запись или источник данных.
- [x] Перед диагностикой/коррекцией рабочей БД создать и проверить резервную копию, сохранить только обезличенные агрегаты и идентификаторы проверки.
- [x] Составить таблицу «замечание → причина → затронутые данные → код → тест → приёмка» и обновлять её по мере выполнения.

### Матрица прослеживаемости

| № | Замечание | Причина | Данные | Реализация | Автоматическая проверка | Приёмка |
|---|---|---|---|---|---|---|
| 1 | Лишнее требование комментария | Локальная валидация расходилась с настройкой аудита | Пороги тарифа | Единая политика обязательности основания | Валидация, контроллер, аудит | Настройка включена/выключена |
| 2 | Разная типографика и постоянные подсказки | Локальные стили форм | Нет | Общие формы и help-tooltip | Компоненты, клавиатура, narrow-screen | Раздел «Тарифы и сборы» |
| 3 | Дата смены тарифа не отражалась в сумме | Усреднение версий без календарных весов и непрогрессивная ступень | Тарифные периоды, снимки начислений | Версия снимка 4, дневное взвешивание, прогрессивные ступени | Excel 17 755,00 и граничная матрица | Формула и даты на стенде |
| 4 | Ранняя просрочка гаража 103 | Исторический срок требовал отделения снимка от текущей настройки | Начисления, сроки, распределения | Снимки DueDate/OverdueFrom и отчёт сверки | Синтетическая регрессия гаража 103, аудит БД | Обезличенная копия рабочих данных |
| 5 | Общий итог не равен сумме месяцев | Разные выборки и повторный учёт переносов | Начальные остатки, начисления, платежи | Одна серверная модель знаков и периода | Контрольные итоги и границы периода | Помесячная сверка на копии |
| 6 | Долг Петрова расходился с отчётом | Текущая ставка использовалась вместо истории | Ставки, занятость, выплаты | История ставки/занятости и общая модель долга | Ведомость, отчёт, PostgreSQL-сценарии | Обезличенная сверка Петрова |
| 7 | Долг до даты добавления | Не учитывалась граница занятости | Периоды сотрудника | Начисление с месяца добавления | До/после добавления, архив/восстановление | Подтверждение полной ставки первого месяца |
| 8 | Не все премии/штрафы в отчёте | Корректировки не входили во все остатки | Зарплатные корректировки | Версионированные изменение/отмена/восстановление | Success/conflict/cancel/restore/concurrency | Сверка ведомости и отчёта |
| 9 | Фонды не равны кассе и банку | Хвост цепочки и разрешённый минус считались по неполному состоянию | Фонды, нераспределённые средства, касса, банк | Полный tail replay и bounded reconciliation | Инвариант всех видов движений | Восстановленная копия БД |
| 10 | Нет общего журнала и безопасного редактирования | Операции были разделены по экранам | Семь классов финансовых записей | Серверный журнал, общие меню, существующие команды исправления и сторно | API/контроллер/компонент/доступность/PG performance | Фильтры и действия на стенде |

## Этап 1. Комментарии и интерфейс тарифов/сборов

- [x] Воспроизвести удаление и изменение порога электричества при включённой и выключенной настройке обязательности комментариев.
- [x] Устранить локальную frontend/backend-валидацию, которая требует комментарий вопреки выключенной настройке.
- [x] Сохранить обязательное основание только для действительно опасных операций, определённых политикой аудита.
- [x] Проверить сохранение аудита до/после при пустом необязательном комментарии.
- [x] Провести инвентаризацию шрифтов, размеров, межстрочных интервалов и подсказок во всех блоках «Тарифы и сборы».
- [x] Привести типографику к визуальному образцу нерегулярных платежей без локальных исключений CSS.
- [x] Убрать постоянные нижние инструкции в формах сборов; перенести полезные пояснения в общий доступный `FormField` help-tooltip.
- [x] Проверить narrow-screen, keyboard focus, screen-reader labels, disabled/error/loading состояния и отсутствие скачков компоновки.

## Этап 2. Тарифные периоды и расчёт по датам

- [acceptance] Повторно проверить на текущем стенде создание, просмотр и редактирование дат начала/окончания тарифных периодов. Автоматические сценарии и стенд зелёные; ручной проход после авторизации оставлен заказчику, реальные настройки не изменялись.
- [x] Зафиксировать серверный контракт включительных границ `EffectiveFrom`/`EffectiveTo` и запрет пересечений периодов.
- [x] Обеспечить редактирование даты смены тарифа с optimistic concurrency и понятной ошибкой при конфликте.
- [x] Заменить простое среднее ставок на взвешивание по числу календарных дней действия каждой версии в расчётном месяце.
- [x] Реализовать прогрессивное разбиение месячного потребления электричества по ступеням вместо применения одной ставки ко всему объёму.
- [x] При нескольких сменах тарифа внутри месяца рассчитывать ставку каждой ступени как сумму `ставка × дни / количество дней месяца`.
- [x] Сохранять в снимке начисления границы ступеней, объёмы, ставки до/после, дни действия, промежуточные суммы и правило округления.
- [acceptance] Показывать бухгалтеру понятную расшифровку формулы в платеже, начислении и финансовом отчёте. Снимок и платёжная ведомость показывают расчёт; представление в финансовом отчёте защищено тестами и требует ручной приёмки после входа на стенд.
- [x] Не изменять уже оплаченные начисления автоматически; подготовить отдельный безопасный сценарий перерасчёта неоплаченных начислений и отчёт о затронутых строках.
- [x] Проверить совместимость с водой, фиксированными услугами, начислением по людям, годовыми услугами и периодами без смены тарифа.

## Этап 3. Матрица граничных проверок тарифа

- [x] Добавить контрольный тест Excel: сентябрь 2026, смена 02.09, объём 2 000 кВт·ч, ступени 1 100/600/300, итог 17 755,00 руб.
- [x] Проверить смену тарифа в первый день месяца: весь месяц по новой версии.
- [x] Проверить смену в последний день: все предыдущие дни по старой версии, последний день по новой.
- [x] Проверить месяцы на 28, 29, 30 и 31 день, включая февраль високосного года.
- [x] Проверить две и более смены тарифа в одном месяце.
- [x] Проверить одинаковые ставки до/после, нулевое потребление и потребление точно на границе каждой ступени.
- [x] Проверить потребление ниже первой границы, между границами и выше последней открытой ступени.
- [x] Проверить денежное округление только на определённых этапах и отсутствие накопления копеечных расхождений между деталями и итогом.
- [x] Зафиксировать правило для намеренного разрыва тарифного расписания: дни без тарифа дают нулевое начисление и явно показываются в расшифровке; активные дни не нормализуются до полного месяца.

## Этап 4. Просрочка гаража 103 и месячные итоги

- [acceptance] Получить обезличенную хронологию регистрации гаража 103, начислений, сроков оплаты, льготных дней, платежей и распределений. Аудит не нашёл ровно одну активную запись с этим номером; требуется указание заказчика.
- [acceptance] Для каждого членского и целевого начисления вывести ожидаемые `DueDate`, `OverdueFrom`, остаток и фактическое распределение платежей после идентификации нужного гаража заказчиком.
- [acceptance] Установить, является ли дата `07.2026` ошибкой данных, неверным снимком срока, переносом старого долга или дефектом расчёта первого годового взноса после идентификации записи заказчиком.
- [x] Исправить формулу срока/выбор настройки услуги либо подготовить точечную идемпотентную коррекцию ошибочных записей на синтетическом наборе.
- [x] Сверить общий итог с суммой месяцев на уровне одной исходной выборки и единого правила знаков/отменённых операций.
- [x] Исключить двойной учёт начального долга, просрочки, переносов сборов, переплаты и отменённых строк.
- [x] Добавить контрольные суммы по каждой категории: начальный долг, начислено, оплачено, переплата, общий долг, просрочено.
- [x] Проверить фильтры периода: начальная граница, конечная граница, пустой месяц, годовой переход и операции на границе суток.
- [acceptance] После идентификации гаража 103 повторно проверить его и общие месячные итоги без раскрытия персональных сведений; автоматическая синтетическая регрессия уже зелёная.

## Этап 5. Зарплата, задолженность, премии и штрафы

- [acceptance] Построить для сотрудника Петрова обезличенную помесячную сверку: серверный аудит подтвердил наличие сотрудника, истории ставки/занятости без пропусков, повторных дат и пересечений; итоговую помесячную ведомость должен принять заказчик после авторизации.
- [x] Свести ведомость выплат и финансовый отчёт к одному доменному правилу и одной границе расчётного периода.
- [x] Учитывать зарплатную задолженность начиная с месяца/даты добавления сотрудника, не начисляя долг за более ранние периоды.
- [x] Включить все активные премии и штрафы сотрудника в месячные строки, входящий остаток и итог финансового отчёта.
- [x] Исключить отменённые корректировки и не потерять корректировки до начала выбранного отчётного периода.
- [x] Добавить расшифровку задолженности сотрудника по месяцам и типам движения.
- [x] Исправить подтверждённый риск конкурентных штрафов: сериализовать изменение агрегата `сотрудник + месяц` и не позволять двум запросам уменьшить начисление ниже уже выплаченного.
- [x] Проверить изменение ставки сотрудника, архивирование, восстановление, аванс, частичную выплату, переплату и переход года.
- [x] Зафиксировать правило первого неполного месяца: полная месячная ставка начиная с месяца добавления; пропорция дней не применяется.

## Этап 6. Фонды, касса и банковский счёт

- [x] Снять обезличенный баланс-снимок: защищённый аудит восстановленной копии проверил кассу, банк, подписанный нераспределённый остаток, фонды и цепочки операций без публикации сумм и персональных данных.
- [x] Проверить равенство `касса + банк = нераспределённые средства + сумма фондов` после каждой категории движения.
- [x] Найти и классифицировать расхождение: критический флаг создавал не испорченный проводочный остаток, а подмена подписанного бухгалтерского остатка неотрицательным лимитом новых распределений при историческом дефиците.
- [x] Исправить подтверждённую ошибку `ExpenseFundDisbursementService`: разрешённый минус определять по конечному остатку пересчитанной цепочки и правильно фиксировать в аудите.
- [x] Проверять не только конечный, но и каждый промежуточный остаток исторической цепочки при изменении и восстановлении расхода.
- [x] Добавить единый серверный reconciliation/check endpoint или внутреннюю проверку для диагностики расхождения без загрузки всех операций в память.
- [x] Показывать в интерфейсе состав равенства и явную диагностическую ошибку вместо несогласованных итогов.
- [x] Проверить необходимость точечной коррекции старых цепочек после резервной копии: бухгалтерское равенство выполняется, поэтому изменение реальных финансовых записей не требуется; исторический дефицит показывается отдельно от доступного лимита.
- [x] Зафиксировать, что пользовательское «деньги в фондах» включает строку нераспределённых средств; обязательное распределение всего остатка по именованным фондам не вводится.

## Этап 7. Единый журнал и редактирование транзакций

- [x] Инвентаризировать все типы: поступления, выплаты, начисления гаражам, начисления поставщиков, зарплатные выплаты, премии, штрафы, фондовые движения, переводы касса→банк и стартовые корректировки.
- [x] Зафиксировать для каждого типа допустимые поля редактирования, ограничения, права, способ отмены/восстановления и влияние на производные остатки.
- [x] Переиспользовать существующие backend-операции `UpdateIncome`, `UpdateExpense`, `UpdateAccrual` и обновление начислений поставщика; не создавать дублирующие маршруты.
- [x] Добавить отсутствующие безопасные операции изменения/отмены для премий и штрафов сотрудника.
- [x] Добавить оптимистичную конкуренцию и транзакционный пересчёт для изменённых зарплатных корректировок и фондовых цепочек.
- [x] Создать серверно пагинируемый единый журнал с фильтрами по периоду, виду операции, контрагенту, статусу и документу.
- [x] В интерфейсе использовать общие `TablePagination`, `PageNavigator` и `context-menu`; показывать «Редактировать», «Отменить/Восстановить», «История» только при наличии права и допустимого сценария.
- [x] Для защищённых автоматических/импортных строк показывать причину запрета прямого изменения и доступный сценарий корректировки.
- [x] После изменения восстанавливать фокус, обновлять только затронутые данные и обрабатывать loading, empty, validation, conflict, denied и server-error состояния.
- [x] Записывать полный аудит до/после без персональных и финансовых секретов в структурированных логах.

## Этап 8. Данные и миграции

- [x] Определить и реализовать изменения схемы для версий/отмены корректировок зарплаты и истории ставок/занятости; снимок тарифа остаётся версионированным JSON без изменения схемы.
- [x] Оформить изменения только EF Core migrations; не использовать `EnsureCreated`, runtime DDL или ручные необратимые SQL-правки.
- [x] Добавить ограничения целостности для сумм, дат и допустимых состояний новых зарплатных сущностей.
- [x] Подготовить backfill расчётных снимков без изменения оплаченных исторических сумм.
- [x] Сгенерировать идемпотентный migration SQL и проверить повторяемость на PostgreSQL 17 в CI.
- [x] Выполнить миграционную сверку до/после по количеству строк и контрольным финансовым итогам в PostgreSQL-тесте и защищённом аудите восстановленной копии.
- [x] Все временные диагностические SQL, копии данных и выгрузки удалить после проверки; локальные артефакты удалены, серверный аудит удалил временную восстановленную базу.

## Этап 9. Автоматические проверки

- [x] Покрыть unit-тестами дневное взвешивание, прогрессивные ступени, округление, сроки, зарплату, корректировки и фондовый инвариант.
- [x] Покрыть application/service тестами success, empty, invalid, failure, cancel/restore, stale version и permission denied для каждого изменённого метода.
- [x] Добавить controller/DTO/authorization тесты всех новых или изменённых API.
- [x] Добавить PostgreSQL-тесты тарифных периодов, отчётных сверок, реальных запросов и транзакционного пересчёта.
- [x] Добавить конкурентные PostgreSQL-тесты одновременных штрафов, выплат, редактирования операций и фондовых цепочек.
- [x] Добавить frontend component/hook/API/workflow/accessibility-тесты комментариев, тарифного редактора, расшифровок и отчётов.
- [x] Добавить регрессии по гаражу 103 и сотруднику Петрову только на полностью обезличенных синтетических данных.
- [x] Проверить производительность отчётов на реалистичном объёме и единого журнала; запросы ограничены, агрегируются сервером и прошли PostgreSQL performance-тесты.
- [x] Выполнить полный backend/frontend gate, coverage, format, lint, production build, bundle budget, privacy/ignore, encoding, NuGet/npm audit и migration checks.

## Этап 10. Документация, публикация и приёмка

- [x] Обновить пользовательские инструкции по тарифным датам, прогрессивному расчёту, задолженности сотрудников и сверке фондов.
- [x] Добавить понятную сотрудникам ГСК запись «Что нового» для каждого видимого изменения и изменения бизнес-правил.
- [x] Обновить `История выполнения` этой дорожной карты с причинами, проверками, ограничениями среды и оставшимися рисками.
- [acceptance] Провести ручную визуальную приёмку после авторизации: публичная часть стенда открывается штатно, внутренние экраны защищены автоматическими UI-тестами; локальная PostgreSQL-база с проектными реквизитами недоступна, временные артефакты и процессы удалены.
- [acceptance] На резервной копии рабочих данных подтвердить гараж 103, сотрудника Петрова, помесячные итоги и фондовое равенство.
- [acceptance] Заказчик подтверждает формулу Excel 17 755,00 руб., правило первого месяца сотрудника и состав суммы «деньги в фондах».
- [x] Сделать логические коммиты с русскими сообщениями после зелёного локального gate и финального аудита процессов/артефактов.
- [x] После явного запроса на публикацию выполнить push, дождаться полностью успешного GitHub Actions и исправить падения до зелёного состояния.
- [x] После CI проверить деплой на VPS: `garagebalance-staging.service`, nginx, `/health`, frontend и безопасные smoke-проверки без изменения реальных данных.

## Определение готовности

- Все 10 замечаний воспроизводятся до исправления либо документированно подтверждаются уже исправленными в актуальной версии.
- Контрольный пример Excel даёт ровно 17 755,00 руб.; границы тарифных периодов, прогрессивные ступени и округление объяснимы пользователю и защищены тестами.
- Общий итог равен сумме месяцев, задолженность сотрудника совпадает во всех представлениях, премии/штрафы учтены один раз.
- Для гаража 103 и сотрудника Петрова существует обезличенная сверка до/после; ни одна реальная запись не удалена без точного основания и резервной копии.
- Равенство кассы, банка, нераспределённых средств и фондов выполняется после создания, изменения, отмены и восстановления любой поддерживаемой операции.
- Единый журнал показывает все заявленные операции, применяет серверную пагинацию и безопасно редактирует только допустимые типы с полным аудитом и контролем конкуренции.
- Полные локальные и CI-проверки зелёные, миграции повторяемы, синтетические данные и временные процессы удалены, опубликованный стенд прошёл smoke-проверку.

## Открытые риски и вопросы

- Новый Excel явно меняет два прежних правила: простое среднее ставок и непрогрессивное применение одной ступени. Это затронет будущие начисления и потенциальный перерасчёт неоплаченной истории.
- На стенде не обнаружена ровно одна активная запись гаража с номером `103`; без указания заказчика нельзя безопасно выбрать другую или архивную запись для реальной помесячной сверки.
- Аудит сохранил 19 некритических диагностических признаков: 12 исторически ранних сроков, 3 прежних поступления с типом, восстанавливаемым по распределениям, по одному признаку разрыва/одинакового времени фондовой цепочки, исторического дефицита и отсутствия однозначной записи гаража 103. Они не нарушают текущие ограничения целостности, но требуют предметной приёмки заказчиком.
- Реальные финансовые записи не исправлялись: после отделения бухгалтерского остатка от доступного лимита критических расхождений на восстановленной копии нет.

## История выполнения

- 02.09.2026 — полностью изучены оба вложения. DOCX содержит 10 замечаний, без встроенных комментариев, изображений и tracked changes. XLSX содержит один лист и семь формул без ошибок; подтверждены прогрессивные объёмы 1 100/600/300 кВт·ч, дневное взвешивание 1/30 и 29/30 и итог 17 755,00 руб. Требования сопоставлены с текущим `master` `cefc7ff5`: редактор дат тарифных периодов уже существует и требует повторной приёмки, а действующий калькулятор и тесты намеренно используют простое невзвешенное среднее и непрогрессивную ступень, поэтому расчёт должен быть изменён. Подготовлена настоящая дорожная карта; код, БД и исходные вложения не изменялись.
- 03.09.2026 — начата реализация расчёта по датам и порогам. Калькулятор начислений переведён на календарное взвешивание каждой версии тарифа и прогрессивное распределение месячного расхода по ступеням; снимок расчёта обновлён до версии 4, а расшифровка показывает дни, диапазоны, ставки и промежуточные суммы. Зафиксировано безопасное правило разрыва расписания: непокрытые дни дают ноль. Добавлена защита от отрицательного расхода и пересекающихся периодов. Контрольный пример заказчика воспроизводит 17 755,00 руб. Выполнено 25/25 unit-тестов калькулятора, 337 успешных backend-тестов в расширенном финансовом наборе; 4 PostgreSQL-теста собраны, но локально пропущены из-за отсутствия настроенного тестового PostgreSQL. Точечный frontend-сценарий расшифровки прошёл: 1/1 (394 остальных теста файла не запускались). Полный gate ещё не выполнялся, работа по остальным замечаниям продолжается.
- 03.09.2026 — завершена реализация согласованного финансового ядра. История ставок и периодов работы сотрудника вынесена в отдельные интервалы и используется одной расчётной моделью в ведомости и отчётах; долг начинается с месяца добавления сотрудника, учитывает изменения ставки, архивирование/восстановление, премии, штрафы, выплаты и отмены. Для первого неполного месяца до приёмки заказчиком сохранено безопасное действующее правило полной ставки с месяца добавления. Премии и штрафы получили изменение, отмену и восстановление с optimistic concurrency, аудитом и PostgreSQL advisory lock по сотруднику и месяцу. Отчёт контрагента больше не синтезирует зарплату по текущей ставке во frontend и получает все страницы серверной расшифровки, включая исторические ставки и статусы корректировок.
- 03.09.2026 — исправлена цепочка фондов: при изменении/восстановлении пересчитывается весь последующий хвост, проверяется каждый промежуточный остаток, а разрешённый минус определяется по фактическому пересчитанному состоянию. Добавлен bounded reconciliation API и интерфейсное равенство `касса + банк = именованные фонды + нераспределённые средства`, включая стартовые корректировки. Реальные записи не очищались: точечная проверка и возможная коррекция рабочей БД остаются только после резервной копии и обезличенной сверки.
- 03.09.2026 — добавлены две EF Core migration: редактируемые зарплатные корректировки и сохранение истории ставок/занятости с backfill. Идемпотентный SQL успешно сформирован (380 072 байта), список миграций читается; проверить применённость и двойное применение локально нельзя из-за недоступного PostgreSQL. PostgreSQL-тесты и миграционная приёмка должны выполниться в CI/на стенде.
- 03.09.2026 — итоговый локальный gate: backend 2 454 теста (2 196 успешно, 258 PostgreSQL-тестов пропущены, 0 ошибок), frontend 1 023/1 023; frontend coverage 88,66% statements, 80,60% branches, 86,35% functions, 89,59% lines. Backend coverage без PostgreSQL составил 75,64% строк и 66,44% ветвей против обязательных 85%/70%; пороги не снижены, окончательный gate поручен CI с PostgreSQL. Успешны `dotnet format`, frontend lint/build, privacy (1 159 файлов), UTF-8/миграционные тесты, npm/NuGet audit. Production bundle — 271,6 KiB gzip при явном лимите 272 KiB; лимит увеличен с 270 KiB без новых зависимостей, чтобы вместить обязательные интерфейсы редактирования зарплатных корректировок, полного отчёта и сверки фондов, при этом отдельные JS/CSS/initial лимиты сохранены.
- 03.09.2026 — первый CI run `33704808256` выполнил весь PostgreSQL-набор и подтвердил backend coverage 90,78% строк / 74,52% ветвей, но остановился на двух устаревших тестовых предпосылках до публикации. Сквозной бухгалтерский тест ожидал прежний непрогрессивный расчёт электричества 650 руб.; все зависимые контрольные суммы приведены к новому прогрессивному результату 400 руб. и общему платежу 900 руб. Тест зарплатного дня теперь сам создаёт отсутствующую в чистой БД настройку, как предусмотрено production-сервисом. После исправления локальный backend gate повторён: 2 196 успешно, 258 PostgreSQL-тестов пропущены, 0 ошибок; `dotnet format` зелёный. Требуется повторный CI с PostgreSQL.
- 03.09.2026 — второй CI run `33705600585` подтвердил исправление обеих первоначальных ошибок и coverage 90,76% строк / 74,50% ветвей, но выявил оставшееся устаревшее ожидание порядка тех же двух начислений: после уменьшения электричества до 400 руб. сортировка по сумме закономерно ставит его перед начислением 500 руб. Порядок утверждений исправлен; production-код не менялся. Требуется третий CI run.
- 03.09.2026 — третий CI run `33706317758` полностью успешен: PostgreSQL 17 backend 2 454/2 454, coverage 90,80% строк и 74,68% ветвей; frontend 1 023/1 023, coverage 88,66% statements, 80,60% branches, 86,35% functions, 89,59% lines; format, privacy, npm/NuGet audit, lint, production build, bundle и idempotent migration SQL зелёные. Релиз `11bb6364e4e836f588bb5da5dcfdd75e5596d9fa-270` применён на VPS после резервной копии и успешной restore-проверки 44 таблиц; миграции завершены, nginx валиден, `garagebalance-staging.service` запущен. Внешняя проверка подтвердила HTTP 200 для `/health` с `postgreSql: ok`, главной страницы и нового frontend entry asset. Реальные финансовые данные не изменялись вручную; обезличенная приёмка гаража 103, Петрова и фондового равенства остаётся отдельным `[acceptance]` пунктом.
- 03.09.2026 — завершена текущая разработка безопасного перерасчёта и единого финансового журнала. Перерасчёт сначала показывает все затронутые строки, повторно сверяет отпечаток исходных сумм и формул, изменяет только неоплаченные регулярные начисления, сохраняет оплаченные, требует основание, блокирует конкурентное формирование и перестраивает распределения с аудитом. Добавлен backfill только отсутствующих расчётных снимков с версией 0: исторические суммы и уже существующие снимки не изменяются. Единый журнал объединяет семь классов записей, использует серверные фильтры и пагинацию, общие меню и права, показывает защиту автоматических/связанных записей и направляет к поддерживаемой корректировке.
- 03.09.2026 — текущий локальный gate после исправлений: backend 2 464 теста (2 204 успешно, 260 PostgreSQL-тестов пропущены, 0 ошибок), frontend 1 029/1 029; frontend coverage 88,14% statements, 80,19% branches, 85,86% functions, 89,19% lines. Новый журнал покрыт unit/integration-тестами полностью; локальный общий backend coverage без PostgreSQL — 75,71% строк / 66,38% ветвей, обязательные пороги 85%/70% не снижены и должны быть подтверждены CI с PostgreSQL 17. Успешны `dotnet format`, privacy (1 170 файлов), npm/NuGet audit, frontend lint/build и bundle: 276,6 КиБ из 278 КиБ, initial JS 98,1 из 110 КиБ. Идемпотентный SQL сформирован (381 482 байта), модель не имеет изменений после migration; локальное подключение к PostgreSQL недоступно из-за отсутствующих учётных данных. Проверка синтаксиса shell-скрипта локально недоступна из-за отсутствия `bash`, её выполнят CI/Linux и тесты содержимого deployment-скрипта.
- 03.09.2026 — CI run `33713103959` подтвердил backend coverage 90,75% строк / 74,58% ветвей и прошёл 2 463 из 2 464 PostgreSQL-сценариев. Единственная ошибка была в новой тестовой предпосылке: PostgreSQL `jsonb` канонически переставляет поля и пробелы уже существующего снимка, поэтому буквальное сравнение строк ошибочно считало неизменный JSON изменённым. Проверка заменена на смысловое сравнение JSON; production-код и миграция не менялись, развёртывание в неуспешном run не начиналось.
- 03.09.2026 — CI run `33713704494` полностью успешен и развернул релиз `fb66f5fa-272`: backend 2 464/2 464, frontend 1 029/1 029, обязательные пороги покрытия выполнены. Первый защищённый аудит остановился на диагностическом SQL истории окладов, ошибочно ожидавшем отсутствующее поле окончания периода. Проверка исправлена на реальный инвариант уникальности `сотрудник + дата начала`, добавлен тест; исправление опубликовано коммитом `d32de426`.
- 03.09.2026 — повторный аудит `33715597136` выполнил все проверки и выявил один критический флаг сверки кассы, банка и фондов. Причиной оказались не повреждённые проводки, а смешение подписанного бухгалтерского нераспределённого остатка с неотрицательным операционным лимитом новых распределений при историческом дефиците. В коммите `85061742` эти значения разделены во всех слоях, формула сверки использует подписанный остаток, а доступный лимит сохранил прежнее безопасное поведение. Реальные записи БД не изменялись.
- 03.09.2026 — финальный CI run `33716839489` полностью успешен: PostgreSQL 17 backend 2 465/2 465, покрытие 90,75% строк и 74,58% ветвей; frontend 1 029/1 029, покрытие 88,14% statements, 80,19% branches, 85,86% functions и 89,19% lines. Успешны format, lint, production build, privacy, npm/NuGet audit, миграционные проверки и bundle 276,6 КиБ из 278 КиБ. Релиз `8506174200f0f421a94c54264bf46dbb7049849f-274` развернут после резервной копии и restore-проверки 46 таблиц; миграции применены, nginx и `garagebalance-staging.service` исправны, `/health/ready` и frontend отвечают HTTP 200.
- 03.09.2026 — финальный защищённый аудит `33717540454` успешно создал и восстановил резервную копию, выполнил только чтение и удалил временную БД. Критических расхождений — 0; сохранено 19 признаков для предметной приёмки: 12 исторически ранних сроков, 3 прежних типа поступлений, восстановленных по распределениям, и по одному признаку фондовой цепочки, одинакового времени операций, исторического дефицита и отсутствия однозначной активной записи гаража 103. Сотрудник Петров найден, пропусков и пересечений в историях ставки/занятости нет. Точечная коррекция рабочих данных не потребовалась.
- 03.09.2026 — публичная страница стенда визуально открыта в браузере и штатно показывает форму входа. Внутренняя ручная приёмка не выполнялась без учётных данных и остаётся за заказчиком вместе с идентификацией нужной записи гаража 103; автоматические UI-сценарии зелёные. После проверок временные локальные сборки, coverage и диагностические артефакты удалены, запущенных задачей процессов не осталось.
- 03.09.2026 — по двум скриншотам исправлено представление раздела «Тарифы и сборы». Для предустановленных регулярных услуг восстановлено формирование отсутствовавших служебных строк расписания, поэтому сохранённые периодичность и срок оплаты показываются так же, как у добавленных вручную услуг; расчётные данные и API не менялись. В «Объявленных сборах» убрана нижняя строка назначения/цели, основное название переведено с жирного на обычный шрифт и выровнено по образцу нерегулярных платежей. Добавлены регрессии для ежемесячной и ежегодной предустановленной услуги, срока оплаты, DOM и CSS: frontend 1 030/1 030, coverage 88,14% statements / 80,17% branches / 85,88% functions / 89,19% lines; backend 2 205 успешно, 260 PostgreSQL-тестов штатно пропущены локально, 0 ошибок. Успешны lint, production build, bundle 276,6/278 КиБ, format, privacy и npm/NuGet audit. Прямая визуальная проверка опубликованного экрана из встроенного браузера была ограничена ошибкой DNS адреса стенда; рабочая БД не изменялась.
- 03.09.2026 — CI run `33726193603` полностью успешен и развернул на VPS релиз `64740cf1f045ae95eba646f3724ec1181d75bf02-275`: PostgreSQL 17 backend 2 465/2 465, покрытие 90,74% строк и 74,56% ветвей; frontend 1 030/1 030, покрытие 88,14% statements / 80,17% branches / 85,88% functions / 89,19% lines. Успешны format, lint, production build, privacy (1 170 файлов), npm/NuGet audit, миграционный SQL и bundle 276,6/278 КиБ. Перед обновлением создана резервная копия и успешно выполнена restore-проверка 46 таблиц; nginx валиден, `garagebalance-staging.service` перезапущен, `/health/ready` подтверждает PostgreSQL `ok`, а главная страница отвечает HTTP 200. Рабочие финансовые записи вручную не изменялись.
- 03.09.2026 — по повторным скриншотам завершена унификация представления тарифов: все обычные названия основной таблицы используют спокойный обычный шрифт и вертикальное выравнивание как у нерегулярных платежей, а зарплатный фонд вынесен в отдельную подписанную группу со строками отделов. У завершённого по плану объявленного сбора больше нет подписи «Закрыт после выполнения плана»; подпись и комментарий сохраняются только для ручного досрочного закрытия. Реальный экран проверен во встроенном браузере на изолированной PostgreSQL 17 с синтетическими данными, сделаны два снимка результата; VPS и рабочие данные не изменялись. Точечные UI-регрессии 4/4 зелёные. Полный backend gate: 2 465/2 465, coverage 90,73% строк / 74,57% ветвей; полный frontend gate после отдельного повторного запуска: 1 031/1 031, coverage 88,14% statements / 80,20% branches / 85,88% functions / 89,20% lines. Первый параллельный frontend-прогон дал пять ресурсных тайм-аутов при одновременной тяжёлой PostgreSQL-проверке; последовательный повтор прошёл полностью. Успешны format, lint, production build, privacy (1 170 файлов), npm/NuGet audit, release JSON и bundle 276,9/278 КиБ.
- 03.09.2026 — по новому макету переработано представление пороговой тарификации: название услуги теперь занимает отдельную строку группы, а все диапазоны расположены следующими строками и не повторяют название. Единое правило действует для электроэнергии, мусора и любой другой услуги после включения пороговой тарификации; формулы, ставки, расписание, API и рабочие данные не менялись. Реальный экран проверен во встроенном браузере на изолированной PostgreSQL 17 с синтетическими данными, ошибок консоли нет, снимок сохранён для приёмки. Точечные frontend-регрессии 2/2 и полный frontend gate 1 032/1 032 зелёные; coverage 88,15% statements / 80,21% branches / 85,88% functions / 89,20% lines. Полный backend gate на PostgreSQL 17 с ICU locale `und` прошёл 2 465/2 465, coverage 90,73% строк / 74,56% ветвей. Промежуточные прогоны с locale `C` и `ru-RU` выявили только разные правила регистронезависимого поиска и сортировки кириллицы среды; production-код не менялся, окончательный нейтральный прогон полностью зелёный. Успешны format, lint, production build, privacy (1 170 файлов), npm/NuGet audit, release JSON и bundle 277,0/278 КиБ.
- 03.09.2026 — исправлено последовательное переключение режимов любого обычного тарифа. Ошибка возникала, когда второй запрос отправлял уже устаревшие версии услуги и тарифа после первого сохранения; интерфейс теперь получает актуальные версии, объединяет их с выбранным пользователем режимом и один раз безопасно повторяет запрос, не затирая свежую ставку или расписание. Для воды при возврате из фиксированного режима сохраняется тип счётчика и расчётная база `meter_water`, поэтому включение порогов автоматически возвращает «По счётчику: Да» и создаёт водяные диапазоны в м³. Реальный сценарий «Вода: по счётчику Да → Нет; пороговая тарификация Нет → Да» проверен во встроенном браузере на изолированной PostgreSQL 17: оба переключателя стали «Да», появились три диапазона, конфликт и ошибки консоли отсутствуют, снимок сохранён. Добавлены frontend-регрессия автоматического обновления/повтора и PostgreSQL-интеграция двух последовательных запросов с возвратом серверных версий. Полный frontend gate прошёл 1 033/1 033, coverage 88,15% statements / 80,22% branches / 85,91% functions / 89,20% lines; полный backend gate на PostgreSQL 17 прошёл 2 466/2 466, coverage 90,73% строк / 74,57% ветвей. Успешны format, lint, production build, privacy (1 170 файлов), npm/NuGet audit, release JSON и bundle 277,3/278 КиБ. Первый backend-прогон на стандартных настройках одноразового Windows PostgreSQL упёрся в контрольные точки и тайм-ауты среды; повтор на той же изолированной базе с отключённым только для тестов синхронным WAL прошёл полностью, production-настройки и тестовые лимиты не менялись.
- 03.09.2026 — выровнены соседние карточки «Нерегулярные платежи» и «Объявленные сборы»: на широком экране обе растягиваются до высоты общей строки, а их пагинация остаётся закреплённой у нижнего края. Поэтому верхняя и нижняя границы совпадают при любом различии в количестве записей; на узком экране сохраняется штатное расположение карточек одна под другой. Реальный production-интерфейс проверен во встроенном браузере на изолированной PostgreSQL 17 с тремя нерегулярными платежами и двумя сборами: обе карточки имеют высоту ровно 390 px, разница верхних и нижних границ 0 px, ошибок консоли нет, снимок сохранён. Точечные frontend-тесты 2/2 и полный frontend gate 1 033/1 033 зелёные; coverage 88,15% statements / 80,22% branches / 85,91% functions / 89,20% lines. Полный backend gate на PostgreSQL 17 прошёл 2 466/2 466, coverage 90,73% строк / 74,57% ветвей. Успешны format, lint, production build, privacy (1 170 файлов), npm/NuGet audit, release JSON и bundle 277,3/278 КиБ. Рабочие финансовые данные и расчётные правила не менялись.
- 03.09.2026 — перед публикацией выполнена полная интеграционная проверка основной ветки. После `fetch --all --prune` подтверждено, что рабочие и удалённые ветки, stash, дополнительные worktree и pull request отсутствуют, а три неопубликованных коммита образуют линейное продолжение `master`; старые недостижимые состояния уже представлены окончательными коммитами в истории и не восстанавливались, чтобы не дублировать устаревший код. Backend прошёл 2 466/2 466 тестов на одноразовой PostgreSQL 17 с coverage 90,73% строк / 74,56% ветвей; frontend после устранения гонки в тестовом ожидании прошёл 1 033/1 033 с coverage 88,15% statements / 80,22% branches / 85,91% functions / 89,20% lines. Успешны format, lint, privacy (1 170 файлов), npm/NuGet audit, production build и bundle 277,3/278 КиБ. Idempotent migration SQL размером 381 482 байта дважды применён к чистой БД, showcase prepare/audit успешны. В браузере проверены вход, тарифы, контрагенты, платежи, фонды, отчёты и «Что нового»: ошибок консоли нет. Отдельный Docker Compose стенд собран и поднят на изолированных портах; PostgreSQL, API и frontend стали healthy, прямой и nginx-проксированный health-check вернули HTTP 200, критических записей в логах нет. Все созданные для проверки процессы, контейнеры, volumes, images и временные данные удалены.
- 04.09.2026 — по повторному скриншоту обнаружен оставшийся вариант ложного конфликта при включении пороговой тарификации воды: прежнее восстановление выполняло только одну повторную попытку, поэтому второе последовательное обновление версии снова выводило ошибку. Сценарий сначала воспроизведён регрессионным frontend-тестом с двумя конфликтами подряд, затем переключение переведено на ограниченную серию из трёх безопасных повторов; перед каждым повтором заново загружаются актуальные версии услуги и тарифа, а выбранный режим накладывается на свежие ставку, расписание и вид счётчика. При исчерпании повторов интерфейс сохраняет подтверждённое сервером состояние и показывает понятную ошибку. Полный frontend gate прошёл 1 033/1 033 теста с coverage 88,16% statements / 80,22% branches / 85,91% functions / 89,21% lines; backend на одноразовой PostgreSQL 17 прошёл 2 466/2 466 тестов с coverage 90,73% строк / 74,57% ветвей. Успешны format, lint, privacy (1 170 файлов), npm/NuGet audit, production build, release JSON, генерация idempotent migration SQL и bundle 277,3/278 КиБ.
- 04.09.2026 — по скриншоту исправлено положение горизонтальной прокрутки в таблице «Объявленные сборы»: после выравнивания высоты соседних карточек прокручиваемый контейнер сохранял высоту только по трём строкам, поэтому полоса оставалась посередине карточки. Регрессионный style-тест сначала воспроизвёл дефект, затем контейнер растянут на всё свободное место до пагинации. Изолированная визуальная проверка подтвердила одинаковую высоту карточек 392 px и нулевой зазор между нижней границей прокручиваемой области и пагинацией; рабочие данные не изменялись. Проверки прошли: точечный frontend-набор 50/50, полный frontend-набор 1033/1033 (покрытие 89,21% строк и 80,22% ветвей), backend 2466/2466 (покрытие 90,73% строк и 74,57% ветвей), lint, production build, лимиты bundle, npm audit, NuGet-аудит, privacy, форматирование и идемпотентный скрипт миграций.
- 04.09.2026 — повторяющийся конфликт воды исследован на VPS без изменения рабочих данных. Для услуги интерфейс показывал действующий с 02.09.2026 тариф, тогда как сохранённый указатель услуги оставался на закрытом 01.09.2026 периоде; сервер сравнивал присланную версию действующего тарифа с версией закрытого и поэтому отклонял все повторы. Смена режима теперь использует тариф, который выбран из связанной с услугой сетки периодов, и не принимает посторонний тариф. Регрессия сначала воспроизвела исключение optimistic concurrency, затем прошла на SQLite и отдельной PostgreSQL 17; PostgreSQL-сценарий подтверждает включение водяных порогов и создание новой версии без ручной коррекции БД. Полный backend gate: 2468/2468, покрытие 90,73% строк / 74,57% ветвей; frontend: 1033/1033, покрытие 88,16% statements / 80,22% branches / 85,91% functions / 89,21% lines. Успешны format, lint, production build, bundle 277,4/278 КиБ, privacy (2 148 файлов), npm/NuGet audit, release JSON и генерация идемпотентного migration SQL (381 482 байта).
- 04.09.2026 — в карточку сотрудника добавлены точные даты принятия и увольнения: дата принятия расположена над отделом, дата увольнения — над ставкой; увольнение необязательно, а неверный обратный интервал блокируется до отправки. Зарплата начисляется за полные граничные месяцы, включая месяц принятия и месяц увольнения, и одинаково учитывается в ведомости и отчётах; сохранение старой версией интерфейса не стирает существующий период занятости. Схема БД не менялась, использованы существующие периоды занятости. Реальная форма проверена во встроенном браузере на изолированной PostgreSQL 17 с синтетическим сотрудником. Полный backend gate на PostgreSQL 17: 2473/2473, coverage 90,75% строк / 74,60% ветвей; frontend: 1033/1033, coverage 88,17% statements / 80,22% branches / 85,91% functions / 89,21% lines. Успешны format, lint, production build, bundle 277,6/278 КиБ, privacy (1 170 файлов), npm/NuGet audit, release JSON и генерация идемпотентного migration SQL (381 482 байта).
- 04.09.2026 — по решению заказчика с экрана «Платежи» временно скрыты действия «Единый журнал операций» и «Перерасчёт неоплаченных начислений». Серверные механизмы, финансовые записи, автоматическое формирование начислений и расчёты не менялись; отображение можно вернуть после отдельной переработки интерфейса. Реальная страница проверена во встроенном браузере на изолированной PostgreSQL 17 с синтетическим администратором: обе кнопки, журнал и диалог отсутствуют. Полный backend gate на PostgreSQL 17: 2473/2473, coverage 90,73% строк / 74,58% ветвей; frontend: 1033/1033, coverage 87,81% statements / 79,92% branches / 85,43% functions / 88,90% lines. Успешны format, lint, production build, bundle 275,8/278 КиБ, privacy (1 170 файлов), npm/NuGet audit, release JSON и генерация идемпотентного migration SQL (381 482 байта).
